# 清数智算 W26 Agent 对话质量与证据链详细 Handover

**交接日期：** 2026-07-28
**交接主题：** 真实选股候选进入个股空间后的 Agent 回答质量、基金与固收资料扩充、长会话效率、直接事件证据
**权威仓库：** `/Users/chr/Documents/qingtianshu`
**当前分支：** `codex/structured-ai-writeback`
**当前 HEAD：** `892a3751c19f0e4e74c92543c7a8539d041b8e24`
**远端状态：** `origin/codex/structured-ai-writeback` 已同步至 `892a375`
**服务地址：** `http://127.0.0.1:8773`
**当前目标状态：** 仍在进行中，不得标记完成

**续接更新：** 2026-07-29 已完成北方国际 Pass 5—9、一次长会话流式优化前后对照，以及连续主营结构追问的真实 DeepSeek 修复。主营专项最终 Run `9f07844b-9410-48c6-98a6-f4c9c7ae2995` 已能使用当前公司的真实分部、地区、毛利率和披露边界回答，但这只证明主营专项链路，不代表经营改善、相对行业增强和估值约束三个模板已经完成验收，W26 仍不得标记完成。

---

## 0. 给下一位 Agent 的一分钟续接说明

请先完整阅读本文件，不要从头重做已经完成的页面、K线、流式输出、数据库和李总策略工作。

下一步最高优先级不是继续改页面样式，也不是继续增加 Guard 文案，而是修复下面这条真实失败链：

> 用户从透明选股进入某一只股票的长期研究空间后，本来应该使用该股票已经准备好的公告、财务、经营现金流、行业、事件与反方证据，由 DeepSeek 即时回答；但目前某些问法会被误路由回“全市场选股”，导致 Agent 只拿到选股候选表，没有拿到该股票的完整研究包。随后，知识检索又可能混入其他股票的数字；当前数字守卫只检查数字是否在整份上下文出现过，没有校验这个数字属于哪只股票，因此可能放过跨股票数字污染。

本交接已经用北方国际真实页面和真实 `deepseek-v4-pro` Run 证明该问题存在。下一位 Agent 应直接从第 8 节的 W26 实施顺序开始。

### 不可偏移的重点目标

后续 Agent 必须把以下两组工作视为直接影响对话体验的核心主线，不得降级为普通待办，也不得用页面美化、模板文案或增加 Guard 代替：

1. 继续抽样经营改善、相对行业增强和估值约束模板进入个股空间后的真实 DeepSeek 回答，确认模型实际使用当前股票的公告、财务、经营现金流、行业口径和反方证据，而不是机械复述候选入口文案。每个模板必须有真实候选、真实 Run、Prompt、最终回答、截图和人工评分；只验证字段已透传或自动化测试通过不算完成。
2. 继续扩充基金产品事实、债券与利率数据、长会话效率和直接事件证据。扩充的目的不是增加资料数量，而是让 Agent 能在当前问题中准确检索、按主体和时间使用证据，并更快、更直接、更易读地回答用户。

这两组目标必须同时反映在实现、真实模型抽样、浏览器体验、验证记录和后续 handover 中。

### 当前不要做的事

- 不要继续堆页面、卡片、正则提示或泛化 Guard，优先修复路由、证据装配和主体绑定。
- 不要把预生成报告正文直接返回给用户；报告只能作为本轮模型的新证据。
- 不要为演示放宽选股规则或制造候选。
- 不要让 AI 自动确认正式判断、任务、风险画像、交易计划或复盘。
- 不要暂存或删除 `artifacts/`。
- 不要使用 `git add -A`。

---

## 0.1 2026-07-29 续接后的最新结论

### 已经改善的部分

- 北方国际真实问题已稳定进入 `stock_research`，不再执行全市场选股作为当前回答。
- 财务问题强制带出报告期、营收同比、净利润同比和经营现金流；收益窗口同时提供 5/20/60 日起止日期。
- 经营现金流覆盖倍数高于 1 不再被确定性证据层自动解释成盈利质量没有恶化。
- 财报原文没有明确说明时，证据层不再把财务费用归因为利息、汇兑或融资成本。
- 财务、企稳类追问从错误的 `comprehensive` 收敛为 `mixed`，只加载行情、财务、财报质量和利润现金流驱动等相关模块。

### 真实 DeepSeek Pass 5—9

同一北方国际问题连续运行五次，全部为真实 `deepseek-v4-pro`、`stock_research / completed`。证据保存在：

`artifacts/w26-north-international-retest-20260729/`

| 样本 | Run | 主要结果 | 人工结论 |
|---|---|---|---|
| Pass 5 | `7017b2e0-2e0b-4a59-8f66-306fa9406296` | 使用了一季报、现金流、借款公告与均线，但把财务费用擅自解释为利息或汇兑成本，并把现金流覆盖倍数解释成利润已变成“真金白银” | 不通过 |
| Pass 6 | `3dc25b34-bf26-42c0-b869-b20c7e0e48d0` | 能区分财务背景和股价直接原因，但把销售收现率上升直接解释为回款节奏改善 | 不通过 |
| Pass 7 | `6b95e54f-d633-4712-b2e1-9f78b58b28fd` | 正确指出财报不等于回撤直接原因，但把经营现金流净额下降写成“公司实际收到的钱少了” | 不通过 |
| Pass 8 | `25af918e-237e-4934-bf53-4a6a1f073a48` | 能说明现金流覆盖倍数上升可能源于净利润降得更快，且没有把销售收现率直接写成回款改善 | 接近通过，但单轮结果不能证明稳定性 |
| Pass 9 | `264eef76-2201-460c-8523-c2c60c4a3236` | 使用了财务、现金流、公告和技术事实，但首句把基本面压力写成与回撤“有关”，又把经营现金流净额称为现金流入，并混淆公告窗口 | 不通过 |

这五次抽样说明：字段已经进入 Prompt、Guard 通过、单次回答看起来完整，都不能证明金融解释稳定正确。以下四类错误仍是硬性失败项：

1. 把经营现金流净额、销售商品收到的现金和一般“现金流入”混为一谈。
2. 把静态毛利桥、费用机械拆解写成公司已经确认的经营原因。
3. 把财务压力从“同期背景或反方事实”外推成股价回撤原因。
4. 把不同公告的日期合并描述，导致窗口内外归属错误。

### 长会话流式优化前后

真实追问：

> 请用三段话继续说明：财务压力能确认什么、不能解释什么，以及近5日走平为什么还不能叫企稳。

| 指标 | 优化前 | 优化后 |
|---|---:|---:|
| Run | `ab3b1df5-9049-4bf9-aa8e-39294f3094b3` | `ee20b968-c6d5-467e-aff6-d63c3d1ccc80` |
| 路由 | `comprehensive` | `mixed` |
| Prompt 字符 | 92,100 | 33,827 |
| 输入 Token | 35,575 | 14,738 |
| 首个安全正文 | 19.178 秒 | 13.741 秒 |
| 总耗时 | 27.534 秒 | 20.605 秒 |
| 估算成本 | 0.01644 美元 | 0.00718 美元 |
| 安全正文更新 | 9 次 | 9 次 |

优化后不再自动加载公告、事件、历史条件走查和与当前问题无关的展望模块，输入 Token 减少约 58.6%，总耗时减少约 25.2%。但首个安全正文仍未达到本报告建议的 10 秒门槛，最终回答仍出现“现金流入”“下跌途中的短暂喘息”等不够准确或过度判断的表达，因此性能改善不能替代内容质量验收。

### 当前最重要的实施断点

1. 继续改善证据字段和生成合同，明确区分经营现金流净额、销售收现、现金流入与回款质量；不要主要依赖继续堆 Guard。
2. 静态毛利桥必须显式标为“按报表数据静态测算”，不得被模型改写成已确认业务原因。
3. 公告逐条携带 `announcement_date / window_start / window_end / in_window`，不允许模型把多条公告合并判断窗口。
4. 完成经营改善、相对行业增强、估值约束三个真实模板，每个保存真实候选、真实 Run、Prompt、页面截图和人工评分。
5. 继续基金正式文件与完整费率、债券和利率事实、直接事件原文证据及 10 轮长会话浏览器验收。

---

## 1. 当前工作树与运行状态

### 1.1 Git 状态

2026-07-29 续接时：

```text
## codex/structured-ai-writeback...origin/codex/structured-ai-writeback
 M app/providers/market.py
 M app/services/agent_evidence_compaction.py
 M app/services/agent_output_guard.py
 M app/services/agent_output_guard_stock.py
 M app/services/agent_prompt_contracts.py
 M app/services/analysis.py
 M app/services/chat_context.py
 M app/services/chat_knowledge_context.py
 M app/services/chat_routing.py
 M app/services/earnings_quality.py
 M app/services/financial_drivers.py
 M app/services/research_plan.py
 M app/skills/a-share-filing-evidence/SKILL.md
 M app/skills/financial-drivers/SKILL.md
 M tests/...
?? HANDOVER_QINGSHU_AGENT_QUALITY_W26_20260728.md
?? artifacts/
?? 清数智算工作日报-20260728.md
```

说明：

- `artifacts/` 是本地浏览器截图、真实模型抽样和验收结果，必须保留，但默认不进入 Git。
- `清数智算工作日报-20260728.md` 是本轮新建日报，尚未暂存和提交。
- 本 handover 本身也是新文件，是否提交由后续交付步骤决定。
- 当前存在 W26 代码与测试改动，包含路由、证据压缩、财务语义、收益窗口、Prompt合同和相关回归；尚未完成最新全量门禁和范围受控提交。
- 不得用 `git add -A`；交付时只能显式暂存实现、测试和文档，排除 `artifacts/`、环境文件、数据库、日志、缓存和用户工作区。

### 1.2 运行状态

2026-07-29 续接时 `/health`：

- Web 服务：`ok`
- Hermes：已启用
- 默认文字模型：`deepseek-v4-pro`
- 领域数据库：PostgreSQL，Schema v5
- 运维与持久化队列：PostgreSQL，Schema v4
- 外部 Worker：4 个 active
- 队列：无 ready、delayed、retrying 积压
- 近 24 小时失败任务：0
- 数据健康：52/54 healthy，1 attention，1 critical

数据健康不再是全绿；交付前必须读取具体 attention/critical 项并判断是否影响用户路径，不能只用 `/health=ok` 宣称生产就绪。

### 1.3 当前自动化基线

W25 已记录的历史基线：

- `832 tests collected`，全量通过
- Ruff 通过
- Python `compileall` 通过
- 全部 `app/static/*.js` 的 `node --check` 通过
- `uv lock --check` 通过
- `git diff --check` 通过

W26 最新代码当前只完成了 Agent Prompt合同、收益窗口日期、财报质量、财务现金流拆解、ResearchPlan、Agent Guard、证据压缩及Chat/API相关定向测试。全量测试、Ruff、`compileall`、全部静态JavaScript、`uv lock --check` 和最新浏览器门禁仍需重跑；不得把 W25 的 832 项历史基线当作 W26 当前全绿证据。
- 桌面 1280×720 与移动 390×844 共 20 条浏览器门禁：`20 passed / 0 failed`

注意：以上是 W25 已验证基线。本 handover 编写过程中没有重新跑全量测试，因为没有修改代码。

---

## 2. 必读文件与权威顺序

建议按以下顺序阅读：

1. 本文件：当前真实失败、根因和 W26 执行顺序。
2. `PRODUCT_OPTIMIZATION_GOAL_AND_PROGRESS.md`：用户十项长期目标及 W23—W25进度。
3. `HANDOVER_QINGSHU_20260724.md` 第 65—68 节：W22—W25详细实现与验证。
4. `README.md`：当前产品功能、接口和运行说明。
5. `VERIFICATION.md`：历史验证记录；注意它保留历史阶段，不能把旧数字当成当前基线。
6. `docs/UX_AGENT_OPTIMIZATION_TRACKER_20260728.md`：W1—W21真实体验问题与修复记录。
7. `docs/PRODUCT_PIPELINE_ARCHITECTURE.md`：数据、Worker、选股和回测流水线。

同事方案只作为设计输入，不是当前工程权威。可继续参考摘要优先、渐进披露、K线交互和清晰分组，但不得吸收静态目标价、演示胜率、虚构行情或没有后端证据的结论。

---

## 3. 已完成且不要重复建设的能力

### 3.1 信息架构和页面

- 一级入口已经统一为：市场总览、透明选股、我的关注、个股研究、金融顾问、复盘中心、个人中心。
- “AI投研对话”已改名为“金融顾问”。
- 金融资料库已从一级导航降为资源入口。
- 390px 下七项导航完整可见，不依赖横向滚动。
- 个股研究概览已改为摘要优先，完整证据放入独立页签。
- 交易复盘无记录时只展示三步上手路径，不再堆无效筛选和重复按钮。

### 3.2 K线

- 我的关注和个股研究共用同一真实行情组件。
- 支持分时、日K、周K、月K、真实年K。
- 支持近3月、6月、1年、2年和5年。
- 支持放大、十字光标、OHLCV、均线、成交量、拖拽、缩放和双击复位。
- 年K由日线按自然年聚合，不能退回“近1年日K冒充年K”。

### 3.3 流式回答

- Hermes真实 delta 已接入私有 SSE。
- 安全累计正文可在终态前显示。
- 用户主动滚动后停止自动跟随。
- 最终答案不会清空后重新逐字播放，也不会用另一段摘要替换已经显示的长文。
- W20 中兴通讯真实 Run 首个安全正文约 4.236 秒，总耗时约 14.343 秒。

### 3.4 数据库和后台

- 领域库和运维队列都使用 PostgreSQL。
- 持久化任务队列、租约、重试和恢复机制已运行。
- 两个独立 Worker 持续刷新行情、公告、财务、研究报告、基金产品、事件、复盘等数据。
- 自选股走后台预分析；非自选股可走在线刷新。

### 3.5 透明选股 W25

四个模板：

| profile | 用户名称 | 关键边界 |
|---|---|---|
| `quality` | 经营改善候选 | 营收、净利润和ROE规则只形成候选，不证明改善质量 |
| `trend` | 相对行业增强候选 | 行业比较是同标签简单均值，不是官方行业指数 |
| `value` | 估值约束观察候选 | 命中PE/PB约束不等于低估 |
| `pullback` | 回撤后待复核候选 | 近5日转正不等于企稳或反转 |

W25 已完成：

- 候选新增 `research_focus` 和 `attention_flags`。
- 风险线索可覆盖单日急跌、高估值、利润下降、低ROE和高负债。
- 字段已进入 `entry_context`、个股空间、股票工作区、Agent证据压缩和Prompt。
- 回撤模板按近5日收益升序排列。
- 页面已修复重复“先核验”和重复标点。

但 W25 只证明字段“能透传”，没有证明所有真实问法都一定进入 `stock_research` 路由。W26 实测已经找到反例。

### 3.6 李总策略

- 零交易成本。
- 实际换仓至少间隔10个交易日。
- 冷却期只执行最新候选集合。
- 策略空仓期间沪深300仍按真实每日收盘连续变化。
- 当前版本：`li_zong_2w_no_cost_full_benchmark_v5`。

此部分不是当前 W26 主线，不要再次改回同暴露冻结基准。

---

## 4. W26 真实 DeepSeek 失败样本

### 4.1 用户路径

1. 打开透明选股中的“回撤后待复核候选”。
2. 选择真实候选北方国际 `000065.SZ`。
3. 进入该股票的长期研究空间。
4. 打开“AI研究”。
5. 使用 Enter 发送以下问题：

> 北方国际最近20日下跌8.04%，这次回撤最可能与哪些已经确认的公司事件、财务和现金流变化有关？请先直接回答，再说明哪些原因目前没有证据；同时判断近5日0.00%能不能算企稳。请用普通投资者能看懂的话，不要复述选股卡片。

Enter 发送本身正常，流式正文也正常出现。

### 4.2 真实 Run

| 字段 | 值 |
|---|---|
| Conversation ID | `4fa65d33-ca28-4f31-99bb-b099d2256489` |
| Run ID | `6bbaae65-a413-4ae3-a214-bebdfbbc2783` |
| 模型 | `deepseek-v4-pro` |
| 实际 intent | `stock_screen`，错误 |
| 应有 intent | `stock_research` |
| 实际 symbol | `null`，错误 |
| 实际 conversation_scope | `screening`，错误 |
| Prompt字符数 | 34,611 |
| 输入Token | 13,674 |
| 输出Token | 1,153 |
| 路由与证据耗时 | 36.616秒 |
| 首Token | 19.53秒 |
| 首个安全正文 | 20.49秒 |
| 总请求时间 | 65.509秒 |
| 模型调用 | 1次 |
| 输出守卫 | 错误地判定通过 |

Prompt文件：

`/Users/chr/.qingshu/workspaces/29431778-83ef-4cd8-993c-8fcadf953fce/runs/6bbaae65-a413-4ae3-a214-bebdfbbc2783/prompt.md`

### 4.3 回答中的主要错误

Agent 回答称：

- 当前没有公司事件公告。
- 当前没有经营现金流数据。
- 20日年化波动率超过115%。
- 60日最大回撤超过33%。

其中前两项与服务器已保存证据直接冲突，波动率数字则属于跨股票污染。

### 4.4 服务器实际已经拥有的北方国际证据

最新预生成报告：

- Report ID：`11cda567-d5f2-4844-8682-442c056a8afc`
- 生成时间：`2026-07-28T14:18:30+00:00`
- 状态：`preview`

报告已经包含：

#### 行情和技术

- 最新价：9.04 CNY
- 当日涨跌：-1.53%
- 20日收益：-8.04%
- 60日收益：-28.54%
- 20日年化波动率：32.92%
- 60日最大回撤：-37.98%

#### 财务和现金流

- 报告期：2026一季报，财政年度累计口径
- 公告日：2026-04-27
- 营收：23.52亿元
- 营收同比：-35.56%
- 归母净利润：1.10亿元
- 净利润同比：-37.54%
- 加权ROE：0.99%
- 资产负债率：53.47%
- 经营现金流：216,477,658.72元
- 经营现金流/净利润：约1.96
- 已有确定性结论：利润与经营现金流双重承压，并存在应收、应付与收入不同步的待复核线索

#### 公告和事件

报告已经列出：

- 2026-07-16：北方国际投资者关系管理信息
- 2026-06-15：向控股股东申请借款暨关联交易公告及中信证券核查意见
- 2026-06-15：九届二十二次董事会决议公告
- 事件脉络共包括官方披露、治理、财报、股东回报等主题

因此，正确回答不能说“没有事件公告、没有现金流数据”。可以说的是：

> 已有公告和现金流事实，但当前仍不能证明这些因素就是20日股价回撤的直接因果；需要阅读公告原文、投资者关系记录和财报附注，寻找公司对收入下降、订单、项目交付、借款与现金流变化的直接解释。

#### 主营结构

- 最新主营构成截至2025-12-31。
- 工程建设与服务收入占比约46.63%。
- 资源设备供应链收入占比约40.17%。
- 境外收入占比约63.38%。
- 资源设备供应链收入同比下降约39.28%。
- 主营结构数据来自年报口径，不能自动解释2026一季报或近期股价回撤。

### 4.5 跨股票数字污染证据

错误回答中的“20日年化波动率超过115%”并不属于北方国际。

本轮 Prompt 检索到一份用户资料：

`我的最新研究行动与观察条件`

该文档是多股票混合内容，摘要中出现：

- 某个其他标的20日年化波动率 115.10%
- 某个其他标的最新价 135.92
- MA20 143.45

由于错误路由为 `stock_screen`，知识过滤没有按 `000065.SZ` 做主体约束，这份用户文档被完整加入 Prompt。

当前数字守卫在 `app/services/agent_output_guard.py` 中把整个 `evidence` JSON 和 `trusted_context` 拼成一段文本，抽取全部数字作为允许值。它验证的是“数字是否在上下文某处出现”，不是“数字是否属于北方国际、属于哪个模块、属于哪个日期”。因此 115.10% 被错误放行。

---

## 5. 已确认的根因链

### 根因一：负向措辞“不要复述选股卡片”触发了选股路由

实测：

```text
_is_stock_screen_query(question) = True
_prefers_bound_candidate_research(question) = False
```

原因：

- `app/services/chat_routing.py` 的 `_is_stock_screen_query()` 看到“选股”就可能判定为全市场筛选。
- 用户虽然表达的是“不要复述选股卡片”，但当前规则不理解否定语境。
- 这类问题不是请求重新筛选，而是在一只已绑定股票里核验入口线索。

### 根因二：绑定股票会话的优先规则过窄

`app/services/chat_context.py:78-108` 的 `_prefers_bound_candidate_research()` 只在消息包含“为什么入选、这个逻辑、风险、核验、反方证据”等少数关键词时返回 true。

真实用户常说：

- “这次下跌和什么有关？”
- “它最近发生了什么？”
- “现金流怎么样？”
- “不要复述选股卡片，直接分析。”

这些都可能不命中当前词表。

`app/services/chat_context.py:471-493` 只有在该函数返回 true 时，才会把绑定 `deep_stock_session` 的 symbol 注入，并清除 `explicit_stock_screen_query`。

### 根因三：错误路由导致只构建全市场选股证据

`app/services/chat_orchestration.py:277-287` 在 `stock_screen_query=true` 时调用 `chat_screening_evidence.build_standard()`。

结果：

- evidence type 为 `stock_screen`
- symbol 为 null
- Prompt包含12只候选和全市场范围说明
- 没有执行 `ChatStockResearchEvidenceService.build()`
- 没有按研究计划加载 fundamentals、earnings_quality、financial_drivers、a_share_information、event_timeline

如果 symbol 正确，代码会走 `app/services/chat_orchestration.py:478-505` 的 `stock_research` 分支，并附加股票工作区上下文。

### 根因四：知识过滤没有隔离普通用户多股票资料

`app/services/chat_knowledge_context.py` 对 `stock_research` 能过滤其他股票生成型 source key，但仍允许普通 `scope=user` 文档进入。

这本身是为了允许用户上传个人研究材料，但如果用户文档同时包含多只股票，不能把其中所有数字都当成当前标的数字。

需要保留用户资料价值，同时增加主体绑定：

- 文档有明确证券代码时，只把当前标的段落用于当前股票。
- 无法分段时，只作为叙事背景，不进入可支持金融数字的 trusted numeric context。
- 多股票汇总文档不能为单股回答提供不带主体绑定的价格、波动率、估值和财务数字。

### 根因五：数字守卫缺少 entity/module/time 三层绑定

`app/services/agent_output_guard.py:394-424` 将所有证据数字放入全局 allowlist。

`app/services/agent_output_guard.py:592-613` 只比较数值是否近似匹配。

需要逐步升级为：

1. entity：数字属于哪只股票、指数、基金或用户画像。
2. module：数字是行情、财务、现金流、估值、行业还是事件统计。
3. time：数字属于哪个交易日、报告期或公告日。

至少对于 `stock_research`，当前股票的行情数字不能由其他股票、全市场候选或用户多股票文档支持。

### 根因六：结构化答案为空，但页面仍显示“事实0、反证0、待核验0”

本轮 `structured_answer.status=unavailable`，但正文其实包含事实、推断和缺口。页面显示：

```text
结构化证据 · 事实 0 · 反证 0 · 待核验 0
```

这会进一步伤害用户信任。

后续应选择一种明确策略：

- 要么保证股票研究 Prompt 输出结构化合同，并在终态校验；
- 要么结构化合同不可用时隐藏数量，不显示三个误导性的0。

不要用简单关键词从正文硬凑正式事实，也不要自动写回用户判断。

### 根因七：验收隔离模式没有真正隔离该股票会话

测试时尝试访问：

`/stocks/000065.SZ?tab=ai&qa=1`

但北方国际已经存在普通用户的唯一长期股票会话，页面最终仍使用：

- conversation quality_scope：`user`
- 不是 `evaluation`

这说明仅加 `qa=1` 不能安全复制同一用户、同一股票的长期研究会话。

后续真实模型验收应使用独立 evaluation 用户和独立浏览器上下文：

1. 创建隔离用户。
2. 创建 `quality_scope=evaluation` 的 conversation。
3. `POST /me/deep-stock` 时传该 conversation_id 和真实 entry_context。
4. 在该隔离会话运行真实 DeepSeek。
5. 不污染普通用户历史、画像和正式研究空间。

---

## 6. 涉及代码地图

### 6.1 必查核心文件

| 文件 | 作用 | 当前关注点 |
|---|---|---|
| `app/services/chat_routing.py` | 纯文本意图与证券识别 | 否定语境中的“选股”误触发 |
| `app/services/chat_context.py` | 会话、历史、绑定股票、知识请求准备 | bound stock 优先级过窄；只取最近40条原始消息 |
| `app/services/chat_orchestration.py` | 选择 stock_screen / stock_research 等证据路径 | 错误分支导致全市场筛选重算 |
| `app/services/chat_stock_research_evidence.py` | 构建单股研究证据 | 正确路由后应加载混合研究计划模块 |
| `app/services/research_plan.py` | 根据问题选择证据模块与Skills | 本问题正确计划为 `mixed` |
| `app/services/chat_knowledge_context.py` | 过滤知识库结果 | 多股票用户文档仍可能污染单股数字 |
| `app/services/agent_evidence_compaction.py` | 压缩证据进入Prompt | 需保留当前标的核心证据，删除无关候选和全文 |
| `app/services/agent_prompt_contracts.py` | Prompt答案要求 | 直接回答、证据边界和非模板化表达 |
| `app/services/agent_output_guard.py` | 数字和政策校验 | 当前数字allowlist不绑定实体 |
| `app/services/agent_output_guard_stock.py` | 股票特定冲突校验 | 可加入波动率、回撤等当前标的绑定 |
| `app/services/chat_persistence.py` | 保存intent、symbol、scope、结构化答案 | 错误路由会把会话继续标记screening |
| `app/static/qs-chat-runtime.js` | 前端发送与SSE状态 | 验收quality_scope、研究计划和终态展示 |
| `app/static/qs-agent-ui.js` | 回答、结构化证据和会话历史UI | unavailable时不要显示误导的0/0/0 |
| `app/static/qs-stock-space.js` | 个股空间与AI页 | 绑定会话和entry_context入口 |
| `app/static/qs-screening.js` | 候选进入个股空间 | entry_context已经贯通，不要回退 |

### 6.2 本问题的正确 ResearchPlan

对北方国际测试问题直接运行 `ResearchPlanService().build()`，结果为：

- focus：`mixed`
- required modules：
  - market
  - fundamentals
  - earnings_quality
  - financial_drivers
  - company_information
  - event_timeline
- optional module：outlook_calibration
- selected skills：
  - a-share-filing-evidence
  - fundamental-evidence
  - earnings-quality
  - financial-drivers
  - evidence-debate
  - a-share-information
  - event-timeline
  - conditional-outlook

说明分析方法已经存在；当前问题主要是没有进入这条路径。

---

## 7. W26 第一优先级修复方案

### 7.1 先写失败回归测试

在 `tests/test_research_plan.py` 或新建专门路由测试中，复用“候选进入深度研究空间”的现有测试结构。

必须加入与真实问题高度一致的样本：

```text
北方国际最近20日下跌8.04%，这次回撤最可能与哪些已经确认的公司事件、财务和现金流变化有关？请先直接回答，再说明哪些原因目前没有证据；同时判断近5日0.00%能不能算企稳。请用普通投资者能看懂的话，不要复述选股卡片。
```

断言：

- intent 为 `stock_research`
- symbol 为 `000065.SZ`
- evidence type 为 `stock_research`
- research_plan.focus 为 `mixed`
- 必须包含 fundamentals、earnings_quality、financial_drivers、a_share_information、event_timeline
- stock_workspace_context.research_entry 保留 profile、research_focus、attention_flags
- Prompt不包含完整12只选股候选列表
- Prompt包含当前股票公告、经营现金流和反方证据

还要加入反向断言：

```text
请重新筛选一批A股候选
```

即使在绑定股票会话中，也应允许用户明确切换到 `stock_screen`。

### 7.2 修复 bound stock 优先级

推荐原则：

> 在已绑定单股的长期研究对话中，默认延续当前股票；只有用户明确要求全市场筛选、明确询问整体市场/行业、明确输入另一只或多只证券、或明确转向基金产品时，才离开当前股票上下文。

不要继续只靠增加“为什么、核验、风险”等正向关键词。

可将决策拆成：

1. 是否存在 `bound_deep_stock`。
2. 是否是明确 broad screening。
3. 是否是明确 market / industry / fund education。
4. 是否提到其他证券或多股比较。
5. 其余默认绑定当前 symbol。

同时修复否定语境：

- “不要复述选股卡片”不是筛选请求。
- “不是让我选股，我只想分析这家公司”不是筛选请求。
- “别重新筛选，直接解释它”不是筛选请求。

### 7.3 确认正确证据路径

路由修复后，不要立刻新增数据源。先验证现有 `ChatStockResearchEvidenceService` 是否已经取得：

- current_quote / recent_bars / metrics
- fundamentals
- earnings_quality
- financial_drivers
- a_share_information
- event_timeline
- business_structure（问题需要时）
- stock_workspace_context.research_entry
- evidence_debate / counter evidence

如果这些模块已经存在但 Prompt 没有保留，再修 compaction；不要重复调用供应商。

### 7.4 清理知识检索污染

对单股研究新增主体过滤：

- source key 明确绑定其他证券的文档直接排除。
- 用户多股票汇总文档只提取当前证券段落。
- 无法可靠分段的用户文档可保留为“用户背景材料”，但其数字不得进入数字守卫 trusted allowlist。
- 资料库文本中的研究方法可以召回；其他股票的行情、波动、估值和财务数字不能召回为当前标的证据。

建议为知识项增加或推导：

- `entities: [symbol...]`
- `numeric_trust: current_subject | contextual_only | none`
- `as_of`
- `source_tier`

### 7.5 修复数字主体绑定

短期可行修复：

- `stock_research` 的金融数字只从结构化 `evidence` 当前股票模块中建立 allowlist。
- `knowledge_context` 默认不作为行情、财务、估值和技术数字的可信来源。
- 只有知识项明确绑定当前 symbol、来源与日期时，才允许其数字支持回答。

中期方案：

- 建立 `NumericFact`：`entity / metric / value / unit / as_of / source_path`。
- 校验回答数字时结合附近指标词，例如“20日年化波动率”必须匹配当前标的 `metrics.annualized_volatility_20d_pct`。
- 价格、MA、回撤、收益、PE/PB、营收、利润、现金流等高风险字段优先做强绑定。

必须加入回归：

- 北方国际 evidence 为32.92%，用户资料含其他股票115.10%。
- 回答写115.10%必须失败。
- 回答写32.92%应通过。
- 回答说“超过30%”可以在有明确证据时通过。

### 7.6 修复结构化证据0/0/0

- 当 `structured_answer.status=unavailable` 时，页面不要显示事实0、反证0、待核验0。
- 可显示“本轮未生成结构化清单，正文与引用仍可阅读”。
- 不要从正文自动创建正式研究对象。
- 如果模型输出结构化合同，必须校验 citation 与 evidence path 后再展示。

---

## 8. 三个选股模板的真实 DeepSeek 抽样方案

用户特别要求继续抽样经营改善、相对行业增强和估值约束。每个模板至少完成一个真实候选，最好两个，并保存Run、Prompt、最终回答、截图和人工评分。

### 8.1 通用步骤

1. 使用生产数据库和真实 Tushare 数据执行模板。
2. 选取真实候选，不制造固定夹具作为真实模型验收结论。
3. 使用独立 evaluation 用户。
4. 将候选完整 `entry_context` 传给 `/me/deep-stock`。
5. 在股票AI页手动发送问题。
6. 记录：路由、symbol、ResearchPlan、模块状态、Prompt长度、首安全正文、总耗时、模型、Guard、回答文本。
7. 对照服务器报告和模块逐句核验。
8. 桌面和390px检查长回答阅读、证据折叠和刷新恢复。

### 8.2 经营改善候选

建议问题：

> 这家公司为什么进入经营改善候选？请不要只复述营收、利润和ROE三个筛选数字。结合最新公告、同报告期财务、经营现金流和主营结构，直接说明改善是否有质量；哪些只是机械变化，哪些原因还没有公司原文支持？最后给出最重要的反方证据。

通过标准：

- 直接回答“改善质量目前较强/一般/无法确认”的证据状态，而不是给推荐。
- 引用报告期和公告日。
- 引用经营现金流或明确说明字段确实缺失。
- 区分主营结构事实、财务机械影响和公司原文因果。
- 至少给一项反方证据。
- 不把历史增长自动外推为未来持续增长。

### 8.3 相对行业增强候选

建议问题：

> 这家公司近20日跑赢所属行业样本，但这种相对强势靠什么？请先说明已确认的价格事实，再用公告、财务、现金流和行业证据检查持续性。不要把跑赢行业本身当成原因，也不要把简单行业均值说成官方指数。

通过标准：

- 明确行业比较是同Tushare标签股票简单均值。
- 不用“资金认可、主力吸筹、趋势一定延续”等无证据因果。
- 说明公司自身事件和财务是否支持相对表现。
- 行业与公司数据日期一致或明确不一致。
- 至少给一项“价格强但基本面未确认”的反方条件。

### 8.4 估值约束观察候选

建议问题：

> 它进入估值约束候选，是不是说明便宜？请直接回答。然后结合PE、PB、报告期、盈利质量、经营现金流、负债和主营结构解释哪些事实支持继续研究，哪些证据不足以判断低估。

通过标准：

- 第一句明确“命中约束不等于低估”。
- PE/PB带市场时间。
- 没有历史分位时不能称低估。
- 同行样本不足时不能称行业最低或显著便宜。
- 必须讨论利润和现金流质量，不能只谈倍数。
- 必须给估值失效或需复核条件，但不能自创买卖阈值。

### 8.5 每个样本的评分表

| 维度 | 0分 | 1分 | 2分 |
|---|---|---|---|
| 直接回答 | 没回答问题 | 后文才回答 | 首段直接回答 |
| 当前标的绑定 | 混入其他股票 | 有模糊项 | 全部主体正确 |
| 公告事件 | 声称缺失或编造 | 仅列标题 | 结合原文边界分析 |
| 财务现金流 | 缺失/错数 | 仅复述筛选值 | 同报告期交叉解释 |
| 行业与估值口径 | 夸大 | 有边界但不完整 | 口径、样本、日期清楚 |
| 反方证据 | 无 | 泛泛风险 | 与当前逻辑直接冲突 |
| 可读性 | 模板化长报告 | 基本可读 | 普通投资者易读 |
| 数字可靠性 | 有错数 | 数字对但时间弱 | 数字、主体、日期均正确 |

单样本建议至少达到14/16，且“当前标的绑定、数字可靠性”不能低于2分，否则不算通过。

---

## 9. 基金产品事实扩充

### 9.1 当前已有能力

`app/services/fund_products.py` 已支持：

- 基金/ETF名称和代码搜索
- 单产品事实
- 2—5个产品比较
- 净值、累计净值、净值日期
- 1月、3月、6月、1年历史收益
- 同类排名字段
- 申购赎回状态
- 页面展示申购费率
- 最低申购/定投金额
- 风险等级上游字段
- 规模、份额、基金公司、基金经理、成立日期
- 持仓摘要
- ETF实时成交报价多源回退
- PostgreSQL快照和后台刷新

当前默认后台关注产品包括：

- 510300
- 510500
- 159915
- 110022
- 000011
- 000198

### 9.2 当前明确缺口

- 管理费、托管费、销售服务费和不同持有期赎回费不完整。
- ETF买卖价差、折溢价、跟踪误差/跟踪偏离缺少稳定历史序列。
- 指数编制规则、行业集中度、地区暴露和前十大持仓变化尚未系统化。
- 基金合同、招募说明书、定期报告、临时公告没有形成可检索全文证据库。
- 债券基金的久期、杠杆、信用等级、利率债/信用债/可转债占比没有结构化入库。
- 具体渠道的到账、资金可用和费用差异无法只靠公开产品页得出。

### 9.3 推荐建设顺序

1. 先补正式文件索引：基金合同、招募说明书、最新季报/年报、费率说明。
2. 从定期报告提取持仓结构、债券类型、信用等级、杠杆、久期和集中度。
3. 建立完整费用合同，区分产品费用、销售渠道折扣和交易费用。
4. 对指数基金/ETF建立指数、跟踪偏离、规模、成交额、价差和折溢价证据。
5. Agent回答时按用户问题只加载需要字段，不把整份产品档案塞入Prompt。

### 9.4 基金验收问题

- “510300和159915适合长期定投吗？先比较底层指数、集中度、费率、跟踪和回撤，再问我必要的个人条件。”
- “我没有证券账户，510300有没有对应联接基金？不要泛称所有ETF都有联接基金。”
- “110022当前完整费用是什么？哪些是产品固定费用，哪些取决于销售渠道和持有时间？”
- “这只债券基金为什么最近回撤？请区分利率、信用、可转债、杠杆和赎回压力，缺数据就明确说。”

---

## 10. 债券与利率数据扩充

### 10.1 当前状态

当前债券和利率能力主要是静态通用知识：

`app/knowledge/common/fixed-income-and-rates.md`

内容只有：

- 债券基本概念
- 利率上升/下降与债券价格的一般关系
- 久期概念
- 信用风险和流动性风险
- 分析债券基金应关注的维度

目前没有验证到独立的利率市场数据Provider、收益率曲线数据库、信用利差数据库或债券基金组合暴露流水线。

### 10.2 建议最小可用数据层

优先建设可公开、可授权、可持续刷新的事实：

- 中国国债关键期限收益率：1Y、3Y、5Y、10Y、30Y。
- 政策利率：逆回购、MLF、LPR等，保留生效日期。
- 信用利差代理：高等级与中低等级信用债相对国债/国开债。
- 可转债市场整体价格与估值代理。
- 债券基金净值、回撤和恢复路径。
- 基金季报中的债券类别、久期、杠杆、信用等级和可转债比例。

### 10.3 数据模型建议

至少分开：

- `rate_curve_snapshots`
- `policy_rate_events`
- `credit_spread_snapshots`
- `fund_fixed_income_exposures`
- `fund_document_index`

每条数据保留：来源、URL、字段口径、日期、抓取时间、授权边界和缺失原因。

### 10.4 Agent回答边界

- “降息利好债基”只能作为一般机制，不能自动解释某只产品当日涨跌。
- 没有久期和持仓结构时不能判断某基金对利率有多敏感。
- 含可转债、权益或低等级信用债的产品不能按纯利率债基金解释。
- 用户适合性仍需结合期限、流动性、回撤承受和已有资产。

---

## 11. 长会话效率与上下文治理

### 11.1 当前问题

`app/services/chat_context.py:443-445` 每次读取最近40条原始消息。

当前没有验证到成熟的滚动会话摘要或“长期事实/未解决问题/最近交互”分层机制。已有证据压缩主要压缩金融 evidence，不等于长对话记忆治理。

北方国际失败Run已经达到：

- Prompt 34,611字符
- 输入13,674 tokens
- 路由与取证36.616秒
- 总耗时65.509秒

其中大量成本来自错误执行全市场筛选和把12只候选送入Prompt。先修路由预计就能显著下降，但长会话仍需治理。

### 11.2 推荐上下文结构

把会话上下文分为四层：

1. 最近原始消息：例如最近6—10轮，保证当前语义连续。
2. 已确认用户事实：只含用户明确确认的风险画像、关注理由、持仓和偏好。
3. 对话摘要：模型可生成候选摘要，但需确定性检查，不得成为正式金融事实。
4. 未解决问题清单：当前股票、未核验证据、用户明确追问、上轮引用。

摘要不能包含：

- 自动确认的投资判断
- 自创价格阈值
- 未经用户确认的风险承受能力
- 把旧模型结论当事实

### 11.3 建议性能门槛

以下是 W26 建议目标，不是当前已达成结果：

- 缓存命中的单股混合研究：路由与证据 ≤ 12秒。
- 首个安全正文 ≤ 10秒；理想 ≤ 6秒。
- 常规单股Prompt ≤ 18,000字符。
- 不发送完整选股候选列表。
- 单轮只加载 ResearchPlan 选中的模块。
- 长会话刷新后仍恢复股票、研究阶段和未解决问题。

### 11.4 长会话验收

至少构造10轮连续对话：

1. 先问价格回撤。
2. 追问公告。
3. 追问现金流。
4. 追问主营结构。
5. 追问行业比较。
6. 追问“刚才说的现金流和公告是否矛盾”。
7. 切换到反方证据。
8. 刷新页面。
9. 继续问“最关键的未决问题是什么”。
10. 检查不重复旧回答、不丢标的、不混入其他股票。

记录每轮 Prompt长度、首正文、总耗时、召回文档和证据模块。

---

## 12. 直接事件证据建设

### 12.1 当前已有链路

已有：

- A股公告
- 新闻
- 社区情绪
- A股财报与F10结构化数据
- 事件脉络分类
- 后台 `a_share_information_refresh`
- 事件时间线快照
- 个股预生成报告
- `a-share-information` 和 `event-timeline` Skills

北方国际样本证明数据已经进入报告，但错误路由让Agent没使用它。

### 12.2 先做“使用正确”，再扩源

优先顺序：

1. 确保问题进入 `stock_research`。
2. 确保当前股票公告、事件和现金流进入Prompt。
3. 确保模型区分“事件存在”和“事件导致股价变化”。
4. 再扩展更多直接来源。

### 12.3 后续可扩展的直接证据

- 交易所问询函、监管措施和回复。
- 业绩说明会、投资者关系活动记录全文。
- 重大合同、中标、订单和项目进度。
- 回购、减持、增持、质押、解禁和融资。
- 诉讼、仲裁、处罚和审计意见。
- 业务所在国政策、汇率、商品价格和项目交付风险。
- 公司对营收、毛利和现金流变化的正式解释。

### 12.4 事件因果等级

建议统一为：

1. 直接确认：公司/监管文件明确说明影响。
2. 高相关：事件时间与业务暴露匹配，但公司未确认股价因果。
3. 研究假设：媒体或行业线索，需要进一步核验。
4. 无证据：不得写成原因。

回答必须明确所处等级。

---

## 13. 回答体验目标

用户已经明确不喜欢机械的：

- 候选事件
- 反方证据
- 失效条件
- 下一步核验

这些概念可以存在于内部分析和必要的证据折叠中，但不应强迫每个回答都用同一套栏目。

### 推荐回答结构

对“为什么跌、是否企稳”类问题：

1. 首段2—4句直接回答。
2. 已确认的最相关事实，按重要性而不是模块顺序。
3. 明确哪些原因仍不能确认。
4. 回答用户的第二问，例如“是否企稳”。
5. 最后只给1—3个真正有价值的核验动作。

### 北方国际理想回答方向

不要求使用固定原文，但应类似：

> 目前可以确认的是，北方国际最新一季报的营收和净利润分别同比下降约35.56%和37.54%，主营规模收缩是回撤期间最值得重视的基本面压力；但现有证据还不能证明它就是近20日下跌的直接原因。服务器已经取得投资者关系记录、向控股股东借款及关联交易等公告，也取得约2.16亿元经营现金流，但还需要阅读原文确认订单、项目交付、借款用途和现金流变化是否与市场担忧有关。近5日收益为0%只能说明短线暂时停止继续下跌，结合20日仍跌8.04%、60日回撤较深和量能不强，暂时不能称为企稳。

注意：以上只是示意。最终回答必须由当前DeepSeek依据本轮证据重新生成，不能保存为固定回复。

---

## 14. 测试与验收命令

### 14.1 定向测试

```bash
cd /Users/chr/Documents/qingtianshu
uv run --extra dev pytest -q \
  tests/test_research_plan.py \
  tests/test_chat_context.py \
  tests/test_symbol_routing.py \
  tests/test_agent_guard.py \
  tests/test_agent_evidence_compaction.py
```

### 14.2 全量和静态门禁

```bash
uv run --extra dev pytest --collect-only
uv run --extra dev pytest -q
uv run ruff check .
uv run python -m compileall -q app tests scripts
for file in app/static/*.js; do node --check "$file"; done
uv lock --check
git diff --check
```

### 14.3 浏览器门禁

```bash
uv run python scripts/browser_smoke.py \
  --base-url http://127.0.0.1:8773 \
  --artifacts artifacts/browser-smoke-w26-agent-evidence
```

浏览器门禁不能替代真实模型抽样。必须另外保存：

- 三个模板的真实候选
- 每个真实Run的JSON或数据库导出
- Prompt路径
- 最终回答
- 桌面和390px截图
- 人工评分表

### 14.4 运行状态

```bash
curl -fsS http://127.0.0.1:8773/health | jq
curl -fsS http://127.0.0.1:8773/ready | jq
```

---

## 15. 推荐的 W26 提交拆分

建议不要一次混入基金、固收和长会话所有改造。

### Commit 1：修复候选到单股Agent路由

- bound stock 默认优先
- 否定“选股”语境
- 真实问题回归测试
- evidence/Prompt断言

### Commit 2：修复知识主体和数字绑定

- 多股票用户文档过滤
- stock numeric allowlist
- 115.10%污染回归
- 结构化0/0/0展示修复

### Commit 3：三个模板真实模型验收与文档

- evaluation用户抽样
- 浏览器截图
- Run和评分
- README、验证、进度、handover更新

### Commit 4及以后：基金和固收数据增量

- 正式文件索引
- 完整费率
- 债券暴露和利率数据
- 长会话摘要与性能

每个提交都要显式暂存路径，排除：

- `.env`
- 数据库
- 日志
- `.runtime`
- 缓存
- 用户工作区
- `artifacts/`

提交后核对远端分支SHA。

---

## 16. 下一位 Agent 的首个90分钟行动清单

### 前15分钟

- 阅读本文件和 W25 文档。
- `git status --short --branch`
- 核对 HEAD 和远端。
- 确认服务和Worker。

### 15—35分钟

- 用真实北方国际问题补失败测试。
- 直接断言 `_is_stock_screen_query=true` 和 bound preference=false 的旧行为。
- 加入显式全市场筛选反向样本。

### 35—60分钟

- 修复 `chat_context.py` 和必要的 `chat_routing.py`。
- 跑定向测试。
- 检查新Prompt是否为 `stock_research`、symbol正确、mixed模块齐全。

### 60—90分钟

- 加入跨股票115.10%数字污染测试。
- 收紧知识数字信任边界。
- 运行一次隔离DeepSeek北方国际复测。
- 记录新Run和耗时；如果回答仍说没有公告/现金流，继续检查 compaction 和Prompt，不要增加固定回复。

---

## 17. 完成 W26 的定义

只有同时满足以下条件，W26才可称为完成：

- 北方国际真实失败问题稳定进入 `stock_research`。
- symbol、conversation scope 和ResearchPlan正确。
- Agent实际使用当前股票公告、财务、经营现金流和事件脉络。
- 不再把其他股票115.10%等数字用于当前股票。
- 经营改善、相对行业增强、估值约束各至少一个真实DeepSeek样本通过评分。
- 回答首段直接解决问题，不机械复述候选卡片。
- `structured_answer`不可用时不显示误导的0/0/0。
- evaluation验收不污染普通用户历史和正式研究空间。
- 定向测试、全量测试、静态检查和双视口浏览器门禁通过。
- 文档更新并进行范围受控的GitHub交付。

即使以上完成，整个长期目标仍不能标记完成，因为基金产品全文与费用、债券利率实时数据、长会话治理、商业数据授权、备份恢复和生产运维门槛仍未全部完成。

---

## 18. 最终交接结论

### 2026-07-29 连续主营结构追问续接结果

北方国际长会话中，用户先问主营结构，再追问“哪些业务最赚钱、哪些目前不能确认”。旧链路把“不能确认”误判为股价异动问题，专项证据压缩因此删除真实 `dimensions` 和 `key_changes`，模型没有看到“工程建设与服务、资源设备供应链”等公司真实分部，随后编造“货物贸易72%”。

本轮已完成以下修复：

1. `stock_price_move.py` 不再把“当前不能确认”等披露边界表达识别为股价异动问题。
2. 主营专项压缩保留完整分部、地区和毛利率数据，去除旧选股入口和季度净利润等无关线索，并新增 `coverage_limits`。
3. 主营回答必须使用至少一个当前公司的真实分部名称；多项错误数字会清除错误流式草稿并基于当前证据包触发一次聚焦重试。
4. 数字校验已支持“从12.043%跌到8.68%”“约9个百分点”“同比下降近4成”等正常金融表述，不再误删整段回答。
5. 主营 Skill 明确毛利贡献不能写成净利润，产品、地区、行业不能拼接，分部毛利率改善不能直接外推为整体盈利质量改善。

真实 Run `9f07844b-9410-48c6-98a6-f4c9c7ae2995` 为 `business_structure / completed / deepseek-v4-pro`：首 Token `2.744s`、首个可见正文 `3.194s`、总耗时 `9.438s`、Prompt `19,471` 字符；没有相关性重试和模型 Guard 重试，最终输出守卫通过。回答按“收入来源—结构变化—反方证据—当前不能确认”组织，并在864px页面中从回答顶部原位开始展示，无需用户向上拖动。

新增回归覆盖连续两轮主营追问、披露边界不触发价格异动压缩、真实分部进入第二轮 Prompt、错误数字聚焦重试、数字误杀修复、旧候选入口隔离和 `coverage_limits` 进入 Prompt。当前全量收集 `876` 项并执行至100%通过；Ruff、Python编译、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 均通过。

这次结果可以作为三个选股模板后续验收的技术范式，但不能替代真实样本闭环。下一步仍必须分别完成经营改善、相对行业增强、估值约束的真实候选、真实 Run、Prompt、最终回答、截图和人工评分；基金正式文件与完整费率、债券及利率数据、十轮长会话和直接事件原文证据仍是明确未完成项。

### 2026-07-30 GitHub 阶段交付断点

- 用户最新优先级是“对话质量还行即可，尽快上传”，明确不要求继续扩大 Guard。
- 估值 evaluation Run `a4f79acd-a7ca-4dae-9332-8f764986c8be` 已完成，`deepseek-v4-pro / api_calls=1`；首 Token `27.119s`、首可见正文 `27.664s`、总请求 `43.180s`。
- 最新代码保留模型丰富首稿，只对低 PE 成因、利润与经营现金流口径、销售收现率和资产负债率等确定性错误做局部修复；没有新增固定字数、段数或模板栏目要求。
- 上传前门禁为 `977 tests collected` 全量通过，Ruff、Python 编译、全部静态 JavaScript、依赖锁和差异检查全部通过。
- GitHub 交付必须显式排除 `.runtime/`、`artifacts/`、数据库、日志、缓存、用户工作区和真实 `.env`。W26 与长期目标仍保持进行中，不因本次阶段推送标记完成。

当前产品的核心问题不是“完全没有金融数据”或“Agent没有Skill”。恰恰相反，北方国际服务器端已经有公告、事件、财务、经营现金流、主营结构、股东和分析师预期等较丰富证据，ResearchPlan也能正确规划所需模块。

最影响对话体验的短板是：

1. 用户自然语言中的“选股”字样可能把单股研究错误切回全市场筛选。
2. 绑定股票会话没有成为足够强的默认上下文。
3. 错误路由使完整单股证据没有进入Prompt。
4. 多股票用户资料可能污染当前股票数字。
5. 数字守卫还没有验证实体、指标和日期归属。
6. Prompt过长和错误取证让用户等待超过一分钟。

因此，下一阶段应围绕“正确路由 → 正确证据 → 正确主体数字 → 直接易读回答 → 真实模型验收”推进。修好这条链，比继续添加更多页面卡片或Guard规则更能改善产品体验。

### 2026-07-30 阶段上传补充

- 用户明确认为“失效条件”难懂；对外已统一改成“什么时候需要重新判断”或“什么情况会推翻当前判断”。底层字段未改，历史数据与 Hermes 输出保持兼容。
- Hermes 集成代码、流式桥接、Prompt/Guard 链、质量审查 Skill、测试和 `.env.example` 随本阶段一起提交；真实密钥、`.runtime/` 和 `artifacts/` 必须继续排除。
- 本次目标是尽快完成阶段 GitHub 交付，不继续扩大 Guard。W26 与长期产品目标仍保持进行中。

### 2026-07-31 个股涨跌原因追问收口

- 已修复“那你现在最有把握能确认什么”退化为综合诊股的问题。连续追问继承上一轮 `price_cause`、历史目标交易日和六个相关证据模块，不再加载股东、同行、估值或展望模块。
- “现在最有把握”被识别为对证据把握的追问，不再强制插入当前报价。历史指定日期的模型证据会移除更新报价和更新日线。
- 第二轮使用 `followup_answer_frame`，只保留目标日相对市场表现、目标日公开事件时间关系和行业/公司无法二选一的关键缺口；邻近日解禁、整套财务和技术指标不进入追问 Prompt。
- 最新真实首问 Run `b456863e-d6b5-47b1-87e5-2cc45798e9a6` 与追问 Run `091f050d-c0b7-4514-b6c7-7991e321e878` 均为 `completed / deepseek-v4-pro / api_calls=1`，最终 Guard 均未介入。追问 Prompt 为 14,947 字符，较失败样本约 94k 明显下降。
- 追问最终为三个自然短段落，没有标题、最新报价、技术分析、估值、股东、同行和解禁猜测；只回答两项确认事实与一项不能确认。
- 上传门禁更新为 `1035 tests collected` 全量通过，Ruff、Python 编译、全部 JavaScript、依赖锁和差异检查均通过。
- 后续继续以真实多轮对话抽样为主，优先改善证据选择和 Prompt 冲突；不要因为局部措辞问题重新堆叠大规模 Guard。当前独立 Worker 离线、`/ready=503`，恢复完整运行态时需要另行启动 Worker。
