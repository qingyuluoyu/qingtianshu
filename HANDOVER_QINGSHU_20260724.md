# 清数智算 MVP 交接说明（2026-07-24）

## 最新续接断点（2026-07-28）

本节是当前续接入口；与下方 2026-07-24 运行快照冲突时，以本节和 `VERIFICATION.md` 顶部最新工作包为准。

- 权威仓库仍为 `/Users/chr/Documents/qingtianshu`，分支仍为 `codex/structured-ai-writeback`。工作树包含连续开发修改，禁止清理、重置或覆盖未审查内容。
- 当前 Web 与独立 Worker 已重新加载最新代码。`/health=200`、`/ready=200`、Hermes 启用、领域库为 PostgreSQL Schema v5，运维/持久化队列为 Schema v4；Worker 默认双并发，active Worker 为 2，ready/delayed/retrying 均为0。54项数据健康为53 healthy、1 attention、0 critical。
- 当前自动化基线为 `822 tests collected` 且全量通过；Ruff、Python `compileall`、全部静态 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。仓库最新双视口报告 `artifacts/browser-smoke-w21-review-detail-retry-final/result.json` 为20通过、0失败。
- 巨型模块治理已经完成多批抽取：API 输入模型、李总策略公共输出、Agent 意图回答合同、Hermes 执行协议、Prompt 证据压缩、金融输出校验/修复，以及证券识别、选股参数、市场/个股上下文恢复等 Chat 规则均已迁移到独立模块。当前 `main.py` 3,299行、`agent.py` 1,393行、`db.py` 5,678行、`demo.html` 1,092行、`qs-agent-ui.js` 995行；`db.py` 与主入口仍需继续按领域拆分，不能把当前抽取视为治理完成。
- 透明选股改为“按条件选股 / 李总策略”互斥页签。条件筛选不再在进入页面时自动进行全市场计算；李总策略按需加载。页签状态写入 URL，刷新恢复通过；桌面和 390×844 均无双区堆叠或横向溢出。
- 李总策略当前按用户最新要求采用零交易成本、任意两次换仓至少间隔10个交易日、冷却期只执行最新候选集合；沪深300只在策略持仓期参与比较，策略空仓期同步冻结。`li_zong_2w_no_cost_same_exposure_v4` 三档生产结果均已发布，以第46节为准；不得沿用第41节连续基准数字。
- 最近真实 Agent 验收继续以 DeepSeek `deepseek-v4-pro` 即时生成。个股涨跌问题只显示一份最终回答和折叠引用，不再自动附加大块结构化报告；同日公司公告、行业与市场事实、媒体线索和收盘后事件按时间边界分开。美股历史原因问题会从更大的已存资讯窗口优先召回目标交易日，多来源线索、反方证据和下一步核验均已在真实对话中验证。
- 金融顾问/教育者第二阶段已经完成：除知识检索外，已增加基金/ETF产品事实库、同口径比较、后台定时刷新和七项风险问卷；草稿必须由用户明确确认后才进入 Agent。真实产品数据、浏览器和 DeepSeek 验收见第48节。
- 证券证据双路径已经完成：当前用户自选股走 `watchlist_preanalysis`，允许按时效复用慢变模块；非自选股走 `online_research`，强制刷新快变证据且不得读取服务器预生成报告。两条路径都必须由当前 DeepSeek Run 重新生成回答，完整合同和真实 Run 见第47节。
- “我的关注”和“个股研究”已共用真实K线组件；关注页进一步按“行情列表 → K线详情 → 下一步研究 → 研究报告”重排，报告区支持滚轮浏览，关键字号提升，390px无横向溢出。内部来源、后台任务和供应商错误不得显示给普通用户。
- W7 已完成透明选股与复盘首次用户路径修复：“通用截面筛选”改为“按条件选股”，李总策略使用中文可核验标签且390px结果分类自动换行；空交易复盘提供“去我的关注”；市场复盘从文章已保存证据重建A股广度、涨跌中位数、成交额和领先板块摘要，并删除内部版本规划。双视口长期门禁现为20通过、0失败，见第50节。
- W8 已把市场总览重排为“全球行情 → A股一眼看懂 → 四个核心指数 → 市场Agent → 市场复盘文章 → 个人待办”，并把条件选股的使用说明、覆盖详情和候选动作重新分层。真实桌面与390px验收、完整门禁和当前断点见第51节；最新浏览器报告为 `artifacts/browser-smoke-w8-market-screening-clarity/result.json`。
- W9 已完成个股涨跌问答的证据裁剪、轻量当前报价路径和日期对齐事件合同；标准问题不再加载完整研究报告或低价值榜单，盘中报价、完整日线和目标交易日保持独立时间锚。
- W10 已修复模板回退、自然舍入、清理后首段丢失和低价值媒体线索扩写。真实 Enter 问答、刷新恢复和390px已复验；无直接原因证据时，回答必须停在“具体驱动仍未确认”，不得用市场买卖力量、资金博弈或缺少利好替代。
- W13 已把基金、ETF和退休理财历史从个股/选股中拆出。新回答持久化 `conversation_scope`，旧历史按产品语义兼容重分类；真实67条历史、刷新恢复、桌面和390px均通过，见第56节。
- W14 已把左栏历史分为“最近8个主题 / 全部历史”，完整搜索、分类和移动端下拉仍覆盖67条；真实展开、收起、顶部恢复和390px均通过，见第57节。
- W15 已把风险画像改为“回答7题 / 保存并核对 / 用户确认”三步，增加回答进度、缺失问题定位和确认后“去问金融顾问”入口；入口只预填不自动发送，真实浏览器没有替用户保存或确认，见第58节。
- W16 已完成已确认画像到真实 DeepSeek 条件化回答；W17进一步修正基金/ETF概念关系、到账与交收边界、纯概念问题误入适合性路径，以及 Markdown 编号被数字守卫误删的问题。W18把个股研究概览从证据堆叠收敛为判断、变化和任务三层摘要，完整证据保留在独立标签。隔离真实 Run、完整回归与浏览器证据见第59—61节。
- W19把没有交易复盘记录时的无效搜索、状态筛选和重复CTA合并为单一三步引导；有筛选空结果时仍保留清除筛选路径。首次Runner虽通过但人工截图发现移动端文字被挤成竖排，最终门禁已增加步骤卡最小宽度检查，见第62节。
- W20修复真实个股长回答的安全首屏时延、因果整段误删、技术口径直出和evaluation刷新隔离；W21进一步重构有记录复盘详情，并统一清理价格/计划重复、未经证实的心理标签、内部术语、双重JSON和自创阈值。真实模型、候选、浏览器和全量证据见第63—64节。
- 最新保守评分仍不因单页优化而上调：功能设计 8.0、架构设计 8.2、实际可用性 7.2、内部 MVP 综合 7.7、正式对外长期使用 6.2；Agent 7.8、前端信息架构 7.8、工程质量 8.0。运维自检仍缺少可核验 PostgreSQL 备份，不能宣称正式生产就绪。

下一批代码治理优先顺序：

1. 扩展基金公告、定期报告全文、费率全口径、指数跟踪误差和更多产品品类；当前免费公共接口没有 SLA 或商业授权，不能宣称完整财富管理数据平台。
2. 继续扩展证券正式披露、全文和高质量媒体来源，并统计时效、失败率、交叉验证覆盖；同行比较缺失不得拖垮其他可用模块。
3. 将 `main.py` 的 Chat 编排、`db.py` 领域仓储和 `demo.html` 页面模块继续拆分，但不得为减行数删除金融证据、历史恢复和用户确认边界。
4. 每个工作包都要保留桌面、390px、失败恢复和真实模型抽样；不得用接口通过代替普通用户体验。

## 1. 交接目的

本文件供下一位 Agent 直接接续“清数智算”主线开发。当前阶段已经完成李总策略近期无前视历史回放、研究型与交易型 R1 闭环、诊大盘/诊个股、自选股每日研究摘要、通用选股数据合同、核心页面双视口发布门禁，以及核心功能/数据/算法/Agent 架构 Word 报告。

2026-07-26 新增断点：李总策略组合级长期回测已经进入真实数据建设。代码、PostgreSQL 表、后台持久任务、API 和前端 3个月/1年/3年区间均已建立；历史市值按交易日全市场截面获取，逐日规则状态与组合净值分开持久化。首批近3个月所需63个历史市值交易日已真实写库，识别出1699只在该区间至少一次严格超过150亿元的股票。逐股无前视状态仍需后台继续补齐，未完成前页面不会发布收益率。架构与后续优化清单见 `docs/PRODUCT_PIPELINE_ARCHITECTURE.md`。

2026-07-27 最新状态：三档长期组合回测已经完成数据修复、重算、独立复核和正式发布。旧的“候选变化即换仓、计入10bp成本、沪深300全程持有”结果仅保留为历史记录；当前唯一有效口径和结果见第40节。

下一位 Agent 不应重新设计已经完成的李总历史回放。结构化 AI 写回、交易复盘、诊断入口、自选股每日摘要、选股数据合同和浏览器 Runner 不得回退。阶段 0 权威口径统一、确定性后端完整 R1 集成链、桌面真实交易复盘链和核心双视口门禁已经完成；后续按正式 PRD 继续提升选股与个股分析可信度，并补生产底座。

## 2. 权威目录与 Git 状态

- 权威仓库：`/Users/chr/Documents/qingtianshu`
- 当前分支：`codex/structured-ai-writeback`
- 远端：`https://github.com/qingyuluoyu/qingtianshu.git`
- Git 上传指南：`/Users/chr/Documents/青树金融交易/GitHub仓库分支与文件上传使用说明.md`
- 产品任务清单：`/Users/chr/Documents/qingtianshu/同事设计与当前实现对比及待完成清单_20260722.md`
- 范围与取舍基线：`/Users/chr/Documents/qingtianshu/清数智算_现状评估与分阶段实施规划_20260724.md`
- 旧清单镜像（只读参考）：`/Users/chr/Documents/青树金融交易/qingshu-agent-demo/同事设计与当前实现对比及待完成清单_20260722.md`
- 七页高保真基准：`/Users/chr/Documents/青树金融交易/AI金融Agent产品方案/清数智算_七页高保真交互演示.html`

本文件不冻结“工作树是否干净”这一易变化状态；接续时必须先检查 `git status --short --branch` 和最近提交。禁止使用 `git reset --hard`、`git clean -fd`、强制推送或批量覆盖。提交前必须逐项审查并显式暂存。

## 3. 当前运行态快照

快照时间：2026-07-24 12:50（Asia/Shanghai）。除健康状态与进程外，下方李总处理数字保留自早期阶段快照；运行数字会继续随后台 Worker 增长，后续引用时必须重新读取，不能把本节数字当成永久常量。

- 服务地址：`http://127.0.0.1:8773/demo`
- 健康检查：`http://127.0.0.1:8773/health`
- 当前进程：PID `62780`
- 当前启动命令：`.venv/bin/python -m app --host 127.0.0.1 --port 8773 --no-browser`
- 数据库：`/Users/chr/.qingshu/qingshu.db`
- `/health`：`ok`
- Hermes：启用
- 默认文字模型：`deepseek / deepseek-v4-pro`
- 数据健康：`54/54 healthy`
- 通用后台任务：运行中
- 李总策略独立 Worker：运行中，默认每 30 秒推进

李总策略当前截面：

- A股名单：`5530/5530`
- 市值严格大于150亿元：`1216`
- 市值未达标：`4310`
- 缺少当日市值：`4`
- 可深度核验：`1169`
- 已深度处理：`1168`
- 剩余深度重试：`1`
- 明确未满足：`5471`
- 数据不完整：`59`
- 当前严格候选：`0`
- 当前触发：`0`
- 近期80交易日无前视回放：`234/1169`
- 已发布去重历史事件：`23`

当前规则漏斗：

`5530 → 1216 → 226 → 202 → 20 → 15 → 5 → 0`

最后5只股票均未通过“近10个交易日无跌幅达到或超过5%的阴线”。这说明当前空候选主要来自规则交集，而不是系统没有运行或完全缺少数据。

## 4. 标准安装与启动

### 4.1 推荐使用 uv

```bash
cd /Users/chr/Documents/qingtianshu
uv sync --frozen --extra dev
cp .env.example .env
uv run qingshu-start
```

服务器或不自动打开浏览器时：

```bash
uv run qingshu-start --host 0.0.0.0 --port 8773 --no-browser
```

### 4.2 标准 Python

```bash
python3 -m venv .venv
.venv/bin/pip install -e '.[dev]'
.venv/bin/qingshu-start --host 127.0.0.1 --port 8773
```

### 4.3 Docker

```bash
docker compose up --build -d
```

Docker 默认不启用 Hermes。需要模型能力时，应在部署环境安全配置 Provider 和密钥，不能把密钥写入仓库、README、测试输出或日志。

运行数据默认写入 `~/.qingshu`，不是仓库内固定绝对路径。README 已说明 uv、标准 Python、Docker 和服务器启动方式。

## 5. 本阶段完成的核心工作

### 5.1 李总策略近期无前视历史回放

新增：

- `app/services/li_zong_history.py`
- `tests/test_li_zong_history.py`

主要能力：

1. 默认回放每只股票最近80个可计算交易日。
2. 每个信号日只传入该日及以前的日线。
3. 使用信号日对应的历史 `daily_basic` 市值，禁止用当前市值回推历史。
4. 年度 ROE 和股东结构只接受公告日不晚于信号日的数据。
5. 连续候选或连续触发按阶段去重，避免同一段行情每天重复生成事件。
6. 保存信号后5/10/20个交易日的个股收益、沪深300收益、超额收益、最大上行、最大下行和累计路径。
7. 基准缺失或观察期不足时保持 `partial`，不猜补结果。
8. 回放结果保存到版本化 `tushare_dataset_snapshots`，数据集键为 `li_zong_history`。

边界：

- 当前是近期80交易日点时回放，不是多年全市场回测。
- 未计交易成本、涨跌停可成交性和实际成交价。
- 历史表现只用于复盘，不能直接推断未来收益。

### 5.2 空候选页面不再是空白

涉及：

- `app/main.py`
- `app/services/li_zong_strategy_service.py`
- `app/services/background.py`
- `app/static/demo.html`

已经实现：

1. 当前严格候选为0时，展示逐规则漏斗。
2. 展示历史真实候选/触发卡片，而不是演示数据或“接近候选”伪装。
3. 每张卡片展示信号日期、当时触发、信号后5/10/20日个股与沪深300表现。
4. 展示个股和沪深300累计路径双线。
5. 支持查看当时完整逐规则证据。
6. 支持“让 Agent 复盘”，把历史事件作为证据装入新一轮即时模型回答。
7. 页面历史区域可滚轮连续浏览。
8. 后台 Worker 即使仍有单只深度重试，也继续小批量推进历史覆盖，避免长期停滞。

接口：

- `GET /v1/stock-strategies/li-zong/history`
- `POST /v1/stock-strategies/li-zong/history/runs`（管理员/后台）

### 5.3 Agent 必须即时生成

李总历史卡片、预生成研究报告、历史对话和资料库内容只能进入本轮证据上下文，不能直接作为最终回复返回。

当前链路：

1. 识别市场/股票/行业/研究意图和多轮上下文。
2. `ResearchPlan` 选择实时模块、稳定证据、资料库和 Skills。
3. 确定性服务刷新行情、财务、事件、策略和历史证据。
4. Hermes 调用 `deepseek-v4-pro` 即时生成。
5. 数字与语义校验后原位完成最终稿。
6. 判断、任务、计划和复盘仅作为结构化候选；用户确认后才写入正式对象。

必须保留的产品要求：

- 不得把已保存报告或历史回答直接冒充本轮答案。
- 回答要区分事实、推断、反方证据、信息缺口和失效条件。
- 金融数字来自确定性数据或算法，不能让模型编造。
- 不自动交易，不生成目标价、仓位、未来概率或收益承诺。

### 5.4 结构化 AI 写回与交易复盘改动

以下改动属于本分支此前已经进行、必须保留的工作：

- `app/services/structured_ai.py`
- `app/services/trade_workflow.py`
- `tests/test_demo_action_plan_contract.py`
- `tests/test_trade_workflow_api.py`
- `RUIZHI_BRANCH_ABSORPTION_CHECKLIST_20260723.md`

本阶段已进一步接通结构化判断、任务、计划与复盘候选。下一位 Agent 在合并或重构时不能只保留李总历史文件而丢失这些改动。

## 6. 李总策略确定性规则

候选池要求以下9条全部通过：

1. 最近完整交易日总市值严格大于150亿元。
2. 最近连续5个已披露完整年度 ROE 每年均不低于10%。
3. 最新已披露十大股东/十大流通股东中，非自然人股东去重数量至少5名。
4. 最近约240个交易日至少6次收盘涨停。
5. 最近约240个交易日至少出现一次连续两日收盘涨停。
6. 最近10个完整交易日至少一次收盘涨停。
7. 最近10个完整交易日没有 `close < open` 且 `pct_chg <= -5%` 的阴线。
8. 最近20个交易日中至少有一天创最近360个交易日复权新高。
9. 连续3个交易日成交量均达到序列开始前20日均量的2倍。

候选通过后，以下三项任一通过则进入盘后人工复核触发池：

1. 当日收盘涨停。
2. 当日相对前收盘高开至少3.5%，且收阳。
3. 当日振幅严格大于9.8%，且收阳。

不要擅自放宽原始严格规则。下一阶段建议增加独立“接近满足观察池”，明确标注 `8/9`、`7/9` 等通过数量和失败原因，但不能把观察池股票称为李总策略候选。

## 7. 数据与算法支撑

### 7.1 数据来源

- 全球指数与黄金：腾讯、东方财富、Yahoo、新浪 XAU。
- A股全市场：Tushare兼容服务，覆盖名单、市值、日线、复权、真实涨跌停价。
- A股财务：Tushare 为主，东方财富 F10 补结构化年度 ROE 和三表证据。
- 股东与分析师预期：Tushare、东方财富。
- 公告、新闻和情绪线索：正式披露聚合、东方财富、新浪、股吧。
- 美股：SEC EDGAR、Nasdaq、腾讯等。
- 用户研究：SQLite 版本化快照、用户股票空间、判断、任务、操作和复盘。

### 7.2 已实现算法

- 收益率、MA20/60、RSI14、MACD、ATR14、布林带、量比、波动率和回撤。
- A股涨跌家数、成交额、涨跌分布分位和行业成分广度。
- 财报质量、利润与经营现金流驱动、三表勾稽。
- 同行业/同行同报告期、同口径比较。
- ResearchPlan 按问题选择行情、财务、股东、主营、预期、事件和估值模块。
- 李总策略确定性规则、点时历史回放和结果路径。
- 结构化判断、任务、计划与复盘候选写回。

### 7.3 当前数据边界

当前公开数据可以支撑 MVP，但不能保证：

- 交易所级逐笔、Level-2、集合竞价和完整盘口。
- 零延迟、生产级授权的全球行情。
- 完整授权新闻、研报全文和券商专有数据。
- 全历史机构持仓与长期行业样本变更。
- 商业 SLA、数据许可台账和多源自动故障切换。

商业化必须采购授权数据并建立许可、监控、灾备和数据质量体系。

## 8. 核心功能与架构 Word 报告

最终文件：

`/Users/chr/Documents/qingtianshu/清数智算MVP核心功能数据算法与Agent架构报告-20260724.docx`

构建脚本：

- `scripts/build_core_architecture_report_20260724.py`
- `scripts/build_core_architecture_visual_report_20260724.py`

报告共10页，包含：

1. 执行摘要。
2. 核心产品功能。
3. 李总策略真实漏斗。
4. 历史信号与沪深300后续走势。
5. 金融数据来源和边界。
6. 确定性算法。
7. Hermes/DeepSeek 即时生成链路。
8. 系统架构。
9. 安装、验证和下一阶段。

报告使用高分辨率中文页面图片嵌入 DOCX，原因是隔离 LibreOffice 环境无法稳定渲染 DOCX 原生中文字体。最终 DOCX 已用标准文档渲染工具生成10页 PNG 和 PDF，并逐页检查：中文完整、无溢出、无重叠、截图清晰、底部与页脚无冲突。

报告运行数字从数据库动态读取。最后一次报告冻结快照为历史回放 `229/1169`、23个事件；后台随后已经增长，不能要求报告数字与实时数据库永久一致。

内部 QA 目录：

`/tmp/qingshu_visual_report_render_final_v3`

该目录属于临时证据，不是正式交付物。

## 9. 自动化与浏览器验收

本阶段最近一次完整自动化记录：

- `558 passed`
- `ruff check app tests scripts` 通过
- Python 编译通过
- 内联 JavaScript `node --check` 通过
- `uv lock --check` 通过
- `git diff --check` 通过
- `tests/test_portability.py` 通过

本次又单独运行李总策略、服务与历史回放测试，全部通过。

提交前仍必须重新执行完整命令，以覆盖最后的文档和脚本改动：

```bash
cd /Users/chr/Documents/qingtianshu
.venv/bin/pytest -q
.venv/bin/ruff check app tests scripts
.venv/bin/python -m compileall -q app tests scripts
git diff --check
uv lock --check
.venv/bin/pytest -q tests/test_portability.py
```

内联 JavaScript 检查应沿用 `VERIFICATION.md` 中现有提取方式，不能只检查独立 `.js` 文件，因为当前主要前端脚本仍内嵌在 `app/static/demo.html`。

真实浏览器已检查：

- 个股研究空间整体页面正常。
- 李总策略漏斗正常。
- 历史信号卡片正常。
- 个股与沪深300曲线正常。
- 页面滚轮浏览正常。
- 截图已进入最终 Word 报告。

下列条目是本交接早期阶段的历史断点，其中“查看当时证据”“让 Agent 复盘”
“接近满足观察池”和诊断入口/整轮失败恢复均已在后续章节完成。当前仍需补做的是
包含真实操作、后续完整交易日与用户确认复盘的可重复 R1 E2E：

- “查看当时证据”按钮级完整 E2E。
- “让 Agent 复盘”按钮级完整 E2E，包括模型即时生成、引用和失败恢复。
- 接近满足观察池的页面和 Agent 问答。
- 多会话历史、诊大盘、诊个股、选股、个股空间之间的完整 R1 E2E。

## 10. 历史未完成主线（已由后续章节与新规划取代）

> 本节保留早期决策，不再作为执行队列。当前范围和顺序以
> `清数智算_现状评估与分阶段实施规划_20260724.md` 第 11 节为准。

### P0-1 接近满足观察池

目标：严格候选为空时仍提供有用研究对象，但不篡改策略。

建议实现：

- `9/9`：严格候选。
- `8/9`：接近满足，突出唯一失败规则。
- `6-7/9`：研究观察，列出缺口和失败项。
- `data_incomplete`：单独分区，不能与规则失败混排。
- 支持按通过数量、失败规则和最新信号排序。
- Agent 必须明确这些股票不是当前严格候选。

### P0-2 统一整体前端

以七页高保真 Demo 和正式 PRD 为准：

- 收敛导航、字号、颜色、卡片密度和信息层级。
- 今日观察不占据不合理的首页主导位置。
- AI研究采用 DeepSeek 式多会话布局，支持历史对话。
- 诊大盘、诊个股使用同一 Agent，但右侧为更干净的专用研究页面并自动联动 K 线。
- 用户不可见后台任务名、数据源失败、内部枚举和调试信息。

### P0-3 选股与个股研究闭环

- 继续按《个股功能增强_主Agent实施说明.md》和 Tushare 说明推进。
- 打通选股结果 → 保存线索 → 股票长期空间 → 即时 Agent → 判断/任务 → 后续变化 → 复盘。
- 强化财报质量、现金流驱动、行业同行、估值、反证和下一证据。
- 自选股中兴通讯、中际旭创、英伟达继续保持服务器预生成报告，但报告只作为即时对话证据。

### P0-4 用户订阅与今日观察联动

- 李总策略订阅、通知偏好、触发去重和人工复核任务。
- 严格候选或触发进入今日观察。
- 不自动写入持仓、不自动生成交易动作。

### P0-5 Agent 可用性

- 降低等待时间。
- 某个证据模块失败时独立重试或降级，不让整轮研究直接失败。
- 保证 Enter 发送、多会话保存、流式原位更新和最终回答不跳成另一段固定文案。
- 继续用真实问题测试回答差异，不能依赖固定市场模板。

### P1 生产底座

- PostgreSQL/正式数据库集群。
- 独立任务队列和 Worker。
- 认证、权限、限流、监控、灾备和多实例一致性。
- 数据授权和商业 SLA。
- 完整交易成本、可成交性与长期样本评估。

## 11. 历史建议起手顺序（不再执行）

> 下列命令和顺序只保留为历史记录。观察池、诊断入口、失败恢复和自选日报已经完成；
> 当前起手顺序是阶段 0 权威口径统一，然后审计并封口真实操作—复盘 R1 E2E。

1. 完整阅读本文件、`README.md`、`VERIFICATION.md` 和任务清单第17节。
2. 查看 `git status` 和 `git diff`，确认没有覆盖已有结构化 AI 改动。
3. 重新读取 `/health` 和数据库，不复用本文件中的旧动态数字。
4. 运行完整测试和静态检查。
5. 更新任务清单中的实时快照和验证结果。
6. 显式暂存本阶段文件，执行 `git diff --cached --check`。
7. 提交并推送 `codex/structured-ai-writeback`。
8. 推送成功后优先实现“接近满足观察池”，随后做统一前端和选股/个股研究闭环。

建议提交命令：

```bash
git add README.md VERIFICATION.md HANDOVER_QINGSHU_20260724.md \
  RUIZHI_BRANCH_ABSORPTION_CHECKLIST_20260723.md \
  app/db.py app/main.py app/services/agent.py app/services/background.py \
  app/services/li_zong_strategy_service.py app/services/li_zong_history.py \
  app/services/structured_ai.py app/services/trade_workflow.py app/static/demo.html \
  tests/test_demo_action_plan_contract.py tests/test_li_zong_history.py \
  tests/test_li_zong_strategy_service.py tests/test_trade_workflow_api.py \
  scripts/build_core_architecture_report_20260724.py \
  scripts/build_core_architecture_visual_report_20260724.py \
  '清数智算MVP核心功能数据算法与Agent架构报告-20260724.docx'

git diff --cached --check
git status --short
git commit -m "feat: add point-in-time Li Zong history and architecture report"
git push -u origin codex/structured-ai-writeback
```

提交前应根据真实 `git diff` 调整显式文件列表，不能机械复制后遗漏或误加文件。

## 12. 必须遵守的边界

- 不把模型回答存档直接当成当前回答。
- 不让 LLM 编造金融事实、阈值、目标价或收益概率。
- 不用未来财务、未来股东或当前市值回推历史信号。
- 不把数据不完整误写成规则失败，也不把规则失败包装成候选。
- 不自动交易、不自动下单、不自动生成仓位。
- 不在前端展示内部错误、供应商异常和后台任务名称。
- 不写入或输出用户 Token、Hermes 密钥和数据库隐私数据。
- 不为局部守卫或低收益细节长期偏离正式 PRD 主线。
- 任何阶段性完成都必须更新 README、验证记录、任务清单和 Handover，并推送 GitHub。

## 13. 2026-07-24 后续主线断点：诊大盘、诊个股与失败恢复

本节晚于前文所有运行态和“建议起手顺序”，接续时以本节和实时 `git status` 为准。李总历史回放、严格观察池、研究型 R1、报告和前三次 GitHub 阶段交付已经完成；不要再执行第 11 节中针对旧工作包的机械暂存命令。

本轮新增：

1. 全局 AI 新会话提供“诊大盘、诊个股、智能选股”一级入口。
2. 诊大盘始终新建独立对话并即时调用 Hermes；诊个股先通过稳定股票范围精确解析，再进入该股票唯一绑定长期会话并自动生成。
3. 股票会话中的市场问题不再更新股票七阶段研究状态。
4. Hermes 未连接时不返回或保存冒充 AI 的确定性摘要；页面显示明确失败和原问题重试。非完整模型终态显著标为降级证据摘要。
5. 真实浏览器已完成大盘、贵州茅台、歧义保护、透明选股、刷新恢复以及 `HERMES_ENABLED=false` 故障注入。
6. 全量 569 项测试和静态门禁通过。

当前仍应优先推进：

- 自选股日常跟踪与预生成报告的更直接用户入口，减少“数据已经存在但普通用户不知道在哪里使用”的断层。
- 将市场复盘升级为有市场状态、观点版本、后续验证和用户确认链的正式对象。
- 为单个失败证据模块增加独立重试，而不是重跑整轮 Agent。
- 继续吸收 PRD 中高收益、低复杂度的前端能力；暂缓 Next.js 全量重写、商业数据授权、正式消息投递和多年交易成本回测。

继续开发前运行：

```bash
cd /Users/chr/Documents/qingtianshu
git status --short --branch
.venv/bin/pytest -q
.venv/bin/ruff check app tests scripts
.venv/bin/python -m compileall -q app tests scripts
uv lock --check
.venv/bin/pytest -q tests/test_portability.py
curl -fsS http://127.0.0.1:8773/health
```

金融与产品边界不变：即时回答只能由本轮 Hermes 生成；历史报告和确定性摘要只能作为证据。任何自动写入正式判断、任务、计划或复盘仍需用户确认；不自动交易，不提供目标价、仓位或确定性收益预测。

## 14. 2026-07-24 后续主线断点：自选股每日研究摘要 V1

本节晚于前文全部断点。诊大盘、诊个股、智能选股入口和整轮 Hermes 失败恢复已经在上一阶段提交并推送；本轮完成“我的关注 → 报告证据 → 即时复核”的日常入口。

本轮新增：

1. `/v1/stock-workspaces` 的每个当前用户资产直接返回 `report_meta`、`report_freshness`、报价/完整日线/财务报告期/报告时间、反方证据、失效条件、下一证据，以及用户隔离的变化 `link_id/read_at/relevance_status`。
2. 页面不再通过全局 `/research-reports?limit=20/100` 猜测当前用户报告；即使存在 25 只非自选报告，用户自选报告仍稳定显示。列表读取只读数据库，不触发 `ResearchReportService.generate()`。
3. “今日研究更新”在首屏显示报告状态、生成时间、完整日线截止日、公共证据快照边界、最新变化和已读/相关性入口；`completed/partial/failed/missing` 不再混写成“最新完整报告”。
4. “阅读报告”改从用户隔离的股票工作空间读取，正文下同时展示六维证据来源与时间、最多三条反方证据、失效条件和下一证据；读取缺失报告时明确说明不会同步生成。
5. “用最新数据复核”进入该股票唯一绑定会话；“让 Agent 即时汇总”新建全局研究会话。两条路径都要求新的 Hermes Run，预生成报告只作证据，不能直出成当前回答。
6. 首轮真实 Hermes 验收主动发现问题被路由为普通 `research_actions`，只返回单一“数据截止时间”。现增加专属“自选股每日研究摘要”路由、丰富用户范围证据和时间合同；模型必须逐只分开写最新报价、最近完整日线与最新财务报告期。
7. 最终真实会话 `/research/0b9cb63d-d037-447f-a42b-c4d008a8daca` 覆盖杰瑞股份、中兴通讯、英伟达、中际旭创；4 只均显示三个独立时间锚、研究优先级、反方证据/压力、失效条件、下一任务和 5 项引用。刷新后同一会话完整恢复。
8. 全量 `572 tests collected` 并全部通过；Ruff、Python 编译、内联 JavaScript、`uv lock --check`、可移植性测试和 diff 空白检查通过。运行态 `/health=ok`、Hermes 启用、数据健康 `54/54`、后台和李总 Worker 正常。

当前边界：

- 服务器预生成报告仍是公共证据快照，不是私人报告；正式判断、任务和变化反馈只存在用户隔离空间。
- 当前报告多数为 `degraded/partial`，页面已诚实标注；不应为了好看把它们改写为完整报告。
- 用户变化反馈已在卡片显示并有后端持久化回归；真实浏览器阶段未替用户点击不可逆的“标为已读/相关”操作。
- 这是本工作包当时的断点：先统一权威口径并封口完整 R1 E2E。两项已在第 15 节完成；接续时以第 15 节为准。结构化市场复盘仍按正式 PRD 后置到 R2，不要回头重做已完成的列表样式和报告算法。

## 15. 2026-07-24 最新断点：完整 R1 交易复盘链与前端可靠性

本节晚于前文全部断点。阶段 0 权威口径已经统一；“期初持仓 → 真实减仓 → 三个后续完整交易日 → 即时复盘候选 → 用户确认归档”不再是未实现项。

本轮完成：

1. 新增 `tests/test_r1_e2e.py`，以确定性数据和 `completed` Agent Stub 跑通新用户搜索、加入研究空间、正式判断、股票绑定对话、观察任务候选、期初持仓、减仓、三个后续完整交易日、复盘候选、用户编辑确认和归档。
2. 同一测试通过重新 GET 所有对象模拟页面刷新，并验证股票空间、对话、任务、候选和复盘的跨用户隔离。
3. 期初持仓、操作、调整、修订和操作计划表单在创建时生成一次幂等键，失败重试复用原 Key；期初持仓默认日改为浏览器本地日期。
4. 页面明确只有减仓/卖出创建复盘，且需三个后续完整交易日；“让 Agent 帮我处理”只预填即时证据核验和待确认任务要求，不自动发送或创建正式任务。
5. 股票历史页与复盘中心先恢复同一 `review_id` 的待确认候选，再决定是否展示“生成草稿”，避免刷新后重复调用 Hermes。
6. 桌面真实浏览器使用独立匿名会话录入中兴通讯 100 股期初持仓和 10 股减仓，派生为 90 股与两个快照。真实 Hermes/DeepSeek 约 20 秒生成本轮新复盘；模型明确区分价格上涨和逻辑正确，未直出保存文案。
7. 候选确认前刷新后原卡恢复且生成按钮隐藏；用户确认、编辑、再次确认、归档和刷新后，状态、用户版本和确定性价格结果均保持。
8. 当前完整自动化为 `578 tests collected` 并全部通过；Ruff、Python 编译、内联 JavaScript、`uv lock --check` 和 diff 空白检查通过。桌面浏览器控制台无日志。

当前边界：

- 已完成的是确定性后端完整集成链和桌面真实浏览器交易复盘链，不是生产发布完成。
- 仓库仍无 Playwright/Cypress 自动化浏览器 Runner；390px 窄屏整链尚未跑完。
- 正式认证、生产数据库、持久化队列、监控、备份恢复和 staging 回滚仍属于后续生产底座。
- 下一小步只补高收益发布门禁：窄屏主链、关键加载失败的明确错误和可重复浏览器 Runner；随后进入阶段 2，统一股票池/数据日/数据版本、证据时间口径、覆盖率和缺失原因。
- 结构化市场复盘继续后置到 R2；不要回头增加新选股策略、深挖列表样式或把预生成报告当成即时 Agent 回答。

## 16. 2026-07-24 最新断点：选股数据合同、真实 Agent 与双视口门禁

本节晚于前文全部断点。第 15 节列出的窄屏、加载恢复和浏览器 Runner 已经完成；通用选股可信度第一阶段也已封口。

本轮完成：

1. `StockScreenerService` 新增 `stock_screen_data_v1`：内容哈希数据版本、股票池/市值/估值/20 日收益/财务候选池覆盖率，以及股票基础、完整日线、估值市值截面和财务指标的来源合同。
2. 每只候选保存独立行情日、财务报告期、财报公告日和字段级缺失原因；Agent 压缩证据保留数据合同但删除内部 `ts_code`。
3. 自然语言路由识别“筛选A股、经营改善模板/候选、相对行业增强、估值约束、回撤后待复核”。真实问题不再误进 `market_brief`。
4. 真实验收发现并修复两个模型—确定性边界问题：
   - 模型曾把压缩后的 10 条当作总候选数，实际确定性结果为 12；现候选总数显式读取完整结果，并由守卫校验。
   - 上市公司“东方财富（300059.SZ）”曾被供应商名称清理规则误删；现名称+证券代码与本轮证据匹配时保留公司实体，运维语境仍继续拦截。
5. 最终真实 Hermes/DeepSeek Run `4847439a-6f21-40e0-a4b2-3d1178a73f7e` 为 `completed`，12/12 候选全部呈现；回答引用数据版本 `stock-screen-v1-1b1f95521300f2a1`、行情日 `2026-07-23`、财务报告期 `2026-03-31` 和估值覆盖缺口。
6. 股票空间核心接口失败时不再伪装为空，页面明确列出失败模块并提供原位重载；重载不会清除判断、任务、持仓或历史对话。
7. 新增 `scripts/browser_smoke.py` 和 `tests/test_browser_smoke_contract.py`。Runner 只读检查桌面 1280×720 与移动 390×844 下的今日观察、透明选股、个股概览、个股历史、复盘中心和 AI 研究。
8. 390px 目视验收发现个股概览 `grid-template-areas` 遗漏证据地图，形成隐式第二列；已修成 `primary → map → changes → tasks` 完整单列并增加宽度断言。
9. 最终浏览器结果 `12 passed / 0 failed`，报告：`/private/tmp/qingshu-browser-smoke-20260724-final/result.json`。
10. 最终门禁：`589 passed in 41.43s`；Ruff、Python 编译、内联 JavaScript、`uv lock --check`、`git diff --check` 和可移植性 6 项全部通过。

当前运行态：

- 服务：`http://127.0.0.1:8773/demo`
- 进程：PID `62780`
- 工具会话：`64033`
- `/health=ok`
- Hermes：启用
- 数据健康：`54/54 healthy`
- 通用后台和李总 Worker：运行中

下一步：

1. 先检查当前分支是否已提交推送；本节写入时工作树仍待最终显式暂存、提交和推送。
2. 继续做选股/个股分析的高收益可信度工作：单证据模块原位重试、更多财务/公告原文核验、跨报告期比较边界和用户可理解的缺失解释。
3. 正式认证、生产数据库、持久化队列、监控、备份恢复、staging 和回滚仍属于生产底座，不得因本地 Runner 通过而宣称生产发布完成。
4. 不新增黑盒综合分，不自动交易，不提供目标价、仓位、胜率或确定性收益预测。

## 17. 2026-07-24 最新断点：PostgreSQL 主库与持久化队列

本节晚于前文全部断点。生产数据库和持久化队列已从“待办”变为可运行实现。

已完成：

1. `QINGSHU_DATABASE_URL` 支持 PostgreSQL；不设置时继续使用
   `QINGSHU_DB_PATH` SQLite。本地开发兼容不变。
2. 现有 6000 余行业务仓储通过 PostgreSQL 连接池和受控 SQL 方言适配运行；
   核心业务读写、完整应用启动和迁移后运行已在 PostgreSQL 17 验证。
3. 新建 `app/operational_db.py`：持久化任务、周期计划、原子抢占、租约心跳、
   过期恢复、指数退避、最大尝试失败归档、取消、重试、幂等键和健康统计。
   运维 schema v2 同时持久化跨进程 SSE 事件，Web 使用单一轮询线程广播。
4. `BackgroundScheduler` 不再用 `time.monotonic()` 保存调度状态。18 类刷新和
   李总策略统一注册为数据库周期计划；`app/worker.py` 是独立 Worker 入口。
5. Compose 包含 `postgres`、`qingshu-agent`、`qingshu-worker`；生产 Web 使用
   `BACKGROUND_WORKER_MODE=external`，可横向扩 Worker。
6. 新增管理员队列 API，但不在用户网页展示。
7. 新增 SQLite→PostgreSQL 迁移工具；真实 52MB 样本迁移 67 表、18,107 行，
   逐表计数一致，迁移后健康检查和新用户写入通过。
8. 自动化：常规 `601 passed, 5 skipped`；真实 PostgreSQL 专项 5 项全部通过；
   Ruff 通过；重启后的浏览器 Runner `12 passed / 0 failed`。

下一步优先级：

1. 增加 PostgreSQL 定时备份、恢复演练和保留策略。
2. 用户工作区从单机/共享卷迁移到对象存储或共享文件系统。
3. 增加队列延迟、失败率、租约恢复次数和 Worker 存活指标告警。
4. 在 staging 使用与生产一致的 PostgreSQL、Worker 数和密钥管理完成回滚演练。
5. 继续选股/个股分析证据深度和单模块重试；不回头扩低收益 Guard。

当前本地服务已重启为新代码：`http://127.0.0.1:8773`，PID `75046`，工具会话
`83675`；业务库为 SQLite schema v1、运维库为 schema v2（本地开发模式），embedded
Worker 正常，队列无积压，数据健康 54/54。

## 18. 2026-07-25 最新断点：PostgreSQL 唯一运行库、Agent 时延与全景报告

本节晚于前文全部断点，覆盖第 3、15、16、17 节中的旧运行态和未完成判断。当前权威仓库仍为 `/Users/chr/Documents/qingtianshu`，分支仍为 `codex/structured-ai-writeback`；工作树包含大量连续开发改动，禁止清理、重置或批量覆盖。

当前运行态（2026-07-25 10:40，Asia/Shanghai）：

- Web：PID `7732`，`http://127.0.0.1:8773`；`/health=ok`、`/ready=ok`。
- Worker：PID `1862`，外部独立进程，当前活跃 Worker 1 个。
- 领域数据库：PostgreSQL Schema v2；运维数据库与持久化队列：PostgreSQL Schema v4。应用已取消 SQLite 运行时回退，未配置合法 `QINGSHU_DATABASE_URL` 时拒绝启动。
- 队列无 ready、delayed、retrying 或过期 running 积压；最近24小时历史失败仍保留在运维统计中，因此就绪接口带 `recent_failed_jobs_present` 警告但状态为 `ok`。
- 数据健康：54 项中 52 healthy、2 attention。A股成交额完整历史仍在积累；英伟达事件信息较旧。
- 李总策略当前全市场覆盖率 100%，严格候选和触发均为 0；不要为展示效果放宽九条正式规则。

本轮 Agent 修复与真实验收：

1. 标准文字对话默认 `max_iterations=4`，深入研究默认 `6`；未完整结束的 Hermes 文本和内部迭代错误不再显示为正常答案。
2. 标准个股仍用 `deepseek-v4-pro`，但默认 `reasoning_effort=none`；Prompt 删除重复资料和非本题长证据，行情原因回答必须分为价格事实、候选事件、反方证据和下一步核验。
3. 真实中兴通讯 Run `835688a0-3f59-41a8-b8f7-661c5f693118`：单次模型调用，Prompt 38,076 字符，首个安全可见内容 3.638 秒，总耗时 14.792 秒，输出守卫通过，四部分完整。页面等待后没有二次跳文。
4. 标准大盘 Run `26017438-fe5a-4c0e-97ac-9dedffbb1162`：首个安全可见内容 10.964 秒，总耗时 20.124 秒，单次模型调用。
5. `2026-07-23 09:29:31` 的A股成交额不完整快照已从历史比较排除；当前只保存2026-07-24约1.94万亿元的完整收盘口径，历史比较保持 `building_history`，不再输出 `+9487.8%`。

最终门禁：全量收集并通过 `651` 项；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。

报告交付链：

- Markdown：`reports/清数智算_产品实现全景报告_20260725.md`
- 构建脚本：`scripts/build_full_implementation_report_20260725.py`
- DOCX：`output/report/清数智算_产品实现全景报告_20260725.docx`
- PDF：`output/report/清数智算_产品实现全景报告_20260725.pdf`

报告已经更新为651项测试和最新真实时延，修复个人工作区深色框对比度、Agent链路图换行/箭头重叠、第17页过疏和表格首行重复表头标记。接续者必须重新构建、运行 a11y 审计并逐页检查最新渲染；不得用旧 `tmp/report_render_20260725_v7` 页面作为最终证据。

下一阶段优先级：

1. 继续补强同日公司、行业、宏观和利率事件的直接来源；没有直接证据时保持“具体驱动未确认”。
2. 用多次 P50/P95 验收标准、深入和长资料问题，不能把单次14.792秒当固定 SLA。
3. 扩充金额单位、交易日一致性、历史序列跳变、陈旧资讯和多源差异异常样本。
4. 在不回退现有功能的前提下拆分大型 `main.py`、`agent.py`、`db.py` 和单文件前端。
5. 不自动写入正式判断、任务、计划或复盘；不自动交易，不输出目标价、仓位、胜率或确定收益。

## 19. 2026-07-25 最新断点：状态一致性、中文证券名称、导航连续性与最终报告

本节晚于前文全部断点。第18节要求重新构建和检查的全景报告已经完成；测试基线已经从当时的651项更新为当前实际653项。接续者不得再使用旧报告或旧渲染目录作为当前证据。

本轮完成：

1. 统一研究快照状态和普通用户文案。我的关注真实显示“4/4只股票已有研究快照”，四张卡片均为“今日已更新”；公共页面不再暴露模型守卫、输出校验、Hermes失败或核心证据包错误等工程术语。历史数据库原始记录继续保留用于审计。
2. 新增 `app/services/security_master.py`，A股用户可见名称优先使用稳定证券主数据。报告、股票空间、关注资产、研究变化、研究行动和资料库公共返回层会纠正旧英文回退名；变化事件会在返回前递归转换标题、事实摘要和嵌套证据，同时保留数据库原始记录。已验证株冶集团、杰瑞股份和广合科技，刷新后的关注页不再出现 `Yantai Jereh Oilfield Services Group Co., Ltd.`。
3. 修复启动导航竞态。`workspaceNavigationVersion` 保证用户主动导航优先于 boot 初始路由恢复；刷新后首次点击复盘中心，立即和5秒后都保持 `/reviews?tab=trades`。
4. 旧市场文章中的缺失指标占位符在公共返回层转换为跨市场时点说明，新文章生成逻辑不再产生空百分比模板。
5. 全量实际收集并通过 `653` 项；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。
6. 最终报告已经重建：
   - Markdown：`reports/清数智算_产品实现全景报告_20260725.md`
   - 构建脚本：`scripts/build_full_implementation_report_20260725.py`
   - Word：`output/report/清数智算_产品实现全景报告_20260725.docx`
   - PDF：`output/report/清数智算_产品实现全景报告_20260725.pdf`
7. 最新 DOCX 使用文档运行时和指定字体配置渲染为25页，已经逐页目视检查；无截断、重叠、缺字或破损表格。a11y 审计结果 high=0、medium=0、low=0。
8. 服务重启后重新执行仓库 Chrome 冒烟门禁：桌面和390px共12条核心页面检查全部通过，无横向溢出、控制台错误、页面异常或非预期失败响应。真实会话刷新后仍为4/4份今日研究快照；首次点击复盘中心，5秒后仍保持 `/reviews?tab=trades`。

当前运行边界：

- `/health=ok`、`/ready=ok`；领域库 PostgreSQL Schema v2，运维库与持久化队列 Schema v4，独立 Worker 活跃。
- 当前无 ready/delayed/retrying 或过期 running 积压；历史失败任务记录仍触发非阻断警告，不应误写成当前队列失败。
- 2026-07-25 11:50 运行快照中，54项数据健康有51项健康、3项提示：A股成交额完整历史仍在积累，中兴通讯与英伟达的直接事件信息较旧；无 critical 项。这是会随后台同步变化的运行态快照，不是固定产品基线。
- 三项直接体验问题已经关闭。下一轮继续聚焦同日直接事件证据、模型多次P50/P95、跨源数据质量异常样本，以及正式认证、对象存储、外部告警和灾备门禁。
- 工作树仍包含大量连续开发改动，禁止 reset、clean、批量覆盖或机械暂存；提交前必须按真实 diff 显式选择文件并重新检查敏感内容。

## 20. 2026-07-25 最新断点：项目重评分与选股页渐进披露

本节晚于第19节。当前目标不是继续堆功能，而是按真实用户路径持续提高功能可达性、Agent等待体验和长期使用效率。

本轮完成：

1. 新增详细评分：`reports/清数智算_项目详细评分与持续优化记录_20260725.md`。当前功能设计7.9、架构8.0、实际可用性7.3、内部MVP综合7.7、正式对外6.4。
2. 真实标准大盘 Run `326a6f44-7480-471a-9df2-bb3aac3fb663` 使用 `deepseek-v4-pro`，completed且守卫通过；首个安全可见内容28.700秒、总耗时35.513秒、API调用2次。回答质量可用，但时延必须继续作为P0问题。
3. 透明选股页面不再在通用筛选加载后把李总策略插到上方。运行时顺序固定为：顶部快速入口 → 通用截面筛选 → 李总策略。
4. 李总策略历史回放默认折叠，只显示覆盖和规则漏斗；用户点击后才渲染23条历史详情，再点击可收起。历史样本、表现曲线、证据和Agent复盘没有删减。
5. AI研究全局历史隐藏零消息空会话；归档从含义不明、触屏不明显的“×”改为明确文字、完整可访问名称和二次确认。真实会话中空会话与旧“×”均为0。
6. `scripts/browser_smoke.py` 新增信息层级和默认折叠断言。桌面与390px共12项全部通过；两种视口下 `generalBeforeStrategy=true`、历史按钮 `aria-expanded=false`、历史详情不可见。
7. 当前全量653项测试通过；Ruff、compileall、内联JavaScript、`uv lock --check` 和 `git diff --check` 通过。

下一轮优先级：

1. Agent标准问题首个安全内容P50小于8秒、P95小于20秒；先分析两次API调用和Provider首Token长尾，不通过放宽守卫换速度。
2. 等待期间显示稳定的确定性事实进度卡，但不能先显示一段伪回答再用最终回答替换。
3. 历史对话按股票/市场分组，清理空会话和同题重复噪声，把“×”改成明确归档菜单。
4. 继续收敛今日观察，只把真实变化和必须处理的任务放在第一屏。

当前目标仍为 active；本轮只完成第一轮高收益优化，不能将“持续优化使用体验和功能”提前标记完成。

## 21. 2026-07-25 最新断点：标准大盘 Agent 单次调用、误替换修复与三次真实基线

本节晚于第20节。标准大盘 Agent 的高等待和“先生成一段好回答、随后跳成较差摘要”已经完成一轮真实定位、修复与多次验收；接续者不得再把第20节的 35.513 秒旧 Run 当作当前标准问题表现。

本轮完成：

1. `market_brief / economy` 与标准个股一样使用 `reasoning_effort=none`。模型仍是 `deepseek-v4-pro`，标准大盘最大输出 1000 tokens、最大迭代 4；没有改用预存文案，也没有更换便宜模型。确定性行情、广度、成交额、板块和资讯仍在每轮实时准备。
2. 修复前同 Prompt A/B 已证明根因：`reasoning_effort=low` 的旧 Run `326a6f44-7480-471a-9df2-bb3aac3fb663` 首 Token 27.573 秒、请求 35.513 秒、API 调用 2 次；同 Prompt `none` 可在约 3 秒得到首 Token，并以一次调用完成。
3. 第一次正式页面复测 Run `3fd76999-fb44-4389-ad8e-f43ce27b7d97` 暴露真实误替换：DeepSeek 已生成包含价格事实、上涨/下跌家数、反方证据和下一步核验的完整回答，但校验把“不能直接确认新下跌趋势”误判为确认下跌趋势，并因未写平盘家数拒绝全文，页面最终显示了较差的确定性摘要。
4. 第二个成交额问题 Run `4744d68f-88d0-46f9-a460-b404c815dbf4` 再次证明模型原答案可用，但“不能直接证明资金净流入”被反向识别为声称资金净流入，同时自然的上涨/下跌家数回答被要求额外写平盘数。
5. 校验语义已修复：
   - “不能直接确认/无法直接确认”属于明确否定，不再触发下行趋势过度推断；
   - “不能直接证明/说明、无法直接证明/说明”属于资金因果边界，不再触发资金流向过度推断；
   - 用户询问涨跌家数与固定分类时，必须给出上涨数、下跌数和证据中的固定分类；只有明确询问平盘/不涨不跌家数时才强制平盘数。
6. 修复后完成三次真实浏览器与数据库联合验收：
   - `666f8d72-5ec0-4e03-b287-77f244689b43`，解释 A 股普跌原因，总耗时 14.233 秒；
   - `124d5638-64fe-46ae-98d5-f7b40d6ad9c4`，解释成交额和涨跌家数，总耗时 12.113 秒；
   - `968c0c74-9e39-42ba-8a3c-52e43d37875a`，判断普跌与结构性行情，总耗时 10.158 秒。
7. 三次均为 `deepseek-v4-pro`、`reasoning_effort=none`、`api_calls=1`、`status=completed`、输出校验通过。首 Token 为 2.222—3.218 秒，中位数 2.714 秒；页面最终答案约在 10.246—14.540 秒出现，中位数约 12.2 秒。
8. 浏览器以约100ms间隔记录消息区变化。三次都只从空的“研究中”状态切换到最终回答，没有先展示一段模型长文、再被另一段摘要替换。最终回答分别覆盖价格事实、广度或成交额、反方证据/不能确认部分和下一步核验。
9. 详细评分报告已更新：功能设计 7.9、架构设计 8.0、实际可用性 7.5、内部 MVP 综合 7.8、正式对外长期使用 6.4；Agent 分项由 7.2 更新为 7.6。
10. 当前完整回归为 `654 tests collected` 并全部通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。仓库浏览器门禁桌面与390px共 `12 passed / 0 failed`，产品页控制台无 warning/error。

当前运行态（约 2026-07-25 12:45，Asia/Shanghai）：

- Web 为本轮最新代码，PID `39428`，工具会话 `45332`，地址 `http://127.0.0.1:8773`。
- 外部 Worker PID `27934`，活跃 Worker 1 个。
- `/health=ok`、Hermes 启用；领域库 PostgreSQL Schema v2，运维与持久化队列 PostgreSQL Schema v4。
- 54项数据健康为50 healthy、4 attention、0 critical；这是运行态快照，不是固定基线。

下一步优先级：

1. 页面虽然约0.1秒即显示研究进度、模型约3秒已有私有安全片段，但为避免未经完整校验正文被替换，最终回答仍在约10—15秒后一次性显示。下一步优先展示确定性的“已读取行情/广度/资讯”事实进度卡，不提前展示可能被改写的模型正文。
2. 继续分别采样标准/深入、市场/个股、长资料问题；样本足够后再报告 P50/P95，当前三次只能称为小样本基线。
3. 按股票/市场和日期整理同题历史对话；继续降低今日观察第一屏的信息密度。
4. 保持正式事实用户确认边界；不要自动写入判断、任务、计划或复盘，不自动交易，不输出目标价、仓位或确定收益。

当前目标继续保持 active；Agent 标准大盘问题本轮已显著改善，但持续优化使用体验和功能尚未完成。

## 22. 2026-07-25 最新断点：历史对话日期分组、同题折叠与真实交互验收

本节晚于第21节。标准大盘 Agent 的一次调用与误替换修复保持不变；本轮继续处理长期使用时最明显的历史对话噪声，没有删除任何真实会话。

本轮完成：

1. AI研究桌面历史侧栏按“今天 / 昨天 / 最近7天 / 更早”分组，并显示每组数量。当前真实用户共有19条非空对话，页面显示今天11个、昨天8个。
2. 同一日期、规范化标题完全相同的会话只直接展示最新一条；旧记录通过“n个同题旧对话”展开。当前形成四组折叠：成交额1条、A股普跌2条、中兴通讯1条、美股1条。
3. 展开状态保存在当前页面状态中；如果用户打开的活动会话属于旧记录，该组会自动展开，避免活动项被隐藏。移动端历史下拉仍保留全部会话，数据库记录没有合并或删除。
4. 真实浏览器验证“A股今天为什么普跌”展开后由1条变为3条，按钮 `aria-expanded=true`；再次点击恢复1条和 `aria-expanded=false`。打开一条旧记录后 URL 正确切换，该组三条记录继续可见。
5. 桌面截图确认日期标题、重复折叠和活动态层级清晰。重新检查今日观察后，1280×720首屏仍由全球市场和A股核心指数构成，个人待办位于更下方，因此本轮不做无收益重排。
6. 当前完整回归为 `654 tests collected` 并全部通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。
7. 仓库浏览器门禁桌面与390px共 `12 passed / 0 failed`；控制台错误、页面异常、非预期失败响应和横向溢出均为0。报告位于 `tmp/browser-smoke-20260725-conversation-groups/result.json`。

当前运行态（约 2026-07-25 13:00，Asia/Shanghai）：

- Web 地址仍为 `http://127.0.0.1:8773`，`/health=ok`，Hermes启用。
- 领域库为 PostgreSQL Schema v2；运维库与持久化队列为 PostgreSQL Schema v4；独立 Worker 活跃。
- 54项数据健康为50 healthy、4 attention、0 critical；这是运行态快照，会随后台同步变化。

下一步优先级：

1. 继续分别采样标准/深入、市场/个股、长资料问题，形成可靠的P50/P95，而不是用三次最好样本代替长期指标。
2. 为Agent等待阶段增加稳定的确定性事实进度卡，但不能先展示一段可能被改写的模型正文。
3. 历史对话下一阶段只补搜索、近义主题筛选和“继续上次研究”；任何语义聚类都必须保留全部原始会话并可展开。
4. 今日观察暂不重排；只有发现真实重复、不可达或首屏优先级错误时再改。

当前评分维持功能设计7.9、架构设计8.0、实际可用性7.5、内部MVP综合7.8、正式对外6.4。当前目标继续保持 active。

## 23. 2026-07-25 最新断点：Agent 确定性证据进度、个股上下文与历史恢复竞态

本节晚于第22节。第22节提出的确定性事实进度卡已经落地；接续者不得再把“等待期只有空白研究状态”作为当前行为，也不得把历史会话刷新后立即发送的旧竞态当作未修复问题。

本轮完成：

1. `app/main.py` 新增 `_agent_evidence_progress(intent, evidence)`，`evidence_ready` 事件携带 `evidence_progress`。市场问题展示同日代表性指数、全市场涨跌家数、成交额、板块与资讯数量；个股问题展示研究重点、最近完整日线日期、计划模块覆盖和可追溯来源数量。
2. `app/static/demo.html` 新增稳定证据进度卡。后续模型和校验阶段只更新卡片顶部状态，真实证据摘要始终保留；最终答案仍原位替换同一条消息，不提前展示未经校验的模型正文。呼吸动画只作用于状态圆点，移动端保持两列紧凑布局。
3. 真实市场问题约58毫秒显示路由状态、307毫秒显示证据卡、3.923秒进入草稿校验、10.078秒显示最终答案。卡片当时包含6项代表性指数、沪深京5530只、上涨555/下跌4939、成交额19442.25亿元、10个板块和10条资讯。
4. 真实个股问题约134毫秒显示财务估值核验、963毫秒显示历史走查、2.053秒显示“本轮已读取的个股证据·中兴通讯”；日线日期已由原始 ISO 时间统一显示为 `2026-07-24`。
5. 新增 `_prefers_stock_context_followup()` 并扩展 `_needs_stock_market_context()`。“它相对、该股、这只股票、这家公司、相对行业/板块、更强/更弱”等强个股指代优先继承最近证券，即使上一轮误入市场问题也会继续向前寻找股票标的。
6. 真实追问“它相对通信设备行业更强还是更弱”正确返回中兴通讯 `-2.56%`、通信设备行业 `-3.54%`、相对少跌约 `0.98` 个百分点，以及行业50只成分股中3涨47跌；不再返回泛化大盘广度。单股回答同时写入 `research_targets`，刷新后可靠恢复 K 线和研究对象。
7. `state.workspaceBootReady` 与 `state.workspaceBootPromise` 关闭历史 URL 刷新后立即发送的竞态。恢复未完成时，`sendChat()` 自动等待并显示“正在恢复对话”，随后把原问题发送到原 conversation ID，不要求用户重新输入。
8. 真实刷新后立即按 Enter：约80毫秒显示恢复状态、1.339秒问题进入原历史对话、2.171秒显示中兴通讯个股证据卡；URL 始终保持原会话。
9. 当前完整回归为 `657 tests collected` 并全部通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。
10. 仓库浏览器门禁桌面与390px共 `12 passed / 0 failed`；控制台错误、页面异常、非预期失败响应和横向溢出均为0。报告为 `tmp/browser-smoke-20260725-agent-evidence-progress/result.json`。

当前运行态（约2026-07-25 13:40，Asia/Shanghai）：

- Web 地址 `http://127.0.0.1:8773`，Hermes启用；领域库 PostgreSQL Schema v2，运维库与持久化队列 PostgreSQL Schema v4。
- 独立 Worker 活跃；队列 ready/delayed/retrying/expired 均为0。
- 54项数据健康为50 healthy、4 attention、0 critical；这是会随后台同步变化的运行态快照。

评分更新为功能设计7.9、架构设计8.0、实际可用性7.7、内部MVP综合7.9、正式对外6.4；Agent分项7.8、前端信息架构7.6。当前目标继续保持 active，下一轮优先扩大市场/个股/长资料问题样本、补同日直接事件证据，并继续收敛高频页面的信息密度。

## 24. 2026-07-25 最新断点：历史会话搜索、类型筛选与移动端长期使用

本节晚于第23节。历史对话已经从日期分组和完全同题折叠，继续扩展为可搜索、可按研究类型筛选的长期使用界面；没有删除、合并或改写历史消息。

本轮完成：

1. `Database.get_conversation()` 和 `list_conversations()` 新增最近非空 `last_intent` 子查询，公共会话列表返回该字段。该字段只用于导航分类，不改变对话或 Run 的审计记录。
2. AI研究桌面侧栏新增紧凑搜索框和类型选择；会话少于8条时自动隐藏。搜索覆盖标题和最近消息预览，筛选包括个股、大盘、选股、关注和其他。
3. 390px 页面新增同一搜索与筛选工具，筛选后的历史下拉保留当前对话；没有匹配项时明确显示“没有匹配的历史对话”，不会显示成空白或“新的研究对话”。
4. 分类规则采用明确题面语义优先、最近 Agent 意图辅助。历史误路由记录不再污染界面：
   - “它相对通信设备行业更强还是更弱”归入个股；
   - “今天A股的下跌广度和成交额”归入大盘；
   - “个股研究｜杰瑞股份/广合科技/株冶集团”不再沿用旧 `stock_screen` 显示为选股；
   - “候选事件”不再被理解为候选股票，“自选股”不再被理解为选股。
5. 真实用户当前25条有效会话：个股11、大盘11、关注2、其他1；输入“贵州茅台”只显示1条。日期分组、完全同题折叠、活动对话恢复与归档能力均保留。
6. 390×844 实测搜索、类型选择和历史下拉宽度约为223、78、336像素；根页面宽度与视口均为390像素，无横向溢出。桌面侧栏新增控件保持一行紧凑布局。
7. 当前完整回归为 `657 passed`；Ruff、Python `compileall`、内联 JavaScript 解析、`uv lock --check` 和 `git diff --check` 通过。
8. 仓库浏览器门禁桌面与390px共 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260725-conversation-filter/result.json`；真实页面控制台 warning/error 为0。

评分更新为功能设计7.9、架构设计8.0、实际可用性7.8、内部MVP综合7.9、正式对外6.4；Agent分项维持7.8，前端信息架构更新为7.8。当前目标继续保持 active；下一轮优先补近义主题提示、具体股票筛选或“继续上次研究”，同时继续扩大真实 Agent 样本和直接事件证据。

## 25. 2026-07-25 最新断点：资讯持久刷新与数据健康语义修复

本节晚于第24节。第24节记录的 `50 healthy / 4 attention` 已不是当前运行态；其中三个事件信息提示已经通过真实 PostgreSQL 重复同步和来源轮询审计关闭。

本轮完成：

1. 根因不是 Worker 停止。A 股信息与美股财务后台任务当时均为 completed，但 `DataHealthService._information_checks()` 读取按内容发布时间排序的第一条新闻的 `fetched_at`，把内容时间错误当成管道最近轮询时间。
2. `ChinaInformationService` 现在分别记录公告、公司新闻、股吧的 `status/items/polled_at`；`USEquityFundamentalsService` 记录 SEC 监管文件轮询；`GlobalInformationService` 记录公司新闻轮询。后台任务把每只股票、每个来源的结果写入 PostgreSQL `background_job_runs.summary_json`。
3. `DataHealthService` 优先依据最近成功后台轮询判断管道时效，并同时返回 `poll_age_seconds`、`content_fetch_age_seconds` 和 `freshness_basis`。成功轮询但没有新内容是 healthy；只有部分来源真实失败时才是 attention；缺少所有内容仍是 critical。
4. 美股小时任务不再只刷新估值、财务与 SEC 文件，同时刷新 Nasdaq 公司新闻。A 股信息任务显式包含默认研究标的，不依赖标的恰好存在于某个用户关注列表。
5. 第一次真实重跑暴露 PostgreSQL 重复新闻写入缺陷：兼容层把所有 `news_items` upsert 强制改为 `ON CONFLICT(id)`，普通新闻重新抓取时新随机 ID 无法接住 `UNIQUE(symbol, source, url)`，A 股和美股均出现 `UniqueViolation`。
6. `Database.upsert_news_items()` 已改为先按稳定 ID 或 `symbol + url` 更新，未命中再 `ON CONFLICT DO NOTHING` 插入，并在并发冲突后重试更新。来源名称变化会更新原记录；同一 URL 重复抓取会刷新标题、摘要和 `fetched_at`，不创建重复项。
7. 真实生产库重跑通过：6只 A 股的公告、公司新闻和股吧全部为 `ok`；NVDA 的 SEC 监管文件返回12条，Nasdaq公司新闻返回20条，均为 `ok`。中兴通讯、中际旭创和英伟达健康项均为 `freshness_basis=successful_source_poll`。
8. 最新数据健康为 `53/54 healthy`、`1 attention`、`0 critical`。唯一提示是 A 股成交额历史仍在积累，并继续排除 2026-07-23 盘前不完整快照；这不是实时数据或资讯管道故障。
9. 当前 `/health=ok`、`/ready=ok`；Hermes启用，领域库 PostgreSQL Schema v2，运维库与持久化队列 PostgreSQL Schema v4，独立 Worker 活跃，队列 ready/delayed/retrying/expired 均为0。
10. 当前完整回归为 `660 tests collected` 并全部通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。
11. 仓库浏览器门禁桌面与390px共 `12 passed / 0 failed`；控制台错误、页面异常、非预期失败响应和横向溢出均为0。报告为 `tmp/browser-smoke-20260725-data-health/result.json`。

评分更新为功能设计7.9、架构设计8.1、实际可用性7.9、内部MVP综合8.0、正式对外6.4；金融数据体系8.0、Agent分项7.8、前端信息架构7.8。当前目标继续保持 active；下一轮优先补强同日直接事件证据和更多真实 Agent 问题样本，其次完善具体股票筛选与“继续上次研究”，不要把来源成功轮询等同于已经获得涨跌因果证据。

## 26. 2026-07-25 最新断点：继续最近研究与 Agent 路由快速恢复

本节晚于第25节。“继续上次研究”已经完成，后续不得再把它列为未实现项；本轮同时修复了直接打开历史 URL 时被无关首页数据阻塞的问题。

本轮完成：

1. `app/static/demo.html` 在新对话入口区新增紧凑的“继续上次研究”。它选择当前用户最新一条非空、非 evaluation 会话，显示标题、研究类型、消息数和更新时间。
2. 点击入口复用原 `conversation_id` 和 `openConversation()`；不会调用 `sendChat()`、不会启动 Hermes、不会复制保存回答、不会新建 Run，也不会删除或合并历史记录。失败时只显示可重试的用户文案。
3. 真实桌面页面显示最近会话“分析中兴通讯当前的价格、行业和公告证据。”，8条消息。点击进入 `/research/bae4238b-1dbd-4548-8bb2-a68acccb6be9`，25条历史总数不变，中兴通讯 K 线、证据快照和8条历史消息完整恢复。
4. 390×844 下入口和按钮完整可见；真实点击进入同一原会话。临时浏览器视口覆盖已重置，页面留在 `/research/new` 供继续验收。
5. 启动流程对 Agent 路由改为：健康状态与用户就绪 → 并行读取历史会话和股票研究绑定 → 恢复目标会话 → 后台异步加载自选、今日观察、市场、行业、文章和资料库。普通页面原有启动路径不变。
6. 修复前直接打开历史 URL 曾约8—10秒只显示“正在读取历史对话”；修复后同一真实浏览器刷新约167毫秒出现已保存证据快照、最终纠正回答和25条会话列表。该数字是本机同一会话复测，不是正式 SLA。
7. 新增两个前端合同测试；完整回归为 `662 tests collected` 并全部通过。Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。

当前运行态：`/health=ok`，Hermes启用；领域库 PostgreSQL Schema v2，运维库和持久化队列 Schema v4，独立 Worker 活跃；数据健康 `53/54 healthy`、`1 attention`、`0 critical`。

评分更新为功能设计8.0、架构设计8.1、实际可用性8.0、内部MVP综合8.0、正式对外6.4；Agent分项7.8、前端信息架构7.9。当前目标继续保持 active；下一轮优先补同日直接事件证据、扩大真实 Agent 样本，并增加具体股票筛选或谨慎的近义主题提示。

## 27. 2026-07-25 最新断点：历史研究按具体股票筛选

本节晚于第26节。具体股票筛选已经完成，后续不得再把它列为未实现项；筛选只信任结构化证券对象，不使用脆弱的标题名称猜测。

本轮完成：

1. `Database.get_conversation()` 和 `list_conversations()` 返回 `research_targets`。来源为最近 Assistant metadata 中的 `research_targets/symbol`，并补充与会话唯一绑定的 `deep_stock_sessions` 证券对象；证券代码统一 `.SH → .SS`，重复目标去重。
2. `public_conversation()` 公开该字段。没有证券对象的会话返回空数组；现有数据库表和 Schema 版本不变，不新增迁移。
3. 前端在原研究类型选择器中动态加入“具体股票”选项，并显示每只股票可筛选的会话数量。桌面选择器由72px调整为86px，390px由78px调整为96px，没有增加第二排筛选控件。
4. 具体股票筛选同时要求 `conversationScopeKey(item) === "stock"`。真实发现一条“A股下跌广度和成交额”旧会话错误保存了中兴 symbol；该会话现在仍归大盘，不会污染中兴通讯筛选。没有明确 symbol 的旧“它/该股”会话不会被猜测加入。
5. 真实用户当前具体股票选项：中兴通讯4条，贵州茅台、广合科技、杰瑞股份、株冶集团各1条。中兴筛选显示 `4 / 25`；贵州茅台筛选显示唯一历史，并成功打开原 conversation ID `8ebd8fbc-8c34-4a05-bd59-abb4069edefb`。
6. 390×844 真实页面显示贵州茅台选项、过滤后的历史下拉和输入框；临时视口覆盖已重置，页面留在 `/research/new`。
7. Web 与独立 Worker 已重启。重启前查询持久化队列没有 running 项；重启后短暂补跑计划任务并清零，当前 ready/delayed/retrying/expired 均为0，Worker active。
8. 完整回归为 `663 tests collected` 并全部通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。

当前运行态：`/health=ok`，Hermes启用；领域库 PostgreSQL Schema v2，运维库和持久化队列 Schema v4；数据健康 `53/54 healthy`、`1 attention`、`0 critical`。

评分更新为功能设计8.0、架构设计8.1、实际可用性8.1、内部MVP综合8.0、正式对外6.4；Agent分项7.8、前端信息架构8.0。当前目标继续保持 active；下一轮优先补同日直接事件证据和更多真实 Agent 样本，其次谨慎处理近义主题提示与批量整理。

## 28. 2026-07-26 最新断点：Hermes 执行拆分、浏览器门禁同步与保守重评分

本节晚于第27节。当前产品仍处于持续优化目标中，不能因为测试数量增加或受控环境可运行就宣称普通用户体验已经成熟。

本轮完成：

1. 新增 `app/services/agent_hermes_execution.py`，集中承接模型路由、Token/迭代预算、Hermes 一次性进程、流式桥接、超时终止和用量统计。
2. `AgentService` 继续持有金融输出清理、数字校验、语义冲突和必答证据规则，通过 `GuardedStreamCallbacks` 注入执行层。该拆分没有放宽策略规则、证据边界或用户确认要求。
3. `app/services/agent.py` 从 10,063 行降至 9,739 行；新增两项委托边界测试，防止未来把金融验证或用户状态写入错误地下沉到模型运行时。
4. 完整浏览器 Runner 首次重跑为 `9 passed / 3 failed`：两个失败来自仍要求李总策略默认显示的旧断言，一个失败来自个股容器先显示、卡片尚未异步渲染的竞态。没有把这些失败直接冒充产品通过。
5. Runner 现在真实点击“通用筛选 → 李总策略 → 通用筛选”，验证两个结果区互斥、`section=li_zong` URL 状态、返回恢复和历史详情默认折叠。个股概览等待核心卡片真实出现后再检查 390px 宽度。
6. 服务重启并加载拆分后代码后，桌面与 390px 共 `12 passed / 0 failed`；控制台错误、页面异常、非预期本地失败响应和页面级横向溢出均为0。最终报告为 `/private/tmp/qingshu-browser-smoke-20260726-final/result.json`。
7. 隔离临时工作区真实调用新 Hermes 执行模块：`deepseek-v4-pro` 返回“本轮模型运行测试成功。”，首 Token `3.444s`、首个受控可见片段 `3.488s`、单次可见更新；没有写入正式用户会话。
8. 当前完整回归为 `681 passed`；`ruff check app tests scripts`、Python `compileall`、内联 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
9. 当前运行服务仍为 `/health=ok`，Hermes启用；领域库 PostgreSQL Schema v2，运维库与持久化队列 Schema v4，独立 Worker 活跃；数据健康 `53/54 healthy`、`1 attention`、`0 critical`。

保守评分更新为：功能设计8.0、架构设计7.9、实际可用性7.2、内部MVP综合7.7、正式对外长期使用6.2；Agent 7.8、前端信息架构7.8、工程质量7.5。评分下降不是功能回退，而是改用普通用户视角，把首次使用成本、信息密度、Agent长尾质量、巨型单体和生产门槛纳入扣分。

下一步继续保持目标 active：优先拆 `agent.py` 的证据压缩与输出校验/修复，再拆 `main.py` 的 Chat 编排；随后拆 `demo.html` 的路由状态、对话、股票空间、选股和复盘模块。每轮都必须运行定向测试、完整回归、JavaScript 检查和真实双视口浏览器门禁。

## 29. 2026-07-26 最新断点：Agent Prompt 证据压缩模块化

本节晚于第28节。Hermes执行与Prompt证据压缩均已完成物理拆分；后续不得再把这两项列为未完成，下一块是输出校验/修复。

本轮完成：

1. 新增 `app/services/agent_evidence_compaction.py`，集中承接递归公开证据过滤、市场日期对齐、市场状态计算、市场资料和对话压缩，以及个股、比较、研究行动和选股 Prompt 证据压缩。
2. `AgentService` 保留原静态方法名作为兼容入口，只委托纯函数模块；真实金融输出校验、用户确认边界和运行编排没有迁移或放宽。
3. 大盘对话仍只携带最近四条用户问题，不把旧助手回答重新当金融证据；市场资料最多保留两条高相关短摘录；个股资料优先用户资料并限制摘录长度。
4. 新增5项模块边界与纯函数测试，覆盖递归移除 provider/url/source 等私有字段、市场历史裁剪、市场和个股委托入口，以及用户资料优先级。
5. `app/services/agent.py` 从9,739行降至8,122行；`app/services/agent_evidence_compaction.py` 为1,678行。职责已经分离，但新模块后续仍可按市场/个股进一步拆分，不能把“从一个大文件搬到另一个文件”视为最终治理。
6. 当前完整回归为 `686 passed`；Ruff、Python `compileall`、内联 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

保守评分更新为：功能设计8.0、架构设计8.0、实际可用性7.2、内部MVP综合7.7、正式对外长期使用6.2；Agent 7.8、前端信息架构7.8、工程质量7.7。

用户已把同事3.3版本吸收加入长期目标。权威入口是 `HANDOVER_COLLEAGUE_3.3_ABSORPTION_GUIDE.md`，本轮已完整阅读。执行顺序保持：先继续治理 `agent.py` 输出校验/修复、`main.py`、`db.py` 和 `demo.html`；边界稳定后，从同事隔离副本优先吸收 `qs-charts.js`、K线拖拽、十字光标、OHLC/成交量联动和高保真布局。禁止解压覆盖权威库、读取或复制 `.env`、复用 SQLite 队列或旧李总策略。

## 30. 2026-07-26 最新断点：李总策略首次加载不再空白

本节晚于第29节。李总策略冷启动体验已经修复并通过真实双视口门禁；后续不得重新恢复为“接口完成后才显示整个面板”。

1. `app/static/demo.html` 为 `liZongPanel` 增加 `idle/loading/ready/error` 状态。切换李总策略后面板立即可见；无缓存时显示读取状态，失败时提供重新加载，有最近结果时刷新失败继续展示最近结果。
2. `scripts/browser_smoke.py` 先要求面板在1.5秒内出现，再等待结果进入 `ready`；不再用单纯扩大总体超时掩盖首次反馈问题。
3. 真实 Chrome 门禁：桌面面板 `0.091s` 可见、`2.899s` ready；390px 面板 `0.079s` 可见、`3.389s` ready。六个核心页面、双视口共 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-li-zong-first-load/result.json`。
4. 第一次完整回归发现 `tests/test_api.py` 仍要求切页时直接隐藏李总面板；该旧合同已改为验证按分区显示并进入 loading。最终 `686 tests collected` 且全部通过，Ruff、compileall、内联 JavaScript、锁文件和差异检查通过。
5. 当前服务工具会话为 `29719`，地址 `http://127.0.0.1:8773`；Hermes启用，领域库 PostgreSQL Schema v2，运维库与持久化队列 Schema v4，独立 Worker active=1，队列无积压，数据健康 `53/54 healthy`、`1 attention`、`0 critical`。
6. 运维检查唯一降级项为本机备份目录缺少最新 PostgreSQL 备份。它与本次前端修复无关，但继续阻止正式生产就绪声明。

评分维持功能设计8.0、架构设计8.0、实际可用性7.2、内部MVP综合7.7、正式对外长期使用6.2；此次修复解决了一个明确的首次反馈缺陷，但不应凭单一页面修复提高整体成熟度。下一块仍是拆分 `agent.py` 的输出校验与修复，然后治理 `main.py` Chat 编排、`db.py` 仓储和 `demo.html` 模块。

## 31. 2026-07-26 最新断点：Agent 输出守卫完成拆分并重跑最终门禁

本节晚于第30节。Agent 输出校验/修复已经完成物理拆分；后续不得再把它列为未完成，也不得把旧的 `686 passed` 或 `agent.py 8,122 行` 当作当前基线。

1. 新增 `app/services/agent_output_guard_common.py`、`agent_output_guard_market.py`、`agent_output_guard_stock.py` 和 `agent_output_guard.py`。共用规则、市场规则、个股规则与总校验/修复编排不再混在 `AgentService` 中。
2. `AgentService` 保留原静态方法入口并委托 `AgentOutputGuard`；旧模块级私有函数保留兼容导出。拆分没有删除金融规则、真实候选为空边界、用户确认要求或内部信息过滤。
3. `app/services/agent.py` 现为 3,461 行；四个守卫模块分别为 78、1,293、1,390 和 2,106 行。相较最早约 10,467 行，Agent 单体累计减少约 7,006 行，但 `main.py`、`db.py` 和 `demo.html` 仍需治理。
4. 新增 `tests/test_agent_output_guard_modules.py`，验证兼容导出、无循环依赖、市场和个股规则、当前报价必答边界。最终完整回归为 `691 tests collected` 且全量通过；Ruff、compileall、内联 JavaScript、锁文件和差异检查全部通过。
5. 最终代码上的真实 Chrome Runner：桌面与 390px 六个核心页面共 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-output-guard-core/result.json`。
6. 隔离目录真实调用 `deepseek-v4-pro` 返回“本轮模块化验证完成。”；最终 `guard_passed=true`，没有不受支持的数字或语义冲突，未写入正式用户会话。
7. 当前服务地址仍为 `http://127.0.0.1:8773`；Hermes 启用，领域库 PostgreSQL Schema v2，运维库和持久化队列 Schema v4，独立 Worker active=1，队列无积压，数据健康 `53/54 healthy`、`1 attention`、`0 critical`。
8. 运维自检唯一失败为 `postgres_backup_not_healthy`：`/Users/chr/.qingshu/backups` 尚无可核验备份。该项继续阻止正式生产就绪声明。

最新保守评分为：功能设计8.0、架构设计8.1、实际可用性7.2、内部MVP综合7.7、正式对外长期使用6.2；Agent 7.8、前端信息架构7.8、工程质量7.9。下一步依次治理 `main.py` Chat 编排、`db.py` 领域仓储和 `demo.html` 模块；随后按同事3.3吸收指南引入图表交互，不吸收 SQLite 队列、旧李总策略或平行交易模型。

## 32. 2026-07-26 最新断点：Chat 纯路由规则迁出 main.py

本节晚于第31节。Chat 的纯识别与历史恢复规则已经迁出，但 `/users/{user_id}/chat` 的证据获取、服务调用、流式发布和结果落库编排仍在 `main.py`，后续不得把本轮描述成 Chat 编排已经全部完成。

1. 新增 `app/services/chat_routing.py`，承接证券名称/代码识别、选股意图与参数、市场问题/焦点、日期与涨跌方向、上下文追问、历史证券/意图恢复，以及财报、业务、股东、研报、事件、研究行动等纯路由规则。
2. `app.main` 继续导入同名私有函数，保留现有内部调用和测试兼容；新模块没有反向导入 `app.main`。`main.py` 从约6,679行降至5,703行，新模块1,069行。
3. 第一次定向回归发现行业比较追问保持中兴标的但缺少 `stock_market_context`。抽取时遗漏了工作树新增的“相对行业/板块、所属行业、更强/更弱”触发词；恢复后原 API 测试通过。以后抽取必须以当前工作树和行为测试为准，不能从 Git 基线盲目复刻。
4. 新增4项模块边界测试。当前完整回归为 `695 tests collected` 且全量通过；Ruff、compileall、内联 JavaScript、锁文件和差异检查全部通过。
5. Web 与独立 Worker 重启加载全部纯路由模块后，Chrome 桌面与390px共 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-chat-routing-complete/result.json`。
6. 新进程健康状态正常：双 PostgreSQL Schema v2/v4、Worker active=1、队列无积压、数据健康 `53/54 healthy`；隔离真实 `deepseek-v4-pro` 调用返回“本轮模块化验证完成。”且 `guard_passed=true`。

最新保守评分为：功能设计8.0、架构设计8.2、实际可用性7.2、内部MVP综合7.7、正式对外长期使用6.2；Agent 7.8、前端信息架构7.8、工程质量8.0。下一步继续把 Chat 证据装配和流式/落库编排移到应用服务层，然后治理 `db.py` 与 `demo.html`；正式吸收同事3.3图表仍排在工程边界稳定之后。

## 33. 2026-07-26 最新断点：Chat 持久化、SSE 发布拆分与冷启动导航竞态修复

本节晚于第32节。Chat 回答落库和流式事件发布已经迁出 `main.py`；会话准备、知识查询、证据装配和服务调用顺序仍在主路由，所以后续不得把 Chat 编排整体标成完成。

1. 新增 `app/services/chat_persistence.py`，统一保存 Assistant 消息、知识/市场/证据来源、研究对象、结构化卡片、模型层级和选股 Profile。个股涨跌问题不会把历史资料库摘录冒充为本轮直接事件证据。
2. 新增 `app/services/chat_streaming.py`，统一发布确定性进度、Hermes delta、reset、引用、待确认写回候选、结构化失败和完成事件。主路由不再直接拼装这些 SSE 协议细节。
3. 新增 7 项模块测试；正确的网页合同、浏览器合同和两个新模块定向回归共 10 项通过。`app/main.py` 当前 5,509 行，`app/services/chat_routing.py` 1,069 行，`chat_persistence.py` 179 行，`chat_streaming.py` 153 行。
4. 第一次最终浏览器 Runner 为 `11 passed / 1 failed`。失败只出现在桌面选股冷启动：李总策略请求已经 ready，但初始化流程随后恢复默认通用分区，把用户刚点击的面板重新隐藏。移动端因时序不同没有复现，说明不能只用单视口替代真实竞态验收。
5. 根因是 `openScreeningSection()` 未增加 `workspaceNavigationVersion`。修复后主动导航会阻止启动流程覆盖用户选择；对应合同已加入 `tests/test_api.py::test_demo_page_is_the_default_human_facing_entry`。
6. 最终桌面与 390px 六个核心页面共 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-chat-orchestration-rerun/result.json`。控制台错误、页面异常、非预期失败响应和页面级横向溢出均为 0。
7. 当前完整回归为 `702 tests collected` 且全量通过；Ruff、Python `compileall`、内联 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
8. 当前 `/health=ok`、Hermes 启用，领域库 PostgreSQL Schema v2，运维库和持久化队列 Schema v4，独立 Worker active=1，ready/delayed/retrying/expired 均为 0。数据健康为 53 healthy、1 attention、0 critical；唯一提示是 A 股成交额完整历史仍在积累，系统已排除盘前不完整快照。
9. 运维管理接口继续要求认证；此前正式自检的唯一生产缺口仍是没有可核验 PostgreSQL 备份。本轮不把业务门禁通过表述为完整生产就绪。

最新保守评分为：功能设计8.0、架构设计8.3、实际可用性7.2、内部MVP综合7.7、正式对外长期使用6.2；Agent 7.8、前端信息架构7.8、工程质量8.1。下一步继续迁移 Chat 会话准备、知识查询和证据装配，再治理 `db.py` 领域仓储与 `demo.html` 模块；边界稳定后按同事3.3指南吸收图表交互。

## 34. 2026-07-26 最新断点：Chat 请求准备上下文完成拆分

本节晚于第33节。会话准备、路由结果汇总、图片校验、资料库检索请求和用户消息保存已经迁出；金融证据获取与各服务调用顺序仍在 `main.py`，下一轮应从证据装配继续，而不是重复拆本节内容。

1. 新增 `app/services/chat_context.py`，提供 `ChatRequestContextService` 和显式的会话不存在、图片不存在、图片文件失效异常。服务不依赖 `app.main`。
2. 新服务建立或恢复当前用户研究对话，读取最多40条最近历史，保留新对话自动命名；证券、市场、选股、李总策略、财务、股东、分析师预期和事件上下文继续调用 `chat_routing.py` 的既有规则。
3. 市场问题仍强制检索证据层级、市场因果和趋势风险三份通用资料；李总、通用选股、多股比较、分析师预期和事件问题的专属检索词保持不变。用户消息仍在任何回答分支之前写入原对话。
4. 图片仍必须属于当前用户且工作区文件实际存在；通过后标记已使用并切到 vision 层级。API 对失效会话、非本人图片和失效文件继续返回原有404/410。
5. `app/main.py` 从5,509行降至5,382行，`chat_context.py` 为342行。新增4项测试；正确的定向集合17项通过，原行业比较追问完整API链保持中兴通讯标的与同日行业证据。
6. 当前完整回归为 `706 tests collected` 且全量通过；Ruff、compileall、内联 JavaScript、锁文件和差异检查全部通过。
7. Web与独立Worker重启加载新模块后，`/health=ok`、Hermes启用，领域库PostgreSQL Schema v2，运维库和队列Schema v4，Worker active=1，retrying/expired为0，数据健康53/54。
8. 最终桌面与390px六个核心页面共 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-chat-context/result.json`。

评分维持功能设计8.0、架构设计8.3、实际可用性7.2、内部MVP综合7.7、正式对外长期使用6.2；Agent 7.8、前端信息架构7.8、工程质量8.1。下一步迁移金融证据装配和服务调用编排，再拆 `db.py` 与 `demo.html`；之后按同事3.3指南吸收图表交互。

## 35. 2026-07-26 最新断点：Chat Agent 执行收尾编排完成拆分

本节晚于第34节。证据准备后的 Agent 调用、结构化结果、深度研究观察、补证任务、回答保存和完成事件已经迁出；具体金融证据分支仍在 `main.py`，下一轮应继续迁移证据装配，不要重复拆执行收尾。

1. 新增 `app/services/chat_execution.py`，由 `ChatAgentExecutionService` 承接证据就绪事件、Agent 调用参数、受控流式回调和路由/证据耗时记录。
2. Structured AI 卡片及待确认写回候选仍在 Agent 回答之后生成。该步骤异常时只发布 `failed=true` 的结构化事件，原金融回答、Run和证据仍保存并返回，防止用户再次遇到有效正文被失败文案替换。
3. 服务继续调用 `DeepStockResearchService.observe_chat()` 与 `EvidenceTaskService.capture_from_chat()`，并通过既有 `ChatResponsePersistence` 保存回答；研究对象、知识来源、模型层级和结构化结果协议不变。
4. 拆分时发现 `tests/test_api.py` 仍从 `app.main` 引用 `_agent_evidence_progress`；已保留兼容导出，不能因内部迁移破坏既有入口。
5. `app/main.py` 从5,382行降至5,309行，`chat_execution.py` 为148行。新增3项测试，定向集合16项通过。
6. 当前完整回归为 `709 tests collected` 且全量通过；Ruff、compileall、内联JavaScript、锁文件和差异检查全部通过。
7. 新进程真实HTTP对话“美股为什么收盘跌了”返回 `market_brief / preview`、580字回答、有效Run/Assistant消息/Conversation ID且 `error=null`，证明新编排经过真实路由与数据库链。
8. 桌面与390px六个核心页面最终 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-chat-execution/result.json`。Web、Hermes、PostgreSQL双库和独立Worker正常，数据健康53/54。

评分维持功能设计8.0、架构设计8.3、实际可用性7.2、内部MVP综合7.7、正式对外长期使用6.2；Agent 7.8、前端信息架构7.8、工程质量8.1。下一步拆具体金融证据装配，再治理 `db.py` 和 `demo.html`，最后按3.3指南吸收图表交互。

## 36. 2026-07-26 最新断点：大盘证据装配完成拆分

本节晚于第35节。大盘/市场问题的证据装配已经迁出 `main.py`；个股、财务、选股、研究行动等证据分支仍在主路由，下一轮不要重复拆大盘路径。

1. 新增 `app/services/chat_market_evidence.py`，显式市场问题使用 `MarketNewsService.infer_market()`，连续追问从历史消息恢复 `market_key`。
2. 服务统一构建问题焦点、代表指数、目标交易日和 `market_drivers`，市场资讯与指数分析使用同一交易日；A股行业专题也使用同日快照。
3. 伦敦金问题额外读取 `london_gold` 实时市场卡；实时快照失败时只返回空的聚焦实时项，不破坏其他已经取得的金融证据。
4. `app/main.py` 从5,309行降至5,272行，`chat_market_evidence.py` 为73行。新增4项测试；定向集合7项通过。
5. 当前完整回归为 `713 tests collected` 且全量通过；Ruff、compileall、内联JavaScript、锁文件和差异检查全部通过。
6. 新进程真实HTTP问题“美股为什么收盘跌了”返回 `market_key=us`，指数仅含标普、纳指、道指、罗素2000，目标日2026-07-23，回答580字且Run/消息落库正常。
7. 桌面与390px六个核心页面最终 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-chat-market-evidence/result.json`。队列无积压，数据健康53/54。

评分维持功能设计8.0、架构设计8.3、实际可用性7.2、内部MVP综合7.7、正式对外长期使用6.2；Agent 7.8、前端信息架构7.8、工程质量8.1。下一步优先拆个股/财务证据装配，再治理 `db.py` 和 `demo.html`；工程边界稳定后按3.3指南吸收图表交互。

## 37. 2026-07-26 最新断点：李总组合回测正确性与构建速度修复

本节晚于第36节。组合回测已从“接口和页面存在”推进到真实生产数据库持续建设；当前仍需等待全量历史覆盖后才能发布3个月、1年、3年收益率，不能把建设中状态称为最终完成。

1. 修复稳定快照时间倒退：`Database.latest_tushare_dataset_snapshot()` 和批量最新快照在读取 stable 数据时优先按 `as_of_date` 取新，再按生成时间取同日修订。生产库原先已有2026-07-24全市场快照，但一次后来生成的2026-07-21重试曾被误读为最新；新逻辑已实时恢复读取7月24日。
2. 修复长窗口提前计算：此前3个月市值完整时，逐股状态仍错误覆盖全部756日，尚未取得的旧市值被按0处理，未来可能污染1年和3年结果。现在只计算当前已经完整的最大窗口；63日、252日、756日就绪时分别扩大并重算。
3. 新增 `STATE_INPUT_VERSION` 以及量价快照、历史市值窗口、起止日期的组合输入指纹。旧错误状态自动失效；任一快照或历史市值修订也会触发重算，不能仅凭日期覆盖冒充完成。
4. 历史规则评估改为等价滚动引擎，保留原逐日确定性实现作为参考测试。生产真实快照10只股票×63日逐项一致，速度约13.2倍；2只股票×756日逐项一致。合成252日测试覆盖ROE、股东、涨停、连续涨停、新高、放量和触发状态。
5. 后台默认每批获取30个历史市值交易日、核验100只既有快照股票，并以最多4并发补齐4只缺失深度快照。网页新增建设进度条、百分比和预计剩余计算批次，仍不向普通用户暴露供应商错误。
6. 新增稳定快照不回退、阶段窗口、输入版本失效和优化/参考结果一致性测试；李总策略、Tushare与持久化任务相关回归118项通过。
7. 当前 Web 与双并发外部 Worker 已重启。阶段检查时3个月窗口为63/63个市值交易日、1699只历史股票范围；1年窗口已达到252/252，3年市值仍继续补齐。收益率必须等全部对应股票状态完成后再发布。

下一断点：继续观察 `li_zong_backtest_refresh`，优先完成3个月和1年逐股状态，再完成756日市值与3年状态；随后在真实浏览器验收收益曲线、最近换仓、区间切换和390px布局，并将最终结果接入 Agent 证据路由。

## 38. 2026-07-27 最新断点：短窗口优先、历史退市股票与选股首屏加载修复

本节晚于第37节。63、252、756个历史市值交易日均已完整，三档均进入真实逐股规则核验；收益率仍须等各自股票范围完整后才发布。

1. 修复透明选股直达页的启动顺序。过去直接打开李总策略时，页面会先等待关注列表、行情、文章、资料库和个股空间，导致策略版本、日期和范围十余秒显示为占位。现在先加载当前路由，再在后台补其他模块；真实内置浏览器重载后约3.65秒取得 `li_zong_v1`、2026-07-24和5531只股票范围。
2. 回测批次新增“最早未完成可见窗口”优先级：先补齐阻塞3个月结果的股票，再补1年，最后扩展3年。较长窗口就绪后，不再按股票代码顺序让3年扩展任务拖住短窗口发布。
3. 修复历史退市或长期停牌股票永久卡住的问题。旧逻辑只向前搜索最近5个交易日的逐股 `daily_basic`；历史股票池中的退市股票会反复得到数据不完整。现在使用已保存的历史全市场市值截面直接定位最后可用交易日，同时保留真实 `stock_basic` 查询。
4. 逐股稳定快照新增 `requested_as_of_date`，区分“系统已查询到窗口末端”与“股票最后实际交易日”。长期停牌或退市后没有新日线，不再被误判为同步从未到达窗口末端。
5. 明确退市日之后，候选状态退出，并以最后可得复权收盘价作为强制退出代理，在下一市场日完成组合退出并计入统一摩擦成本。该代理会在页面和结果边界中明确说明，不冒充真实退市整理期成交。
6. 外部逐股数据单批上限从4只提升到12只，但同时请求仍限制为4只；这样提高持续吞吐，不提高瞬时并发。页面剩余批次改称“至少剩余”，明确外部数据补齐时间另计。
7. 新增路由优先加载、三档优先级、历史市值提示、请求截止日和退市强制退出测试。相关定向集合40项通过，Ruff、Python编译、JavaScript语法和差异检查通过。
8. 修复李总策略首屏携带大体积内部元数据的问题。候选接口不再把4301只股票的逐股 `data_versions` 映射发送给普通网页，并让覆盖率与规则漏斗复用同一次候选状态读取。生产实测响应由约345KB/6.07秒降至约6KB/2.04秒；真实内置浏览器直达页约2.69秒进入 `ready`，显示策略版本、2026-07-24数据日、5531只股票范围和回测进度，无页面错误。
9. 生产观察发现第二层队列优先级遗漏：旧稳定快照只有在覆盖检查阶段才会暴露预热窗口不足，过去会被追加到同步队尾，使后续1年股票抢在剩余3个月阻塞股票前同步。现已在形成最终外部同步批次前按3个月、1年、3年顺序重新排序，并新增回归测试。

下一断点：等待并核验3个月、1年、3年真实结果依次发布；检查候选集合、换仓时间、收益曲线、年化、回撤和退市代理记录，再完成桌面与390px浏览器验收和Agent结果解释入口。

## 39. 2026-07-27 最终交付：李总策略三档真实等权回测发布与双视口验收

本节晚于第38节。李总策略近3个月、近1年、近3年的真实组合回测已经全部发布，不再处于建设中。三档统一使用2026-07-24为结果数据日，严格保持原9条候选规则，不放宽条件、不制造演示候选；候选集合变化后在下一完整交易日开盘等资金换仓，候选为空时持有现金，主净值计入单边10个基点摩擦成本并对照沪深300。

### 39.1 三档真实结果

| 区间 | 交易日与范围 | 完整/缺口数据 | 区间收益 | 年化收益 | 沪深300 | 区间超额 | 最大回撤 | 换手/成本 | 组合更新 |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 近3个月 | 2026-04-23至2026-07-24，63日，1699只 | 1661/38，覆盖率97.7634% | -7.9447% | -27.9941% | -2.8652% | -5.0795% | -14.2057% | 1742.4793% / 1.7415% | 22次 |
| 近1年 | 2025-07-11至2026-07-24，252日，1948只 | 1910/38，覆盖率98.0493% | +17.4759% | +16.8269% | +15.8011% | +1.6747% | -21.3835% | 5559.9989% / 6.7226% | 71次 |
| 近3年 | 2023-06-12至2026-07-24，756日，2106只 | 2063/43，覆盖率97.9582% | -37.2487% | -13.8828% | +20.9331% | -58.1818% | -50.8943% | 7759.9989% / 5.0660% | 93次 |

换手为各次组合调整换手率累计；成本为所有真实换仓成本相对初始净值的累计比例。三档历史上曾进入候选的股票分别为12、29、35只。独立复算中，近3个月、近1年、近3年分别有1、5、469个净值观察日处于现金状态，其中包含区间开始后等待首个信号于下一交易日开盘执行的日期。

### 39.2 独立正确性核验

1. 直接从 PostgreSQL 读取每日 `candidate_qualified` 集合、复权开收盘价和沪深300历史，不调用回测服务的组合计算函数，独立重建三档组合。数据库候选集合变化与API的22、71、93条换仓记录逐条一致，所有换仓日均为对应信号日后的下一市场交易日。
2. 每次换仓后的开盘持仓市值保持等额，最大浮点差小于 `1.2e-16`；候选为空时持仓数为0、净值按现金收益0延续。独立复算净值与发布曲线的逐点最大差小于 `5e-9`。
3. 三档独立复算的区间收益、最大回撤、沪深300收益、累计换手和累计成本均与API发布值一致。63、252、756个净值点数分别与三个目标交易日窗口完全一致。
4. `002670.SZ`、`300201.SZ`、`600372.SS` 已使用最佳可得历史进入规则计算；后续任务的 `symbols_synced` 不再重复包含这三只股票，证明停牌导致的少量实际观测缺口不会触发永久重试。
5. 38至43只明确缺口股票继续逐日保存为 `data_incomplete`，不进入候选；页面披露完整数量、缺口数量和覆盖率，但不向普通用户展示供应商错误文本。

### 39.3 真实页面与工程门禁

1. 桌面真实浏览器逐一切换近3个月、近1年、近3年，三档均显示正确区间、完整/缺口覆盖、收益指标、沪深300、净值曲线和最近换仓记录；刷新后仍回到李总策略分区，并完整恢复默认近1年结果、策略版本和数据日。
2. 390×844真实视口冷启动成功，策略摘要、三档区间按钮、指标、曲线和换仓记录均可读取；近3年切换成功。验收结束后已清除临时视口覆盖，恢复浏览器默认尺寸。
3. 完整回归为 `754 tests collected` 且全量通过；唯一输出为上游 Starlette TestClient 弃用提示，不影响测试结果。Ruff、Python `compileall`、19个JavaScript文件的 `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。
4. `li_zong_backtest_refresh` 已从回填期间临时30秒恢复为60秒，独立 Worker 保持运行，三档结果会按持久化计划继续检查输入版本和必要修订。

### 39.4 仍需保留的产品边界

这组结果验证的是规则历史表现，不是收益承诺。当前模型尚未模拟涨跌停排队、停牌后的实际成交时点、冲击成本、分红税和真实佣金阶梯；历史退市股票使用最后可得复权收盘价作为强制退出代理，并已在结果边界中明确披露。近3年大幅跑输沪深300且最大回撤超过50%，说明严格规则本身仍需要金融专家复盘，而不是继续通过放宽规则来追求更好看的历史收益。

三档结果当前已通过确定性API和网页发布，但“用户在 Agent 对话中直接追问某一回测区间，并自动装配该区间指标、候选变化与反方证据”的专门证据路由仍未作为本次组合回测交付的一部分完成。下一阶段应复用同一份已发布结果作为 Agent 证据，不让模型自行重算或编造收益数字。

## 40. 2026-07-27 最终修订：双周换仓、同暴露基准与历史缺口修复

本节晚于并正式废止第39节的组合计算口径、结果表和测试基线。第39节保留用于说明问题发现前的历史状态，不得再把其中的22/71/93次换仓、10bp成本、全程沪深300收益或旧覆盖数字作为当前产品结果。

### 40.1 当前唯一有效的组合口径

1. 交易成本固定为0；结果中的换仓成本和累计成本均为0，不再因频繁调仓机械侵蚀净值。
2. 收盘后候选信号最早在下一交易日开盘执行，任意两次实际换仓至少间隔10个交易日。冷却期内候选仍按日更新，但只保留并执行到期时最新的候选集合，不排队执行过时信号。
3. 沪深300采用 `same_exposure_only` 同暴露基准：策略持仓时基准同步持有沪深300，策略空仓时基准与策略净值同时冻结；再次入场时两者均使用当日开盘价恢复暴露。空仓期不再计入与沪深300的收益率比较。
4. 组合已持有的股票若缺少真实复权收盘价，回测拒绝发布，不再沿用上一日价格制造无波动横线。区间内合法停牌和明确退市代理仍按披露边界处理。
5. 历史市值提示日期只用于该日市值判断，不得截断日线、复权因子和涨跌停价的完整查询窗口。上市前自然没有行情的日期不算缺口；股票首次出现真实复权收盘价后，后续无法形成确定规则状态或真实行情的日期均计入覆盖缺口。
6. 沪深300缓存不能只相信元数据声称的截止日，必须核验目标交易日的真实指数行；策略进出场日缺少指数开盘价时拒绝计算，不能以收盘价替代。
7. 长期停牌股票是否已经同步到目标窗口，改由市场历史版本和查询截止日共同判定，不再依赖可能被其他任务覆盖的同步标签。该修复消除了 `002670.SZ` 对同一历史数据的无限重试。

### 40.2 三档正式发布结果

| 区间 | 策略收益 | 年化收益 | 同暴露沪深300 | 超额收益 | 最大回撤 | 完整/总数 | 实际换仓 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 近3个月 | -9.8251% | -33.0734% | -0.5643% | -9.2608% | -30.7675% | 1374/1691 | 6次 |
| 近1年 | +44.9446% | +43.1060% | +23.0523% | +21.8922% | -30.3571% | 1591/1949 | 21次 |
| 近3年 | -9.0819% | -3.0024% | -5.0056% | -4.0764% | -61.7949% | 1714/2106 | 38次 |

三档最短实际换仓间隔均为10个交易日，成本均为0；所有连续空仓日期均满足策略与基准同时冻结。近1年结果显著优于同暴露沪深300，但近3个月和近3年仍跑输，且近3年最大回撤达到 -61.7949%。这说明换仓频率和基准口径修复了比较方法，但没有把规则本身包装成稳定有效策略。

### 40.3 四类根因与修复证据

1. **换仓频率错误**：旧实现每次候选集合变化都在下一开盘换仓，形成22/71/93次高频调整，与用户要求的最短两周不符。现由组合状态机强制10个交易日冷却，并新增冷却期只执行最新目标集合的回归测试。
2. **基准暴露错误**：旧实现策略空仓时沪深300仍持续波动，导致现金期也被纳入相对收益。现逐点保存 `benchmark_exposed`，离场与再入场均用交易日开盘价同步切换暴露。
3. **价格横线与覆盖误报**：旧实现可能沿用持仓旧价格，同时把上市后存在规则缺口的股票计入完整覆盖。现持仓缺真实收盘价直接报错，覆盖统计只忽略上市前日期，并将上市后缺口计入不完整。
4. **历史同步边界错误**：旧实现可能用历史市值提示日截断行情窗口、相信缺真实行的沪深300缓存，并因同步标签变化重复补同一股票。现分别通过完整行情截止日、真实指数行核验和市场历史版本修复。

### 40.4 独立复算与运行门禁

1. 三档均已直接从 PostgreSQL 的逐日候选、复权行情和沪深300真实历史独立重建组合；不调用正式组合计算函数。发布曲线与独立复算逐点最大误差约 `5e-9`。
2. 独立复算确认三档最短换仓间隔均为10个交易日，换仓成本均为0，连续空仓日的策略净值和基准净值均保持不变。
3. 沪深300快照为756行，最后一行是 `2026-07-27`；此前尾部被旧价格延续的福元医药已经恢复真实波动。
4. 完整自动化回归更新为 `761 passed`，仅有1个 Starlette 上游弃用提示；李总组合、Tushare快照定向测试、Ruff、Python `compileall`、全部 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 均应在最终提交前再次执行。
5. 常规持久化计划保持 `li_zong_strategy_refresh=30秒`、`li_zong_backtest_refresh=60秒`。Web 与独立 Worker 必须共同运行；数据质量审计若恰好在 `market_intraday_refresh` 执行期间产生瞬时 critical，应等待任务完成后的下一轮审计确认，而不能把瞬时状态冒充长期数据故障。

### 40.5 继续保留的金融边界

回测仍未模拟涨跌停排队、停牌后真实成交时点、冲击成本、滑点、分红税和真实券商佣金阶梯；“零交易成本”是本轮用户指定的比较条件，不是对真实交易摩擦的否认。覆盖缺口股票继续排除并披露，不能用补值制造完整样本。当前结果只能用于验证这套确定性规则在历史数据中的表现，不能作为未来收益、买卖时点或自动交易承诺。

## 41. 2026-07-28 最新修订：沪深300改为全区间连续基准，并开始优化 Agent 阅读体验

本节晚于第40节，并正式废止第40节中 `same_exposure_only`、空仓期基准冻结及其三档基准收益。第40节仍保留为需求变更历史；当前唯一有效口径以本节和生产数据库中的 `li_zong_2w_no_cost_full_benchmark_v3` 为准。

### 41.1 当前唯一有效的回测比较口径

1. 策略仍不计交易成本，候选集合变化后最早在下一完整交易日开盘执行，任意两次实际换仓至少间隔10个交易日；候选为空时策略持有现金，现金收益按0计算。
2. 沪深300从每个回测窗口首个真实收盘点归一化，在63、252、756个目标交易日内连续计算。策略空仓不会冻结指数，用于衡量持有现金相对市场的机会成本。
3. 兼容字段 `benchmark_exposed` 在所有日期均为 `true`；新字段 `benchmark_policy=continuous_full_period`、`benchmark_trading_days` 明确公开比较口径。
4. 组合版本升级为 `li_zong_2w_no_cost_full_benchmark_v3`，旧口径缓存不会被接口当成当前结果读取。

### 41.2 三档生产结果与独立复算

| 区间 | 策略收益 | 连续沪深300 | 超额收益 | 净值点 | 策略空仓点 | 空仓期间指数实际变化段 | 实际换仓 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 近3个月 | -9.8251% | -1.4036% | -8.4215% | 63 | 23 | 22 | 6次 |
| 近1年 | +44.9446% | +17.0437% | +27.9009% | 252 | 90 | 89 | 21次 |
| 近3年 | -9.0819% | +21.6699% | -30.7518% | 756 | 516 | 515 | 38次 |

直接从 PostgreSQL 读取三档已发布快照，以首尾 `benchmark_nav` 独立重算沪深300区间收益，结果逐档与发布字段完全一致到4位小数。三档 `benchmark_trading_days` 分别为63、252、756；空仓期均能找到相邻交易日指数净值变化，证明新结果不是只修改了说明文字。

### 41.3 Agent 与页面体验本轮改动

1. 个股和大盘 Prompt 改为先回答用户最想知道的结论，再给关键依据；不再强制用“同日价格与市场对照、候选事件、反方与核验”等固定报告栏目开头。没有实质内容的栏目必须省略，普通投资者第一次遇到必要术语时只做一句短解释。
2. 私有 SSE 仍不展示未校验模型草稿；最终通过校验的正文改为按自然段在原消息节点安全渐进显示，不会先出现一篇长文再跳成另一篇摘要。
3. 长回答完成时默认把回答开头锚定在消息区顶部；短回答保持完整可见。用户在生成期间主动滚动或触摸消息区后，页面不再抢回滚动位置。
4. 一级入口改为更直白的“市场总览”“AI投研对话”“金融资料库”；全球市场交易状态、时间和来源说明等关键辅助文字由9—10px提升到11px，并允许行情时间说明换行，减少截断。

### 41.4 真实模型与浏览器验收

1. 中兴通讯问题 Run `91f6d48d-6af3-4516-bec8-bc03979e6a3c` 使用 `deepseek-v4-pro`、一次模型调用、输出守卫通过。第一段先纠正7月27日最新报价上涨0.71%，随后直接解释7月24日下跌更像通信设备行业与大盘同步；页面不再以旧的“同日价格与市场对照”栏目开头。
2. 美股问题第一次复测仍先列指数事实，因此继续收紧 Prompt，明确第一传达句必须回答“为什么”。最终 Run `84c77088-76bc-46e5-8eef-419741f58c42` 第一段直接回答：7月23日下跌更像大型科技公司资本开支担忧叠加油价压力；`deepseek-v4-pro` 一次调用、输出守卫通过、请求总耗时约9.15秒。
3. A股问题 Run `4501f8be-5594-4edc-b578-cd88f57a6a0a` 第一段直接回答“今天A股全面走强，赚钱效应突出”，随后给5532只股票中5194只上涨、成交额约2.09万亿元等确定性依据；一次模型调用、输出守卫通过、请求总耗时约9.12秒。
4. 三次最终回答均在原消息节点完成，没有出现长文跳成另一段摘要。515px与523px长回答完成后消息区均停留在回答开头附近而非滚到末尾；页面控制台无产品 warning/error。
5. 真实李总策略页面默认近1年显示策略 `+44.94%`、连续沪深300 `+17.04%`、超额 `+27.90%`、21次换仓；切换近3个月显示 `-9.83% / -1.40% / -8.42%`，切换近3年显示 `-9.08% / +21.67% / -30.75%`，图例明确写“空仓期指数仍继续变化”。
6. 仓库浏览器门禁覆盖桌面与390px的市场总览、透明选股、个股概览、个股历史、复盘中心和AI投研对话，共 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260728-agent-ux/result.json`。
7. 最终完整回归为 `762 tests collected` 且全量通过；唯一提示为 Starlette TestClient 上游弃用警告。Ruff、Python `compileall`、全部静态 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。

至此第10项目标已完成并验收；回答质量与阅读体验完成了本轮高影响改进，但仍属于需要持续抽样、继续降低模板感的长期目标，不能因三次成功样本宣称全部完成。

## 42. 2026-07-28 W2：我的关注与个股研究统一K线体验

本节晚于第41节。W2已完成K线部分的真实实现与双视口验收；长期交互目标仍处于进行中，下一工作包继续处理信息架构、字号密度和金融顾问能力。

### 42.1 当前实现

1. 新增 `app/static/qs-kline-explorer.js` 作为共用行情组件。“我的关注”和“个股研究”都通过同一组件访问 `/stocks/{symbol}/intraday` 与 `/stocks/{symbol}/history?range=2y`，不再分别维护简化蜡烛图和交互日K。
2. 周期与范围被明确拆成两组：周期为分时、日K、周K、月K；查看范围为近3月、6月、1年、2年。分时只显示当日。产品不把“近1年日K”错误称为“年K”；若未来增加年K，必须是真实年度OHLCV聚合。
3. `qs-charts.js` 新增周/月OHLCV聚合和日历范围计算；MA5/10/20始终先在全量周期数据上计算，再按可见窗口绘制。交互继续支持拖拽平移、滚轮和键盘缩放、十字光标、日期/OHLCV提示、成交量与双击复位。
4. 新增可关闭的行情详情层。桌面弹层最大1180px、图表440px高；390px使用全屏详情和310px图表。关闭详情后，周期和范围同步回原页面；刷新后恢复用户最后选择。
5. 修复了实测发现的口径拼接问题：历史日K、周K、月K的顶部价格和涨跌均来自同一周期的最后两根K线；盘中现价只在分时周期展示，不再把盘中现价与历史周期涨跌拼在一起。
6. 页面加载失败时只提供“行情图表暂未完整返回”和重新加载按钮，不显示供应商报错、后台任务名或内部枚举。

### 42.2 测试与真实页面证据

1. `tests/test_qs_charts_contract.py` 覆盖周/月开高低收和成交量聚合、范围切片、月K小样本最小可见根数、同周期涨跌，以及两处页面共用组件、真实接口和放大详情合同。
2. 完整测试为 `762 tests collected` 且全量通过；唯一提示为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript `node --check`、`uv lock --check` 与 `git diff --check` 全部通过。
3. 桌面真实页面：我的关注默认真实分时成功，切换周K与近1年成功；刷新后仍恢复周K/近1年；1180×688详情层显示完整K线、MA和成交量。个股研究的中兴通讯日K/近1年使用同一组件，页面无横向溢出。
4. 390×844真实页面：个股研究图表宽342px、高235px，页面、组件和工具栏横向溢出均为0；全屏详情为390×844、图表310px高，详情与页面横向溢出均为0。
5. Web已重启加载新增静态资源；`/health=ok`、Hermes启用、数据健康53/54且1项同步关注、独立Worker活跃2个、持久化队列无等待任务。后台Worker未因Web重启而中断。
6. 仓库浏览器Runner已把“我的关注K线”和“个股K线”加入桌面与390px长期门禁；后续每次改动都要继续验证真实行情，而不是只检查Canvas存在。

下一断点已由第43节接续。

## 43. 2026-07-28 W2续：我的关注信息架构与可读性验收

本节晚于第42节。W2本轮范围已经完成：不仅统一K线，也把“我的关注”从后台模块堆叠改为普通用户可以顺着完成任务的页面。长期交互目标仍为 active，下一轮进入对话阅读、金融顾问人格与知识检索，不得因为单页通过就宣称整体体验成熟。

### 43.1 页面重排与文案

1. 页面顺序固定为“关注股票与行情 → 选中股票的分时/K线详情 → 下一步研究 → 关注股票研究报告”。原先四张“今日研究更新”大卡不再挡在股票列表前。
2. 列表顶部新增股票数量、持仓/关注数量、行情覆盖和“点击股票查看详细行情”提示。原“研究资产概览”与列表统计重复，已从普通用户界面隐藏；既有DOM节点只为当前渲染函数兼容保留。
3. “近期研究任务”改为“下一步研究”；“今日研究更新”改为“关注股票研究报告”；主按钮改为“让 Agent 汇总关注股票”。
4. 报告列表桌面两列、手机单列，最大高度分别为430px和540px，使用自身纵向滚动容器。真实鼠标滚轮测试中，报告区从 `scrollTop=0` 滚到 `420`，页面没有跳到其他模块。
5. 报告标题提升到14px、正文13px、时间和状态11—12px；K线控件说明12px、图例和指标标签11px。个股摘要中的状态和详情同步提升，降低9—10px密集小字。
6. “服务器公共证据快照”“后台尚未形成这只股票”等内部术语已从关注页、报告阅读入口和Agent复核问题中移除，改为“公开证据研究底稿”“研究报告正在准备”等用户语言。已有报告仍只作为证据，Agent必须基于当前问题和最新数据重新生成回答。

### 43.2 浏览器与合同证据

1. 1280×720实测：股票列表、K线、任务、报告的纵向顺序与设计一致；共4只股票、行情覆盖4/4；报告容器 `clientHeight=430`、`scrollHeight=648`、两列且 `overflow-y=auto`。
2. 390×844实测：页面文档宽390px、关注页内容宽340px、K线Canvas宽304px；整页和可见子元素横向溢出均为0。“进一步筛选”完整换行显示，不再藏在横向滚动区。报告单列，`clientHeight=540`、`scrollHeight=1410`。
3. 页面可见区域未出现“服务器公共证据快照”或“后台尚未形成”。研究资产概览节点保持隐藏，避免重复摘要。
4. `tests/test_api.py` 新增页面顺序、用户文案、报告滚动和内部术语门禁；`tests/test_qs_charts_contract.py` 继续保障两处页面共用真实K线组件。完整回归为 `762 tests collected` 且全量通过；Ruff、compileall、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 通过。
5. 仓库浏览器门禁覆盖桌面与390px共16条核心路径，16通过、0失败；报告位于 `/tmp/qingshu-browser-smoke-20260728-watchlist-ia/result.json`。
6. Web已重启为本轮代码，`/health=200`、Hermes启用、数据健康53/54；独立双Worker没有因Web重启而中断，持久化队列queued=0、running=2。

下一断点：W3补“用户主动滚动后停止自动抢滚动”的浏览器断言，并继续抽样长回答；随后进入W4金融顾问/教育者人格、基金ETF知识库、自选股预分析与非自选股在线取证Skill。其他页面的小字与高密度卡片仍需逐页治理。

## 44. 2026-07-28 W3：安全流式正文、用户阅读优先与动态回答合同

本节晚于第43节。W3本轮范围已经验收：用户不再只看进度卡等待完整回答，也不会在主动向上阅读时被后续片段或最终答案拉回底部；已有安全流式正文不会在最终校验后清空并重新播放。长期产品目标仍为 active，下一工作包进入金融顾问/教育者知识与检索体系。

### 44.1 实现

1. `agent_hermes_execution.py` 给通过部分确定性守卫的累计句子增加 `is_guarded_partial=true`；`chat_streaming.py` 只在当前用户与随机请求ID对应的私有SSE中转发这一标记。未通过守卫的原始delta仍不进入页面。
2. `qs-agent-ui.js` 新增安全正文渲染。安全正文出现后，普通研究进度事件不再覆盖正文；最终回答在同一个消息节点完成，若已有安全片段则不走“清空后按段播放”的路径。
3. 新增统一的用户阅读优先状态。滚轮、触摸、鼠标按下、PageUp/PageDown、Home/End和方向键都会停止当前回答的自动跟随；安全片段更新与最终答案都尊重该状态。
4. `agent_prompt_contracts.py` 要求个股与大盘第一段先直接回答用户最想知道的结论和确认边界；后续结构按问题动态生成，没有实质内容时省略“候选事件”“反方证据”“下一步核验”等空栏目。普通投资者第一次遇到必要术语时，只补一句简明解释。
5. `scripts/browser_smoke.py` 新增不写业务数据的真实DOM行为门禁：在桌面与390px页面中构造长对话，验证安全片段显示、进度不覆盖、用户滚动停止跟随、最终滚动保持、正文一致和无逐段重放。

### 44.2 真实模型与浏览器证据

1. DeepSeek Run `eeef5111-fb1f-4935-8be1-36a5ac5b9ba3` 使用 `deepseek-v4-pro`，状态completed。问题要求普通投资者理解中兴通讯经营、现金流与行业风险；回答第一段直接指出“收入增长但利润和现金流承压”，随后用通俗语言解释毛利率、经营现金流、存货与半年报核验点，没有机械套用固定栏目。
2. 该Run首Token为5.619秒、首个安全可见片段为14.779秒、请求完成为19.558秒；678个原始delta形成7次安全可见更新，最终安全片段1083字符，`withheld=0`、`deferred=20`。
3. 第二次真实长回答在安全片段出现后由 `scrollTop=1522` 主动上滚到 `1162`；最终回答在19.609秒到达后仍保持 `1162`，`userNavigatedDuringRun=true`，证明没有抢回底部。
4. 仓库长期浏览器门禁 `artifacts/browser-smoke-w3/result.json` 共16通过、0失败。桌面测试从1113上滚到933，390px从1146上滚到966；两者在后续片段与最终答案到达后均保持原位置，`progressiveReplaySeen=false`。
5. 完整回归为 `762 tests collected` 且全量通过；唯一提示为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript语法、`uv lock --check`、`git diff --check` 均通过。
6. 当前 `/health=ok`、Hermes启用、PostgreSQL领域库和运维库正常、独立Worker 2个；数据健康53/54，剩余1项为同步关注，未阻塞本轮验收。

下一断点：进入W4。优先建立金融顾问兼教育者的人格和可检索基础知识，覆盖股票、基金、ETF、债券、利率、风险收益与资产配置；同时把自选股预分析证据路径和非自选股在线取证Skill写成版本化合同，并继续用“是什么、为什么、怎么核验、适不适合我”真实问题验收。其他页面小字与高密度卡片仍需逐页治理。

## 45. 2026-07-28 W4第一阶段：金融顾问人格、知识检索与适合性问答

本节晚于第44节。W4第一阶段已经通过代码、真实模型和双视口门禁，但 W4 整体只能标记为部分完成：本轮建立了通用金融教育与适合性边界，尚未建立基金/ETF实时产品数据库、正式风险问卷、完整资产配置或两条证券取证 Skill 合同。

### 45.1 已实现能力

1. 新增 `financial-products-basics.md`、`fund-etf-practical-guide.md`、`fixed-income-and-rates.md` 和 `risk-return-and-allocation.md` 四份通用知识。基金问题按题强制检索相关资料，并排除无关个股财报、研报和预生成研究报告。
2. Agent 系统人格与 `general-research` Skill 改为金融顾问、研究助手和教育者。概念问题解释“是什么、收益从哪里来、主要怎样亏、实际怎么检查”；产品比较只保留底层资产、流动性、费用和使用门槛等真正相关维度。
3. `build_financial_advisor_context()` 区分概念与个人适合性。适合性缺少资金使用时间、可接受回撤、应急储备、负债或已有资产时返回 `needs_profile`；Prompt只允许给条件逻辑和最多两个关键问题，不允许直接给唯一产品、比例、目标收益或定投操作。
4. 修复“ETF”被当成证券代码、基金产品选择触发全A股筛选，以及“个人语境 + 根据条件选择”未进入适合性路径三类路由问题。“基金与ETF”快捷入口保持在 AI 投研对话页并进入 `general_research`。
5. 新增 `agent_financial_advisor.py`。它不是另建一套拒答系统，而是在既有通用数字守卫前，把适合性回答中自创的期限、百分比、回撤阈值和过强类别结论改成定性表达，使后续守卫无需删除整条有用追问。

### 45.2 真实问题揭示的缺陷与修复

1. 第一次快捷问答被识别为概念问题，模型自创“一两年、五年以上”。根因是“应该根据哪些条件选择”不在适合性识别词中；现用个人语境与选择/配置意图联合判断。
2. 后续真实 DeepSeek 回答在第二条追问里自创 `5%`。旧 `drop_unsupported_numeric_lines_v1` 删除整行，导致页面写“告诉我两点”却只剩一问。当前在完整数字守卫前先把百分比改成定性波动情景，完整保留两条追问。
3. 继续抽样发现“半年内”“三年长期不用”“只能选某类”和“跌 明显幅度”等变体；这些真实措辞已经进入 `tests/test_financial_advisor.py`，避免只针对一次输出修复。

### 45.3 最终真实验收

1. 最终 Run `ec4899fc-4c86-4d71-b1d0-f7b1c3c7bf9d` 使用 `deepseek-v4-pro`、一次模型调用；intent 为 `general_research`，适合性状态为 `needs_profile`，只引用基金/ETF与金融产品基础知识。
2. 首 Token 6.531秒、首个安全可见片段6.733秒、请求总耗时18.069秒；输入2995 tokens、输出665 tokens，输出守卫通过，`withheld=0`、`deferred=0`。
3. 最终页面回答先解释基金与ETF不是简单二选一，再说明交易账户、买卖方式、费用、流动性和底层资产差异；最后完整追问资金期限与亏损承受情况，没有直接替用户选产品，也没有具体收益、期限或回撤阈值。
4. 完整回归为 `777 tests collected` 且全量通过；唯一提示为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
5. 最终浏览器门禁 `artifacts/browser-smoke-w4-final-2/result.json` 覆盖桌面与390px共16条核心路径，16通过、0失败；真实点击快捷入口未进入个股研究或透明选股，页面控制台无产品 warning/error。
6. 当前 `/health=ok`、Hermes启用、PostgreSQL双库正常、独立Worker 2个、队列无等待积压；数据健康53/54且0 critical。

本节当时的下一断点已由第48节完成：基金/ETF产品数据、同口径比较、正式风险问卷与用户确认边界已经落地。完整财富管理、商业数据授权和完整资产配置仍未完成。

## 46. 2026-07-28 最新修订：李总策略恢复同暴露基准并完成 v4 生产验收

本节晚于第41节，并按用户之后的明确要求废止第41节的 `continuous_full_period` 连续基准口径。第41节保留为需求变更历史，当前唯一有效口径是本节和生产数据库中的 `li_zong_2w_no_cost_same_exposure_v4`。

### 46.1 当前唯一有效口径

1. 交易成本固定为0；候选集合变化后最早在下一完整交易日开盘执行，任意两次实际换仓至少间隔10个交易日，冷却期只执行最新候选集合。
2. 沪深300只在策略实际持仓期间保持同等市场暴露；策略转为空仓时，基准在同一交易日开盘同步退出并冻结；策略再次入场时，基准在同一交易日开盘恢复。空仓期不计入与沪深300的相对收益比较。
3. 当前字段为 `benchmark_policy=same_exposure_only`，`benchmark_trading_days` 等于策略实际持仓交易日；每个净值点保留 `benchmark_exposed`，便于逐日核验。
4. 组合版本升级为 `li_zong_2w_no_cost_same_exposure_v4`，旧 v3 连续基准缓存不会被当前接口读取。
5. 持仓股票缺少真实复权收盘价时拒绝发布，不沿用上一日价格制造无波动横线。区间内明确可解释的停牌和退市代理继续按既有边界处理，数据不完整股票从候选中排除并披露覆盖率。

### 46.2 三档已发布生产结果

| 区间 | 策略收益 | 同暴露沪深300 | 超额收益 | 最大回撤 | 完整/总数 | 实际换仓 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 近3个月 | -9.8251% | -0.5643% | -9.2608% | -30.7675% | 1383/1691 | 6次 |
| 近1年 | +44.9446% | +23.0523% | +21.8922% | -30.3571% | 1600/1949 | 21次 |
| 近3年 | -9.0819% | -5.0056% | -4.0764% | -61.7949% | 1723/2106 | 38次 |

三档累计交易成本均为0，实际最短换仓间隔均为10个交易日。近3个月、1年、3年分别有20、84、501个连续空仓冻结段，逐点检查中基准变化违规均为0；基准参与比较的交易日分别为40、162、240日。

### 46.3 数据缺口与无波动复核

1. 当前明确缺口股票分别为308、349、383只，均以 `data_incomplete` 排除并在页面披露，不会通过补值伪装为可计算候选。
2. 三档均能完整计算，说明实际持仓日不存在缺失收盘价；缺少持仓价格会直接抛错并拒绝发布。
3. 3年曲线仍有3段持仓净值恰好不变，逐段回查原始状态均有真实开盘价和收盘价：`603108.SS` 在2023-11-21/22收盘同为23.70元，`002244.SZ` 在2024-10-18/21收盘同为9.90元，`603300.SS` 在2025-02-19/20收盘同为9.07元。它们是相邻交易日真实收盘相同，不是数据缺口或旧价格填充。

### 46.4 页面与工程验收

1. 真实页面默认近1年显示策略 `+44.94%`、同暴露沪深300 `+23.05%`、超额 `+21.89%`、21次组合更新且标注 `≥10日`。
2. 切换近3个月显示 `-9.83% / -0.56% / -9.26% / 6次`；切换近3年显示 `-9.08% / -5.01% / -4.08% / 38次`。图例明确写“沪深300同暴露基准”“零交易成本；策略空仓期不计入比较”。
3. 实测发现图表读屏标签仍残留“连续区间累计收益曲线”，已修正为“沪深300同暴露累计收益曲线”并加入静态资源合同测试；最终页面控制台无 warning/error。
4. Web 与独立 Worker 已重新加载代码，三档生产快照均为 v4。后续不得恢复第41节连续基准口径，也不得用第41节三档指数收益作为当前产品结果。

## 47. 2026-07-28 W5：自选股预分析与非自选股在线取证双路径

本节晚于第46节。W5 已完成代码、合同、完整回归和两条真实 DeepSeek 路径验收；后续不得重新允许非自选股读取服务器预生成报告，也不得把预生成正文直接返回给用户。

### 47.1 合同与实现

1. 新增 `app/services/stock_research_contract.py`，合同版本为 `stock_evidence_path_v1`。自选股使用 `watchlist_preanalysis`，非自选股使用 `online_research`；合同随 Run input 和压缩 Prompt 持久化。
2. 新增 `watchlist-stock-research` 与 `online-stock-research` Skills。自选股每轮刷新行情、资讯、估值和事件，财报质量、主营、股东、预期、同行和历史走查等慢变模块可按 ResearchPlan 时效复用；只有在线刷新失败时才允许按模块使用最近服务器证据回退。
3. 非自选股不加载数据库中碰巧存在的服务器报告，行情、公司资讯、财务估值和事件时效固定为0并实际尝试刷新。`precomputed_report_reference=null`、`force_online_refresh=true`，报告正文不进入证据或 Prompt。
4. 两条路径的 `answer_source` 均为 `new_model_run`。预生成报告只允许保存版本化证据引用，不得回放、拼接或复述正文。
5. Prompt 与 Skills 强制区分公司/监管正式披露和媒体线索，不把混合列表统称为媒体材料；不预告固定证据数量；内部“风险委员会、研究管理层、多方证据结论”必须翻译成自然用户语言。

### 47.2 真实缺陷与修复

1. 中兴通讯自选股 Run `d3ce09af-8c79-48ab-aa23-8b86b50c7133` 使用 `deepseek-v4-pro`，状态 completed，路径为 `watchlist_preanalysis`；行情、公司资讯、财务估值和事件为本轮刷新，慢变模块按合同复用，页面未显示内部路径、供应商错误或预生成报告正文。
2. 贵州茅台第一次在线 Run `30130dc6-c9ea-4431-a04d-e1bff9d567b1` 已正确走 `online_research`，但完整 DeepSeek 回答写“当日微跌0.61%”时，旧守卫只识别“下跌/跌幅”等词，误判为没有提供当前涨跌并回退确定性摘要。
3. 当前守卫加入“微涨/微跌”自然措辞识别，并新增真实措辞回归。重试 Run `76d1a251-9f54-4e6a-a290-db7b0ca2fd69` 状态 completed，模型 `deepseek-v4-pro`，`online_research`、`force_online_refresh=true`、`precomputed_report_reference=null`、`fallback_used=false`，输出守卫通过。
4. 该重试首Token 5.181秒、首个安全可见内容6.752秒、模型17.791秒、总请求19.565秒。页面显示1340字符针对性回答，不再显示“即时解读暂未形成完整结论”。
5. Web 重启后用完全相同问题复验 Run `20b818fe-a0ab-4067-8aba-1b470ff974e0`：状态 completed、模型 `deepseek-v4-pro`、路径 `online_research`，强制在线刷新、无报告引用、无回退、守卫通过。数据库最终回答对“风险委员会”“研究管理层”“多方证据结论”三类内部措辞命中均为0，证明最新 Prompt 已在真实服务中生效；同行比较仍作为明确证据缺口展示。

### 47.3 最终门禁与下一断点

1. 完整回归为 `784 tests collected` 且全量通过；唯一提示为 Starlette TestClient 上游弃用警告。
2. Ruff、Python `compileall`、全部静态 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。
3. 贵州茅台本轮11个计划模块中取得10个，同行比较仍为明确缺口；该缺口没有阻塞行情、公告资讯、财务、事件、股东、预期和历史走查进入模型。
4. 下一断点：扩展正式披露全文和高质量在线来源、建立来源成功率与时效统计；继续缩短普通个股回答，清理后台分析角色口吻。基金/ETF实时产品库和正式风险问卷由第48节接续。

## 48. 2026-07-28 W4第二阶段：基金/ETF产品库、风险画像与真实问答

本节晚于第47节。基金/ETF产品事实、正式风险问卷、后台刷新、页面入口和真实 DeepSeek 比较已经完成；第47节末尾“仍属于下一阶段”的判断由本节废止。

### 48.1 数据、数据库与后台

1. 新增 `app/providers/funds.py` 和 `app/services/fund_products.py`。东方财富提供产品搜索、净值、历史收益、规模、申赎状态、基金公司与经理等事实；ETF场内价格优先东方财富，失败时回退腾讯和新浪。
2. 领域库升级到 PostgreSQL Schema v5，持久化基金产品快照和每位用户的风险画像版本。接口包括 `/fund-products/search`、`/fund-products/{code}`、`/fund-products?codes=...`、`/me/risk-profile`、`/me/risk-profile/draft` 和 `/me/risk-profile/confirm`。
3. 持久化队列新增 `fund_product_refresh`，默认每1800秒刷新 `510300、510500、159915、110022、000011、000198`。最近真实任务6只全部完成、0失败；Worker保持双并发，队列无等待积压。
4. 风险问卷答案先保存草稿；只有当前用户明确确认具体版本后，Agent才能读取正式画像。修改答案会使页面重新要求保存与确认；浏览器验收没有擅自写入或确认用户正式事实。

### 48.2 前端与真实数据验收

1. AI投研首页新增显眼的“基金与ETF”卡片，支持名称/代码识别、歧义选择和多产品比较。产品卡明确区分净值与ETF场内成交价，并展示净值日期、历史收益、规模与申赎状态。
2. 个人中心新增七项通俗风险问卷，草稿、待确认和已生效状态对普通用户可见；没有草稿时确认按钮禁用。
3. 桌面比较结果展开后为三张主入口一行、基金比较整行；390×844无横向溢出，基金结果与问卷均可滚动阅读。
4. 真实数据截至2026-07-27：510300净值4.7583、场内价4.753、+1.11%；159915净值3.6146、场内价3.612、+2.96%；110022净值2.908。净值和场内价格不共享同一时间字段。

### 48.3 真实缺陷与修复

1. 第一轮基金适合性回答中，`needs_profile` 输出修复把产品证据中的真实百分比与“近1年”等窗口错误替换成模糊文字。当前只屏蔽模型自创的个人亏损阈值，保留 `fund_product_context` 中受支持的收益数字和期限窗口。
2. 浏览器日志发现问题中的 `510300` 曾触发 `/stocks/510300/history?range=3mo`。前端已新增基金产品诊断上下文与专用研究依据，基金问题不再进入股票K线路由；最终复验日志没有该请求。
3. 最终 Run `552e7127-18da-455b-a6e7-0134b311a788` 使用 `deepseek-v4-pro`、状态 completed、输出守卫通过。回答保留510300与159915近1月、3月、6月、1年的真实收益，并明确画像未确认时不能替用户选择唯一产品。

### 48.4 最终门禁与下一断点

1. 完整回归 `792 passed in 289.45s`；唯一提示为 Starlette TestClient 上游弃用警告。
2. Ruff、Python `compileall`、全部静态 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
3. 当前 `/health=ok`、Hermes启用、领域库Schema v5、运维/队列Schema v4、独立Worker 2个、队列无等待积压、数据健康53/54且0 critical。
4. 下一断点：扩展基金公告与定期报告全文、费率全口径和指数跟踪误差；继续做债券、利率、资产配置与中老年真实任务抽样，同时保持“用户确认后才成为正式画像”的边界。

## 49. 2026-07-28 W6：个股研究信息归位与普通用户渐进披露

本节晚于第48节。W6只调整信息架构、标题、字号与长期浏览器门禁，不删除七阶段研究、证据、任务或用户正式对象。

### 49.1 本轮改动

1. `deepStockJourney` 从“研究概览”移入“AI研究”；绑定对话通过 `syncAgentPlacement()` 放在七阶段流程之前。概览只承担行情、当前判断、最重要问题、代表证据和摘要入口。
2. 个股概览标题改成普通用户任务语言。完整证据地图每类默认显示1条代表证据，其他证据仍可在“查看全部”或“数据与证据”标签恢复。
3. 复盘页 `REVIEW` 改为“研究复盘”。个人中心内层标题改为“我的研究空间”，账户标识、服务状态、后台刷新、隐私和版本边界放进默认关闭的 `<details>`，不再与风险画像平铺争抢首屏。
4. 个人中心技术字段字号提升到12px、说明文字11px；折叠区展开后仍保持现有真实ID和后端状态绑定，没有制造虚构账号、会员或安全能力。
5. `scripts/browser_smoke.py` 新增 `/account`，并为复盘和个人中心加入中文标题、默认折叠、提示文案与最低字号门禁。Runner仍只执行读取与页面内显示切换，不写正式用户对象。

### 49.2 验收证据

1. 真实390×844个股概览无横向溢出，四类核心卡片宽344px，证据正文12px，七阶段流程不可见。
2. 真实390×844“AI研究”中 `#agentSection` 位于 `#deepStockJourney` 之前；对话可直接使用，七阶段流程随后展示。
3. 个人中心桌面和390px均显示“我的研究空间”，服务说明默认关闭；展开后技术字段12px、说明11px且无横向溢出。
4. 复盘中心桌面和390px均显示“研究复盘”。
5. `artifacts/browser-smoke-w6-information-architecture/result.json` 为18通过、0失败；覆盖桌面和390px的市场、选股、关注、个股概览/K线/历史、复盘、个人中心与AI阅读行为。
6. 完整回归仍为 `792 tests collected` 且全量通过；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
7. 当前运行态 `/health=ok`、Hermes启用、PostgreSQL Schema v5/v4、外部Worker 2个；队列无ready/delayed/retrying积压，数据健康53/54且0 critical。

### 49.3 下一断点

继续按普通用户首次任务审计市场总览、透明选股和复盘内容区；缩短个股证据与回答正文；验证已确认风险画像后的个人中心摘要体验。不得把本轮信息架构完成表述为十项目标全部完成。

## 50. 2026-07-28 W7：透明选股与复盘首次用户路径

本节晚于第49节。W7处理真实390px走查暴露的普通用户理解与空状态问题，不改变李总策略规则、正式交易复盘确认边界或文章后台生产机制。

### 50.1 本轮改动

1. 选股分区改为“按条件选股 / 李总策略”；李总策略英文 `DETERMINISTIC STRATEGY` 改为“规则公开 · 每项可核验”。390px下七个结果分类自动换行，不再依赖用户发现横向滚动。
2. 选股页标签、规则、指标、区间和说明文字提升字号；“按条件选股”首屏用用户任务语言解释行业、市值和估值筛选。
3. 交易复盘为空时，列表与详情都提供“去我的关注”，引导用户先记录真实操作。页面没有自动创建计划、操作或复盘。
4. 市场文章公开层从每篇文章保存的 `market_brief` 证据确定性重建标题和摘要，优先展示A股市场日期、沪深京股票总数、上涨/下跌/平盘家数、涨跌幅中位数、当日累计成交额和领先板块；历史文章正文与内部证据继续保留，但不把“跨市场分时”作为用户摘要。
5. 市场复盘删除“当前 MVP”“后续将”等内部规划，只保留历史事实与不预测下一交易日的边界。
6. 浏览器 Runner 新增 `/reviews?tab=market`，并长期检查选股中文标签、390px策略分类换行、空交易复盘入口、市场摘要事实和内部规划清理。

### 50.2 验收证据

1. 真实市场复盘显示4篇按时间排序的文章。2026-07-27文章公开摘要为沪深京5,532只、上涨5,194只、下跌286只、平盘52只、涨跌幅中位数+2.72%、当日累计成交额20,883亿元，并展示当时领先板块；其他日期使用各自保存证据，不复用同一摘要。
2. 真实交易复盘空状态同时显示用户说明和“去我的关注”；真实李总策略页显示中文可核验标签，390px `flex-wrap=wrap`、页面宽390px且无横向溢出。
3. `artifacts/browser-smoke-w7-novice-paths/result.json` 为桌面与390px十个核心页面共20通过、0失败；控制台错误、页面异常、非预期本地失败响应和页面级横向溢出均为0。
4. `uv run --extra dev pytest --collect-only` 收集793项，`uv run --extra dev pytest -q` 全量通过；唯一提示为Starlette TestClient上游弃用警告。
5. `uv run ruff check .`、Python `compileall`、全部静态JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。
6. Web已安全重启并加载文章公开层；领域库Schema v5、运维/持久化队列Schema v4、外部Worker 2个，持久化队列状态正常。重启没有停止PostgreSQL或Worker。

### 50.3 下一断点

继续用首次用户任务审计市场总览和透明选股结果解释，缩短普通个股回答与证据句；验证已有真实交易记录时的复盘详情路径和已确认风险画像摘要。不要把20条只读页面门禁表述为正式生产就绪或完整金融内容验收。

## 51. 2026-07-28 W8：市场总览与条件选股清晰度

本节晚于第50节。W8吸收同事高保真方案中“市场广度优先、清晰分组、任务导向标题”的优点，但继续拒绝静态演示数字、黑盒评分和自动投资结论。

### 51.1 本轮改动

1. 市场页顺序固定为“全球行情 → A股一眼看懂 → 四个核心指数 → 市场Agent → 市场复盘文章 → 个人待办”。删除重复的“我的研究脉冲”，个股报告和机会不再混入市场文章。
2. 新增“A股一眼看懂”：直接展示市场状态与上涨/下跌家数、全市场涨跌中位数、当日累计成交额和领涨方向。四张卡在桌面为四列、390px为两列。
3. 市场Agent入口改为“问 Agent：今天市场发生了什么？”，示例为“今天A股为什么普涨？哪些行业在领涨？”，按钮改为“分析市场”。
4. 条件选股结果新增“这批结果怎么用”；行情日期、财务报告期、覆盖率和数据版本收进默认关闭的详情。候选增加“为什么出现在这里”，普通候选动作精简为“加入我的关注 / 研究这只股票”。
5. 选股结果解释、入选理由和关键辅助文字提高到13px左右；浏览器烟测内数值解析统一使用 `Number.parseFloat`。

### 51.2 真实验收与工程证据

1. 真实市场页显示2026-07-27普涨：上涨5,194只、下跌286只，中位数+2.72%，成交额2.09万亿元、较上一完整交易日+7.41%，领涨方向为个护小家电+10.03%；市场文章仅4篇大盘内容。
2. “打开市场复盘”真实点击进入 `/reviews?tab=market`，市场复盘页签保持选中，4篇文章按新到旧显示。
3. 真实执行“相对行业增强候选”得到12只候选；覆盖详情默认关闭，12张卡动作数均为2。390px候选卡宽332px、解释与入选理由13px，页面无横向溢出。
4. `artifacts/browser-smoke-w8-market-screening-clarity/result.json` 为桌面与390px十个核心页面共20通过、0失败；控制台错误、页面异常、非预期本地失败响应和页面级横向溢出均为0。
5. 全量 `793 passed in 115.37s`；唯一提示为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
6. Web已重启；`/health=ok`、Hermes启用、领域库Schema v5、运维/队列Schema v4、外部Worker 2个、ready/delayed/retrying均为0、53/54健康且0 critical。

### 51.3 下一断点

继续处理普通个股回答与证据句过长、已确认风险画像的个人中心摘要，以及已有真实交易记录时的复盘详情路径。市场复盘仍只是已保存事实文章入口，不得表述为带判断版本和后续验证链的正式结构化市场复盘对象。

## 52. 2026-07-28 W9：涨跌问答证据裁剪与轻量行情路径

本节晚于第51节。标准个股涨跌问答已经从完整研究证据包中拆出轻量路径；后续不得为了“回答丰富”重新塞回整份研究报告、无关慢变模块或低价值媒体榜单。

### 52.1 当前合同

1. 只优先装配当前报价、上一完整交易日、同日代表性指数、全市场广度、精确行业可用性和日期对齐事件；当前报价、历史日线和用户指定日期保持独立时间锚。
2. 同日正式披露、开盘前/交易时段媒体线索、收盘后事件和邻近日事件分别存放。收盘后与邻近日内容不能解释目标交易时段。
3. 融资余额、股权登记榜单等低价值标题不进入因果候选；完整来源仍可在折叠证据区审计，但不得扩写资金敏感、登记日卖出、获利回吐或除权故事。
4. 回答继续由本轮 `deepseek-v4-pro` 即时生成，并经过受控安全流式和最终校验；保存报告与确定性摘要只作证据或故障边界。

### 52.2 边界

精确行业盘中序列和高质量同日正式来源仍可能缺失。价格、指数与行业横截面只能确认同步或分化，不能证明个股因果；缺少直接事件证据时必须保留“具体驱动仍未确认”。

## 53. 2026-07-28 W10：自然回答、模板回退与最终验收

本节晚于第52节；当前最新续接断点已由第54节覆盖。

### 53.1 本轮完成

1. 支持“约3%”“5000多只”“超过5500只”“约51%上涨”等与确定性数据一致的自然舍入，避免正常口语触发整段回退。
2. 修复回答清理后首段消失、页面直接从第二项开始的问题；最终回答不再回退到“候选事件 / 反方证据 / 下一步核验”固定模板。
3. 普通用户明确要求“小白能看懂、直接说、别写成报告”时使用短自然段。没有确认具体驱动时，最后一句必须停在确认边界，不得用市场买卖力量、资金博弈、正常波动、缺少利好承接或安慰性结论补故事。
4. Web 已重新加载最新 Prompt；外部 Worker 保持双并发。重启后14个到期任务已清零，最终 `/health=200`、`/ready=200`、ready/delayed/retrying为0，数据健康53/54且0 critical。
5. `scripts/check_operations.py` 的业务库、运维库、队列延迟、失败率和Worker门禁均通过，但总体仍为 `degraded`；唯一失败是 `/Users/chr/.qingshu/backups` 缺少可核验 PostgreSQL 备份。

### 53.2 真实页面与工程证据

1. 真实内置浏览器使用 Enter 发送“中兴通讯今天为什么跌？请用小白能看懂的话直接说，别写成报告。”，新会话为 `/research/6c4190ef-d445-4828-a473-88fef752115c`。
2. 最新回答正确展示34.36元、盘中下跌2.52%、尚未收盘、市场广度和公司层面确切原因仍不清楚；没有再写“跟着大盘走弱”“没有利好撑住”或“市场买卖力量自然变化”。
3. 390×844下页面宽与滚动宽均为390px，输入框可见；刷新后同一URL、用户问题和最终回答完整恢复。
4. `artifacts/browser-smoke-w10-final/result.json` 覆盖桌面与390px十个核心页面，共20通过、0失败。
5. 当前测试集合为805项且全量通过；Prompt合同定向测试、Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

### 53.3 下一断点

1. 当前模型偶尔仍会主动附加“收盘后再看公告、继续观察板块”等非必要建议；下一轮从回答合同和证据表达继续压缩，不回到大规模正则堆叠。
2. 为真实 Agent 自动验收建立隔离的 evaluation 用户或会话，避免重复问题继续污染普通用户历史；原始历史不得擅自删除。
3. 继续扩展交易所/监管原文、公司公告全文、精确行业盘中数据和高质量媒体来源，并统计时效、失败率与交叉验证覆盖。
4. 工作树仍包含W1—W10连续改动，禁止 reset、clean、批量覆盖、机械暂存或擅自提交推送。

## 54. 2026-07-28 W11：对话快捷入口可发现性与双视口门禁

本节晚于第53节；当前最新续接断点已由第55节覆盖。W11只调整普通用户进入研究能力的组织、字号和长期浏览器合同，不改变 Hermes/DeepSeek 路由、正式写回边界或金融分析方法。

### 54.1 本轮完成

1. 真实390×844走查发现 `#quickActions` 容器宽360px、内容宽570px，但 CSS 为 `overflow:hidden`；“更多能力 · 9”之外还有数量不一致、10px字号和功能重复问题。
2. 快捷入口重组为“常用问题 / 专项工具”。首屏直接展示大盘解读、个股分析、行业板块、自选股变化、基金与 ETF、财报公告、多股比较、反方风险和操作前检查；按钮12px、分组11px，浅色禁用感已修正。
3. 删除“公司对比 / 多股比较”和“跟踪变化 / 关注组合”的重复入口。市场与持仓、研究任务、利润现金流、股东结构、分析师预期和事件脉络收进“全部工具”，展开后按钮改为“收起工具”。
4. 快捷入口改为两组自然换行。390px 收起态宽度与滚动宽度均360px、高度与滚动高度均157px；桌面宽度与滚动宽度均696px、高度与滚动高度均84px，不再横向或纵向裁切首屏能力。
5. `scripts/browser_smoke.py` 新增分组、可见按钮、最低字号、裁切、展开和收起恢复合同；Runner只点击页面内展示开关，不提交研究问题或修改正式对象。
6. 完整回归首次发现 `VERIFICATION.md` 记录了本机 PostgreSQL 备份绝对路径，违反安装文件可移植性合同；已改为通用的“配置的 PostgreSQL 备份目录”，定向可移植性6项通过。

### 54.2 验收证据

1. `artifacts/browser-smoke-w11-quick-actions/result.json` 为桌面与390px十个核心页面共20通过、0失败；控制台错误、页面异常、非预期本地失败响应和页面级横向溢出均为0。
2. 手机首屏10个入口全部可见；“全部工具”展开后6项次级能力均取消隐藏，再次收起后恢复原首屏和6项隐藏状态。
3. 绝对路径修复后已重新完整执行回归：`805 tests collected` 且全量通过；唯一提示为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
4. 当前服务仍为 `/health=200`、`/ready=200`，Hermes启用、PostgreSQL Schema v5/v4、外部Worker 2个、队列无ready/delayed/retrying积压、数据健康53/54且0 critical。唯一生产运维门禁仍是缺少可核验 PostgreSQL 备份。

### 54.3 下一断点

1. 建立隔离 evaluation 用户或会话，阻止自动 Agent 验收继续制造普通用户重复历史；不得擅自删除现有真实历史。
2. 继续压缩模型主动附加的非必要观察建议，并补交易所/监管原文、精确行业盘中数据和高质量同日媒体来源。
3. 新会话首屏仍同时存在即时入口、研究依据、快捷入口和输入框；下一轮用首次用户任务继续评估纵向层级，但不要重新隐藏本轮已经公开的高频能力。
4. 工作树包含W1—W11连续改动，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 55. 2026-07-28 W12：新建对话首屏重排与完整输入可达

本节晚于第54节，是当前最新续接断点。W12处理新会话的信息层级和移动端可达性，不改变 Agent 证据合同、流式输出、正式写回边界或基金产品事实口径。

### 55.1 真实问题与实现

1. 原390px新会话入口容器为 `275px clientHeight / 847px scrollHeight`，个股、选股和基金比较藏在内部滚动区；输入区底部为938px，超过844px视口。桌面同时展示等待识别、欢迎语、快捷入口和四张开始卡，重复层级明显。
2. `syncAgentEntryHubVisibility()` 现同步 `entry-active` 状态。新会话只保留四个独立入口、移动端历史切换和输入区；用户发送第一条消息或Agent开始运行后，开始入口让位，正常消息、W11快捷追问和研究侧栏恢复。
3. `renderDiagnosisPlaceholder()` 不再主动展示空行情框。只有实际识别市场、股票、多股比较或基金产品后，`qs-market.js` 才展开真实研究侧栏。
4. 入口标题改为“看今天大盘 / 研究一只股票 / 按规则选股 / 比较基金与 ETF”。桌面保留简短说明；390px保留标题、输入和动作，省略重复说明，使用两列高频入口和两张整行专项入口。
5. 手机端 Agent Section 高度改为继承可用工作区，不再用 `100dvh - 16px` 忽略顶部导航。登录页输入区压缩为一行工具区；移动端历史搜索筛选在开始页隐藏，历史会话下拉仍保留。
6. 基金卡的第一次压缩删除了“场内成交价与基金净值会分开显示”精确文案，完整回归及时发现。当前已恢复该边界，基金与风险画像定向合同重新通过。

### 55.2 验收证据

1. 最终桌面新会话 `hubClientHeight=268 / hubScrollHeight=268`，四张入口全部在容器内；输入区底部691px，小于720px视口。
2. 最终390px新会话 `hubClientHeight=511 / hubScrollHeight=511`，四张入口全部在容器内；输入区底部821px，小于844px视口。
3. 新会话的欢迎消息、快捷问题、研究状态条、空行情框和移动端搜索筛选均不可见；浏览器用只读DOM加入一条用户消息后，开始页退出，W11快捷能力完整恢复并继续通过展开/收起合同。
4. `artifacts/browser-smoke-w12-agent-landing-release-2/result.json` 为桌面与390px十个核心页面共20通过、0失败。
5. 完整回归在恢复基金边界后为 `805 tests collected` 且全量通过；唯一提示仍为Starlette TestClient上游弃用警告。文档收口后Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 再次通过。

### 55.3 下一断点

1. 建立隔离 evaluation 用户或会话，避免真实 Agent 回答验收继续污染普通用户的67条历史对话；不得删除现有真实历史。
2. 继续抽样“有历史对话、已确认风险画像、有真实交易记录”的用户路径，而不是只优化空状态。
3. 内容质量下一优先级仍是精确行业盘中数据、同日正式来源和回答尾句压缩；不要重新投入低收益的大规模正则守卫。
4. 工作树包含W1—W12连续改动，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 56. 2026-07-28 W13：基金理财历史独立分类与旧会话兼容

本节晚于第55节，是当前最新续接断点。W13只修复历史对话的信息架构和稳定分类，不改写用户历史、回答正文、正式对象或 Agent 即时生成边界。

### 56.1 真实问题与实现

1. 真实67条历史中，“基金和ETF有什么区别”被分类为个股；“我快退休了，有一笔闲钱，不知道该选股票基金还是债券基金”被分类为选股。根因是宽泛“股票/选股”正则早于基金语义，且后端基金问答沿用 `general_research`。
2. `ChatResponsePersistence` 现把稳定 `conversation_scope` 写入 Assistant metadata。基金产品事实或金融顾问上下文写为 `funds`；个股、大盘、选股和关注分别写入既有分类。没有新增数据库列或Schema迁移。
3. `Database._conversation_row()` 从最近 Assistant metadata 读取白名单分类，`public_conversation()` 公开该字段。旧消息没有字段时，前端继续使用标题和最近消息兼容分类。
4. 旧会话分类把基金、ETF、LOF、REIT、QDII、股票基金、债券、固收、存款、养老、退休、资产配置和风险画像放在宽泛股票/选股判断之前。桌面与移动端均新增“基金理财”，历史切换器同步展示该标签。

### 56.2 验收证据

1. 真实内置浏览器刷新后恢复67条历史；选择“基金理财”显示12/67条，全部为基金、ETF或退休理财问题，真实个股没有混入。
2. 390×844下“筛选移动端研究类型”同步选中“基金理财”，历史切换器12条结果均显示“基金理财 · 标题”；刷新后再次筛选结果不变。
3. `artifacts/browser-smoke-w13-conversation-scope-release/result.json` 在两个视口各用67条只读历史验证旧会话推断、显式后端分类、个股不误伤和双端筛选同步；十个核心页面共20通过、0失败。
4. 完整回归为 `807 tests collected` 且全量通过；唯一提示为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

### 56.3 下一断点

1. 自动验收仍应迁到隔离 evaluation 用户或会话；现有67条真实历史不得擅自删除、合并或改写。
2. 新 `conversation_scope` 会随每次新回答稳定写入；尚未再次回答的旧会话继续依赖兼容规则，遇到新的真实误分类应优先补明确语义或后端元数据，不要堆叠宽泛正则。
3. 内容质量优先级仍是精确行业盘中数据、同日正式来源和回答尾句压缩；历史分类完成不代表 Agent 金融证据覆盖已经充分。
4. 工作树包含W1—W13连续改动，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 57. 2026-07-28 W14：最近对话与全部历史分层

本节晚于第56节，是当前最新续接断点。W14只优化历史发现与占用，不归档、删除、合并或改写任何用户对话。

### 57.1 真实问题与实现

1. 真实1280×720页面左栏历史可视高度132px，但67条对话、35个主题的内容高度达到2126px；“最近对话”实际装入全部历史，用户需要在很小的框中翻找。
2. 新状态 `conversationHistoryExpanded` 默认关闭；`recentConversationItems()` 按日期与标准化主题保留最近8个主题。如果当前 conversation ID 属于更旧主题，则额外固定显示该主题。
3. 默认元信息改为“最近8个主题 · 共67个对话”。历史区底部使用固定可见的“查看全部历史（67）”；展开恢复全部内容并改为“只看最近对话”，收起后自动回到最新对话顶部。
4. 最近视图只影响桌面历史列表。搜索、基金/个股/大盘/选股等分类和具体股票筛选仍先在完整集合上执行；移动端历史切换器仍保留新建入口与全部67条历史。

### 57.2 验收证据

1. 真实桌面页面默认显示8个主题，DOM行数由67降到30（同题旧记录仍在折叠主题内），内容高度由2126px降到567px；固定按钮完整位于132px可视区内。
2. 真实点击展开后恢复67行、35个主题；点击收起恢复8个主题且 `scrollTop=0`。搜索“历史回放”命中2条旧会话，搜索结果不显示无关展开开关。
3. 真实390×844下桌面侧栏列表隐藏，历史下拉仍有68项；分类包含基金理财和具体股票，页面宽与滚动宽均390px。
4. `artifacts/browser-smoke-w14-recent-history-release/result.json` 在桌面与390px各验证67条只读历史的最近8主题、当前旧会话固定、展开/收起、搜索旧历史和分类全量；十个核心页面共20通过、0失败。
5. 完整回归为 `808 tests collected` 且全量通过；唯一提示为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

### 57.3 下一断点

1. 自动 Agent 验收仍应迁到隔离 evaluation 用户或会话，防止普通用户历史继续增长；不得清理当前真实67条。
2. 继续抽样已确认风险画像和真实交易记录用户的首次进入路径，优先发现功能层级、术语和信息密度问题。
3. 内容质量继续优先补精确行业盘中数据、同日正式来源和非必要尾句压缩；不得用历史界面优化替代金融证据建设。
4. 工作树包含W1—W14连续改动，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 58. 2026-07-28 W15：风险画像到金融顾问的清晰路径

本节晚于第57节，是当前最新续接断点。W15只优化用户理解和下一步，不改变“正式画像必须由用户确认”的产品边界。

### 58.1 实现与边界

1. `/account` 风险画像新增固定三步条：回答7个问题、保存并核对、确认让 AI 使用；回答数量、进度条和下一道缺失问题实时更新。
2. 未完整回答时提交不会调用草稿 API，只高亮并滚动到第一道缺失问题，再把焦点放到该题第一个选项。答案修改后确认按钮保持禁用，直到保存草稿。
3. 草稿状态明确写“尚未用于 AI”；确认状态显示已确认版本与约束。确认后的“去问金融顾问”只调用 `startNewConversation()`、预填 `chatInput` 并聚焦输入框，函数中没有 `sendChat()`，因此不会自动发问或持久化新的回答。
4. Browser Runner 通过页面本地合成 DOM 覆盖草稿与确认视觉状态，不发出写请求，不替真实用户确认。真实内置浏览器只读走查未选择、保存或确认任何选项。

### 58.2 验收证据

1. 真实390×844页面：根宽与滚动宽390px；三步单列；首个问题卡300px；保存、确认按钮均40.5px；页面截图人工检查无裁切。
2. 自动化状态合同：初始0/7且确认禁用；缺失提交聚焦 `fund_use_horizon`；完整填写为7/7并进入“保存并核对”；草稿确认可用；确认态出现“去问金融顾问”。
3. `artifacts/browser-smoke-w15-risk-profile-final/result.json` 覆盖桌面与390px十个核心页面，共20通过、0失败；没有控制台错误、页面异常、非预期失败响应或横向溢出。
4. 完整回归为 `808 tests collected` 且全量通过；首次执行只发现旧 API 前端合同仍检查原按钮文案，更新为新文案后完整重跑通过。Ruff、Python `compileall`、全部JavaScript、`uv lock --check` 和 `git diff --check` 通过。

### 58.3 下一断点

1. 用隔离 evaluation 用户执行“真实保存草稿 → 用户确认 → 进入顾问 → DeepSeek即时回答 → 刷新恢复”的完整浏览器链；不得借用普通用户画像完成自动验收。
2. 顾问回答质量继续依赖基金、ETF、债券、利率、费率和风险数据，不能因为画像体验完成就宣称财富管理数据完整。
3. 运维唯一明确生产缺口仍是没有可核验 PostgreSQL 备份；`/health` 与 `/ready` 通过不等于灾备就绪。
4. 工作树包含W1—W15连续改动，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 59. 2026-07-28 W16：风险画像到真实顾问回答质量闭环

本节晚于第58节，是当前最新续接断点。W16没有新增自动写回权限；正式画像仍只来自用户确认，自动验收只使用隔离 evaluation 用户。

### 59.1 真实失败与修复

1. 隔离用户 `042dedc2-5ffe-4698-aadc-c0c8fe907946` 已确认七项画像。失败基线会话 `a1249ce3-c880-41ca-9e24-11042e2a73e8`、Run `a91a5b11-af49-4da0-87b4-8017c5bdff54` 仍否认画像存在、重复追问资金期限、自创期限和亏损阈值并扩写五类产品。
2. `build_financial_advisor_context()` 现只把问卷真实覆盖的资金期限、回撤、应急储备和负债加入 `provided_fields`；问卷没有询问持仓，因此 `已有资产` 必须由对话补充。
3. “结合我、根据我、基于这些已知条件、优先比较、下一步”等连续适合性追问保持 `product_suitability`。补充持仓前为 `needs_profile`，补充后为 `ready_for_conditional_guidance`，不会再降成 `concept_only`。
4. Prompt 明确禁止否认画像、重复追问已覆盖字段、列超过三类产品、输出“最适合/收益更高”或用问题数量预告制造残句；回答清理补充过强类别措辞、悬空追问、拼接句和重复编号修复。
5. 通用数字校验允许常见指数名称作为实体标签；禁止词校验区分“保证收益”和“不能保证收益”，避免安全边界反而触发整轮回退。

### 59.2 验收证据

1. 最终隔离会话 `dc1d6227-b29d-4cff-a6f2-de81dafb253e` 保存于 `artifacts/w16-financial-advisor/final-conversation.json`。第一轮 Run `94396c99-a386-4c5f-bc54-d62eb943db70` 只缺并追问“已有资产”；第二轮 Run `f8ddf214-6730-4d8b-9b88-9aa7115fe75a` 为 `ready_for_conditional_guidance`、`missing_fields=[]`。
2. 失败基线回答1067字；最终第一轮795字、第二轮860字。长度下降不是唯一质量标准，但最终样本不再否认画像、重复问卷、自创具体阈值或扩成五类百科。
3. 真实内置浏览器只读检查个人中心：七题、草稿/确认边界、禁用状态和说明完整，控制台错误为0；没有替普通用户保存或确认画像。一次普通“基金与 ETF”快捷入口点击向既有会话 `6c4190ef-d445-4828-a473-88fef752115c` 追加测试问答，未改正式画像，也未删除或改写该历史；后续不得再用普通用户会话做自动 Agent 验收。
4. `artifacts/browser-smoke-w16-financial-advisor-final/result.json` 为桌面与390px十个核心页面20通过、0失败。完整回归815项全量通过；Ruff、compileall、全部JavaScript、锁文件和diff检查通过。
5. 当前8773服务已重新加载最新代码；`/health=ok`、Hermes启用、PostgreSQL领域库和持久化队列正常，独立Worker继续运行。

### 59.3 下一断点

1. 在可隔离浏览器会话中补齐“填写画像 → 保存草稿 → 用户确认 → 去问金融顾问只预填 → 手动发送 → 刷新恢复”的单一端到端证据；不得读取或注入浏览器Cookie绕过隔离。
2. 建立具体基金与ETF候选比较前，继续补产品合同全文、完整费率、跟踪偏离、流动性和正规销售渠道适当性事实；类别级回答不能包装成唯一产品推荐。
3. 抽样更多真实问题，尤其是退休现金流、已有股票仓位、短期用款和债券利率变化；继续压缩非必要解释，并把“普通基金一天只有一个价格”等绝对表述改成精确产品口径。
4. 工作树包含W1—W16连续修改，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 60. 2026-07-28 W17：基金与ETF概念回答精度和结构修复

本节晚于第59节，是当前最新续接断点。W17只改进金融教育回答的事实口径、路由和展示完整性，不扩大Agent写回权限，也不把类别知识升级成具体产品推荐。

### 60.1 真实失败与实现

1. 初始真实 Run `d2f03b6d-dda8-4814-ae20-7b611de81a17` 仍把“小白能看懂”当作个人适合性，回答包含“一天只有一个净值”“到账需要几个工作日”等过度概括。
2. `build_financial_advisor_context()` 新增明确个人画像引用边界；单纯要求通俗表达保持 `concept_only`，已确认画像不会自动把概念问题升级为适合性问答。
3. 基金/ETF知识、Skill和 Prompt 统一口径：ETF属于基金；默认比较常见场外开放式基金与场内ETF；净值确认、连续成交报价、资金可用、到账、交收、完整成本和ETF联接基金存在性分别说明，不使用必然结论。
4. Run `3aa9e9ce-5aaa-4b1b-a6bc-1bdbd9971565` 的模型原文包含三项完整编号，但数字守卫把 Markdown 加粗的第1、3项当作无证据数字删行。守卫现识别普通、加粗、项目符号和标题中的列表序号，并保留真正无证据金融数字校验。

### 60.2 验收证据

1. 关系解释 Run `412cb1ea-0fc8-4dc0-a663-a2091c6f3236` 为 `concept_only`，第一句明确ETF是基金的一种，不再追问画像，也不再写“一天只有一个价格/净值”。
2. 到账问题 Run `5f63fb4b-34b1-4c0e-88ba-ef34a9ba74ff` 直接回答“不一定”，并把资金可用、到账和交收绑定到具体产品、渠道与券商规则。
3. 无证券账户最终 Run `7650ad15-b5e3-458a-b44a-25b9e840eba4` 三项完整，`output_guard.passed=true`、`unsupported_numbers=[]`、没有 `answer.repaired.md`；完整 API 证据在 `artifacts/w17-fund-etf-accuracy/`。
4. `artifacts/browser-smoke-w17-fund-etf-accuracy/result.json` 为桌面与390px十个核心页面20通过、0失败。完整回归818项全量通过；Ruff、compileall、全部JavaScript、锁文件和diff检查通过。

### 60.3 下一断点

1. 最终无证券账户样本为428字符，事实和结构已通过但仍略长；下一轮继续缩短概念回答和非必要尾句，禁止机械截断。
2. 继续抽样债券、利率、退休现金流和具体基金规则；具体费率、到账天数、流动性和产品状态必须来自当前产品事实或合同，不能依赖通用知识补写。
3. 隔离 evaluation 用户继续用于自动Agent验收；不得删除或改写普通用户历史，也不得借验证创建正式判断、任务、计划或复盘。
4. 工作树包含W1—W17连续修改，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 61. 2026-07-28 W18：个股研究概览信息架构收敛

本节晚于第60节，是当前最新续接断点。W18只优化用户信息层级，没有减少后端证据、改变Agent生成机制或扩大正式写回权限。

### 61.1 真实审计与实现

1. 真实浏览器依次审计市场总览、AI投研新对话、透明选股和个股研究。前三条路径现有层级可以继续使用；个股研究概览同时展示行情、K线、研究重点、正式判断、五类证据地图、变化和任务，是当前最明显的“后台审计式堆叠”。
2. 对照 `AI金融Agent产品方案/清数智算_七页高保真交互演示.html`，只吸收摘要优先和渐进披露原则。概览移除重复的 `.stock-research-map`，保留行情/K线、当前研究重点、当前研究判断、最近变化和下一步研究。
3. “当前研究判断”新增“查看完整证据”；独立证据页继续保留六维覆盖、已确认事实、关键反证、信息缺口、失效条件和所有研究模块。该按钮已在真实浏览器点击验证，不是无功能占位。
4. 最近变化和下一步研究默认各展示最高优先级两项，同时保留总数和进入完整标签的入口。完整任务仍由 `renderStockSpaceTasks()` 使用全部后端任务渲染，不因概览压缩而丢失。
5. 前端合同和浏览器 Runner 同步更新：概览必须只出现判断、变化、任务三块；390px必须逐块可见且不超宽；完整证据必须仍在独立页。

### 61.2 验收证据

1. `artifacts/browser-smoke-w18-stock-overview-hierarchy/result.json` 为桌面与390px十个核心页面20通过、0失败；移动端三块概览卡均为344px，无横向溢出、控制台错误、页面错误或失败响应。
2. 桌面和移动截图分别为 `artifacts/browser-smoke-w18-stock-overview-hierarchy/desktop-stock-overview.png` 与 `mobile-390-stock-overview.png`；视觉检查确认概览从四层重复证据堆叠收敛为三块摘要。
3. 完整回归818项全量通过；Ruff、compileall、全部JavaScript、锁文件和diff检查通过。
4. 运行服务 `/health=ok`、`/ready=ok`、Hermes启用，PostgreSQL领域库和持久化队列正常，2个Worker保持active。

### 61.3 下一断点

1. 继续按首次用户任务审计真实交易复盘详情和Agent长回答，优先处理影响理解和行动的重复层级，不再回到低收益局部守卫优化。
2. 个股概览的变化与任务已只显示两项；后续若增加新的用户摘要，必须证明不会与证据页、任务页或历史页重复。
3. 页面清晰度改善不代表金融证据完备或生产就绪；正式认证、备份恢复、商业数据授权、对象存储、外部告警和容量压测边界保持不变。
4. 工作树包含W1—W18连续修改，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 62. 2026-07-28 W19：交易复盘首次使用路径去重

本节晚于第61节，是当前最新续接断点。W19只优化交易复盘空状态和筛选空结果，不改变交易复盘生成条件、价格计算或用户确认边界。

### 62.1 真实问题与实现

1. 没有交易复盘的真实桌面和390px页面仍展示搜索框、状态筛选、左侧空结果和一个“去我的关注”，右侧又重复说明与同名按钮。没有任何记录时，这些筛选没有价值，用户也看不出复盘形成条件。
2. 无记录且无筛选时，`.review-run-workspace` 进入 `empty-onboarding`：侧栏隐藏，单页说明三步真实路径——选择关注股票、记录减仓或卖出、等待三个完整交易日。唯一按钮“去记录一次真实操作”已真实点击验证进入 `/watchlist`。
3. 有筛选条件但结果为空时不进入首次使用模式；侧栏继续显示，唯一操作改为“清除筛选”。真实选择“可生成”后页面正确显示筛选空结果，清除后状态和搜索词归零并恢复三步引导。
4. 同事方案只提供结构参考。当前实现没有吸收其虚构交易表、盈亏、胜率、纪律评分、目标仓位和价格结论；复盘仍以真实用户操作和确定性日线为准。
5. 浏览器Runner新增空状态模式、侧栏可见性、三步标题、唯一CTA和移动步骤卡宽度合同；对应静态合同测试也同步更新。

### 62.2 验收证据

1. 首次 `artifacts/browser-smoke-w19-review-onboarding/result.json` 为20通过、0失败，但人工查看 `mobile-390-reviews.png` 发现三列步骤在390px被压成竖排。该结果只保留为失败发现证据，不能作为最终验收。
2. 修复后 `artifacts/browser-smoke-w19-review-onboarding-final/result.json` 为桌面与390px十个核心页面20通过、0失败；移动三张步骤卡均为324px，页面无横向溢出、控制台错误、页面错误或失败响应。
3. 真实内置浏览器验证：初始可见按钮只有“去记录一次真实操作”；点击进入我的关注；状态筛选为空时只有“清除筛选”；清除后恢复三步引导。
4. 完整回归818项全量通过；Ruff、compileall、全部JavaScript、锁文件和diff检查通过。
5. `/health=ok`、`/ready=ok`、Hermes启用，PostgreSQL领域库和持久化队列正常，2个Worker active；备份检查仍为skipped。

### 62.3 下一断点

1. 用隔离evaluation会话继续审计真实Agent长回答：首个安全片段、用户主动上滚、最终回答替换和回答结构都必须通过实际交互，不只检查接口。
2. 在已有真实交易复盘数据的隔离用户上审计详情页，重点检查计划、执行、价格结果、逻辑结论和改进项是否重复或使用内部术语。
3. 浏览器Runner绿色后仍必须查看桌面与390px截图；无溢出、元素可见不能单独证明文字可读。
4. 工作树包含W1—W19连续修改，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 63. 2026-07-28 W20：真实个股长回答流式验收与刷新隔离

本节晚于第62节，是当前最新续接断点。W20只修复真实Agent回答的安全可见时延、无证据因果局部处理、技术口径展示和evaluation刷新恢复，不改变DeepSeek即时生成、最终金融守卫或用户正式写回边界。

### 63.1 真实失败与实现

1. 失败基线 Run `f88bf16e-c65d-4c9b-a8df-58e4034d845f` 约23秒后才一次显示最终回答，`first_visible_seconds=null`、`visible_events=0`。流式通道本身正常，真正阻塞点是“成分未复权行情口径”完整性要求在增量阶段提前拦住所有安全片段。
2. `AgentService` 现仅在最终回答执行该全文完整性要求；流式阶段仍保留逐句数字、时间和语义安全检查。最终回答的金融校验强度没有降低。
3. 行业因果错误由整段删除改为按句删除，安全的价格事实不会被同段错误解释连带清空。Prompt 明确禁止把行业同步或相对表现事实升级为“行业普跌中的跟随”等未证实因果；可能原因只能引用本轮已有同日候选事件。
4. `qs-format.js` 负责拆分回答技术脚注，`qs-agent-ui.js` 将“成分行情口径补充”默认收进“数据口径”折叠项。刷新 evaluation 会话时按持久化 `quality_scope` 恢复“AI 实时研究 · 验收隔离”和隔离历史。
5. `qs-agent-ui.js` 已收回至995行，重新满足单个前端模块不超过1000行的门禁。

### 63.2 真实模型、刷新与浏览器证据

1. 修复后真实会话 `7c7fc6a3-a021-45dc-b1c0-94992f34a436`、Run `69754916-d8f2-4b55-9dae-ac3928e4418e` 使用 `deepseek-v4-pro`，首 Token 2.744秒、首个安全正文4.236秒、总耗时14.343秒，安全可见更新8次、累计529字符。
2. 最终正文保留价格、行业同步背景和相对表现事实，同时明确具体驱动尚未确认；再说明候选事件时间边界、反方事实和下一步核验。技术口径默认折叠，不再作为正文尾巴展示。
3. 真实浏览器刷新后回答与原会话恢复，`agentMode` 仍为“AI 实时研究 · 验收隔离”，只显示4条evaluation历史，不混入67条普通历史。
4. 最新仓库门禁 `artifacts/browser-smoke-w20-agent-stream-refresh-final/result.json` 覆盖桌面与390px十个核心页面，20通过、0失败；两种视口均验证安全片段、进度保护、用户上滚保持、最终正文一致、无全文重放和无横向溢出。最终桌面与移动Agent截图已人工查看。
5. 完整回归820项全量通过；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 通过。

### 63.3 下一断点

1. 下一产品优先级转向有真实记录的交易复盘详情，检查计划、实际执行、确定性价格结果、逻辑结论和下一次改进是否重复或使用开发内部术语。
2. 对缺少直接事件证据的涨跌问题继续明确“具体驱动未确认”，不得为了更像结论而补写行业或公司因果；但不要继续增加低收益正则代替数据取证。
3. 后续真实Agent自动验收继续使用隔离evaluation会话，刷新恢复必须保持；不得污染、删除或改写普通用户历史。
4. 工作树包含W1—W20连续修改，禁止 reset、clean、批量覆盖、机械暂存、提交或推送，除非用户明确授权阶段交付。

## 64. 2026-07-28 W21：有记录交易复盘详情与真实候选质量修复

本节晚于第63节，是当前最新续接断点。W21只改进交易复盘详情的信息分层、模型草稿规范化和真实用户可读性；价格计算、复盘生成条件与用户确认权限保持不变。

### 64.1 真实失败与实现

1. 隔离用户 `f33cd618-0f7e-4674-b67e-a7f3a59a62eb` 的首条复盘 `e396a6d0-53bb-439f-ac5f-590191ea541c`、失败候选 `61c71575-2057-497d-856e-5c39be64336d` 重复成本、操作价和亏损卖出，输出无证据心理诊断，并暴露内部字段与模型自创阈值。
2. `TradeWorkflowService.normalize_review_agent_draft()` 统一分离价格、逻辑和计划范围；`StructuredAIService` 在候选入库前必须调用同一规范化。模型 `bias_tags` 不再直接采用，改为从记录缺失、触发条件和执行差异派生可核验流程标签。
3. `parse_review_agent_answer()` 支持双层JSON字符串；分句清理会保留同一句中的安全证据，并修复被删除前句导致的“但/然而”开头。`thesis/watch_items/workspace`、版本、冻结状态和自创数字/X%阈值均不再进入用户候选。
4. 复盘详情页面改为六段用户任务语言，价格事实区合并四个指标，计算口径默认折叠；桌面和390px提高列表、状态、标题和辅助文字字号。

### 64.2 真实模型与验收证据

1. 最终复盘 `454cff62-6166-486f-98c1-a75600120b5d` 的真实 Run `92615f3d-cf16-42b9-b12c-aba6837a260f` 使用 `deepseek-v4-pro`，状态 `trade_review / completed`，输出守卫通过；候选 `c7ac783e-a94b-47e2-a899-91d61907cae4` 保持 `pending_confirmation`。
2. 最终候选逻辑段不含操作价、成本、盈亏、后续价格路径、计划比较、心理诊断、内部字段、版本/冻结术语、JSON外壳或自创阈值。计划差异单独说明，改进项只要求用户保存可核验数据与结论。
3. `artifacts/browser-smoke-w21-review-detail-retry-final/result.json` 为桌面与390px十个核心页面20通过、0失败；复盘详情六段标题、四个价格指标、默认折叠口径、最低字号和无横向溢出均通过，桌面与移动截图已人工查看。
4. 完整回归822项全量通过；Ruff、compileall、全部JavaScript、锁文件和diff检查通过。运行服务 `/ready=ok`，PostgreSQL领域库Schema v5、队列Schema v4，2个Worker active，队列无ready/delayed/retrying积压。

### 64.3 下一断点与交付说明

1. 下一产品优先级回到直接金融证据、选股/个股分析可信度和整体首次用户路径；不要继续用更多低收益局部清理代替资料获取和分析方法建设。
2. 价格事实仍不能自动证明逻辑对错；心理与行为偏差只能作为用户可核验的问题，不得由模型直接确诊。正式复盘仍需用户编辑和确认。
3. 用户已在本轮明确授权完成验收后提交并推送当前阶段。交付前必须显式审查暂存范围、排除 `.env`、数据库、工作区、日志、缓存和 `artifacts/`，并完成敏感扫描、`git diff --cached --check` 与远端SHA复核。

## 65. 2026-07-28 W22：条件选股结果渐进披露

本节晚于第64节，是当前最新续接断点。W22只收敛条件选股的模板语言、候选卡阅读层级和持续浏览器门禁；筛选算法、排序、数据合同、Agent即时生成和用户确认边界保持不变。

### 65.1 真实问题与实现

1. 默认“相对行业增强候选”真实返回12只股票，但每卡首层同时展示6项指标、3条重复理由和2个按钮。高涨幅、高估值候选虽然有页面级风险说明，用户仍需自己从整张后台式表格判断首先应该看什么。
2. 四个模板在前端改为直白任务语言，并新增“适合什么时候使用”的动态提示。后端仍保留稳定profile key和原确定性描述，避免破坏Agent路由、API与历史研究上下文。
3. 候选卡首层固定为3项模板核心指标和一条基于已取得数字的“先核验”提示。完整行情、估值、已取得财务字段、入选理由和缺口进入默认关闭的逐股详情；未取得的非必要财务字段不会以成排破折号制造噪声。
4. 左侧实际规则改为默认关闭的详情区域，执行后摘要直接显示规则条数。加入关注和进入个股研究仍是唯一两个候选动作，没有新增推荐、评分或交易按钮。
5. `scripts/browser_smoke.py` 新增只在浏览器DOM中渲染的确定性候选夹具。该验收不提交表单、不写数据库，但能让桌面和390px截图覆盖真实的已填充卡片状态，并长期检查三项指标、折叠详情、完整依据、核验提示和移动端卡宽。

### 65.2 验收证据

1. 真实生产数据执行默认模板返回12只候选；桌面人工检查确认首层只显示近20日、行业超额和PE，完整详情展开后仍保留可用数据与3条入选理由。
2. `artifacts/browser-smoke-w22-screening-disclosure-final/result.json` 为桌面与390px十个核心页面20通过、0失败。桌面和移动选股截图都已人工查看；390px候选卡完整显示三项指标、核验提示、折叠详情和两个动作，无横向溢出。
3. 完整回归822项全量通过；Ruff、compileall、全部JavaScript、锁文件和diff检查通过。
4. 运行服务 `/ready=ok`；PostgreSQL领域库Schema v5、持久化队列Schema v4，2个Worker active，队列无ready/delayed/retrying积压。

### 65.3 下一断点与交付说明

1. 下一轮优先进入“候选 → 个股研究空间 → 针对性Agent问题”的真实完整路径，检查选股线索是否被正确带入、个股证据是否足以回答上涨驱动、财务持续性和反方风险；不要继续只优化候选卡外观。
2. 对经营改善、估值约束和回撤模板分别执行真实数据验收，重点确认核心指标与下一核验问题确实对应模板，而不是只验证默认趋势模板。
3. 条件选股仍是确定性缩小范围工具，不得增加综合分、推荐等级、目标价、仓位或未来胜率。正式判断、计划、任务和复盘继续要求用户确认。
4. 用户已明确授权本轮完成后上传GitHub。交付必须只暂存本轮代码、测试和文档，继续排除 `artifacts/`、环境文件、数据库、工作区、日志与缓存，并核对远端SHA。
