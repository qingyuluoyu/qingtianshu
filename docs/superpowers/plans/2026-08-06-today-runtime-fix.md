# 今日观察真实运行问题修复与前端收口

## 工作包 1：固定真实合同与请求边界

1. 从候选环境响应中提取可提交、脱敏的 Today fixture。
2. 将真实合同测试改为仅使用仓库 fixture，验证成交额比较、空历史和数值直通。
3. 删除不稳定模块的 API/Query，并将首页次级请求延后到首屏核心请求完成后。

## 工作包 2：运行问题收口

1. 保留后端现有 `_turnover_history()` 的根级输出；前端只在至少两点时显示柱图。
2. 删除资金流向、异动机会、持仓和候选环境 500 的研报模块，避免首页主动触发对应失败请求。
3. 将指数固定为五个核心标的，指数历史改为低优先级、30 分钟缓存和失败隐藏。

## 工作包 3：恢复组件边界

1. 从 `TodayPage` 抽出 Header、指数、三项市场卡、行业、个人研究、全球市场、数据健康和通用卡。
2. 保持页面只负责编排 Query、次级加载开关和网格布局。
3. 增加页面级行为测试，不使用网络 mock 伪造业务结果。

## 工作包 4：候选版本验收

1. 运行前端单测、类型构建与受影响后端测试。
2. 在隔离端口启动当前分支的生产构建和后端，采集真实请求、控制台和桌面/移动端截图。
3. 仅在所有验证完成后提交 `codex/today-runtime-fix`。
