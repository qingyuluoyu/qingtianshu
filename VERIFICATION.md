# 清数智算 MVP 验证记录

> 最近验证：2026-07-28（Asia/Shanghai）
>
> 本文件保留历史工作包证据；若历史“当前边界/下一阶段”与顶部基线、文末最新工作包或
> `清数智算_现状评估与分阶段实施规划_20260724.md` 冲突，以后者为准。

## 自动化测试

```bash
uv run pytest
```

结果：当前基线 `824 tests collected` 且全量通过；`ruff check .`、Python 编译、全部静态 JavaScript `node --check`、`git diff --check` 和 `uv lock --check` 全部通过。此前 wheel/sdist 已完成全新 Python 3.13 安装与运行自检；本轮未重新宣称构建产物已更新，提交前仍以当前源码和锁文件为准。

## 2026-07-28 W23：连续沪深300基准、真实年K与候选到Agent证据链

- 李总策略组合版本升级为 `li_zong_2w_no_cost_full_benchmark_v5`。策略侧继续采用零交易成本、任意两次实际换仓至少间隔10个交易日、冷却期只执行最新候选集合；沪深300净值改为“当日真实收盘价 ÷ 区间首日真实收盘价”，完整区间每日连续计算，`benchmark_policy=continuous_full_period`、`benchmark_trading_days`等于区间交易日数、所有点`benchmark_exposed=true`。
- 仅基准口径变化时，不再强迫全市场逐股规则全部重跑：服务只接受同一`STATE_INPUT_VERSION`、零成本、最短10日换仓的v4稳定结果，保留策略净值与换仓记录，用同区间真实沪深300收盘重算基准后发布v5。`debug.benchmark_migrated_from`保留迁移来源；新交易日仍由正常后台逐股状态机重算，不能用迁移跳过新增数据。
- 首次三档同日v5审计快照均发布至2026-07-27：近3个月策略`-9.8251%`、沪深300`-1.4036%`、超额`-8.4215%`；近1年`+44.9446% / +17.0437% / +27.9009%`；近3年`-9.0819% / +21.6699% / -30.7518%`。逐点以历史指数收盘独立复算，三档最大误差均小于`5e-9`。
- 2026-07-28 20:15交付核对时运行态已经分档前推：近3个月为策略`-5.1586%`、沪深300`-4.2219%`、超额`-0.9367%`，近1年为`+44.9446% / +13.6962% / +31.2484%`；两档均已完成至2026-07-28，基准点独立复算最大误差分别为`4.85e-9`和`4.98e-9`。近3年仍稳定在2026-07-27，后台继续建设，不能宣称三档均已完成2026-07-28。
- 空仓期连续性不是文字声明：近3个月`2026-05-28 → 2026-05-29`策略净值保持`1.09511264`，指数净值`1.03036974 → 1.02573768`；近1年和近3年也分别找到连续空仓而指数变化的真实日期段。
- K线组件新增`yearly`自然年OHLCV聚合和“年K”按钮；年K按需读取5年真实日线，新增近5年范围，页面明确标注“由日线OHLC聚合的年K”。桌面与390px真实页面切换中兴通讯年K均成功，390px可见图表宽342px、工具栏无溢出。
- 候选到Agent链路补齐：`research_entry`的来源、模板、数据日、候选状态、入选理由、缺失项与限制会进入`stock_workspace_context`并在Prompt压缩后保留。绑定个股研究对话中询问“为什么进入候选、逻辑是否成立”时保持`stock_research`，不再误路由为全市场`stock_screen`；候选只能作为待核验线索，不能自动成为正式判断或推荐。
- 仓库浏览器门禁`artifacts/browser-smoke-w23-benchmark-yeark-agent-context/result.json`覆盖桌面和390×844十个核心页面，共`20 passed / 0 failed`；真实浏览器另行切换三档回测和年K/近5年，页面无横向溢出、用户可见加载错误或控制台warning/error，截图已人工查看。
- 完整回归`824 passed`；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check`、`git diff --check`与敏感文件扫描均通过。运行服务`/health=ok`、`/ready=ok`，PostgreSQL领域库Schema v5、队列Schema v4和双Worker继续运行。

当前边界：v5按区间独立发布；截至2026-07-28 20:15，近3个月和近1年已完成至2026-07-28，近3年仍展示2026-07-27稳定结果。持久化Worker继续严格追补长窗口，未完整前不会替换为假完整结果。连续基准只改变比较口径，不改善策略本身；近3年仍显著跑输沪深300，必须由金融专家继续复盘规则。

## 2026-07-28 W21：有记录交易复盘的事实、逻辑与用户确认分层

- 隔离用户 `f33cd618-0f7e-4674-b67e-a7f3a59a62eb` 的真实失败样本显示：逻辑复盘重复操作价、持仓成本和亏损卖出；模型无证据输出“确认偏差、可得性启发、叙事谬误”；正文还暴露 `thesis/watch_items`、版本和冻结状态，并自创具体阈值。
- 后端新增统一复盘草稿规范化：价格与盈亏只保留在确定性价格结果；逻辑复盘剔除价格路径和计划比较；模型心理标签不直接入库，改为“证据未留档、触发条件待确认、执行与计划不一致”等可核验流程标签。内部字段、版本/冻结术语和自创数字阈值统一改成用户可读表达。
- 真实模型继续暴露并修复三类边缘输出：清理后以“但”开头；用 `X%` 形式举自创阈值；将完整JSON再次编码成字符串。`parse_review_agent_answer()` 现支持双层JSON解码，分句清理保留同句中的安全证据，不把整句误删。
- 前端有记录详情固定为六段：后续价格事实、当时为什么操作、判断依据复盘、与原计划的差异、下次怎么做得更好、待你确认的记录问题。价格计算口径默认折叠；桌面和390px均提高标题、正文、列表与状态字号。
- 最终真实 `deepseek-v4-pro` Run `92615f3d-cf16-42b9-b12c-aba6837a260f`、候选 `c7ac783e-a94b-47e2-a899-91d61907cae4` 为 `trade_review / completed`。逻辑段没有价格/盈亏、计划内容、心理诊断、内部字段、JSON外壳或自创阈值；候选仍为 `pending_confirmation`，没有直接写入正式复盘。
- 最新双视口门禁 `artifacts/browser-smoke-w21-review-detail-retry-final/result.json` 覆盖1280×720与390×844十个核心页面，共 `20 passed / 0 failed`；桌面与移动复盘截图已人工查看。完整回归 `822 passed`；Ruff、compileall、全部静态JavaScript、锁文件和diff检查通过。
- 运行态 `/ready=ok`，领域库 PostgreSQL Schema v5、运维库 Schema v4，队列 ready/delayed/retrying 均为0，2个Worker active，最近24小时后台失败率为0。

当前边界：本轮改善的是复盘候选的分层、可读性和用户确认边界，不代表模型已经能独立判断投资心理或操作对错。价格事实仍不能自动证明逻辑正确，正式复盘仍必须由用户编辑并确认；下一轮优先回到直接金融证据、选股/个股分析质量和整体首次用户路径，不继续堆低收益局部清理规则。

## 2026-07-28 W20：真实个股长回答首屏时延、因果边界与验收隔离

- 失败基线 Run `f88bf16e-c65d-4c9b-a8df-58e4034d845f` 约23秒后才一次显示最终回答，`first_visible_seconds=null`、`visible_events=0`。根因不是前端没有消费流，而是流式阶段提前执行“成分未复权行情口径”全文完整性检查，所有安全片段都被暂缓。
- 同一失败样本还暴露两个阅读问题：一处无证据行业因果使整个首段被删除，只剩一句行情；“成分行情口径补充”作为技术说明直接拼在用户正文末尾。
- 流式阶段现延后该全文完整性检查，最终回答仍执行完整金融校验；行业因果错误改为按句删除，保留同段已核验价格事实。Prompt 明确禁止把行业同步事实扩写为“行业普跌中的跟随”等因果，询问可能原因时只能使用本轮已有同日候选事件。
- 前端将“成分行情口径补充”拆为默认折叠的“数据口径”，不再打断回答正文。打开或刷新 evaluation 对话时，会从持久化 `quality_scope` 恢复“AI 实时研究 · 验收隔离”及隔离历史，不把普通用户67条历史混入自动验收。
- 修复后真实会话 `7c7fc6a3-a021-45dc-b1c0-94992f34a436`、Run `69754916-d8f2-4b55-9dae-ac3928e4418e` 使用 `deepseek-v4-pro`：首 Token 2.744秒、首个安全正文4.236秒、总耗时14.343秒，安全可见更新8次、累计529字符。最终回答先说明价格与行业同步背景，同时明确具体驱动尚未确认，再给出事件边界、反方事实和下一步核验。
- 刷新真实 evaluation 对话后，回答、原会话和验收模式均恢复，页面只显示4条 evaluation 历史。最新仓库双视口门禁 `artifacts/browser-smoke-w20-agent-stream-refresh-final/result.json` 覆盖1280×720与390×844十个核心页面，共 `20 passed / 0 failed`；安全流式、用户滚动位置、最终正文一致性、无全文重放、页面溢出和控制台错误均通过。桌面与390px截图已人工检查。
- 完整回归为 `820 tests collected` 且全量通过；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 通过。`qs-agent-ui.js` 为995行，继续满足单个前端模块不超过1000行的门禁。

当前边界：W20解决的是已有真实证据下的流式可见性、错误因果局部修复和技术口径展示，不代表所有涨跌问题都已有充分直接事件证据。下一轮应转向有真实记录的交易复盘详情，检查计划、执行、价格、逻辑与改进项是否重复或术语化，不再继续堆低收益正则和局部守卫。

## 2026-07-28 W19：交易复盘空状态从重复双栏改为三步上手路径

- 真实桌面与390px审计发现：当前用户没有交易复盘时，页面仍展示无效搜索框、状态筛选、左侧空结果和“去我的关注”，右侧又重复同一说明与按钮。用户不知道复盘如何形成，两个CTA也违反“重复功能合并”的目标。
- 对照同事高保真复盘方案后，只吸收“先说明用户下一步，再展示交易详情”的结构原则；没有吸收演示中的胜率、纪律评分、目标仓位、止盈止损价或静态结论。
- 无任何交易复盘且没有筛选条件时，双栏工作区现切换为单一三步引导：选择一只关注股票、在“任务与操作”记录减仓或卖出、等待三个完整交易日。页面只保留一个“去记录一次真实操作”按钮，真实点击会进入“我的关注”。
- 有历史记录但当前筛选为空时，不会误显示首次使用引导：搜索和状态筛选继续保留，左侧只提供一个“清除筛选”，右侧说明当前没有匹配记录。真实交互已验证清除后会恢复无记录用户的三步引导。
- 第一次仓库浏览器门禁为20通过、0失败，但人工查看390px截图发现三张步骤卡仍横排，中文被挤成竖排。最终实现把移动端改为单列，并把每张步骤卡宽度不得低于280px加入Runner；最终实测三张卡均为324px。这证明“无横向溢出”不能替代视觉可读性检查。
- 最终报告 `artifacts/browser-smoke-w19-review-onboarding-final/result.json` 覆盖1280×720与390×844十个核心页面，共 `20 passed / 0 failed`；无横向溢出、控制台错误、页面错误或失败响应。完整回归为 `818 tests collected` 且全量通过；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
- 运行态复核：`/health=ok`、`/ready=ok`、Hermes启用，PostgreSQL领域库与持久化队列正常，2个Worker active；备份检查仍为skipped，生产边界不变。

当前边界：本轮只解决没有复盘记录时的首次使用路径；已有交易复盘的计划—执行—价格—逻辑详情结构和用户确认边界未改变。下一轮应继续审计真实Agent长回答和有记录复盘详情，避免重复摘要与内部术语。

## 2026-07-28 W18：个股研究概览从证据堆叠改为摘要优先

- 真实首次用户路径审计覆盖市场总览、AI投研新对话、透明选股和个股研究。市场总览的个人待办已在底部，新对话四个任务入口和透明选股双页签层级清楚；最高影响问题集中在个股概览：行情、K线、研究重点、正式判断、五类证据地图、变化和任务同时展开，重复展示证据，页面更像后台审计台而不是用户研究空间。
- 对照同事高保真方案后只吸收“摘要优先、详情渐进披露”的信息架构原则，没有吸收静态结论、目标价或未经当前证据验证的内容。研究概览现保留行情与K线、当前研究重点/判断、最近变化和下一步研究；原五卡证据地图不再在概览重复渲染。
- “当前研究判断”新增“查看完整证据”，进入独立“数据与证据”标签；该页仍完整保留六维证据覆盖、已确认事实、关键反证与压力、信息缺口、判断失效条件和财务/股东/事件模块。正式判断的新增和编辑仍必须由用户主动保存确认。
- 最近变化和下一步研究在概览只展示最高优先级两项，并显示总数；完整变化进入数据与事件，全部任务进入“任务与操作”。浏览器实际点击已验证证据入口可用，独立页显示6张覆盖卡和4类研究证据，不存在功能丢失或仅用CSS隐藏的情况。
- 前端合同改为断言概览只包含判断、变化和任务三块，证据页仍包含已确认证据、关键反证、缺口和失效条件；浏览器 Runner 的390px宽度门禁也改为检查这三块真实核心卡片。
- `artifacts/browser-smoke-w18-stock-overview-hierarchy/result.json` 覆盖1280×720与390×844十个核心页面，共 `20 passed / 0 failed`；移动端三块核心卡片均为344px，页面无横向溢出、控制台错误、页面错误或失败响应。完整回归为 `818 tests collected` 且全量通过；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
- 运行态复核：`/health=ok`、`/ready=ok`、Hermes启用，领域库与持久化队列均为PostgreSQL，2个Worker保持active。

当前边界：本轮解决的是个股研究空间最明显的信息重复和首屏密度问题，没有改变金融证据生成、Agent即时回答或正式写回权限。后续应继续按首次用户任务审计复盘详情和真实Agent回答长度；不能因为页面更清楚就上调生产就绪结论。

## 2026-07-28 W17：基金与ETF概念准确性、简洁度与结构完整性

- 真实失败样本 Run `d2f03b6d-dda8-4814-ae20-7b611de81a17` 虽然知道“ETF是基金的一种”，但仍把“小白能看懂”误判为个人适合性，写出“一天只有一个净值”“到账需要几个工作日”等过度概括。根因是已确认画像会把单纯的“小白”表达风格升级为适合性问题，知识与 Prompt 对价格确认、交收和到账边界也不够强。
- `build_financial_advisor_context()` 现将“请用小白能看懂”保留为 `concept_only`；只有明确引用个人条件、风险画像或询问选择/配置时才进入适合性路径。概念回答不会因为用户已确认画像而重复追问。
- 通用知识库与 `general-research` Skill 明确：ETF是基金的一种；口语中的“基金和ETF”通常比较常见场外开放式基金与场内ETF；场外基金按交易日净值确认申赎，场内ETF在交易时段有连续成交报价；到账、资金可用、交收、费用与定期投入均需核对具体产品、市场和渠道，不能写成ETF一定更快、更便宜或所有基金“一天一个价格”。
- Prompt 增加基金与ETF概念精度合同：纯概念问题只保留短结论、最多三项关键差异和核验提醒；不强制凑满三项，不添加无必要类比，不延伸个人适合性建议。没有证券账户时，只能在具体产品确有对应联接基金的前提下说明该场外形态，不能泛称多数ETF都有联接基金。
- 真实无证券账户回答 Run `3aa9e9ce-5aaa-4b1b-a6bc-1bdbd9971565` 暴露了另一个独立问题：模型原文的 `**1.** / **3.**` 被数字守卫当作无证据数字删行，只留下第2项。输出守卫现识别 Markdown 加粗、项目符号和标题中的有序列表编号；最终 Run `7650ad15-b5e3-458a-b44a-25b9e840eba4` 三段完整、`unsupported_numbers=[]`、没有触发行级修复。
- 真实 DeepSeek 最终样本还包括：关系解释 Run `412cb1ea-0fc8-4dc0-a663-a2091c6f3236` 为 `concept_only`，不再追问画像；到账问题 Run `5f63fb4b-34b1-4c0e-88ba-ef34a9ba74ff` 直接否定“ETF一定更快”，并要求核对产品、渠道与券商规则。全部响应保存于 `artifacts/w17-fund-etf-accuracy/`。
- `artifacts/browser-smoke-w17-fund-etf-accuracy/result.json` 覆盖1280×720与390×844十个核心页面，共 `20 passed / 0 failed`。完整回归为 `818 tests collected` 且全量通过；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

当前边界：概念关系、交易渠道、净值/成交报价和到账边界已通过真实模型抽样；最终无证券账户样本仍为428字符，事实和结构合格但略长于180—320字目标。下一轮应继续通过回答规划和抽样压缩重复解释，不能用机械截断破坏完整句子。具体基金选择仍需产品合同、完整费率、流动性与正规销售渠道适当性事实。

## 2026-07-28 W16：已确认风险画像到真实 DeepSeek 顾问回答

- 使用隔离 evaluation 用户 `042dedc2-5ffe-4698-aadc-c0c8fe907946` 保存并确认七项风险画像；未写入普通用户正式画像。失败基线会话 `a1249ce3-c880-41ca-9e24-11042e2a73e8`、Run `a91a5b11-af49-4da0-87b4-8017c5bdff54` 明明已有确认画像却声称“目前还没有确认的风险画像”，并重复追问期限、自创期限与亏损阈值、扩写五类产品百科。
- 根因是风险画像入口问题未命中适合性语义，`financial_advisor_context` 被降为 `concept_only`；七题问卷还曾被错误解释为已经覆盖“已有资产”。现在已确认画像只覆盖资金使用时间、可接受回撤、应急储备和负债，只有用户补充持仓后才进入 `ready_for_conditional_guidance`。
- 连续对话现识别“基于这些已知条件继续缩小优先比较范围”等自然追问；画像已齐全时不得重复追问、不得用“最适合、最优、收益更高”收尾，改为说明优先比较方向、推理依据与下一步核对项。回答清理同时修复“你可以如果继续细化”和重复编号残句。
- 输出校验不再把“不能保证收益”误判为“保证收益”，也不会把“沪深 300”这类常见指数名称当作无证据数字后删除整段；正向保证和买入建议仍会被拒绝。
- 最终隔离会话 `dc1d6227-b29d-4cff-a6f2-de81dafb253e` 使用真实 `deepseek-v4-pro` 完成两轮。Run `94396c99-a386-4c5f-bc54-d62eb943db70` 正确复述四项已确认事实并只追问已有资产；用户补充“主要持有银行存款，没有基金和股票”后，Run `f8ddf214-6730-4d8b-9b88-9aa7115fe75a` 进入 `ready_for_conditional_guidance`，`missing_fields=[]`，未重复追问或输出收益承诺。完整证据位于 `artifacts/w16-financial-advisor/`。
- 真实内置浏览器只读检查 `/account`：七题、草稿/确认边界和按钮状态完整可见，页面控制台错误为0；未替普通用户选择、保存或确认画像。一次点击普通“基金与 ETF”快捷入口用于检查真实回答展示，向既有会话追加了测试问答但没有改变正式画像；该历史未被删除或改写，后续自动验收必须继续使用隔离 evaluation 用户。
- `artifacts/browser-smoke-w16-financial-advisor-final/result.json` 覆盖1280×720与390×844十个核心页面，共 `20 passed / 0 failed`。当前完整回归为 `815 tests collected` 且全量通过；唯一提示仍为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

当前边界：已确认画像与真实 DeepSeek 回答已在隔离用户的 API/持久化链完成，浏览器端只读界面和通用回答展示分别验收；尚未在同一个隔离浏览器会话中完成“填写、确认、预填、发送、刷新恢复”全链。具体基金选择仍需要当前产品事实、完整费率和正规销售渠道适当性核验，不能把本轮类别级解释当成产品推荐。

## 2026-07-28 W15：风险画像到金融顾问的清晰路径

- 真实 `/account` 走查确认原流程虽然已有7题、草稿和确认边界，但“保存草稿 / 确认并用于 AI”更像后台状态操作；用户看不到回答进度，漏题时只能收到文字错误，确认后也没有明确下一步。
- 页面现固定展示“回答7个问题 → 保存并核对 → 确认让 AI 使用”三步；进度条同步显示已回答数量和下一道未完成问题。提交不完整答案时只高亮并聚焦第一道缺失问题，不发出保存请求。
- 答案修改、草稿待确认和已确认使用不同状态与下一步文案。确认后的画像卡新增“去问金融顾问”；它只打开新的 AI 对话并预填一条适合性问题，不自动发送，不创建判断、计划、任务或复盘，也不替用户修改正式画像。
- 浏览器 Runner 使用只读真实页面和本地合成 DOM 状态覆盖未填写、缺失定位、答案已修改、草稿待确认和已确认五种状态；Runner 仍不发出 `POST/PUT/PATCH/DELETE`，不会替真实用户确认画像。
- 真实内置浏览器 390×844 验收：页面与滚动宽均390px，三步流程单列，问题卡宽300px，保存和确认按钮高40.5px；操作区和边界文案均完整可见。临时 viewport 已恢复。
- `artifacts/browser-smoke-w15-risk-profile-final/result.json` 覆盖桌面与390px十个核心页面，共 `20 passed / 0 failed`；风险问题13px、选项12px、按钮最小高度40px，确认后金融顾问入口可见。
- 当前完整回归为 `808 tests collected` 且全量通过；首次回归只发现旧合同仍检查原按钮文案，更新为“确认让 AI 使用”后重跑通过。唯一提示仍为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

当前边界：本轮没有替任何真实用户填写、保存或确认画像；产品事实仍依赖免费公共接口，生产运维仍缺可核验 PostgreSQL 备份，不能把本轮 UX 验收表述为正式生产就绪。

## 2026-07-28 W14：最近对话与全部历史分层

- 真实1280×720页面一次渲染67条历史、35个主题；左栏可视高度仅132px，内容高度2126px。用户需要在狭小区域翻完整历史，且无法快速判断“最近”与“全部”的边界。
- 默认历史现限制为最近8个主题；如果当前打开的是更旧会话，会额外固定其主题，避免当前上下文从历史区消失。页面明确显示“最近8个主题 · 共67个对话”。
- “查看全部历史（67）”固定在历史区底部，无需先滚到底；展开后恢复完整67条和35个主题，按钮改为“只看最近对话”。收起后列表回到顶部。
- 搜索和类型筛选始终基于完整67条，不受最近视图限制；真实搜索“历史回放”找到2条旧会话，筛选结果不会混入“查看全部”开关。移动端继续使用包含全部历史的下拉，不渲染桌面长列表。
- 真实390×844页面保留68个下拉项（新建入口加67条历史），分类选项完整，页面滚动宽度仍为390px。桌面历史内容高度从2126px降至567px，固定入口在132px可视区内。
- `artifacts/browser-smoke-w14-recent-history-release/result.json` 在两个视口各用67条只读历史验证最近8主题、当前旧会话固定、完整展开/收起、旧会话搜索和筛选不截断；十个核心页面共 `20 passed / 0 failed`。
- 当前完整回归为 `808 tests collected` 且全量通过；唯一提示仍为Starlette TestClient上游弃用警告。

## 2026-07-28 W13：历史对话基金理财独立分类

- 真实67条历史中，“基金和ETF有什么区别”被标成个股，“我快退休了……股票基金还是债券基金”被标成选股。根因是前端用标题宽泛正则分类，并在识别基金产品前先命中“股票/选股”；后端又只返回 `general_research`，无法稳定区分基金教育与普通研究。
- 新回答现在把 `conversation_scope` 写入最近 Assistant metadata；基金产品事实或金融顾问上下文稳定写为 `funds`，个股、大盘、选股和关注沿用各自分类。数据库不新增列或迁移，`list_conversations()` 从最近元数据读取并公开该字段。
- 旧对话没有 `conversation_scope` 时继续兼容重分类；基金、ETF、LOF、REIT、QDII、股票基金、债券、固收、养老、退休、资产配置和风险画像优先于宽泛“股票/选股”判断。桌面与移动端新增“基金理财”筛选，历史切换器显示“基金理财 · 标题”。
- 真实内置浏览器刷新后仍读取67条历史；选择“基金理财”显示12/67条，均为基金、ETF或退休理财问题，“中兴通讯为什么大跌”等个股未混入。390×844下移动端筛选同步选中，12条历史均带“基金理财”标签。
- `artifacts/browser-smoke-w13-conversation-scope-release/result.json` 在桌面和390px各构造67条只读历史，验证旧基金问题、退休理财问题、后端显式分类、真实个股不误伤和双端筛选同步；十个核心页面共 `20 passed / 0 failed`。
- 当前完整回归为 `807 tests collected` 且全量通过；唯一提示仍为Starlette TestClient上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

## 2026-07-28 W12：新建对话首屏重排

- 原390px入口区为275px可视高度、847px内部内容，用户只能直接看到第一项；输入区底部为938px，超过844px视口。桌面同时显示空研究侧栏、欢迎回答、快捷追问和四张开始卡。
- 新会话现只展示“看今天大盘 / 研究一只股票 / 按规则选股 / 比较基金与 ETF”、历史切换和输入区。开始对话后入口自动让位，消息、快捷追问和研究侧栏恢复。
- 空占位行情框不再主动显示。实际识别市场、股票、多股或基金产品后才展开对应行情、证据和研究依据。
- 390px最终 `hubClientHeight=511 / hubScrollHeight=511`，输入区底部821px；桌面为 `268 / 268`，输入区底部691px。入口标题14px、桌面说明11px，四项全部在容器内。
- 基金卡继续明确“场内成交价与基金净值会分开显示”；完整回归曾发现该文案被压缩删除，恢复后基金与风险画像合同通过。
- `artifacts/browser-smoke-w12-agent-landing-release-2/result.json` 覆盖桌面与390px十个核心页面，共 `20 passed / 0 failed`。浏览器门禁同时验证从开始页进入对话后，W11快捷能力恢复且展开/收起稳定。
- 恢复基金边界后的完整回归为 `805 tests collected` 且全量通过；文档更新后Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 再次通过。

## 2026-07-28 W11：对话快捷入口可发现性

- 真实390×844走查发现快捷入口容器宽360px、实际内容宽570px，但横向溢出被隐藏；部分能力完全不可见，按钮和分组均只有10px，“更多能力 · 9”与实际隐藏数量也不一致。
- 快捷入口重组为“常用问题 / 专项工具”。首屏直接显示大盘解读、个股分析、行业板块、自选股变化、基金与 ETF、财报公告、多股比较、反方风险和操作前检查；“公司对比”和“跟踪变化”两个重复入口已删除。
- 市场与持仓、研究任务、利润现金流、股东结构、分析师预期和事件脉络保留在“全部工具”中。展开后按钮改为“收起工具”，再次收起恢复稳定首屏；现有专项按钮 ID 和消息路由没有改变。
- 390px 收起态 `clientWidth=360`、`scrollWidth=360`、`clientHeight=157`、`scrollHeight=157`；桌面为 `696/696/84/84`。按钮字号12px、分组11px，浅色禁用感已修正。
- 浏览器 Runner 增加分组、首屏按钮、最低字号、裁切、展开和收起恢复合同。`artifacts/browser-smoke-w11-quick-actions/result.json` 覆盖桌面与390px十个核心页面，共 `20 passed / 0 failed`。
- 首次全量回归发现本文件记录了本机 PostgreSQL 备份绝对路径，违反安装文件可移植性合同；已改为通用的“配置的 PostgreSQL 备份目录”。修复后重新执行 `805 tests collected` 并全量通过；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 同步通过。

## 2026-07-28 W10：涨跌问答自然表达与最终发布门禁

- 修复自然表述兼容：模型可使用“约3%”“5000多只”“超过5500只”“约51%上涨”等与确定性证据一致的自然舍入，不再因为没有逐字复述精确值而误触发整段回退。
- 修复回答清理后首段丢失、页面直接从第二项开始的问题；最终回答继续在同一消息节点完成，不再跳回“候选事件 / 反方证据 / 下一步核验”的固定模板。
- 涨跌原因上下文继续剔除融资余额榜单、股权登记榜单等低价值标题；完整来源保留在折叠证据区，但模型不得用它们扩写资金敏感、获利回吐、登记日卖出或未披露事项等故事。
- 普通用户明确要求“小白能看懂、直接说、别写成报告”时，回答改为短自然段。若同日公司、行业或市场证据不能确认具体驱动，结尾必须停在“具体驱动仍未确认”，不得再用“市场买卖力量自然变化、缺少利好承接”等兜底归因，也不得安慰用户“公司没事”。
- Web 与双并发外部 Worker 已重新加载最新代码。首次恢复时队列有14个到期任务，两个 Worker 已清零；最终 `/health=200`、`/ready=200`、53/54 healthy、1 attention、0 critical，ready/delayed/retrying 均为0。
- 正式运维自检仍为 `degraded`，当前唯一失败是 `postgres_backup_not_healthy`：配置的 PostgreSQL 备份目录没有可核验备份。业务与队列门禁通过不等于生产灾备就绪。
- 真实网页通过 Enter 提交同一问题，新会话 `/research/6c4190ef-d445-4828-a473-88fef752115c` 正确显示盘中报价、尚未收盘、市场广度和“公司层面确切原因还不清楚”，没有再用“跟着大盘走弱”“没有利好撑住”或“市场买卖力量自然变化”替代缺失证据。刷新后同一 URL、用户问题和最终回答完整恢复。
- 390×844 内置浏览器复验中，页面宽和滚动宽均为390px，输入框保持可见；历史回答异步恢复后仍为原问题和原答案。仓库长期门禁 `artifacts/browser-smoke-w10-final/result.json` 为桌面与390px十个核心页面共 `20 passed / 0 failed`。
- `805 tests collected` 且全量通过；新增 Prompt 合同定向测试通过。Ruff、Python `compileall`、全部静态 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

## 2026-07-28 W9：涨跌问答证据裁剪与轻量行情路径

- 个股涨跌问题优先装配当前报价、上一完整交易日、同日市场广度、精确行业可用性和日期对齐事件，不再把完整个股研究报告、低价值媒体榜单或无关慢变模块塞入标准问答。
- 当前报价与历史日线分开表达：盘中报价必须说明“尚未收盘”，收盘后报价不得写成盘中；用户指定日期时不得用更新日线替代目标日。
- 同日公司公告、交易时段媒体线索、收盘后事件和邻近日事件分别进入不同证据槽。收盘后和邻近日内容不能解释当日交易时段；证据不足时必须保留未确认边界。
- 回答路径保留 DeepSeek `deepseek-v4-pro` 即时生成、一次模型调用优先、受控安全流式和最终守卫，不把保存报告或确定性摘要冒充本轮 AI 回答。

## 2026-07-28 W8：市场总览与条件选股清晰度

- 市场页不再把全球行情、A股细节、Agent、个人关注和多类文章混成同一信息流。当前顺序固定为“全球行情 → A股一眼看懂 → 四个核心指数 → 市场Agent → 市场复盘文章 → 个人待办”。
- “A股一眼看懂”直接展示市场状态、上涨/下跌家数、全市场涨跌中位数、当日累计成交额和领涨方向。真实页面为2026-07-27普涨，上涨5,194只、下跌286只，中位数+2.72%，成交额2.09万亿元、较上一完整交易日+7.41%，领涨方向为个护小家电+10.03%。
- 市场文章区只展示4篇大盘文章；个股报告、机会和重复关注脉冲已移出。Agent入口标题为“问 Agent：今天市场发生了什么？”，示例问题直接询问A股普涨与领涨行业。
- “打开市场复盘”真实点击后进入 `/reviews?tab=market`，市场复盘页签保持选中并显示4篇按时间排序的市场事实文章。
- 真实执行“相对行业增强候选”得到12只候选。页面先解释“这批结果怎么用”，数据日期、覆盖范围和版本详情默认折叠；每只候选显示“为什么出现在这里”，只保留“加入我的关注 / 研究这只股票”两个动作。
- 选股结果解释和入选理由由12px提高到13px。390px下候选卡宽332px、页面宽与滚动宽均为390px，12张卡的动作数均为2，覆盖详情保持关闭。
- `artifacts/browser-smoke-w8-market-screening-clarity/result.json` 覆盖桌面与390px十个核心页面，共 `20 passed / 0 failed`；页面级横向溢出、控制台错误、页面异常和非预期本地失败响应均为0。
- 全量 `793 passed in 115.37s`；唯一提示仍为 Starlette TestClient 上游弃用警告。Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
- Web已重启并加载W8样式；`/health=ok`、Hermes启用、PostgreSQL Schema v5/v4、外部Worker 2个、队列ready/delayed/retrying均为0，数据健康53/54且0 critical。

## 2026-07-28 W7：透明选股与复盘首次用户路径

- 真实390px首次用户走查发现：李总策略仍有英文标签，七个结果分类依赖横向滚动；“通用截面筛选”术语生硬；空交易复盘没有可执行入口；市场复盘虽然已有文章，但标题和摘要重复使用“跨市场分时”，且向用户展示内部版本规划。
- 透明选股现改为“按条件选股 / 李总策略”。李总策略顶部改为“规则公开 · 每项可核验”，结果分类在390px自动换行；选股标签、规则、指标和说明文字同步提升可读性。
- 空交易复盘同时在列表和详情区提供“去我的关注”，引导用户先记录真实操作；没有替用户创建、修改或确认任何正式对象。
- 市场文章公开层会从文章保存的 `market_brief` 证据确定性重建标题和摘要，展示市场日期、沪深京股票总数、上涨/下跌/平盘家数、涨跌幅中位数、当日累计成交额和领先板块。真实页面最新四篇文章已按时间排列，示例为2026-07-27上涨5,194只、下跌286只、平盘52只、中位数+2.72%、成交额20,883亿元。
- 市场复盘移除“当前 MVP”“后续将”等内部规划文案；历史边界只说明文章是当时事实，不代表当前行情，也不提供下一交易日预测。
- 仓库浏览器 Runner 新增 `/reviews?tab=market` 和上述长期断言。`artifacts/browser-smoke-w7-novice-paths/result.json` 为桌面与390px共 `20 passed / 0 failed`；所有路径无页面级横向溢出、控制台错误、页面异常或非预期本地失败响应。
- `uv run --extra dev pytest --collect-only` 收集 `793` 项，`uv run --extra dev pytest -q` 全量通过；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。唯一提示仍为 Starlette TestClient 上游弃用警告。
- Web 已安全重启并加载新文章公开层；领域库 PostgreSQL Schema v5、运维/队列 Schema v4、外部 Worker 2个，持久化队列状态 `ok`，重启过程中没有停止 Worker 或数据库。

## 2026-07-28 W4第二阶段：基金/ETF产品事实与风险画像

- 新增基金产品搜索、详情和同口径比较服务，产品事实保存到 PostgreSQL Schema v5。后台新增持久化 `fund_product_refresh` 计划，默认每30分钟刷新6只基金/ETF；最近一次真实任务为 `requested=6 / completed=6 / failed=0`。
- 真实数据源验证：东方财富基金搜索和详情可用；东方财富 ETF 实时接口在当前代理下偶发断连，已增加腾讯实时行情、再到新浪行情的两级回退。2026-07-27数据中，510300净值4.7583、场内价4.753、场内涨幅+1.11%；159915净值3.6146、场内价3.612、场内涨幅+2.96%；110022净值2.908。
- 新增七项风险问卷及草稿/确认版本链。未保存草稿时确认按钮禁用；答案变化后必须重新保存和确认；Agent 只读取已确认版本，测试覆盖重复确认、版本边界和跨用户隔离。本轮浏览器没有替用户保存或确认正式画像。
- 桌面真实页面完成产品搜索、代码识别、歧义选择和510300/159915比较；比较结果展开后保持三张主入口一行，产品比较整行展示。390×844无横向溢出，基金卡和风险问卷可滚动阅读。
- 第一轮真实问答暴露两个问题：`needs_profile` 输出修复错误地把有证据的收益百分比改成模糊措辞；前端又把问题中的六位基金代码当作股票请求 `/stocks/510300/history`。当前只保留产品证据中受支持的百分比和“近1年”等窗口，并增加基金产品研究侧栏，复验日志不再出现该股票历史请求。
- 最终 Run `552e7127-18da-455b-a6e7-0134b311a788` 使用 `deepseek-v4-pro`，状态 completed，输出守卫通过，首Token 15.493秒、首个安全可见内容15.976秒、请求总耗时34.696秒。回答准确列出两只ETF四个同口径收益窗口，并在画像未确认时拒绝替用户选唯一产品。
- 最终完整回归 `792 passed in 289.45s`；唯一提示为 Starlette TestClient 上游弃用警告。Ruff、Python `compileall`、全部静态 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。运行态 `/health=ok`、Hermes启用、领域库Schema v5、运维/持久化队列Schema v4、独立Worker 2个、队列无等待积压、数据健康53/54且0 critical。

## 2026-07-28 W5证券证据双路径

- 新增 `stock_evidence_path_v1`：当前用户自选股走 `watchlist_preanalysis`，非自选股走 `online_research`；合同、Skill、刷新模块、复用模块、缺失模块、数据时间和报告引用边界均写入 Run。
- 非自选股强制在线刷新行情、公司资讯、财务估值和事件；即使数据库已有服务器报告，也不读取报告证据或正文。自选股仍刷新快变模块，慢变模块可按时效复用；只有在线刷新失败时才允许按模块使用服务器证据回退。
- Prompt 和 Skills 明确正式披露、监管文件与媒体线索分层，不把混合列表统称为媒体材料，不预告固定证据数量，并要求把“风险委员会、研究管理层”等内部分析角色翻译成自然用户语言。
- 真实贵州茅台首次在线 Run 暴露守卫误判：“微跌0.61%”没有被旧方向词识别，导致完整 DeepSeek 回答被回退为机械摘要。当前加入“微涨/微跌”识别并用真实措辞回归。
- 最终重试 Run `76d1a251-9f54-4e6a-a290-db7b0ca2fd69` 使用 `deepseek-v4-pro`，状态 completed；`online_research`、`force_online_refresh=true`、`precomputed_report_reference=null`、`fallback_used=false`，输出守卫通过。首Token 5.181秒、首个安全可见6.752秒、总请求19.565秒。
- Web 重启后的同题复验 Run `20b818fe-a0ab-4067-8aba-1b470ff974e0` 再次为 `deepseek-v4-pro / completed / online_research`，强制在线刷新、无报告引用、无回退且守卫通过；最终回答未出现“风险委员会”“研究管理层”或“多方证据结论”，证明最新 Prompt 已在真实服务生效。该轮同行比较仍明确标为缺口。
- 自选股中兴通讯 Run `d3ce09af-8c79-48ab-aa23-8b86b50c7133` 已验证 `watchlist_preanalysis`：快变模块本轮刷新，慢变模块按合同复用，页面不显示内部路径、供应商错误或预生成报告正文。
- 本阶段完整回归 `784 tests collected` 且全量通过；Ruff、Python `compileall`、全部静态 JavaScript、`uv lock --check`、`git diff --check` 通过。同行比较仍可能是明确缺口，但不再阻塞其他可用模块和本轮模型回答。

## 2026-07-28 李总策略同暴露基准 v4

- 按用户最新要求将当前唯一组合版本升级为 `li_zong_2w_no_cost_same_exposure_v4`：交易成本为0，任意两次实际换仓至少间隔10个交易日；沪深300只在策略持仓期保持同等暴露，策略空仓期同步冻结，不计入相对收益比较。
- PostgreSQL 已发布三档 v4。近3个月策略/同暴露沪深300/超额为 `-9.8251% / -0.5643% / -9.2608%`；近1年为 `+44.9446% / +23.0523% / +21.8922%`；近3年为 `-9.0819% / -5.0056% / -4.0764%`。
- 独立逐点检查中，近3个月、1年、3年分别存在20、84、501个连续空仓冻结段，基准变化违规均为0；三档实际最短换仓间隔均为10个交易日，累计成本均为0。
- 回测不再用缺失收盘价制造持仓横线：持仓缺少真实复权收盘价会拒绝发布。3年持仓期仅有3段净值恰好不变，逐段核验均有真实开盘与收盘数据，只是相关股票相邻交易日收盘价确实相同，并非数据缺口或旧价格填充。
- 真实页面已切换近3个月、1年、3年并读取当前结果；默认近1年显示 `+44.94% / +23.05% / +21.89% / 21次（≥10日）`，图例为“沪深300同暴露基准”“策略空仓期不计入比较”。辅助技术标签也已从“连续区间”修正为“同暴露累计收益曲线”，页面控制台无 warning/error。

## 2026-07-28 W4金融顾问第一阶段

- 新增金融产品、基金/ETF、债券利率、风险收益与资产配置四份通用知识；基金问题强制检索相关资料，不混入无关个股报告。
- 修复 ETF 证券误识别、基金选择误触发全A股筛选、个人条件选择未进入适合性三类路由问题。
- `financial_advisor_context` 在信息不足时标记 `needs_profile`；回答只能解释条件并追问，不能凭年龄、金额或新手身份直接推荐产品。
- 实测发现模型自创百分比会被旧数字修复整行删除，造成“两问只剩一问”。当前在完整守卫前先做定性自然化，保留完整追问，再进入原确定性数字校验；真实措辞变体均已加入回归。
- 最终 DeepSeek Run `ec4899fc-4c86-4d71-b1d0-f7b1c3c7bf9d` 使用 `deepseek-v4-pro` 一次调用，首Token 6.531秒、首个安全片段6.733秒、总耗时18.069秒；输出守卫通过，无不受支持数字，完整追问资金期限与亏损承受。
- 桌面与390px最终浏览器门禁为 `16 passed / 0 failed`，报告位于 `artifacts/browser-smoke-w4-final-2/result.json`。当前运行态 `/health=ok`、独立Worker 2个、队列无等待积压、数据健康53/54且0 critical。
- 本行是第一阶段冻结边界；基金/ETF产品数据与正式风险问卷已由上方第二阶段完成。完整资产配置、更多产品品类和商业数据授权仍未完成，因此 W4 整体仍为部分完成。

## 2026-07-26 目标交易日资讯召回与后台队列消积压

- 修复美股历史原因问题只读取最新 30 条资讯、导致目标交易日新闻被次日密集报道挤出的缺陷。指定 `target_market_date=2026-07-23` 时，真实 PostgreSQL 数据库现可从 76 条已存美股资讯中召回目标日 17 条，因果证据状态为 `same_date_multi_source`。
- 真实页面提问“7月23日美股为什么下跌”已由 DeepSeek v4 Pro 即时生成回答，明确使用同日科技巨头资本支出、AI投入回报和油价突破 100 美元等候选线索，并用次日标普小幅修复、指数内部跌幅分化和非唯一因果边界作为反方证据。
- 非 A 股市场简报不再请求或公开 A 股热门板块。美股证据栏由 8 项降为 7 项，移除了跨市场噪音，同时减少一次不必要的板块数据请求。
- 单 Worker 下实时行情与李总策略平均耗时合计超过调度周期，曾造成 10 个等待任务和 `/ready` 队列滞后降级。生产默认 `JOB_WORKER_CONCURRENCY` 提升为 2；重启后两个活跃 Worker 将等待任务从 10 个清至 0，未增加失败任务。
- 真实页面回归今日观察、个股研究、AI研究和复盘中心均正常；完整 `731` 项测试、Ruff、Python 编译、全部静态 JavaScript 语法、锁文件和差异检查通过。

## 2026-07-26 Chat 纯路由模块化

- 新增 `app/services/chat_routing.py`，迁移证券名称/代码识别、选股意图与参数、市场问题识别、市场关注焦点、日期/涨跌方向解析、上下文追问识别、历史证券/意图恢复，以及财报、股东、研报、事件、研究行动等纯路由规则。
- `app/main.py` 继续通过同名导入兼容原调用与测试；新模块不导入 `app.main`，避免循环依赖。`main.py` 从约 6,679 行降至 5,703 行，新模块为 1,069 行。
- 第一次定向回归发现“它相对通信设备行业是更弱还是更强”没有生成 `stock_market_context`。原因是抽取时遗漏了工作树中新加入的相对行业触发词；已恢复“相对行业/板块、所属行业、更强/更弱”并用原 API 集成测试验证，不能把从基线文件复制代码当作安全抽取方法。
- 新增 `tests/test_chat_routing_modules.py`，覆盖主模块兼容导出、无反向导入、市场/个股追问语义和历史研究对象恢复。当前完整回归为 `695 tests collected` 且全量通过。
- 重启 Web 与独立 Worker 后，最终 Chrome Runner 桌面和 390px 共 `12 passed / 0 failed`；报告位于 `tmp/browser-smoke-20260726-chat-routing-complete/result.json`。
- 新进程 `/health=ok`、Hermes 启用、领域库 PostgreSQL Schema v2、运维库和队列 Schema v4、独立 Worker active=1；ready/delayed/retrying/expired 均为 0，数据健康 53 healthy、1 attention、0 critical。
- 隔离真实 `deepseek-v4-pro` 调用再次返回“本轮模块化验证完成。”，`guard_passed=true`，未写入正式用户会话。

当前保守评分：功能设计 `8.0`、架构设计 `8.2`、实际可用性 `7.2`、内部 MVP 综合 `7.7`、正式对外长期使用 `6.2`；Agent `7.8`、前端信息架构 `7.8`、工程质量 `8.0`。

## 2026-07-26 Chat 回答持久化、SSE 发布与冷启动导航竞态

- 新增 `app/services/chat_persistence.py`，统一保存 Assistant 消息及其知识来源、市场来源、证据来源、研究对象、结构化卡片、模型层级和选股 Profile。个股涨跌问题不会把旧资料库摘录标成当前回答的直接事件证据。
- 新增 `app/services/chat_streaming.py`，统一发布确定性进度、Hermes 增量、reset、引用、待确认写回候选、结构化失败和完成事件。`main.py` 仍保留证据查询、服务调用顺序和异常边界，因此本轮是继续拆分 Chat 编排，不是宣布编排治理完成。
- 新增 `tests/test_chat_persistence.py` 3 项、`tests/test_chat_streaming.py` 4 项；连同网页合同的定向测试共 10 项通过。`app/main.py` 当前为 5,509 行，`chat_routing.py` 1,069 行，`chat_persistence.py` 179 行，`chat_streaming.py` 153 行。
- 第一次最终浏览器门禁为 11/12：桌面李总策略数据已 ready，但面板被冷启动恢复流程重新隐藏。根因是 `openScreeningSection()` 没有增加 `workspaceNavigationVersion`，启动流程误判用户尚未导航并恢复初始 `general` 分区。
- 修复后用户主动切换会增加导航版本，异步初始化不再覆盖用户选择。桌面与 390px 六个核心页面最终 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-chat-orchestration-rerun/result.json`；控制台错误、页面异常、非预期失败响应和页面级横向溢出均为 0。
- 当前完整回归为 `702 tests collected` 且全量通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。
- 运行态 `/health=ok`、Hermes 启用，领域库 PostgreSQL Schema v2，运维库和持久化队列 PostgreSQL Schema v4，独立 Worker active=1；ready/delayed/retrying/expired 均为 0。数据健康 53/54，唯一 attention 是 A 股成交额完整历史仍在积累并排除了盘前不完整快照。
- 管理运维接口继续要求认证；已有运维自检证据显示唯一生产门槛是本机没有可核验 PostgreSQL 备份。本轮没有把 liveness、测试通过或未认证的管理接口表述为生产就绪。

当前保守评分：功能设计 `8.0`、架构设计 `8.3`、实际可用性 `7.2`、内部 MVP 综合 `7.7`、正式对外长期使用 `6.2`；Agent `7.8`、前端信息架构 `7.8`、工程质量 `8.1`。

## 2026-07-26 Chat 请求准备上下文迁出 main.py

- 新增 `app/services/chat_context.py`，统一负责研究对话建立/恢复、最近历史读取、证券与意图上下文路由结果、图片归属与文件生命周期校验、针对问题的资料库检索请求，以及本轮用户消息保存。
- 服务只准备请求上下文，不执行金融分析、不调用 Hermes、不生成回答，也不改变后续李总策略、市场、财务、个股和其他证据分支的顺序。失效会话、非本人图片与已失效图片继续在 API 层分别返回原有 404/410。
- `app/main.py` 从 5,509 行降至 5,382 行；新模块 342 行。新增 4 项测试，覆盖市场问题强制检索三份通用证据规则、行业比较追问恢复中兴通讯、会话状态和图片生命周期；原有行业比较 API 全链继续通过。
- 完整回归为 `706 tests collected` 且全量通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。
- Web 与独立 Worker 重启加载新模块后，`/health=ok`、Hermes 启用，领域库 PostgreSQL Schema v2，运维库与持久化队列 Schema v4，Worker active=1，retrying/expired 为 0，数据健康仍为 53 healthy、1 attention、0 critical。
- 最终桌面与 390px 六个核心页面共 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-chat-context/result.json`。本轮没有因工程拆分上调实际可用性或产品成熟度评分。

当前保守评分仍为：功能设计 `8.0`、架构设计 `8.3`、实际可用性 `7.2`、内部 MVP 综合 `7.7`、正式对外长期使用 `6.2`；Agent `7.8`、前端信息架构 `7.8`、工程质量 `8.1`。

## 2026-07-26 Chat Agent 执行与回答收尾编排迁出

- 新增 `app/services/chat_execution.py`，统一处理证据已就绪事件、Agent 调用、受控流式回调、结构化卡片与待确认写回候选、深度研究观察、补证任务捕获、最终 Assistant 回答保存和完成事件。
- 结构化卡片生成仍是附加能力：即使该步骤异常，已经形成的有效金融回答、Run 和证据仍会保存并返回，页面只收到结构化结果失败事件，不会把正文替换成泛化失败文案。
- `app.main` 继续兼容导出 `_agent_evidence_progress`，避免拆分破坏既有测试和内部调用。`app/main.py` 从 5,382 行降至 5,309 行；新模块 148 行。
- 新增 3 项测试，覆盖模块不反向依赖主应用、完整 Agent/结构化/补证/落库/完成链，以及结构化失败保留有效回答。包含两条原 API 对话链的定向集合 16 项通过。
- 当前完整回归为 `709 tests collected` 且全量通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。
- 新进程真实 HTTP 对话“美股为什么收盘跌了”使用 `execute_agent=false` 验证当前编排：结果为 `market_brief / preview`，回答 580 字，Run `212fb1fc-c7c5-48a9-83e0-23cc1d9e2447`、Assistant 消息和 Conversation ID 均已生成，`error=null`。
- 最终桌面与 390px 六个核心页面共 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-chat-execution/result.json`。运行态 Web、Hermes、PostgreSQL 双库和独立 Worker 正常，队列无 retrying/expired，数据健康仍为 53 healthy、1 attention、0 critical。

当前保守评分仍为：功能设计 `8.0`、架构设计 `8.3`、实际可用性 `7.2`、内部 MVP 综合 `7.7`、正式对外长期使用 `6.2`；Agent `7.8`、前端信息架构 `7.8`、工程质量 `8.1`。

## 2026-07-26 大盘对话证据装配迁出 main.py

- 新增 `app/services/chat_market_evidence.py`，统一处理显式市场识别或连续追问的市场范围、问题焦点、代表指数分析、目标交易日、同日资讯候选、A 股行业专题和伦敦金实时快照。
- 显式问题继续由 `MarketNewsService.infer_market()` 识别；连续追问继续从历史消息恢复 `market_key`。市场资讯接收与代表指数相同的目标交易日，避免把较新或其他市场新闻混入本轮原因解释。
- A 股行业专题使用同一个目标交易日获取行业快照；伦敦金实时快照失败只令 `focused_live_market=null`，不会抹掉已经取得的日线、资讯和回答证据。
- `app/main.py` 从 5,309 行降至 5,272 行，新模块 73 行。新增 4 项测试，包含原市场连续对话、深化回答和市场/自选/个股综合 API 流程的定向集合 7 项通过。
- 当前完整回归为 `713 tests collected` 且全量通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。
- 新进程真实 HTTP 对话“美股为什么收盘跌了”返回 `market_brief / preview`：`market_key=us`，指数仅为标普500、纳指、道指和罗素2000，目标交易日 `2026-07-23`，回答 580 字，Run `d81327e3-5670-4fe9-80e3-4e38f089b996`、Assistant 消息均有效且 `error=null`。
- 桌面与 390px 六个核心页面最终 `12 passed / 0 failed`，报告为 `tmp/browser-smoke-20260726-chat-market-evidence/result.json`。运行态队列 ready/delayed/retrying/expired 均为 0，数据健康仍为 53 healthy、1 attention、0 critical。

当前保守评分仍为：功能设计 `8.0`、架构设计 `8.3`、实际可用性 `7.2`、内部 MVP 综合 `7.7`、正式对外长期使用 `6.2`；Agent `7.8`、前端信息架构 `7.8`、工程质量 `8.1`。

## 2026-07-26 Agent 输出守卫模块化与最终门禁

- 新增 `app/services/agent_output_guard_common.py`、`agent_output_guard_market.py`、`agent_output_guard_stock.py` 和总编排模块 `agent_output_guard.py`，分别承接共用过滤、市场规则、个股规则以及完整校验/修复流程。
- `AgentService` 保留原静态方法入口并委托 `AgentOutputGuard`，旧模块级私有函数通过兼容导出继续服务现有调用；拆分没有删除数字核验、当前报价、市场广度、李总策略、因果、情绪、用户确认或隐私边界。
- `app/services/agent.py` 从最早约 10,467 行累计降至 3,461 行；四个输出守卫模块分别为 78、1,293、1,390 和 2,106 行。仍需继续治理 `main.py`、`db.py` 与 `demo.html`，不能把减行数本身视为用户功能提升。
- 新增 `tests/test_agent_output_guard_modules.py`，覆盖兼容导出、无循环依赖、市场/个股规则和当前报价必答边界。当前完整回归为 `691 tests collected` 且全量通过。
- 最终代码上的真实 Chrome Runner 覆盖桌面和 390px 六个核心页面，共 `12 passed / 0 failed`；没有控制台错误、页面异常、失败响应或页面级横向溢出。报告位于 `tmp/browser-smoke-20260726-output-guard-core/result.json`。
- 隔离临时目录真实调用 `deepseek-v4-pro`，返回“本轮模块化验证完成。”；最终输出守卫 `guard_passed=true`，没有不受支持的数字或语义冲突，也没有写入正式用户会话。
- `/health=ok`、Hermes 启用、领域库 PostgreSQL Schema v2、运维库和持久化队列 PostgreSQL Schema v4、独立 Worker active=1；队列无 ready/delayed/retrying/expired 积压，数据健康为 53 healthy、1 attention、0 critical。
- `scripts/check_operations.py` 当前唯一失败为 `postgres_backup_not_healthy`：本机备份目录 `~/.qingshu/backups` 尚无可核验 PostgreSQL 备份。该缺口不影响本轮业务门禁，但继续阻止正式生产就绪声明。

当前保守评分：功能设计 `8.0`、架构设计 `8.1`、实际可用性 `7.2`、内部 MVP 综合 `7.7`、正式对外长期使用 `6.2`；Agent `7.8`、前端信息架构 `7.8`、工程质量 `7.9`。

## 2026-07-26 Agent Prompt 证据压缩模块化

- 新增 `app/services/agent_evidence_compaction.py`，迁移递归公开证据过滤、市场日期对齐、市场 Prompt 压缩、市场状态计算、资料库与对话压缩，以及个股、比较、研究行动和选股证据压缩。
- `AgentService` 保留原静态方法入口，现只委托纯函数模块，因此现有测试、内部调用和金融输出校验入口不需要改写。
- 市场资料仍只保留最高相关的短摘录；大盘连续对话仍只携带最近用户问题，不回灌旧助手金融结论；个股资料仍优先用户资料并限制每条摘录长度。
- 新增 5 项模块边界与纯函数测试，覆盖递归隐私字段过滤、市场历史裁剪、市场/个股委托边界和用户资料优先级。
- `app/services/agent.py` 从 9,739 行进一步降至 8,122 行；新模块 1,678 行。职责得到物理分离，但输出校验/修复仍在 Agent 单体中，整体治理尚未完成。
- 当前完整回归为 `686 passed`；Ruff、Python `compileall`、内联 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

当前保守评分：功能设计 `8.0`、架构设计 `8.0`、实际可用性 `7.2`、内部 MVP 综合 `7.7`、正式对外长期使用 `6.2`；Agent `7.8`、前端信息架构 `7.8`、工程质量 `7.7`。

## 2026-07-26 Hermes 执行拆分与浏览器门禁同步

- 新增 `app/services/agent_hermes_execution.py`，集中负责 DeepSeek/Hermes 模型路由、Token 与迭代预算、一次性进程调用、受控流式读取、超时终止和用量统计。
- `AgentService` 仍持有金融输出清理、数字校验、语义冲突和必答证据边界，并通过 `GuardedStreamCallbacks` 显式注入执行层；保存资料、用户确认和金融证据规则没有下沉到模型运行时。
- `app/services/agent.py` 从 10,063 行降至 9,739 行。新增 2 项委托边界测试，既验证一次性执行委托，也验证流式运行必须收到金融校验回调。
- 浏览器 Runner 不再等待旧版默认可见的李总策略。它会真实点击“通用筛选 → 李总策略 → 通用筛选”，验证两个大区互斥、`section=li_zong` URL 状态和历史详情默认折叠。
- 个股概览门禁改为等待 `.stock-workspace-card.primary` 实际渲染后再测 390px 卡片宽度，避免容器先可见、异步内容尚未生成时误报 0px。
- 修复前完整 Runner 为 `9 passed / 3 failed`；其中两项为旧选股断言，一项为异步竞态。服务重启并加载拆分后代码后，桌面与 390px 共 `12 passed / 0 failed`，最终报告位于 `/private/tmp/qingshu-browser-smoke-20260726-final/result.json`。
- 隔离临时工作区真实调用新执行模块：`deepseek-v4-pro` 返回“本轮模型运行测试成功。”，首 Token `3.444s`、首个受控可见片段 `3.488s`、单次可见更新，`guarded_cumulative_stream_v3` 生效；测试没有写入正式用户会话。
- 当前完整回归为 `681 passed`；Ruff、Python `compileall`、内联 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

当前保守评分：功能设计 `8.0`、架构设计 `7.9`、实际可用性 `7.2`、内部 MVP 综合 `7.7`、正式对外长期使用 `6.2`；Agent `7.8`、前端信息架构 `7.8`、工程质量 `7.5`。评分改按普通用户视角，不再把测试通过或受控环境可运行直接折算成高可用性。

## 2026-07-26 Agent 合同拆分、API 边界与透明选股页签

- `app/main.py` 中 25 组 FastAPI/Pydantic 输入模型迁移到 `app/api_models.py`，原导入路径继续兼容；李总策略内部记录到公共 API 的转换迁移到 `app/services/li_zong_presentation.py`。`main.py` 从约 7,015 行降至 6,679 行。
- `AgentService.run()` 中约 415 行意图专属回答合同迁移到纯函数模块 `app/services/agent_prompt_contracts.py`。自选股即时综合、用户确认式计划、个股涨跌同日事件、普通选股、李总策略和大盘分题合同不再与 Hermes 执行、流式输出和结果落库混在同一方法中；`agent.py` 从约 10,467 行降至 10,063 行。
- 新增 5 项回答合同单元测试，并复用真实 API 集成测试确认个股涨跌、操作计划和自选股摘要生成的 Prompt 内容没有退化。
- 透明选股由“同页同时加载通用筛选与李总策略”改为两个互斥页签。进入通用筛选时不再自动执行全市场计算，避免页面长期停在“筛选中”；李总策略只在用户切换后加载。
- 页签状态写入 URL：通用筛选为 `/research?mode=screening`，李总策略为 `/research?mode=screening&section=li_zong`。刷新后恢复原页签；通用筛选与李总策略不会同时出现在长页中。
- 真实浏览器验收：桌面默认页只显示可操作的通用筛选表单；切换李总策略后显示 5,531 只股票范围、当前 0 候选、23 条真实历史信号及规则漏斗；切回通用筛选后李总策略完全隐藏。390×844 下根宽、页面宽和视口宽均为 390，无横向溢出。
- Web 与独立 Worker 已按原启动方式重新加载当前代码。`/health=ok`、Hermes 启用、领域库 PostgreSQL Schema v2、运维与持久化队列 PostgreSQL Schema v4、独立 Worker active；54 项数据健康为 53 healthy、1 attention、0 critical。
- 完整回归收集并通过 `679` 项；Ruff、Python `compileall`、内联 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

该工作包当时评分：功能设计 `8.0`、架构设计 `8.2`、实际可用性 `8.2`、内部 MVP 综合 `8.1`、正式对外长期使用 `6.4`；现已被上方更保守的用户视角评分取代。

## 2026-07-25 历史研究按具体股票筛选

- 会话列表新增 `research_targets`，来源只包括最近 Agent 消息保存的明确 `symbol/research_targets`，以及与会话唯一绑定的股票研究空间。字段不依赖标题猜测、不调用模型、不改写历史消息。
- AI研究继续复用原“研究类型”下拉；有可靠证券对象时增加“具体股票”分组，不新增第二排控件。当前真实选项为中兴通讯 4 条，贵州茅台、广合科技、杰瑞股份和株冶集团各 1 条。
- 证券元数据和题面语义必须同时成立。一条标题为“A股下跌广度和成交额”、但旧 Agent 曾错误写入中兴 symbol 的会话不会混入中兴通讯结果；缺少明确 symbol 的“它、该股”旧会话也不会被前端猜测归类。
- 桌面真实筛选中兴通讯显示 `4 / 25` 条，最新中兴研究可见，污染的大盘会话被排除。筛选贵州茅台显示唯一历史，点击进入 `/research/8ebd8fbc-8c34-4a05-bd59-abb4069edefb`，历史回答、证据和筛选状态均恢复。
- 390×844 下股票选项复用 96px 类型选择器；贵州茅台标签、过滤后的历史下拉和输入框完整可见。临时视口覆盖验收后已重置，页面回到 `/research/new`。
- Web 与独立 Worker 已重启到当前代码；重启前持久化队列无 running 项，重启后积压已清零。当前 `/health=ok`，Hermes启用，Worker active，数据健康 `53/54 healthy`、`1 attention`、`0 critical`。
- 完整回归为 `663 passed`；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。

当前评分：功能设计 `8.0`、架构设计 `8.1`、实际可用性 `8.1`、内部 MVP 综合 `8.0`、正式对外长期使用 `6.4`；Agent `7.8`、前端信息架构 `8.0`。近义主题聚类、批量整理、同日直接事件证据和正式生产门禁仍未完成。

## 2026-07-25 继续最近研究与历史 URL 快速恢复

- AI研究新对话页新增“继续上次研究”入口，读取当前用户最新一条非空、非评估会话，显示标题、研究类型、消息数和更新时间。点击只调用既有 `openConversation()` 打开原 conversation ID，不触发 Hermes、不复制旧回答，也不创建或合并历史。
- 真实用户当前 25 条历史会话中，入口显示“分析中兴通讯当前的价格、行业和公告证据。”，8 条消息，更新于 07/25 13:29。桌面点击后进入原 URL `/research/bae4238b-1dbd-4548-8bb2-a68acccb6be9`，历史数量仍为 25。
- 390×844 下入口、标题、元信息和“继续对话”按钮完整可见；真实点击进入同一原会话，输入框与历史回答可继续使用。临时视口覆盖已在验收后重置。
- 冷启动原先先等待自选股、今日观察、市场面板、行业、实时行情、文章、资料库和个股空间全部完成，历史 URL 曾约 8—10 秒只显示“正在读取历史对话”。现在 Agent 路由在用户与健康状态就绪后优先并行读取会话和股票研究绑定、立即恢复原对话，其余页面数据异步补齐。
- 同一真实浏览器刷新复测约 `167ms` 出现历史证据快照、最终纠正回答和 25 条会话列表；刷新恢复仍保留中兴通讯标的、K 线和原 conversation ID。
- 新增两个前端合同测试，完整回归为 `662 passed`；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。

当前评分：功能设计 `8.0`、架构设计 `8.1`、实际可用性 `8.0`、内部 MVP 综合 `8.0`、正式对外长期使用 `6.4`；Agent `7.8`、前端信息架构 `7.9`。近义主题聚类、具体股票筛选、批量整理和正式生产门禁仍未完成。

## 2026-07-25 资讯重复刷新、来源轮询审计与真实数据健康修复

- 初始运行态为 `50/54 healthy`、`4 attention`、`0 critical`；三个提示来自中兴通讯、中际旭创和英伟达事件信息“较旧”。后台记录同时显示 A 股信息任务和美股财务任务最近均已完成，因此先判定为时间语义或落库问题，而不是 Worker 未运行。
- 数据健康旧逻辑读取“按内容发布时间排序的第一条新闻”的 `fetched_at`，把内容新旧与管道最近成功轮询混为一谈。现改为优先读取 PostgreSQL 后台任务中每只股票、每个来源的持久化轮询结果；内容发布时间、内容入库时间和来源轮询时间分开返回。
- `ChinaInformationService` 持久化公告、公司新闻和股吧三个来源的 `ok/failed`、条数和轮询时间；`USEquityFundamentalsService` 记录 SEC 监管文件轮询；美股小时任务新增 Nasdaq 公司新闻刷新并将两个来源合并审计。成功轮询但返回零条新内容不再视为故障，真实来源失败仍为 attention。
- 首次真实重跑进一步发现 PostgreSQL `news_items` 的旧兼容层强制使用 `ON CONFLICT(id)`，无法处理普通新闻随机 ID 与 `UNIQUE(symbol, source, url)` 的重复抓取，导致所有来源出现 `UniqueViolation`。现改为先按稳定 ID 或 `symbol + url` 更新，未命中再插入，并在并发冲突后重试更新；来源名称变化会更新原记录而不是制造重复项。
- 修复后真实生产库重跑：A 股 6 只股票全部完成，公告/公司新闻/股吧三个来源均为 `ok`；NVDA 的 SEC 监管文件返回 12 条、Nasdaq 公司新闻返回 20 条，两个来源均为 `ok`。持久化队列无 ready/delayed/retrying/expired 积压。
- 最新 `/system/data-health` 为 `53/54 healthy`、`1 attention`、`0 critical`。中兴通讯、中际旭创和英伟达事件信息均为 healthy，`freshness_basis=successful_source_poll`；唯一 attention 是 A 股成交额历史排除 2026-07-23 盘前不完整快照并继续积累完整收盘历史，不是实时行情或资讯故障。
- `/health=ok`、`/ready=ok`；Hermes 启用，领域库 PostgreSQL Schema v2，运维与持久化队列 PostgreSQL Schema v4，独立 Worker 活跃。
- 全量 `660 tests collected` 并全部通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。新增回归覆盖 PostgreSQL 同一新闻重复抓取、成功轮询但无新内容、部分来源失败，以及 NVDA 的 SEC/公司新闻联合刷新。
- 仓库浏览器门禁在 1280×720 与 390×844 下共 `12 passed / 0 failed`；控制台错误、页面异常、非预期失败响应和横向溢出均为 0。报告位于 `tmp/browser-smoke-20260725-data-health/result.json`。

当前评分：功能设计 `7.9`、架构设计 `8.1`、实际可用性 `7.9`、内部 MVP 综合 `8.0`、正式对外长期使用 `6.4`；金融数据体系 `8.0`、Agent `7.8`、前端信息架构 `7.8`。正式发布门禁没有因内部数据修复而自动完成。

## 2026-07-25 历史会话搜索、研究类型筛选与移动端长期使用

- 会话列表 API 新增 `last_intent`，读取每个对话最近一条非空 Agent 意图；不新增表、不改写历史消息，也不触发模型调用。前端继续一次读取最多 100 条当前用户会话，在本地完成搜索和筛选。
- 桌面最近对话新增一行紧凑搜索和研究类型筛选；少于 8 条会话时自动隐藏。搜索覆盖标题与最近一条消息预览，类型包括个股、大盘、选股、关注和其他。
- 分类不是盲从历史 `last_intent`：明确题面语义优先，最近意图只作辅助。修复前被误记为 `market_brief` 的“它相对通信设备行业更强还是更弱”仍归入个股；被误记为 `stock_research` 的“A股下跌广度和成交额”归入大盘；旧 `stock_screen` 不会把“个股研究｜杰瑞股份”等记录继续显示成选股。
- “候选事件”不等于“候选股票”，“自选股”不等于“选股”。真实列表中，中兴通讯涨跌问题归入个股、美股收盘问题归入大盘、自选股每日摘要归入关注。
- 真实用户当前 25 条有效会话：个股 11、大盘 11、关注 2、其他 1；当前没有真正的选股历史会话。输入“贵州茅台”后只显示 `1 / 25` 条，并保留原日期分组和归档能力。
- 390×844 下搜索框、类型选择和历史下拉分别约 223、78、336 像素宽，页面根宽与视口均为 390 像素。无匹配时移动端下拉保留“当前对话”，并明确显示“没有匹配的历史对话”。
- 完整回归仍为 `657 passed`；Ruff、Python `compileall`、内联 JavaScript 解析、`uv lock --check` 和 `git diff --check` 通过。仓库浏览器门禁桌面与 390px 共 `12 passed / 0 failed`，报告位于 `tmp/browser-smoke-20260725-conversation-filter/result.json`；真实页面控制台 warning/error 为 0。

当前评分：功能设计 `7.9`、架构设计 `8.0`、实际可用性 `7.8`、内部 MVP 综合 `7.9`、正式对外长期使用 `6.4`；Agent 分项维持 `7.8`，前端信息架构由 `7.6` 更新为 `7.8`。近义主题聚类、具体股票筛选和“继续上次研究”仍未完成。

## 2026-07-25 Agent 真实证据进度、个股上下文与刷新发送恢复

- Agent 等待区不再只有泛化“研究中”文案，也不提前展示未经完整校验的模型正文。`evidence_ready` 事件现在携带确定性的 `evidence_progress`；市场问题展示同日代表性指数、全市场涨跌家数、成交额、板块和资讯数量，个股问题展示研究重点、最近完整日线日期、计划模块覆盖和可追溯来源数量。
- 真实市场问题约 `58ms` 显示路由状态，约 `307ms` 显示证据卡，约 `3.923s` 进入草稿校验，约 `10.078s` 原位显示最终回答。证据卡包含 6 项代表性指数、沪深京 5,530 只股票、上涨 555/下跌 4,939、成交额 19,442.25 亿元、10 个板块与 10 条资讯；后续状态只更新卡片顶部，不会让整张卡变淡或被另一段内容替换。
- 真实个股问题约 `134ms` 显示财务估值核验、`963ms` 显示历史走查、`2.053s` 显示“本轮已读取的个股证据·中兴通讯”；日线日期按用户口径显示为 `2026-07-24`，不暴露原始 ISO 时间。
- 修复个股追问被“行业”关键词误切成大盘研究。问题“它相对通信设备行业更强还是更弱”现在继承中兴通讯，回答中兴通讯 `-2.56%`、通信设备行业 `-3.54%`、相对少跌约 `0.98` 个百分点，以及行业 50 只成分股中 3 涨 47 跌；即使上一轮是市场问题，也会向前恢复最近股票标的。
- 单股回答现在写入 `research_targets`，页面刷新后可可靠恢复 K 线和研究对象。
- 修复历史会话 URL 刷新后的立即发送竞态。恢复尚未完成时按 Enter，按钮约 `80ms` 显示“正在恢复对话”，`sendChat()` 等待工作区恢复后自动发送；约 `1.339s` 问题进入原历史会话、约 `2.171s` 显示中兴通讯证据卡，URL 始终保持原 conversation ID，用户无需重新输入。
- 当前完整回归为 `657 tests collected` 并全部通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。
- 仓库浏览器门禁在 1280×720 与 390×844 下共 `12 passed / 0 failed`；控制台错误、页面异常、非预期失败响应和横向溢出均为 0。报告位于 `tmp/browser-smoke-20260725-agent-evidence-progress/result.json`。

当前评分：功能设计 `7.9`、架构设计 `8.0`、实际可用性 `7.7`、内部 MVP 综合 `7.9`、正式对外长期使用 `6.4`；Agent 分项 `7.8`、前端信息架构 `7.6`。当前样本仍不足以宣称正式 P95，正式认证、数据授权、对象存储、外部告警、灾备和容量门禁也未因此完成。

## 2026-07-25 历史对话日期分组与同题折叠

- 真实用户当前共有 19 条非空历史对话：今天 11 条、昨天 8 条。页面现在按“今天 / 昨天 / 最近 7 天 / 更早”分组并显示数量，不再把长期会话逐条平铺。
- 同一日期内，只有标题规范化后完全相同的对话才会折叠；最新一条直接显示，旧记录通过“n 个同题旧对话”按需展开。当前真实数据形成四组折叠：成交额 1 条、A股普跌 2 条、中兴通讯 1 条、美股 1 条旧对话。
- 真实浏览器交互确认：“A股今天为什么普跌”展开后可见 3 条记录、`aria-expanded=true`；再次点击后只保留最新 1 条、`aria-expanded=false`。打开其中一条旧对话后 URL 切换到该会话，分组自动保持展开且活动对话继续可见。
- 移动端原有历史下拉保留完整会话列表；日期分组与折叠只降低桌面侧栏噪声，不删除、合并或改写数据库中的历史消息。
- 本轮没有重排今日观察。1280×720 实际首屏仍以全球市场实时行情和 A 股核心指数为主，“我的研究待办与重要变化”位于更下方，继续改动缺少收益证据。
- 当前完整回归为 `654 tests collected` 并全部通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。
- 仓库浏览器门禁在 1280×720 与 390×844 下共 `12 passed / 0 failed`；控制台错误、页面异常、非预期失败响应和横向溢出均为 0。运行态 `/health=ok`、Hermes 启用、领域库 PostgreSQL Schema v2、运维与持久化队列 Schema v4、独立 Worker 活跃。

当前边界：完全同题折叠已经减少重复噪声，但近义问题仍应分别保留；后续如做语义聚类，必须让用户可以展开全部原始会话，不能自动删除或拼接研究记录。当前评分维持功能设计 `7.9`、架构设计 `8.0`、实际可用性 `7.5`、内部 MVP 综合 `7.8`、正式对外长期使用 `6.4`。

## 2026-07-25 标准大盘 Agent 时延与错误替换修复

- 标准 `market_brief / economy` 在确定性行情、广度和资讯证据已准备完成时改用 `reasoning_effort=none`，模型仍为 `deepseek-v4-pro`，最大输出 1000 tokens，最大迭代 4。目标是避免额外推理消耗输出预算并触发第二次续写，不更换模型或回放预存答案。
- 修复前真实 Run `326a6f44-7480-471a-9df2-bb3aac3fb663` 首 Token `27.573s`、页面首个安全可见内容 `28.700s`、请求总耗时 `35.513s`、API 调用 2 次。
- 优化后完成三次真实浏览器与数据库联合验收：
  - `666f8d72-5ec0-4e03-b287-77f244689b43`：总耗时 `14.233s`；
  - `124d5638-64fe-46ae-98d5-f7b40d6ad9c4`：总耗时 `12.113s`；
  - `968c0c74-9e39-42ba-8a3c-52e43d37875a`：总耗时 `10.158s`。
- 三次均为 `deepseek-v4-pro`、`reasoning_effort=none`、`api_calls=1`、`status=completed`、输出校验通过。首 Token 范围 `2.222—3.218s`，中位数 `2.714s`；真实页面最终答案约在 `10.246—14.540s` 出现，中位数约 `12.2s`。
- 浏览器以约 100ms 间隔记录消息区变化。三次均只从空的研究进度状态切换到最终回答，没有出现长文先展示、随后被固定摘要替换。
- 两次真实失败样本证明替换根因是校验误判，而不是模型没有回答：
  - “不能直接确认新下跌趋势已开启”不再被反向识别为确认下跌趋势；
  - “不能直接证明资金净流入”不再被反向识别为声称资金净流入；
  - 全市场广度问题强制上涨数、下跌数和固定分类；只有用户明确询问平盘家数时才强制平盘数。
- 当前完整回归为 `654 tests collected` 并全部通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 通过。
- 仓库浏览器门禁在 1280×720 与 390×844 下共 `12 passed / 0 failed`；产品页控制台无 warning/error。运行态 `/health=ok`、Hermes 启用、领域库 PostgreSQL Schema v2、运维与持久化队列 Schema v4、独立 Worker 活跃；54 项数据健康为 50 healthy、4 attention、0 critical。

当前边界：三次样本足以证明本轮修改没有依赖一次最好结果，但仍不足以宣称正式 P95。模型私有流在约 3—4 秒已有安全片段，前端为避免未经完整校验的正文被替换，仍在约 10—15 秒后一次性展示最终答案；下一步应优先展示确定性事实进度卡，而不是提前展示可能被改写的模型正文。

## 2026-07-25 项目重评分与透明选股信息层级优化

- 新增 `reports/清数智算_项目详细评分与持续优化记录_20260725.md`。当前评分为：功能设计 `7.9`、架构设计 `8.0`、实际可用性 `7.3`、内部 MVP 综合 `7.7`、正式对外长期使用 `6.4`。评分依据为当前运行服务、数据库、队列、真实浏览器、最新 Agent Run 和全量测试，不沿用旧截图推断。
- 真实浏览器新建标准大盘问题“ A股今天为什么普跌……”。Run `326a6f44-7480-471a-9df2-bb3aac3fb663` 使用 `deepseek-v4-pro`，状态 completed，输出守卫通过；正确给出主要指数、全市场涨跌家数、逆势行业、候选资讯、反方证据和下一步核验。首个安全可见内容 `28.700s`，请求总耗时 `35.513s`，说明当前 Agent 主要短板仍是稳定时延。
- 透明选股页改为通用截面筛选优先，李总策略运行时移动到通用筛选之后；顶部保留“通用截面筛选 / 李总策略”快速入口，不再因异步策略结果插入导致常用表单和结果突然下移。
- 李总策略没有当前候选时，默认只显示近期回放覆盖与逐规则漏斗；23条历史详情按用户点击展开，展开后仍使用独立滚动区。历史真实样本、5/10/20日表现、证据详情和 Agent 复盘入口均保留。
- AI研究历史列表过滤零消息空会话；归档按钮由悬停“×”改为明确的“归档”，带对话标题 `aria-label` 和二次确认。真实浏览器验证空会话为0、旧“×”按钮为0、14条现有历史均有明确归档入口。
- 仓库浏览器门禁增加透明选股渐进披露断言。桌面与390px结果均证明通用筛选位于李总策略之前、历史切换按钮默认 `aria-expanded=false`、历史详情不可见；完整门禁仍为 `12 passed / 0 failed`，无控制台错误、页面异常、非预期失败响应或横向溢出。
- 全量 `653` 项测试通过；Ruff、Python `compileall`、内联 JavaScript `node --check`、`uv lock --check` 与 `git diff --check` 通过。

当前下一优先级：标准 Agent 首屏时延、多次 P50/P95 基线、历史对话去重/分组，以及今日观察的信息密度。不能因为最终回答成功就忽略接近半分钟的首屏等待。

## 2026-07-25 状态语义、A股中文名称与首次导航集中修复

- 关注页不再把已有服务器研究快照误报为“0/4份完整快照”。真实浏览器显示“4/4只股票已有研究快照”，四张卡片均为“今日已更新”；`preview` 仍保留后台审计状态，但不再在用户页误写成未完成。
- 公共 API 返回前统一转换历史技术文案。普通页面不再出现“输出守卫”“完整模型与输出校验”“Hermes输出失败”“核心证据包缺失”等内部术语；Agent 降级卡改为“本轮仅展示已核验事实”。原始数据库记录未被破坏。
- 新增 `app/services/security_master.py`，A股用户可见名称优先读取稳定 `a_share_universe` 主数据。报告、股票空间和变化事件的公共返回层都会纠正历史英文回退名；变化事件会递归转换标题、事实摘要与嵌套证据，但不修改数据库原始审计记录。已验证 `600961.SS → 株冶集团`、`002353.SZ → 杰瑞股份`、`001389.SZ → 广合科技`；刷新后的关注页全文不再出现 Delton Technology、Yantai Jereh 或 Zhuzhou Smelter。
- 前端新增 `workspaceNavigationVersion`。刷新后第一次点击复盘中心，立即和5秒后都保持 `/reviews?tab=trades`；页面标题、激活导航和显示区域一致，启动恢复不会再覆盖用户主动导航。
- 旧市场文章中的缺失指标占位符会在公共返回层转换为跨市场时点说明，新文章生成逻辑不再产生“指数平均单日变化 —%，代表性指数上涨比例 —”等空模板。
- 专项回归覆盖中文证券主数据、历史英文报告公共层纠正、用户文案去内部术语、旧文章占位转换和导航版本控制。最终全量实际收集并通过 `653` 项。
- 最终工程门禁：`ruff check app tests scripts`、`python -m compileall -q app tests scripts`、内联 JavaScript `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。
- 服务重启后，仓库内 Chrome 冒烟门禁覆盖桌面和390px下的今日观察、透明选股、个股概览、个股历史、复盘中心与AI研究，结果为 `12 passed / 0 failed`；无横向溢出、控制台错误、页面异常或非预期失败响应。真实会话刷新后仍显示“4/4只股票已有研究快照”和四张“今日已更新”卡片；首次点击复盘中心，5秒后仍保持 `/reviews?tab=trades`。
- `/health=ok`、`/ready=ok`；领域库 PostgreSQL Schema v2，运维与持久化队列 Schema v4，独立 Worker 活跃。就绪接口仅保留历史失败任务记录警告，当前无 ready/delayed/retrying 积压。
- 全景报告已更新为当前修复和653项基线：`output/report/清数智算_产品实现全景报告_20260725.docx` 与同名 PDF。最新 DOCX 渲染为25页并逐页检查，无截断、重叠、缺字或破损表格；a11y 审计 high=0、medium=0、low=0。

当前边界：本轮关闭的是直接影响信任和操作连续性的三项体验问题，不等同于生产发布完成。同日直接事件覆盖、模型 P95 波动、更多跨源数据异常样本、正式认证、对象存储、外部告警和灾备门禁仍需继续推进。

## 2026-07-25 涨跌原因、Agent 时延与成交额历史异常集中修复

- Hermes 标准文字对话默认 `max_iterations=4`，深入研究默认 `6`，均可由环境变量覆盖；未完整结束的 Hermes 文本和 `maximum tool-iteration` 等内部错误不再作为正常答案返回。
- 标准个股仍使用 `deepseek / deepseek-v4-pro`，默认 `reasoning_effort=none`；个股证据 Prompt 删除重复资料、非本题历史校准和多余来源字段，但完整 Run 证据仍落库。行情原因问题固定要求“价格事实、候选事件、反方证据、下一步核验”四部分。
- 真实浏览器按 Enter 发送“中兴通讯为什么大跌？请区分价格事实、候选事件、反方证据和下一步核验。”，新 Run `835688a0-3f59-41a8-b8f7-661c5f693118` 为 `stock_research / economy / completed`；Provider `deepseek`、Model `deepseek-v4-pro`、`max_iterations=4`、`reasoning_effort=none`、`api_calls=1`、输出守卫通过。
- 该 Run Prompt 为 `38,076` 字符、输入 `15,165` tokens；首 Token `2.937s`、首个安全可见内容 `3.638s`、请求总耗时 `14.792s`。页面回答包含35.00元、-2.56%、A股与通信设备行业同步普跌、候选公告、反方证据和三项核验任务；等待后内容保持稳定，没有再次跳成另一段回答。
- 标准大盘真实 Run `26017438-fe5a-4c0e-97ac-9dedffbb1162` 使用相同模型、`reasoning_effort=low`、单次模型调用，首个安全可见内容 `10.964s`、总耗时 `20.124s`。
- A股成交额历史比较现在只接受完整收盘且市场范围、币种、单位、覆盖一致的会话。`2026-07-23 09:29:31`、总额约202.78亿元的不完整快照已被排除；`/system/data-health` 返回 `history_status=building_history`、`change_vs_previous_pct=null`，不再出现 `+9487.8%`。
- 当前运行态：`/health=ok`、`/ready=ok`、Hermes启用；领域库 PostgreSQL Schema v2、运维库 Schema v4；独立 Worker 活跃，队列无 ready/delayed/retrying 积压。数据健康 `52/54 healthy`、`2 attention`，分别为成交额完整历史仍在积累和英伟达事件信息较旧。
- 最终全量收集并通过 `651` 项；Ruff、`compileall`、内联 JavaScript `node --check`、`uv lock --check` 与 `git diff --check` 通过。

当前边界：本轮真实时延是单次运行证据，不是固定 SLA；深入研究和 Provider 负载仍会波动。同日公司或宏观直接事件缺失时只能保持候选解释，不能把大盘/行业同步性写成唯一因果。

## 2026-07-24 选股数据合同、真实 Agent 路由与双视口发布门禁

- 通用选股新增 `stock_screen_data_v1` 数据合同：数据版本由本轮确定性股票池、行情、估值和财务内容生成哈希，不再使用模糊固定版本；同时分开返回股票池、总市值、估值、20 日收益和财务候选池覆盖率。
- 每只候选保存独立行情日、财务报告期、财报公告日与来源合同；字段缺失不再只返回字段名，而是保存用户可读的 `missing_reasons`。Agent 压缩证据继续保留这些时间和覆盖口径，但移除内部 `ts_code`。
- 自然语言路由补齐“筛选A股、经营改善模板/候选、相对行业增强、估值约束、回撤后待复核”。精确真实问题
  “用经营改善模板筛选A股，并明确说明股票池覆盖、数据版本、行情日、财务报告期和关键数据缺口”
  已验证进入 `stock_screen`，不再误路由为大盘。
- 首轮真实 Hermes/DeepSeek 验收发现模型把压缩后的 10 个项目当成总候选数，而确定性结果实际为 12；现候选总数显式读取完整结果，守卫拒绝与确定性数量不一致的回答，默认 12 只可全部进入模型证据与确定性预览。
- 第二轮真实验收发现“东方财富（300059.SZ）”因名称与数据平台同名，被运维信息清理规则误删整行。现只有“数据源/降级/接口”等运维语境继续拦截；名称和证券代码与本轮候选证据匹配时保留上市公司实体。
- 最终真实 Run `4847439a-6f21-40e0-a4b2-3d1178a73f7e` 状态为 `completed`：12/12 候选全部可见，回答引用数据版本 `stock-screen-v1-1b1f95521300f2a1`、行情日 `2026-07-23`、财务报告期 `2026-03-31`、估值覆盖约 72% 和财务候选池口径；未返回目标价、仓位或收益概率。
- 股票空间核心接口失败时不再伪装为空状态；页面明确列出未返回模块，并提供原位“重新加载股票空间”。重试不会清空判断、任务、持仓或历史对话。
- 新增仓库内 `scripts/browser_smoke.py` 与合同测试。Runner 只读检查 1280×720 和 390×844 下的今日观察、透明选股、个股概览、个股历史、复盘中心和 AI 研究，不点击或提交研究、判断、持仓和复盘表单。
- 390px 实机验收主动发现个股概览网格遗漏 `map` 区域，导致判断、变化、任务和证据地图被挤成隐式第二列；已改为完整单列，并增加核心卡片最小宽度断言。
- 最终浏览器结果为 `12 passed / 0 failed`，报告位于 `/private/tmp/qingshu-browser-smoke-20260724-final/result.json`；页面无页面级横向溢出、控制台错误、页面异常或非预期本地失败响应。首次匿名会话的 `/session 401` 和 `/favicon.ico 404` 属于明确忽略的启动噪声。
- 最终质量门禁：`589 passed in 41.43s`；可移植性专项 `6 passed`；Ruff、`compileall`、内联 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。

当前边界：本轮完成的是选股数据合同、Agent 路由/数量/同名实体正确性、加载恢复和核心双视口发布门禁。它仍不是生产发布完成；正式认证、生产数据库、持久化任务队列、监控、备份恢复、staging 与回滚仍需后续完成。

## 2026-07-24 完整 R1 交易复盘链与前端可靠性封口

- 新增 `tests/test_r1_e2e.py`，用确定性股票池、操作前证据、三个后续完整交易日和
  `completed` Agent Stub 串起同一用户的完整集成链：搜索股票 → 加入研究空间 → 确认
  正式判断 → 建立股票绑定对话 → 即时研究 → 确认观察任务候选 → 期初持仓 → 减仓
  → 三个后续交易日 → `ready` 复盘 → `review_draft` 候选 → 用户编辑确认 → 归档。
- 集成测试通过重新 GET 会话、判断、任务、持仓、候选、复盘和归档中心模拟刷新恢复，
  并证明另一用户不能读取股票空间、会话、任务、候选和复盘。
- 期初持仓、操作、非交易调整、操作修订和新建操作计划的幂等键改为“表单创建时生成一次”；
  同一表单提交失败后的重试复用原 Key，避免服务端已经写入但响应丢失后再点一次形成重复流水。
- 期初持仓默认日期改为浏览器本地日期，不再用 UTC 日期截断，避免中国时区凌晨落到错误自然日。
- 操作表单明确说明：只有减仓或卖出创建交易复盘，且需要三个后续完整交易日；买入和加仓
  不创建复盘。该说明只解释产品状态机，不构成交易建议。
- “让 Agent 帮我处理”现在预填完整的即时核验要求：使用当前实时数据、资料库和证据链，
  区分事实、推断和信息缺口，提供反方证据、时间和失效条件，并生成“待用户确认”的观察任务
  草稿；点击入口本身不会自动发送问题或创建正式任务。
- 股票历史页和跨股票复盘中心在展示“生成 AI 草稿”按钮前，先读取当前用户所有
  `pending_confirmation` 写回。存在同一 `review_id` 的 `review_draft` 时恢复原确认卡，
  不再因页面刷新诱导用户重复运行 Hermes；候选状态核对失败时也不显示第二个生成按钮。
- 真实浏览器使用 `localhost` 独立匿名验收会话，不复用用户当前 `127.0.0.1` 会话：
  中兴通讯录入 100 股期初持仓、记录 10 股减仓，页面派生为 90 股和两个持仓快照；
  操作日期 `2026-07-10` 后三个完整交易日数据已齐，复盘自动进入“可生成”。
- 点击“立即生成草稿”真实调用 Hermes/DeepSeek，约 20 秒后形成新的逻辑复盘：确定性价格
  结果为操作价 35、第三个后续交易日 `2026-07-15` 复权收盘 `39.450001`、价格变化
  `12.71%`；模型明确指出上涨不自动证明逻辑正确，并把“系统验收操作”与真实投资操作区分。
  这段回答由本轮 Run 即时生成，不是保存报告或固定文案。
- 在确认候选前刷新 `/stocks/000063.SZ?tab=history`，原 `review_draft` 确认卡完整恢复，
  “立即生成草稿”按钮不可见；随后用户确认写入、编辑逻辑结果和改进记录、保存并确认、
  归档，再次刷新后仍显示“已归档”、用户版本 2 和确定性价格结果。
- 浏览器控制台日志为空；归档页面已按真实分辨率检查，价格结果、逻辑结果、计划偏离和改进
  信息分层清晰。
- 当前完整门禁更新为 `578 tests collected` 并全部通过；Ruff、Python 编译、内联 JavaScript
  `node --check`、`uv lock --check` 和 `git diff --check` 通过。

当前边界：本阶段建立了可重复的后端全链集成测试，并完成桌面真实浏览器的“期初持仓 →
真实减仓 → 后续完整交易日 → 即时复盘 → 刷新恢复 → 用户确认 → 归档”。此前已分别完成
搜索/选股线索、判断、即时 Agent 和任务确认的真实浏览器链。仓库仍没有 Playwright/Cypress
自动化浏览器 Runner，390px 窄屏整链、正式认证/生产数据库/持久化队列和发布恢复门禁仍未完成；
因此不能把“产品 R1 研究与交易闭环已可用”扩大为“完整生产 R1 已完成”。

## 2026-07-24 李总观察线索到用户任务的研究型 R1 主链

- 真实浏览器从李总 `8/9 接近满足` 观察池选择杰瑞股份 `002353.SZ`，保存线索进入唯一股票研究空间；入口保留数据日 `2026-07-23`、8 条通过理由、唯一失败项“近十日无5%阴线”和“非候选”边界，没有自动变成正式判断。
- 用户在股票空间确认正式判断版本 1：只作为 8/9 研究观察，并同时记录现金流与净利润方向相反、重大合同兑现仍需核验。刷新后判断版本继续存在。
- 李总单股筛选现在也生成结构化 Claim、Citation 与可确认写回，不再只有模型自然语言。真实 Hermes/DeepSeek 仍按本轮规则证据即时生成；历史报告和数据库快照只提供证据。
- 用户明确引用一条任务并要求“不要自行扩写”时，系统只生成这条观察任务候选，不再把模型建议的其他下一步静默追加。确认后任务进入 `ObservationTaskService`，刷新恢复、版本和用户隔离保持不变。
- 候选规则尚未全部通过时，即使某条盘后触发形态的原始条件匹配，Prompt 与最终输出校正也只允许写“形态条件匹配，但不形成触发事件”；正式触发只读取候选成立后的 `triggered_rule_ids`。
- 股票“历史与复盘”页新增研究轨迹：用户判断版本、已保存观察任务、重要变化、绑定对话、报告和六维证据快照在同一页可追溯。没有真实操作时仍保持“尚无交易复盘”，不会伪造价格结果或交易结论。
- “今日观察”优先保留用户已保存任务，不再被通用系统建议挤出前五项。真实点击杰瑞股份任务回到 `/stocks/002353.SZ?tab=tasks`，URL、选中标签和刷新落点一致。
- 本轮真实路径已覆盖：观察池 → 保存线索 → 股票空间 → 用户判断 → 即时 Agent → 精确任务候选 → 用户确认 → 今日观察 → 任务页 → 历史研究轨迹。该阶段尚未发生真实操作，因此交易复盘分支未触发；诊大盘、诊个股与整轮失败恢复已在后续工作包完成。当前仍未封口的是包含真实操作、后续完整交易日与用户确认复盘的可重复 R1 E2E。

## 2026-07-24 李总严格观察池与候选—触发边界

- 新增与正式候选严格隔离的两层观察池：`8/9 接近满足`和`6–7/9 研究观察`。只有已经完整核验全部 9 条候选规则、且不存在数据缺口的当前快照才可进入观察池；`data_incomplete`、未处理股票和历史事件均不会混入。
- 观察池不改变 `candidate_qualified`、`triggered` 或任何参数阈值。正式候选仍要求同一数据日 9 条候选规则全部通过；3 条盘后触发规则只决定候选形成后的重点关注与人工复核层级，不是进入候选的附加条件。
- 新增 `GET /v1/stock-strategies/li-zong/observation-pool`，可按 `near_8_of_9` 或 `watch_6_7_of_9` 查询。返回逐股通过数、明确未通过规则、完整规则证据和严格边界；正式候选接口继续只返回 `qualified/triggered`。
- 真实浏览器冻结快照（数据日 `2026-07-23`）：正式候选与触发均为 0；`8/9` 观察 3 只，分别为广合科技、杰瑞股份和江海股份，三者均明确未通过“近十日无5%阴线”；`6–7/9` 观察 137 只，页面只加载前 30 只并显示 `展示 30/137 只研究观察`。这些数量随下一交易日规则重算而变化，不是固定产品常量。
- 卡片明确显示“非候选”、通过数和失败规则，支持查看逐规则证据、加入研究观察、保存线索进入股票空间和“让 Agent 核验”。广合科技按钮真实进入 `/stocks/001389.SZ?tab=ai`，携带 8/9、失败规则和非候选边界。
- 真实 Hermes/DeepSeek 即时回答逐项引用了广合科技 8 条通过证据和两根 5% 阴线，且明确不是候选。浏览器验收主动发现模型曾错误声称“仍需盘后触发才能进入候选”；现已增加策略追问上下文继承、Prompt 硬约束和最终输出校正。修复后同页实时追问明确回答：候选资格只由 9 条候选规则决定，触发只作用于候选后的复核层级。
- 页面和后端都不会把观察池包装成概率、胜率、荐股或买卖建议；观察层只用于解释“差哪几条”和建立后续核验线索。

## 2026-07-24 李总历史按钮、即时引用与多轮上下文闭环

- 真实浏览器点击历史卡片“查看当时证据”，核对株冶集团 `600961.SS` 在 `2026-07-01` 的 12 条逐规则证据，以及信号后 5/10 日和未完成 20 日观察；页面没有把后见数据混入信号日规则。
- “在 Agent 中继续研究”和“让 Agent 复盘”会携带股票、信号日期、5/10/20 日对照、反方证据和样本边界问题，不再发送与历史事件脱节的通用提示词。
- 历史事件的 9 条候选通过理由可以完整进入股票长期研究空间；后端上下文不再因旧的 8 条上限返回 `422`。页面会绑定正确的 `600961.SS` 会话，不再被先前 `000063.SZ` 会话覆盖。
- 真实 Hermes/DeepSeek 即时回答逐项复盘 `2026-07-01` 信号：5 日个股 `-25.30%`、沪深300 `-4.10%`、超额 `-21.20%`；10 日个股 `-29.86%`、沪深300 `-3.47%`、超额 `-26.39%`；20 日尚未完整，只报告截至当时的 17 日观察，不补算。
- 本轮引用抽屉新增独立“历史回放”来源卡，明确显示股票、信号日期、个股/沪深300/超额收益和“李总策略点时历史回放与沪深300复权日线”，不再只显示当前截面选股证据。
- 服务重启并刷新 `/stocks/600961.SS?tab=ai` 后，同一长期研究会话、历史回答和引用均恢复。长追问即使不重复“李总”字样，只说“同一个历史事件/这次/再次”，仍继承策略和股票上下文，不再被“沪深300”误路由成大盘分析。
- 页面 API 错误现在会展开结构化校验细节，不再显示 `[object Object]`。本轮尚未做 Hermes 故障注入和原位重试按钮验收；这仍属于后续失败恢复工作，不影响已通过的正常即时生成主链。

## 2026-07-24 李总策略近期无前视历史回放与空候选解释

- 新增 `LiZongHistoryService`。每只股票默认回放最近 80 个可计算交易日；每个信号日只传入该日及以前日线，使用信号日历史 `daily_basic` 市值，年度 ROE 和股东只接受公告日不晚于信号日的数据，避免用当前截面回推历史。
- 连续满足阶段按状态转换去重：首次进入候选保存一次，候选后首次进入触发可再保存一次，连续候选或连续触发不重复形成事件。历史结果保存完整逐规则证据和数据版本。
- 沪深300作为统一基准。每个历史事件计算信号日收盘后 5/10/20 个交易日的个股、指数和超额收益，并保存区间最大上行、最大下行、对应日期和累计路径；未来交易日不足时保持待完成，不补造结果。
- 当前候选为 0 时，页面展示真实筛选漏斗 `5530 → 1216 → 226 → 202 → 20 → 15 → 5 → 0`，并展示历史事件、个股与沪深300累计收益双线、逐规则证据和“让 Agent 复盘”入口。历史快照只作为证据，最终回答仍由本轮 Hermes/DeepSeek 即时生成。
- 新增 `GET /v1/stock-strategies/li-zong/history` 与管理员 `POST /v1/stock-strategies/li-zong/history/runs`；后台策略线程在深度股票仍有任务时每轮推进最多 2 只历史股票，空闲时推进最多 5 只，不阻塞实时行情线程。
- 2026-07-24 01:56（Asia/Shanghai）运行态：`/health=ok`、Hermes 启用、数据健康 `54/54`、后台与李总 Worker 正常。全市场名单状态 `5530/5530`，可深度核验 `1169`，已深度处理 `1168`，近期历史回放完成 `234/1169`，发布 `23` 个去重历史事件；覆盖会随后台继续增长，本行是冻结快照而不是常量。
- 已观察到正负样本并存：例如广合科技 2026-04-08 触发后 20 日个股累计 `51.49%`、沪深300 `7.75%`；同一股票 2026-06-30 触发后 10 日个股 `-21.60%`、沪深300 `-3.67%`。这些结果证明页面不是只挑选上涨样本，但当前样本量和覆盖仍不足以评价策略有效性。
- 自动化：完整 `558` 项测试通过；Ruff、Python 编译、内联 JavaScript、lockfile、diff whitespace 和可移植性测试通过。

当前边界：历史覆盖仍由后台增量推进，当前只回放近期 80 个交易日，不是多年全市场回测；未纳入交易成本、涨跌停可成交性和实际成交价。真实本地网页已经完成漏斗、历史卡片、收益曲线、查看证据、正确股票会话、即时生成、可见引用、刷新恢复和多轮追问验收；故障注入后的原位重试仍待完成。

## 2026-07-24 核心功能、数据算法与 Agent 架构报告

- 新增 `scripts/build_core_architecture_report_20260724.py` 和视觉版构建器 `scripts/build_core_architecture_visual_report_20260724.py`。视觉版从当前数据库读取全市场、规则漏斗和历史覆盖指标，避免继续引用已过时的固定数字。
- 生成 `清数智算MVP核心功能数据算法与Agent架构报告-20260724.docx`，共10页，包含真实产品截图、核心功能、数据来源与授权边界、确定性金融算法、李总历史回放、Hermes/DeepSeek 即时生成链路、运行架构、部署与下一阶段。
- 使用标准文档渲染流程生成逐页 PNG 和 PDF，10页均已按原始分辨率检查；中文完整，截图清晰，无内容溢出、页面重叠或页脚冲突。最终报告冻结时历史覆盖为 `229/1169`、23个事件，后台实时数字随后继续增长。
- 新增 `HANDOVER_QINGSHU_20260724.md`，供后续 Agent 直接恢复当前分支、运行态、验证和 Git 交付，不重新执行已经完成的历史回放设计工作。

## 2026-07-23 问题驱动 ResearchPlan、证据复用与股票空间上下文

- 新增确定性的 `ResearchPlanService`。股票问题先按行情原因、财务现金流、股东、主营业务、分析师预期、公告事件、估值同行或综合研究分类；短追问会继承最近用户问题的研究焦点，不要求用户重复描述对象和分析维度。
- 每类问题只选择必要证据模块与金融 Skills。行情原因问题不会再无差别调用财报质量、利润现金流和股东结构等重型模块；财务、股东、主营和估值问题分别走不同的取证路径。
- 当前价格、行情、资讯与最新估值每轮强制刷新；财报质量、利润现金流驱动、主营、股东、预期、事件、同行和历史走查等稳定模块可按模块时效复用最近研究报告证据。刷新失败但存在已保存证据时使用可追溯回退，没有可用证据时只把该模块标为不可用。
- 单个必需模块失败时整轮证据状态为 `partial`，其他模块继续进入 Hermes Prompt，不再把局部数据问题升级成整轮“研究暂未完成”。预生成报告只能提供模块证据，本轮回答仍由当前 Hermes/DeepSeek 实时生成。
- 主要个股 Agent 意图统一注入当前用户的精简股票空间上下文，包括正式判断版本、关系、持仓、计划、观察任务、操作与复盘；不同用户的判断和私人资料经过自动化隔离验证。
- Run 输入保存 ResearchPlan，证据保存逐模块 `fresh / reused / reused_fallback / unavailable / not_applicable` 状态，运行记录同时保存选中 Skills 和 Prompt 字符数。SSE 会发布“正在核验价格与技术结构”“正在核验公告、新闻与事件”等真实阶段。
- 自动化已覆盖问题分类、连续追问继承、无关重型模块不调用、按计划选择 Skills、稳定证据复用、实时模块刷新、单模块部分成功、专项股东 Agent 的股票空间上下文、跨用户隔离和同报告期经营比较继续加载同行模块。

当前边界：已完成问题驱动编排和模块级部分成功，但尚未形成 `accepted / gathering / analyzing / citation / writeback_candidate / completed / partial / failed` 的完整统一事件合同；页面也尚未提供针对某一失败证据模块的独立重试按钮。因此 `COL-AI-05` 与 `COL-AI-06` 仍只算部分完成。

## 2026-07-23 默认即时 AI、可分发 Hermes 链路与首屏收敛

- `ChatRequest.execute_agent` 默认改为 `true`。网页不再让普通用户切换模型供应商；AI 配置可用时，每个问题都基于本轮证据、资料库和研究工具即时生成，预生成报告只能补充证据。需要无模型费用模式时可显式传 `execute_agent=false`。
- Hermes 流式桥接从仅存在于源码 `scripts/` 的文件迁入可安装的 `app` 包，源码入口继续兼容。全新 Python 3.13 环境从 wheel 安装后执行 `python -m app.hermes_stream_bridge --self-test`，真实返回 `delta → boundary → final` 协议。
- 标准安装默认把数据库和用户工作区写入 `~/.qingshu`，避免 wheel 安装后向 `site-packages/data` 写入。一键启动仍默认使用仓库 `data/`，并兼容 uv 创建、没有 pip 的虚拟环境：优先 `uv pip install`，无 uv 时再使用 `ensurepip + pip`。
- AI研究页隐藏供应商开关、来源数量、资料数量、内部运行模式和自动展开的研究过程；保留历史对话、消息、输入框，以及问题识别后按需展开的行情与研究依据。首页把个人待办和相关变化前置到完整全球市场与 A 股结构之前。
- 智能选股服务返回 503 时，页面明确显示“智能选股数据正在准备/服务器尚未配置选股数据”，不再伪装为“0 只候选”；真实零候选仍由正常筛选结果表达。
- 修复一个真实 DeepSeek 回答被误退回模板的原因：合法失效条件与公告日期位于同一段时，旧守卫把前面的日期误当作自创阈值。现在只检查真正包含失效判断的句段，保存的原回答与 Agent 守卫全套测试均通过。
- 真实 `deepseek-v4-pro` 验收：不传 `execute_agent` 询问“中兴通讯今天为什么上涨”时，回答先纠正 7 月 23 日实际下跌的错误前提，再解释 7 月 22 日事实、反证和核验，状态 `completed`；连续追问只补充与当前判断的关系。询问“美股为什么收盘跌了”直接进入 `market_brief`，不再要求股票代码，状态 `completed`。
- 本轮浏览器控制插件拒绝重载本地 `127.0.0.1` 页面，因此没有把新版视觉验收冒充为已完成；运行服务 `/health=ok`、`hermes_enabled=true`、数据健康 `54/54`，页面 DOM 契约和内联 JavaScript 已由自动化验证。仍需在可控制本地页面的浏览器环境补一次新版截图验收。

## 2026-07-23 多股 Hermes 研究、页面恢复与首页事件去重

- 新增 `stock_comparison` 研究意图、服务和专门 Skill。用户可在一句自然语言中选择 2—5 只股票；本轮真实验收使用中兴通讯、中际旭创和英伟达，Hermes/DeepSeek 按当前问题即时生成比较回答，不读取预存答案代替本轮分析。
- 财务指标只有 `report_date + period_basis` 同时一致时才进入横向比较。真实结果中，中兴通讯和中际旭创按 2026 一季报同口径比较，英伟达 FY2027 Q1 单独说明；跨币种股价、市值和绝对金额不直接排序。
- 用户按 Enter 追问“再重点比较盈利质量，并说明哪些项目当前不可比”后，系统从历史消息 metadata 恢复三只股票，无需重复输入名称。左侧会话历史保存本轮四条消息，证明多轮对话已经落库。
- 多股证据允许部分成功：单只股票本轮数据未形成时，只标记该标的不可比，其余标的的确定性结果继续返回；回答保留关键差异、反方证据、下一步核验和可见证据引用。
- 修复服务重启后的旧页面状态：页面启动先连接事件流，连接或重连成功后重新读取 `/health`；真实页面会自动恢复 AI 实时研究状态，随后完成 DeepSeek 流式回答。
- 首页变化按 `symbol + event_type + occurred_at/market_date + rule_version` 的业务身份去重；已经进入“我的研究待办”的变化不再重复出现在右侧“与我相关的重要变化”，同时保留用户已读、相关性和处理状态。过期价格、财务事件不再伪装成今日变化。
- 最终自动化验收：`513 passed`，Ruff、内联 JavaScript 语法、diff whitespace 和 lockfile 一致性全部通过。

当前边界：多股比较不在一张画布叠加多条 K 线，也不生成综合排名、目标价或买卖建议；跨市场报告期、币种和披露制度不一致时，系统只陈述可比部分和待核验缺口。

## 2026-07-23 真实持仓事实链与可分发安装包

- 新增唯一期初持仓、买入/增加/减少/卖出流水、非交易调整、不可变修订链和派生持仓快照；所有数量、价格、费用与成本使用十进制字符串，写接口要求 `Idempotency-Key`。
- 持仓成本使用移动加权平均；部分卖出不改变剩余持仓单位成本，超量卖出整笔回滚。费用为空时不按 0 处理，也不展示伪精确净收益。
- 股票研究空间“任务与操作”已可录入期初持仓、操作、调整和修订；“历史与复盘”展示持仓、操作与快照数量。全部卖出后保留原股票空间，由用户决定转关注或结束。
- 固定验收算例通过：期初 `100 × 10 + 5`，增持 `100 × 12 + 5` 后为 `200` 股、成本 `2210`、均价 `11.05`；卖出 `50 × 15 + 5` 后剩余 `150` 股、成本 `1657.5`、毛结果 `197.5`、净结果 `192.5`。
- 隔离数据目录真实网页验收完成：录入期初、记录增加/减少、刷新恢复、修正最近操作费用并进入“历史与复盘”。费用从 `5` 修正为 `6` 后，已实现净结果从 `192.5` 更新为 `191.5`，操作显示“修订 1”，历史页显示 `2` 笔操作和 `4` 个派生快照。
- 1280×720 桌面视口下 `pageWidth == viewportWidth == 1280`，无横向溢出；页面控制台无 warning/error，历史卡片布局目视验收通过。
- 新增跨用户隔离、幂等重复提交、费用缺失、超量卖出回滚、修订版本冲突、非交易调整和移动加权成本测试。
- 显式限定 setuptools 仅打包 `app`，wheel/sdist 包含网页、Skills 和通用资料库，不包含运行时 `data/`、SQLite、个人工作区或密钥；跨目录安装所需资产已补齐为正式 Python package data。
- 当前边界：操作计划、操作时研究上下文快照、AI 交易复盘草稿、用户确认复盘和完整 R1 E2E 尚未完成，不能把本工作包描述为交易复盘闭环已经完成。

## 2026-07-23 李总策略分类查询与稳定空状态修复

- 真实网页发现李总策略只请求全状态混合的前 200 条记录，再在浏览器中筛选“候选 / 今日触发 / 数据不完整 / 已失效 / 未满足”。当某类结果不在前 200 条时，页面会错误显示为空；现改为切换标签时携带 `status` 单独查询对应分类。
- 快速切换分类使用递增请求令牌，只接收最后一次选择的响应，旧请求不会覆盖用户刚切换的新列表。
- 修复稳定全市场快照下严格候选为 0 时仍显示“正在建立快照”的错误语义。接口现在根据稳定发布状态返回 `ready`，页面明确显示“候选待观察列表当前为空”。
- 真实网页验证“数据不完整”加载 138 条独立结果，“未满足”加载该分类前 200 条；不再复用混合状态列表。页面控制台无 error/warn，桌面布局目视验收通过。
- 全市场深度同步改为最多 2 个受控并发；复用全市场快照中的 `stock_basic / daily_basic` 和同市场日交易日历，策略同步跳过 6 个非必需可选接口，减少重复供应商请求。
- 临时接口失败使用 `sync_incomplete` 状态，同一市场日最多自动重试 3 次；次日重新核验，稳定成功后清零，不把真实空数据当成临时失败循环请求。
- 新增稳定快照零候选、分类请求、并发上限、数据复用和重试状态机回归测试；最终全量 `469 tests collected` 全部通过，Ruff 与内联 JavaScript 解析通过。

## 2026-07-23 我的关注研究资产化、判断版本闭环与指数历史修复

- 新增 `StockAssetListService` 和 `GET /v1/stock-workspaces`，按当前用户返回全部 `watching / holding / ended` 股票空间。每项聚合版本号、关系、优先级、跟踪/流程状态、正式判断、最新变化、下一事项、未完成观察任务、行情和持仓事实状态；单只股票失败只降级该项，不影响整包。
- “我的关注”从旧自选股表改为研究资产视图：支持全部、持仓、关注、已结束，以及高/普通/低、暂停、等待数据筛选；单项关系、优先级和暂停状态修改均使用 `base_version`。结束后空间、判断和历史继续保留，恢复时复用原 `workspace_id`。
- 个股页面“编辑当前判断”不再直接调用旧 watchlist 覆盖正式判断，而是创建用户判断草稿并确认。页面明确显示将创建的新版本；若并发更新返回 409，会刷新最新版本并说明系统没有覆盖新判断。Agent 候选判断确认后也会刷新当前个股空间。
- 真实浏览器对英伟达完成暂停→恢复、高优先级→普通、结束→已结束筛选→恢复的完整可逆操作，最终恢复为关注中、普通优先级、跟踪中；已结束时页面仍显示原判断、最新报告和历史边界。关系修改、状态筛选和恢复过程中控制台无 error/warn。
- 真实视觉验收主动发现“今日需要处理 / 与我相关的变化”在离开今日观察后仍占据其他页面首屏，原因是 `.today-overview-grid` 的显式 `display:grid` 覆盖了 `hidden`。新增 `#todayOverviewGrid[hidden] { display:none !important; }` 后，“我的关注”从首屏顶部直接开始。
- 同一轮验收发现 1440px 桌面下操作按钮被 900px 表格最小宽度挤出，需要横向滚动。六列宽度已重新收敛，操作按钮允许换行；最终表格 `scrollWidth == clientWidth == 816`，页面 `scrollWidth == clientWidth == 1440`。`390×844` 窄屏下页面与表格均无水平溢出，筛选控件仍可见。
- 科创50已加入正式指数目录和腾讯 A 股完整日线 Provider。进一步发现原指数选择只比较时间戳，导致同日较晚的 Yahoo 单点行情覆盖腾讯 100 日历史，创业板、科创50和中证500无法计算涨跌与技术结构。现按“市场日期→非陈旧→历史是否至少两点→样本数→时间戳”选择，同日优先完整历史；真实接口显示六个 A 股指数均为腾讯 100 点日线，首页科创50显示一日 `-2.26%`、五日 `-3.34%` 和 RSI，而不是数据不足。
- 最终全量 `460 tests collected` 全部通过；Ruff 和内联 JavaScript 解析通过；桌面与窄屏真实网页验收通过，产品页面控制台无 error/warn。

当前边界：本工作包完成单项研究资产管理，不包含批量操作、注意事项/行业组合筛选、正式持仓数量成本和敏感列设置；这些仍按 P1-04 后续推进。结束/恢复测试会形成真实关系历史，符合“关系转换保留历史”的领域约束。

## 2026-07-23 多股票策略问答、公开状态清理与研究对象联动

- 使用真实网页新建对话并通过 Enter 发送：“李总策略为什么把国货航标记为量价历史不足？新潮能源为什么仍然数据不完整？请说明当前全市场名单覆盖、深度处理范围和不能下的结论。”Agent 正确识别国货航 `001391.SZ` 与新潮能源 `600777.SS`，逐股解释规则状态，没有退化为只说明全市场覆盖率。
- 最终回答使用 `2026-07-22` 数据快照：全市场名单 `5530`，已有名单状态 `5370/5530`，可深度核验 `1169`，已深度处理 `932/1169`，量价历史不足 `47`，财务历史待实查 `153`，当前候选或触发 `0`。回答明确“0”只代表当前已深度处理范围，不能外推剩余股票，也不构成买卖建议。
- 国货航被解释为上市 `569` 天，未达到 380 个交易日窗口所需约 `581` 天日历缓冲；本轮没有浪费逐股 ROE、股东、复权行情和涨跌停价请求。新潮能源的行情行数已满足长窗口，但三日放量规则因真实停牌或零成交保持数据缺口，同时保留已经明确不通过的规则。
- 浏览器验收主动发现历史快照中的限制说明仍暴露 `data_incomplete`，并出现“。；”拼接。服务层改用公开中文限制说明，Agent 渲染层同时兼容清理旧快照、去重通用限制和中文化未知状态；修复后页面不再显示内部枚举、供应商配额、HTTP 错误或价格链接。
- 同一轮验收发现回答已识别两只股票，但“当前研究对象”仍显示“等待识别市场或证券”。后端现在把本轮研究对象保存进历史消息 metadata；前端新增多股比较状态，显示“国货航、新潮能源 · 多股比较”，并提示指定单只后加载对应日 K 线。刷新页面、重新进入 AI研究后，多股票研究对象仍能从历史对话恢复。
- 新增服务层、Agent 输出守卫、API metadata 和前端入口回归测试。最终全量 `460 tests collected` 全部通过，`ruff check .` 与内联 JavaScript 解析通过；真实页面控制台无 error/warn。

当前边界：全市场深度批次仍由后台持续推进，以上数字是回答生成时的可追溯快照；多股问题当前展示研究对象摘要，不在同一画布叠加多只股票 K 线，用户指定单只后才加载该股票日 K 线。

## 2026-07-22 最新 PRD 与真实页面回归

- 一级导航已统一为“个股研究”，详情页统一为“股票研究空间”；“分析复盘”已改为“复盘中心”。股票研究空间包含研究概览、AI研究、数据与证据、任务与操作、历史与复盘五个标签。
- 浏览器实测中兴通讯研究概览首屏可见当前判断、三条最新重要变化、三项今日待处理、盘中报价、估值、近一年 K 线和六项财务指标。
- “编辑当前判断”已从跳转到“我的关注”改为研究空间内联编辑；保存后仍停留在原页面，并同步自选股、个人资料库和后续 Agent 上下文。实际保存同一判断后编辑器关闭、页面原位刷新。
- 真实 Hermes 请求“美股为什么收盘跌了”返回针对当前交易日的纠错式回答：先指出代表性指数实际收涨，再分别给出价格事实、资讯线索、中期反证和证据边界；不再要求用户先提供证券代码。
- `/health` 实测 Hermes 已启用、后台运行中、数据健康 `53/54`，其中 1 项同步中、0 项严重错误。
- 最新 16 页 Word 报告已重新渲染逐页检查；第 8 页说明文字裁切已修复，未发现新的重叠、乱码或页面溢出。

## 2026-07-22 基础产品闭环验收

- 浅色蓝白金融终端风格已在 1280px 桌面宽度实测，无页面横向溢出；首页五市场卡片、洞察流和热门板块正常呈现。
- “我的关注”实测 3 只默认股票完整显示；添加贵州茅台后自动加载分时，删除后恢复原 3 只，自选股数据库闭环通过。
- 分时、日线、周线三档实测成功；周线由两年日线在前端按自然周确定性聚合，画布与状态正确切换。
- 自选股表格曾在常见桌面宽度挤出操作列，已缩减列宽；浏览器测得每行“分析/删除”操作均位于表格可视边界内。
- 个股研究实测中兴通讯同时呈现近一年日 K、PE/PB/市值、六项财务指标、财报质量、利润现金流、股东、券商预期、官方事件和新闻情绪；页面 `scrollWidth` 与视口一致。
- 全局搜索实测输入“英伟达”并按 Enter，自动进入股票研究空间并选择 `NVDA`。
- 复盘中心新增后端五步流程图和四类回答上下文；宽屏一行展示，窄屏自动纵向排列。
- 浏览器控制台未发现 error 或 warning。
- 真实 Hermes Run `b7c13e91-f125-4377-a06d-e53dc4407e2f` 为 `stock_research / completed`，DeepSeek 单次调用总耗时 `8.611s`、首个安全可见 `4.3s`、最终 233 字。该样本暴露“当前最可能解释”“不存在系统性拖累”过强措辞；现已改为只输出确定事实、最多一项关键边界，并在用户可见清理层转换为“市场资讯反复提及的解释”“当日事实不支持全市场普跌解释”，新增回归测试。

覆盖范围：

- ChatGPT/DeepSeek 式体验页为默认入口，Swagger 只保留为开发者入口；
- 两用户安全会话、数据库记录、工作区、自选股、确认记忆、Prompt 和 Run 隔离；
- 多研究对话创建、列表、连续上下文、重命名、归档和用户隔离；个股追问可恢复证券标的，市场追问可恢复美股等区域焦点。
- 通用资料库自动种子、用户 TXT/Markdown/CSV/JSON 入库、相关性检索、Prompt 注入、工作区副本与删除。
- 独立资料库页面、左侧资料卡移除、通用/个人资料集中展示，以及上传/删除 API；
- 深度诊股的每用户×股票唯一会话、七阶段状态、现有对话绑定、跨用户隔离、工作区 JSON、专门意图推进、守卫/降级不推进和已保存 Run 自动对账；
- 未认证请求返回 401，跨用户 UUID 访问返回 404，用户 API 不返回本机 `workspace_path`；
- HttpOnly + SameSite=Strict Cookie、SHA-256 令牌哈希落库、会话注销与一次性旧 Demo 工作区迁移；
- 用户工作区持久化 `profile.json`、`watchlist.json` 和 `memory/confirmed.json`；
- 五市场盘中接口、交易所节假日/午休/提前收盘判断、数据新鲜度和分钟 K 线落库；
- Yahoo 分钟/日线解析、过期缓存降级、日线 `adjusted_close` 入库；
- 新浪伦敦金一分钟价格聚合五分钟 OHLC；
- 东方财富板块主源和新浪行业降级源；
- 东方财富公告、新浪个股新闻、东方财富股吧帖子解析；
- 腾讯估值字段映射：TTM/动态/静态市盈率、PB、流通市值和总市值；
- 东方财富 F10 报告期财务解析、SQLite 持久化和缓存回退；
- 同类报告期财报质量比较、增长加减速、利润率百分点变化、现金流覆盖、杠杆变化、静态毛利率敏感性、快照幂等与长期资料索引；
- A 股详细利润表、资产负债表、现金流量表字段映射与 SQLite 持久化；SEC Companyfacts 详细科目在缺失时保持空值，不由模型补写；
- 利润桥、费用率、营运资金、销售收现和三类现金流的同类报告期拆解；机械影响、可疑线索与未确认因果分层、快照幂等和长期资料索引；
- A 股财报全文发现、分页完整抓取、内容哈希去重、报告期对齐、原因主题摘录、公司解释/披露事实分层、快照与长期资料索引；
- SEC Companyfacts 按申报期对齐、同比计算、10-Q/10-K 口径区分与旧 SQLite 表迁移；
- SEC submissions 监管文件过滤和 EDGAR 官方原文 URL；
- 腾讯美股 PE、PB、流通/总市值的美元单位换算；
- 三组固定同行样本、同行中位数、相对比值、SQLite 复用和禁止评级边界；A 股同行经营只纳入相同报告日与累计口径，主营构成报告期和合并抵消项单列；
- 数据健康快照持久化、盘中延迟识别、公开摘要与内部详细检查隔离；
- Hermes 语义方向数字/目标价/概率守卫、供应商与运维信息隔离、拒绝回答留档和确定性回退；
- Nasdaq 美股公司新闻标题级公司相关性与荐股/评级类低信号过滤；
- 按目标股票代码过滤社区帖子；
- 关键词 + 互动权重的透明情绪评分及数据库快照；
- 条件展望的三情景、关键价位、失效条件和 `probability=null`；
- 5/10/20 日无前视走查、非重叠评估窗口、时间顺序 70/30 保留集和复权价格口径；
- 校准结果 SQLite 持久化、API 查询、报告接入和失败方向风险提示；
- 牛方、熊方与风险委员会证据分离；
- 后台任务状态、Market Pulse 质量门槛、去重和频率限制；
- 默认研究组合、服务器研究报告去重、查询接口与网页入口；
- 记忆候选查询、确认、拒绝和刷新恢复；未经确认或已拒绝的内容不能进入 Agent Prompt。
- PNG/JPEG/WebP 图片上传、解码重新编码、尺寸限制、非法图片拒绝、跨用户图片隔离、`image_id` 对话路由和 Vision 最终答案提取。
- 中文公司名与用户自选名称解析、未识别任务的可用澄清回答、实时刷新失败时的预生成报告证据回退。
- 整体市场自然语言路由，包括“美股为什么收盘跌了”、纳指、A 股和伦敦金等表达；输入框 Enter 发送、Shift+Enter 换行且不误触中文输入法。
- 普通研究对话支持确定性 preview 与 `/me/chat/refine` 原位深化；统一“研究 Agent”页面直接执行 Hermes，等待期间只显示实时检索文案，不预填固定摘要。
- 统一 Agent 自动识别市场或证券并切换 K 线，个股展示 RSI/MACD/ATR/量比；同时提供“市场洞察”“我的研究”和“分析复盘”三个独立页面。
- 大盘问题焦点分类与资讯重排：反弹/反转、量能资金、板块轮动、涨跌原因、风险和市场全景分别进入不同证据组织；自动化验证两类问题获得不同焦点和回答。
- 市场原因问题同时组合价格事实与带时间戳资讯，RSS 解析与 SQLite 持久化已有自动化覆盖。
- TradingAgents 式七模块分析板（行情、新闻、情绪、基本面、分析师预期、同行、多空与风险委员会）与 Rongxian 式 3/5/10 交易日复核清单，不将分析评分包装为胜率或交易指令。
- 研究结果回填表、固定 T+3/T+5/T+10 交易日协议、未到期进度、幂等重复运行、长期资料索引与“之前的研究后来怎么样”对话路由。
- 社区情绪文字方向守卫：证据为“轻微偏多”时，模型不得改写为“转负”或“偏空”。
- 数字守卫可识别英文资讯标题中的 `down 0.59%` 方向，并允许证据 `4.2783%` 合理四舍五入为 `4.3%`，仍拒绝反向符号和超出舍入范围的数字。
- 数字守卫将 `6.983%→5.768%` 识别为两个绝对费用率，不会被前文“费用减少”误加负号；模型自行计算的百分点必须先由后端写入证据。
- 连续对话守卫允许引用带 Run 记录、已通过守卫的上一轮助手回答中的数字，但不会信任用户消息里的目标价或概率数字。
- 守卫删行后的中文章节与 Markdown 有序列表会重新编号，不再留下跳号清单。

## 五市场实时验证

`GET /markets/live` 实测覆盖 `5/5`：

| 市场 | 标的 | 数据 | 实测状态 |
|---|---|---|---|
| 中国 | 上证综指 | 东方财富当前分钟 | 开盘时实测 0 秒延迟并写入 SQLite |
| 日本 | 日经225 | 东方财富当前分钟 | 开盘时实测约 19—39 秒延迟并写入 SQLite |
| 韩国 | KOSPI | 东方财富当前分钟 | 开盘时实测 0 秒延迟并写入 SQLite |
| 美国 | 标普500 | Yahoo 1 分钟 | 开盘时实测约 1 秒延迟，185 个点 |
| 伦敦金 | 新浪 XAU | 1 分钟价格聚合 5 分钟 OHLC | 开盘时成功，页面明确展示分钟聚合方法和延迟 |

中日韩美股票市场使用 `XSHG/XTKS/XKRX/XNYS` 交易所日历，实测可识别法定休市、盘前、午间休市、正常收盘和美股提前收盘。伦敦金是场外市场，仍按工作日近 24 小时规则判断。当前上交所日历数据覆盖至 `2026-12-31`，依赖升级前后均会检查覆盖范围；超出覆盖时自动降级到常规时段并在 API 标记 `calendar_status=fallback`。

## 后台服务验证

服务器启动后，无用户请求时自动完成十七项任务：

1. `market_intraday_refresh`：五市场 `requested=5, available=5`；
2. `market_pulse_article`：自动通过质量门槛并发布文章；
3. `a_share_information_refresh`：自动刷新默认 A 股和用户自选股；
4. `a_share_filing_refresh`：发现并保存最近三份 A 股财报全文，按报告期提取公司原因说明并索引长期资料；
5. `business_structure_refresh`：刷新默认 A 股及用户自选股的产品、地区、行业主营构成、同口径比较和长期资料；
6. `shareholder_structure_refresh`：刷新默认 A 股及用户自选股的股东户数、十大股东和股权结构快照；
7. `analyst_expectations_refresh`：刷新默认 A 股及用户自选股的评级分布、A/E EPS、研报元数据和历史修订边界；
8. `a_share_fundamentals_refresh`：自动刷新 A 股估值和结构化财务；
9. `us_equity_fundamentals_refresh`：自动刷新英伟达估值、SEC 财务和监管文件；
10. `earnings_quality_refresh`：为默认研究标的和用户自选股生成同类报告期财报质量快照并索引到长期资料库；
11. `financial_driver_refresh`：基于详细三表和同报告期财报原文生成利润、费用率、营运资金和现金流驱动快照并索引到长期资料库；
12. `peer_valuation_refresh`：刷新三只默认标的及九只固定同行的 PE、PB 和市值；
13. `outlook_calibration_refresh`：用五年复权日线刷新三只默认标的的样本外走查；
14. `stock_research_reports_refresh`：自动生成默认标的及用户自选股研究快照。
15. `research_outcomes_backfill`：只用已存日线按 T+3/T+5/T+10 回填研究结果和未到期进度。
16. `market_news_refresh`：预先刷新七个市场范围的驱动资讯并写入长期数据库。
17. `data_quality_audit`：检查行情、日历、财务估值、详细三表、财报全文、主营构成、财报质量、利润驱动、事件、同行样本、报告和其余后台任务。

`GET /system/background` 实测 `running=true`，十七项任务最近一次均为 `completed`。网页通过 `/events` SSE 接收市场、文章、研究报告和数据健康更新事件，同时保留轮询作为断线兜底。

## A 股信息与情绪验证

贵州茅台 `600519.SS` 实时验证：

- 公司公告：8 条；
- 新浪个股新闻：12 条；
- 经 `stockbar_code=600519` 过滤的股吧帖子：12 条；
- 社区情绪：轻微偏多，样本 12，置信度 `low_to_medium`；
- 情绪方法：`keyword_engagement_weighted_v1`。

来源层级在数据库、Evidence Packet 和 Hermes Skills 中保持一致：公告 > 确定性行情 > 媒体新闻 > 社区情绪。

## 财务、估值与海外资讯验证

- 中兴通讯 `000063.SZ`：腾讯 TTM 市盈率、动态/静态市盈率、PB、流通/总市值成功入库；东方财富 F10 最新报告期成功解析营收同比、归母净利润同比、ROE、毛利率、资产负债率和经营现金流；
- 中际旭创 `300308.SZ`：同一链路真实完成，报告明确区分三种市盈率口径和累计财务口径；
- 英伟达 `NVDA`：腾讯美股估值实测 TTM PE 约 `31.25`、PB `6.53`、总市值约 `4.94` 万亿美元；SEC 最新 FY2027 Q1 成功解析营收 `816.15` 亿美元、净利润 `583.21` 亿美元、稀释 EPS `2.39`、毛利率 `74.93%`、经营现金流 `503.44` 亿美元；
- SEC submissions 实测写入 12 条 10-Q/10-K/8-K，最新为 `2026-07-02` 的 8-K，并保留 EDGAR 官方原文链接；Nasdaq 宽泛市场文章继续由相关性规则排除；
- 固定同行样本实测：中兴通讯使用烽火通信/紫光股份/锐捷网络，中际旭创使用新易盛/天孚通信/光迅科技，英伟达使用 AMD/博通/台积电；三组均达到 `3/3` 覆盖；
- 报告只展示本标的数值、同行中位数和相对比值，并明确“小样本横截面，不是完整行业分位或投资评级”；
- 估值不在缺少行业可比和历史分位时被写成“便宜”或“昂贵”。

## 主营业务结构验证

- 中兴通讯、中际旭创和贵州茅台的产品、地区、行业主营构成已写入 `business_segment_rows`，并保存独立 `business_structure_snapshots` 与长期资料；
- 中兴通讯 2025 年报产品收入结构中，运营商网络占主营收入 `46.9448%`，政企业务占比由 2024 年报 `15.306%` 升至 `27.7993%`；
- 最新收入结构来自 2025 年报；产品毛利率只能取得 2025 中报参考期，系统明确分开两个报告期，不把中报毛利伪装成年报口径；
- 2025 中报毛利率参考中，运营商网络为 `52.9406%`，政企业务为 `8.2715%`，报告据此区分收入扩张与利润贡献；
- 真实 Hermes Run `d1a73b63-f264-4a98-a43d-137b0e51c87f` 为 `business_structure / completed`，回答正确区分收入期和毛利参考期，未输出评级或交易建议。

## A 股分析师一致预期与研报验证

- 中兴通讯实测覆盖 `11` 家机构，近六个月样本为买入类 `9` 家、增持类 `2` 家；页面和 Agent 均明确“只描述研报样本，不构成交易建议”；
- 中兴通讯 EPS 汇总实测为 `2025A 1.174392`、`2026E 1.417364`、`2027E 1.664545`、`2028E 1.770625`，A/E 标识与“预测不是公司指引”边界保持一致；
- 中际旭创实测覆盖 `31` 家机构，贵州茅台覆盖 `43` 家机构；三只 A 股都能返回最新研报元数据，证据包不保存或输出目标价；
- 只有两个不同内容的历史快照才计算同财年 EPS 上修/下修和覆盖机构变化；首个快照必须回答“尚不能判断”。
- 真实 Hermes Run `13a87709-5393-4417-b124-120c2b52c00a` 为 `analyst_expectations / completed`，输出守卫一次通过，完成了评级样本、A/E EPS、修订边界和最新研报的针对性回答。

## 财报质量验证

- 四只真实标的均已保存独立 `earnings_quality_snapshots` 并建立 `earnings-quality:{symbol}` 长期资料：中兴通讯为“盈利质量承压”，中际旭创和贵州茅台为“增长为正但质量仍需复核”，英伟达为“增长与盈利兑现较一致”；
- 中兴通讯 2026 一季报对比 2025 一季报：营收同比 `+6.13%`、净利润同比 `-46.58%`、毛利率下降约 `6.00` 个百分点、经营现金流/归母净利润约 `-1.51`，系统明确列为收入、利润率和现金兑现相互矛盾；
- 中际旭创营收和利润高增长、毛利率与净利率改善，但经营现金流/归母净利润约 `0.587`，系统不会把它写成完全一致，而是保留现金兑现复核项；
- 英伟达 FY2027 Q1 正确匹配 FY2026 Q1，而不是上一期 10-K 或 Q3，避免把不同累计报告期误作环比；
- 毛利率敏感性由后端确定性计算：可说明“按本期营收静态测算”的量级，但 Skill 强制声明它不是实际毛利变动或利润下降原因的归因；
- 最新四份服务器研究报告都已带入财报质量与利润现金流驱动证据，基本面模块真实证据计数为 `27—33`，不再因错误读取 `periods` 字段而只显示 1 条。

## 利润与现金流驱动验证

- A 股五只已刷新标的各保存 `36` 行详细三表，覆盖 `2023-06-30` 至 `2026-03-31`；英伟达保存 `24` 行 SEC XBRL 详细科目；
- 四只默认研究标的均已保存 `financial_driver_snapshots` 并建立 `financial-drivers:{symbol}` 长期资料；中兴通讯为“利润与经营现金流双重承压”，中际旭创、贵州茅台和英伟达为“利润与经营现金流共同改善”；
- 中兴通讯 2026 一季报对比 2025 一季报：归母净利润变化 `-11.427` 亿元；收入规模的毛利机械影响 `+6.922` 亿元，毛利率变化 `-5.995` 个百分点对应毛利机械影响 `-20.975` 亿元；财务费用变化对应 `-6.81` 亿元机械影响；
- 同期存货增速快于收入 `10.772` 个百分点、销售收现率下降 `10.285` 个百分点、经营现金流/归母净利润由 `0.755` 变为 `-1.51`，均只标记为复核线索；
- 已列毛利与费用科目仍存在营业利润桥接差额，系统明确保留为未确认项，不擅自命名为减值、补贴、研发或公允价值变化；
- 英伟达使用 FY2027 Q1 对 FY2026 Q1 的同类报告期，毛利桥、销售及管理费用、研发费用和经营现金流均来自 SEC 已申报科目；缺失的扣非利润和销售收现不会补写。

## 单股研究与条件预测验证

真实 `600519.SS` 集成流程成功组合：

- 1/20/60 日收益、MA20/MA60、波动率和最大回撤；
- 用户关注理由；
- 最新公告、媒体事件和社区情绪；
- 未来 5—20 个交易日的向上、区间和下行三种条件情景；
- 牛方、熊方和风险委员会；
- 具体关键价位与失效条件；
- 明确 `probability=null`，不输出目标价、胜率和确定性涨跌概率。

真实联调曾发现“MA60 已在当前价上方，却被写成未来跌破条件”的逻辑错误；现已改为动态选择当前价上下方最近有效关键位，并加入测试。

## 历史走查与样本外校准验证

方法版本：`fixed_rule_prequential_oos_v2`。

- 每个信号仅使用该日及以前的数据，未来价格只在信号生成后用于评分；
- 5/10/20 日信号间隔等于评估周期，减少未来窗口重叠；
- 时间上最后 30% 信号作为保留样本外区间，规则不在该区间重新调参；
- 优先使用复权收盘价，避免拆股、分红和送转造成虚假收益跳变；
- `probability` 继续为 `null`，历史方向频率只作为规则诊断，不写成未来胜率。

真实五年日线结果：

| 标的 | 有效点数 | 10 日总样本 | 10 日样本外样本 |
|---|---:|---:|---:|
| 中兴通讯 | 1210 | 113 | 34 |
| 中际旭创 | 1210 | 113 | 34 |
| 英伟达 | 1255 | 117 | 36 |

真实结果并非一味支持价格规则：部分标的的偏强或偏弱标签在保留区间方向不一致。系统会将其加入风险委员会并降低价格权重，而不是隐藏失败结果。

## 深度诊股、资料库与对话质量真实联调

- 新增独立“深度诊股”页和 `GET/POST /me/deep-stock`：每个用户与每只股票只有一个持久研究会话，保存七阶段状态、绑定对话、Run、证据模块、未决问题、最新报告和工作区 `deep-stock/{symbol}.json`；继续深聊会进入同一历史对话并预填下一阶段问题。
- Chrome 为网页体验用户真实建立中兴通讯会话 `7eb2eb9a-e4cc-430b-a872-e269791b721c`；原关注理由自动完成第一阶段，随后四次 Hermes 深度回答依次覆盖主营业务、财务现金流、估值同行、事件/情绪/反方证据和失效条件，最终页面显示 `7/7`，七个阶段均为“已完成”。
- 四个真实 Run 均为 `deep / completed`：`8deff365-e2bd-425e-8a22-8a9ac0789e9d`（`business_structure`，42 秒）、`a66de38f-9f04-475e-b4bb-905238a6de88`（`stock_research`，42 秒）、`bba05874-9684-4449-abdf-419bf6715326`（`stock_research`，39 秒）、`dd00f126-2bd3-41a5-9d1b-ff383f27111f`（`stock_research`，49 秒）。四次回答都通过最终守卫；两次少量删行修复保留其余有效回答。
- 第一轮真实回答暴露“专门主营业务意图的证据包本身就是模块，状态机却只识别综合包嵌套字段”的映射缺陷；已修复专门意图推进，并用已保存 completed Run 自动对账，阶段从 `1/7` 恢复到 `2/7`，无需重新调用模型。
- 最后一轮真实回答在守卫删行后出现有序列表跳号；已让删行修复同时重排 Markdown 列表，并增加回归测试。原历史回答保留原貌用于审计，后续 Run 使用修复后的可读格式。
- 真实会话自动形成三条持久补证任务：地区内产品与客户结构、订单与在手订单、地缘/出口管制与区域政策影响，均为 `pending_external`；估值同行回答还暴露三家固定同行的同报告期经营数据和主营构成缺口，已登记为 `D-108`。
- 独立“资料库”页在 Chrome 实测显示通用资料 39 份、个人资料 1 份，左侧只保留导航与历史对话，不再常驻资料卡片；文件选择自动化受 Chrome 扩展“允许访问文件网址”权限限制，上传/删除 API 和页面逻辑继续由自动化测试覆盖。
- A 股完整快照实测 `5,528/5,528`，上涨 `3,107`、下跌 `2,300`、平盘 `121`，总成交额 `29,734.49` 亿元；涨跌幅中位数 `+0.588%`、第一/第三四分位 `-1.3382% / +3.213%`，固定 ±3% 分档合计严格等于 5,528。守卫明确禁止把成交额写成净流入或解释为买卖双方双重计数。
- 对话质量复盘刷新后当前用户得分 `79/100`，覆盖 6 个对话、22 条消息和 11 个 Run；识别 1 个历史 guarded Run、6 个行级修复和 4 个慢 Run。中兴深度流程四次均超过 30 秒，确认流式生成与阶段耗时拆分仍是 P0 体验改进项。

## Hermes

- 本地版本：Hermes Agent `0.18.2`；
- CLI 最小调用成功返回 `HERMES_OK`；
- 当前标准解读实际 provider/model：`deepseek / deepseek-chat`；深入与视觉路由未显式覆盖时继续使用 Hermes 当前配置；
- FastAPI → 用户工作区 → Skills → Hermes → usage → SQLite Run 端到端成功；
- 使用完整英伟达真实证据包再次端到端验证：输入约 3.1 万 tokens，模型回答通过证据守卫，Run 状态为 `completed`；
- 市场回答真实发现并修复两类误拦截：“跌 5.40%”与证据中 `-5.3957%` 的语义等价，以及历史“下跌 / 上涨占比”被误识别为未来概率；
- 连续市场对话真实发现并修复第三类误拦截：模型引用上一轮已通过守卫的资讯时点时，不再因本轮证据未重复该分钟数字而整段回退；仅带 Run 记录的历史助手回答可作为可信数字上下文，用户输入仍不能放宽守卫；
- 最终前端真实 Run `69b2cec1-a53a-4553-b111-3bd4056df183` 状态为 `completed`，`deterministic_numeric_and_policy_guard_v2` 通过，无证据外数字、目标价、未来概率、供应商、降级或内部任务信息；
- 守卫拒绝测试已验证：模型新增 `目标价9999` 和 `未来上涨概率80%` 时，Run 状态变为 `guarded`，前端只返回确定性摘要；
- 真实测试发现并修复候选记忆被模型误称为长期记忆的问题；
- 当前共 24 个 Skills；新增 `event-timeline`、`analyst-expectations`、`shareholder-structure`、`business-structure` 与 `research-actions` 等专门能力，要求官方事件/媒体线索、分析师预测/公司指引、研报评级/交易建议、股东变化/资金动机、收入期/毛利参考期严格分层；`a-share-filing-evidence` 要求公司财报原文解释与独立验证因果分层；`financial-drivers` 强制同类报告期三表拆解、机械影响/公司解释/线索/未确认因果分层，并禁止模型自行相减生成新数字；`general-research` 负责连续对话与资料库上下文，`research-tracking` 负责长期证据变化复核，`research-outcome` 负责 T+3/T+5/T+10 结果复盘。
- 多模态真实 Run `9503bb41-5570-4d9a-b222-9928042406cf` 状态为 `completed`，路由为 `visual_research / vision`，最终答案协议成功去除 Hermes CLI 推理块，守卫通过且无证据外图中数字。

## 网页验证

- 默认 `/` 重定向 `/demo`；
- 页面已从 `localStorage user_id` 切换到 HttpOnly 会话，不再写入用户 ID 凭证；
- 真实旧工作区迁移成功：原有中兴通讯、中际旭创、英伟达和 `600519.SS` 共4只自选股及关注理由全部保留；
- 刷新页面后会话持续有效，`/me/watchlist/brief` 和 `/me/chat` 真实返回当前用户的个性化数据；
- 点击“记录风险偏好”后真实显示“待确认的个人偏好”卡片和“确认保存 / 暂不保存”按钮；未确认卡片刷新后仍可恢复；
- 真实确认“我更重视最大回撤，不希望只看短期收益”后，Run `2daddfec-2e7d-4ecd-b359-2b5006dfcffd` 状态为 `completed`，Prompt 包含该 confirmed 记忆，Hermes 在回答“今天大盘怎么样？”时主动优先解释最大回撤和高波动风险；
- 页面已实现左侧功能栏；
- “市场洞察”作为默认首页，内嵌盘中市场、可滚动分类洞察、热门板块和底部提问框；
- “研究 Agent”是独立单屏页面，消息区内部滚动，宽屏使用历史对话栏、窄屏使用历史对话下拉切换；
- Hermes 可用时显示“AI 深度解读”开关和“标准 / 深入”研究深度；取消勾选可立即切换到确定性研究；
- 真实浏览器切换已验证：取消勾选后 `agentMode=确定性研究`，回答不发起模型调用；恢复勾选后 `agentMode=AI 标准解读`；
- 自动刷新倒计时、开盘市场优先、K 线 Canvas 和数据新鲜度均已接线；原始供应商异常不进入用户页面；
- 顶部状态徽标由数据健康摘要驱动：健康显示“数据已连接”，异常或延迟只显示“部分数据同步中”；详细检查项不会进入页面；
- 默认出现中兴通讯、中际旭创和英伟达，每只均可点击读取服务器最新报告；
- 真实浏览器最终验收：洞察流 4 条、容器 `260px`、内容高度 `370px`，滚轮可翻阅；无横向溢出；页面文本不含“后台自动更新”“最新后台文章”“ProxyError”“数据源不可用”或“开发者接口”；
- 所有空态文案已使用“正在同步”，页面源码和渲染结果不再出现“盘中数据暂不可用”或“板块数据暂不可用”；
- 点击中际旭创报告实测弹窗打开，并包含 TTM 市盈率、历史走查分布和“不提供胜率、目标价或确定性涨跌概率”的边界。
- 点击英伟达报告实测弹窗打开，包含最新 SEC 8-K、FY2027 Q1 财务、美元估值和 `4.94` 万亿美元市值；页面不再显示“财务未接入”或“公告未接入”。
- 同一英伟达弹窗实测展示 AMD、博通、台积电同行样本，TTM PE 同行中位数与相对比值可读，且未生成高估/低估结论。
- 真实浏览器完成图片上传、“AI 图像研究”模式、Hermes 回答和附件自动清空；发送后模式恢复为“AI 标准解读”，页面未显示推理过程、`session_id`、供应商或内部任务。
- 用户原句“中兴通讯为什么大跌”真实浏览器路由为 `stock_research`，Run `b1beaac4-6afa-4ffc-b310-c03644b85d43` 状态为 `completed`；标准解读在约 24 秒内返回，输出守卫通过，社区情绪与“轻微偏多”证据方向一致。
- 真实浏览器按 Enter 发送“美股为什么收盘跌了”成功，Run `0a3e2772-053f-463f-b928-55a58e9dc025` 为 `market_brief / completed`；回答包含标普、纳指价格事实，多条市场资讯线索，并明确区分“当前最可能解释”和“未证明唯一因果”。
- 同一对话再次按 Enter 追问“那主要风险是什么”，Run `544f14e7-4d7b-43ee-8633-6b7eb13e4e16` 为 `market_brief / completed`；区域焦点继续为 `us`，回答优先解释纳指回撤、趋势、波动和事件风险，未退回“需要具体研究对象”的澄清文案。
- 2026-07-21 诊大盘针对性回答验收：按 Enter 依次发送“A股这次是反弹了吗”和“今天哪些板块在领涨”，等待期间只显示 Hermes 实时检索文案，不出现固定市场摘要；Run `720088f5-c20e-4bf1-b844-ac0b5403de22` 与 `b15b539f-ce87-43da-9807-558ca51c6fa4` 均为 `market_brief / completed`。第一问围绕 1/5/20 日收益、均线、RSI/MACD 和趋势确认条件，第二问围绕市场广度、领涨板块与轮动结构，两份回答明显不同；A 股上证综指分钟 K 线正常联动，对话历史保存为同一会话四条消息，页面控制台无错误或警告。
- 2026-07-21 最新版服务重启后再次从真实网页按 Enter 验收：Run `f0644d47-626a-494b-aee4-b3babbd828e6` 将“A股今天为什么分化”路由为 `sector_rotation`，回答聚焦半导体产业链、板块广度与反方证据；Run `a96663c2-701e-47db-b882-c44ddfefee87` 将“反弹还是反转”路由为 `trend_reversal`，回答聚焦 5/20 日收益、MA20/MA60 和趋势确认条件。两份回答明显不同，均为 `completed`，页面控制台无错误或警告。
- 2026-07-21 再次复现“看起来每次都一样”的直接根因：Hermes 已生成针对“反弹还是反转”的完整回答，但守卫把基于 `-6.6582%` 与 `-10.6591%` 归纳的“跌幅 6%—11%”误判为证据外数字，Run `b7678ff6-d615-49ba-89d9-31250783f1c4` 因此变为 `guarded` 并显示固定摘要。现已允许有证据支持的整数百分比区间，并增加“仅删除证据外数字所在行后重新验守”的安全修复；策略禁令、私有运维信息和语义冲突仍不能修复放行。
- 修复后在“诊大盘”同一会话按 Enter 连续发送三类 A 股问题：`4ab8b08c-84a6-4b47-b05b-e8e48aca9614`（“A股这次是反弹还是反转？”/ `trend_reversal`）、`7dbf0248-33e8-459d-aa00-346687dc7a72`（板块领涨与普涨判断 / `sector_rotation`）、`4a8bc031-8530-4941-af4e-a9d4b0104e79`（主要风险 / `market_risk`）均为 `market_brief / completed`，数字与政策守卫全部通过。三份回答分别围绕趋势确认、市场广度和回撤波动展开，内容与引用资讯明显不同；上证综指分钟 K 线持续实时更新。
- 2026-07-21 针对“诊大盘每次回复一样”再次完成真实修复与浏览器验收：标准解读改为 `deepseek / deepseek-chat`，问题焦点 Prompt 从此前约 `16—20 KB` 压缩到约 `12—14 KB`，资料库摘录不再重复注入。按 Enter 发送“今天A股上涨主要是哪些板块在带动？是普涨还是结构性行情？”的 Run `6ab58be0-7122-4bb6-b4fe-f2ce30044ede` 约 `10` 秒完成，回答聚焦半导体设备、材料、制造和封测，并明确全市场涨跌家数仍待补证；同一会话追问“那A股现在主要风险在哪里？”的 Run `786188a7-6e84-4e14-bba8-fb055ec349c6` 约 `9` 秒完成，切换到 MA20/MA60、波动率和最大回撤，两份回答结构与证据明显不同。新增守卫会删除把上证/深证误写成大小盘风格、把放量写成增量资金入场、把媒体标题写成系统性信号、发明典型投资者回撤阈值及把日线 09:30 日期锚点写成收盘时间的句子；删行后产生的空标题也会自动清理。
- 2026-07-21 最新一轮“诊大盘”修复将连续对话中的上一轮助手正文移出大盘 Prompt，只保留用户问题；市场问题强制检索“清数智算证据层级、市场涨跌原因的证据规则、市场趋势与风险分析规则”。输出守卫改为只认可模型实际收到的聚焦证据，并由代码预先计算 MA20/MA60 距离和跨指数波动率比较，避免其他市场数字或字段名中的数字误放行。浏览器按 Enter 发送“A股现在最需要警惕的风险是什么？请重点说趋势失效条件。”，Run `dd3ad306-50c3-449b-9a14-f5e5a90a2ed8` 为 `market_brief / completed`、焦点为 `market_risk`、守卫 `passed=true`；回答围绕两大代表指数的 5/20 日累计收益、MA20/MA60、波动率和最大回撤生成，没有回退固定全景模板。
- 同轮新增守卫覆盖：缺少全市场涨跌家数时声称普涨、把 5/20 日均量比写成今日放量、把“中期偏弱”升级为已确认下行趋势、虚构未来交易日窗口和量能/回撤阈值、用单期最大回撤推断当前低位或累计损失、用年化波动率证明多次日内摆动、虚构 MA5/A-B-C 浪、输出 `technical_state`/`market_drivers`/`UTC` 等内部字段。
- 2026-07-21 吸收 Rongxian 研究生命周期后的真实验收：新增 `deterministic_research_action_board_v1`，把报告、变化事件、回撤、波动、MA20、量能、主营缺口和 T+3 复核计划转换为个人行动清单，并保存到 `research_action_snapshots` 和用户资料库。分析复盘页真实显示 4 个自选标的、11 项需要复核、7 项待补证和 11 项继续观察；中际旭创为风险复核，中兴通讯、贵州茅台和英伟达为优先研究。
- 同次浏览器按 Enter 发送“我的研究行动和观察条件是什么？”：约 1 秒出现确定性行动摘要，随后 Hermes Run `1c551467-7dff-4f3c-9855-9777370d432a` 原位完成深度解读并通过数字守卫；回答使用已确认的最大回撤偏好，逐标的给出触发证据、下一步和待补证项，不生成目标价、仓位或买卖信号。
- 历史对话较多时真实发现侧栏底部“分析复盘”不可达；已为侧栏增加独立纵向滚动，浏览器滚动后可正常点击并进入白盒页。
- 2026-07-21 再次真实浏览器验收：在“诊大盘”按 Enter 发送“昨夜美股为什么跌”，`0.753` 秒出现只包含标普/纳指和美股资讯的确定性回答，并自动呈现“美国股市 · 标普500”分钟 K 线；Hermes Run `2b58bea0-a993-41b0-a8e2-fffbf1d134c5` 在约 `23` 秒后原位替换回答，对话消息数保持 `2`。
- 同次验收在“诊个股”按 Enter 发送“中兴通讯最近有什么变化”，`0.646` 秒出现长期证据变化摘要，自动呈现 `000063.SZ` 日 K 线、RSI14、MACD 柱、ATR14 和 5/20 日量比；Hermes Run `a78739b9-8f6f-4181-be29-74746fa17bf2` 在约 `48` 秒后原位补全基本面、反证和下一次复核动作，对话消息数保持 `2`。
- 真实浏览器按 Enter 发送“中兴通讯之前的研究后来怎么样？”：约 `1.5` 秒返回 T+3 未到期进度、阶段变化、最大上行/下行和区间情景；最大下行在没有回撤时正确显示 `0%`，引用只包含中兴通讯长期档案和结果复盘。Hermes Run `bc5caa85-d426-47bb-97c1-68b7322da43e` 约 `27` 秒完成并通过数字守卫。
- 真实点击“财报质量”后立即得到中兴通讯同类报告期确定性摘要；Hermes Run `95012ad7-bb09-4cf4-974b-57de72e0ed14` 为 `earnings_quality / completed`，回答明确区分已确认的利润率与现金流矛盾和仍不能确认的精确原因。
- 同一对话直接追问“为什么利润下降”可继承中兴通讯；真实测试先发现模型新增静态测算数字而被守卫拦截，随后将毛利率敏感性移入确定性证据。修复后 Run `45965a20-0813-48ae-b310-bd37401a9022` 为 `completed`，数字守卫通过，回答明确“静态量级不是实际利润归因”。
- 真实点击“利润现金流拆解”后约一秒返回详细确定性摘要；Hermes Run `b561fcb7-036b-4549-ab59-e732a1099704` 为 `financial_drivers / completed`，守卫 `passed=true`，回答将毛利率收缩和财务费用列为机械落点，同时把存货、应付和销售收现保留为线索；
- 同一对话按 Enter 追问“那现金流为什么转负”成功继承中兴通讯；Run `cb6aa542-07b6-44e1-a7e0-2329bf1b322b` 为 `financial_drivers / completed`，守卫通过，回答把经营现金流、销售收现、存货、应付和季节性边界分开说明；
- 真实中兴通讯 `2026一季报` 正文两页共 `9,499` 字落库；财务费用原文明确写出“汇率波动产生汇兑损失而上年同期为收益及净利息收入减少”，经营现金流原文明确写出“销售商品、提供劳务收到的现金减少及购买商品、接受劳务支付的现金增加”。
- 真实浏览器按 Enter 发送“中兴通讯财务费用为什么大幅上升？公司报告怎么解释”，确定性首屏只展示财务费用主题，不再铺开存货、投资收益等无关内容；Hermes Run `3b9bc263-e0ca-4392-a0ae-667806162a57` 为 `financial_drivers / completed`，数字与政策守卫通过，并明确公司解释不等于独立验证的唯一因果。
- 后台 `a_share_filing_refresh` 实测 `requested=5, completed=5, documents=9`；五只 A 股共保存九份最近财报全文，中兴通讯年度报告等长文通过并发分页完整入库。
- 同次验收首页上证综指开盘分钟线实测 `0` 秒延迟，日经约 `19—39` 秒，KOSPI `0` 秒；最新数据健康审计为 `51/51 healthy`。
- “分析复盘”页真实加载七步流程、十六类工具和四个最新研究案例；页面文本不含密钥、内部提示词、供应商故障或后台任务名称。

## 2026-07-21 统一研究 Agent 与证据追问迭代

- 默认页面已改为“市场洞察”，实时市场不再单独分页；洞察按“大盘、个股、机会”分类，机会明确表示待复核研究任务，不是买入信号。底部问题框发送后会建立可持久化的新对话并进入统一“研究 Agent”。
- 左侧已移除“诊大盘/诊个股”，统一 Agent 自动判断市场或证券：真实测试“美股为什么收盘跌了”显示“美国股市 · 标普500”分钟 K 线，“中兴通讯最近为什么下跌？”显示 `000063.SZ` 日 K 线；修复了“A股”被通用代码规则误识别为美股代码 `A` 的问题。
- Agent 页面在 `751×910` 窄屏和 `1440×900` 宽屏均实测不产生页面纵向溢出：消息区内部滚动；窄屏使用历史对话下拉框，宽屏显示独立历史对话栏。历史切换、归档入口、新建对话、回车发送与 Shift+Enter 换行均已验证。
- “我的研究”独立页实测加载 4 个自选标的及预生成报告；“分析复盘”实测加载 7 步分析流程、16 类工具、4 个近期案例和 4 个研究行动标的。用户页面不显示“后台自动更新”“供应商故障”“数据源不可用”等内部状态。
- 市场连续追问新增“反方、反证、失效条件、不成立、证据缺口”等上下文词，真实问题“这段判断的反方证据和失效条件是什么？”会继承 `market_brief / us / market_risk`，不再退化为通用资料问答，也不再混入中兴通讯、中际旭创等无关个股档案。
- 用户明确市场时，市场证据包切换到该区域。美股实测从原先标普/纳指两条扩充为标普500、纳斯达克综合、道琼斯工业指数、罗素2000四条代表性指数；VIX 不进入股票指数广度统计，避免正反方向混算。
- 真实 Run `6205c827-762e-4093-80a8-5a924a3559b1` 验证了四指数、专属资料过滤、市场资讯和失效条件链路；连续实测同时暴露并推动修复最大回撤/ATR 混用、三大与四指数混称、均线方向矛盾、错误领跌/波动率排名、虚构波动区间和自造时间窗口等问题。
- 当前输出守卫进一步禁止：把单日跌幅定义为恐慌或窄幅回调、把盘中反弹解释为承接买盘、把价格回撤写成估值压缩或均值回归依据、把代表指数平均收益写成日均收盘价、用未定义的成交量确认均线、把最大回撤写成波动容忍度，以及在用户明确询问时省略“失效条件”。若错误内容占回答主体，系统返回包含行情、资讯和 MA60 复核条件的确定性安全摘要。
- 本轮最终自动化回归已在后续续跑中更新为 `235 tests collected` 并全部通过，`ruff check app tests scripts` 通过。当前 8773 服务已加载最新代码并保持运行。

## 2026-07-21 报告交付后的目标续跑

- 真实窄窗口 `450×910` 验收发现导航只显示无文字图标、Agent 输入区被重复页头挤出首屏。移动端导航现已显示“市场洞察 / 研究 Agent / 我的研究 / 分析复盘”四个明确入口，并补充 `aria-label`；Agent 页隐藏重复工作区页头，消息区内部滚动。
- 上述四入口是 2026-07-21 的历史布局，已被 2026-07-23 PRD 六页导航替代；当前以今日观察、我的关注、个股研究、AI研究、复盘中心、个人中心六个正式一级页面为准。
- 修复后浏览器实测 `bodyClientHeight=910`、`bodyScrollHeight=910`，输入区底部坐标 `901`，整页无纵向溢出；历史对话下拉、自动 K 线、快捷研究动作、研究深度、图片入口和发送按钮均留在同一首屏。
- 从“市场洞察”底部提问框真实发送“今天A股上涨主要由哪些板块驱动”，成功跳转统一 Agent、建立历史对话并联动上证综指 K 线。第一次回答暴露了后端证据焦点错误：问题中的“反方证据”被误判为主要风险主题，导致 Hermes Prompt 丢弃了系统已经取得的板块排行。
- 已将“反方 / 反证 / 相反证据”改为次级输出要求：当问题明确询问板块、趋势、量能或涨跌原因时，优先保留实际研究主题；只有没有其他明确主题时才归入风险与失效条件。复合问题现正确路由为 `sector_rotation`。
- 同一历史对话用 Enter 追问后，真实 Run `48e68e2d-d47d-44ff-bbda-ed70ba737cf3` 为 `market_brief / completed`，证据焦点为 `sector_rotation`，包含 `10` 条板块排行；回答列出次新股 `+8.53%`、电子器件 `+7.74%`、玻璃行业 `+7.33%`、机械行业 `+5.99%`、陶瓷行业 `+5.29%`、化工行业 `+4.99%`，并区分可确认的板块分化、缺少交叉验证的解释和不能外推的持续性。
- 用户可见模型语言清理新增 `pct_change → 涨跌幅`，后续回答不再暴露该内部字段名。
- 新增 `GET /markets/breadth` 并真实取得沪深京 A 股 `5,528/5,528` 完整覆盖：上涨 `3,107`、下跌 `2,300`、平盘 `121`，固定分类为“上涨家数占优”；缓存必须通过完整性校验，过期时明确标记并由后台刷新。
- Chrome 真实页面按 Enter 发送“今天A股到底是普涨还是结构性行情？请直接给出全市场上涨、下跌、平盘家数，并说明固定分类和不能确认的部分。”，Run `1b7f2992-1504-41ee-916a-b2d239b2ab46` 为 `market_brief / completed`；页面自动联动上证综指分钟 K 线，回答包含三项家数和固定分类，并明确缺少个股涨幅分布、全市场成交额和成分贡献度时不能确认结构性行情。
- 连续真实模型复盘发现并修复四类守卫问题：下跌家数占比被误判为负收益、分类规则中的 65% 被误判为自造阈值、精确净差 807 写成“约 800”被误删、分类规则说明被当成重新分类；同时新增真实事实守卫，禁止把缺失收益的创业板错误配上深证成指或沪深300的涨幅。
- QA Run `41a3e5ea-371f-42a9-8f2e-b8995b6f8812` 实际暴露“创业板和沪深300涨幅约4.6%—4.8%”的指数数字错配；系统保留安全回答并新增自动化回归，后续缺失收益的指数只能显示数据缺失。
- 完整自动化回归 `235 tests collected` 并全部通过；Ruff 通过。服务健康检查、Hermes 和后台状态继续由运行中服务实时展示。

## 2026-07-22 D-108 固定同行经营证据闭环

- 新增 `peer_operating_snapshots`，按标的、锚定报告期、方法和证据指纹持久化固定同行经营快照；同一内容幂等更新，证据变化会触发研究报告重新生成。
- 中兴通讯固定样本真实刷新结果：中兴通讯、烽火通信、紫光股份、锐捷网络财务报告期均为 `2026-03-31`，口径均为财政年度累计；三家同行 `3/3` 进入营收同比、净利润同比、毛利率、净利率、经营现金流/净利润和资产负债率比较。
- 四家公司主营构成报告期均为 `2025-12-31`；产品分类名称不视为统一口径。紫光股份产品项同时保留 `合并抵消 -6.3475%`，避免只展示正占比后产生错误加总。
- 快照已写入通用资料 `peer-operating:000063.SZ`，标题为“中兴通讯固定同行经营与主营构成”；最新中兴服务器研究报告已包含同一经营比较证据。
- 修复真实问答路由：含“分别比较 / 同行比较 / 公司对比 / 同报告期经营差异”的问题进入 `stock_research` 完整编排，不再被“主营构成”关键词抢占；资料检索只保留当前标的的 `peer-operating:{symbol}`，不再混入中际旭创同行文档。
- 新增同行专用输出守卫：禁止公司排名、最高/最低/优于/劣于，禁止用业务结构自动解释经营差异，校验主营构成日期、同行公告日、合并抵消、内部方法 ID，以及净利润“元→亿元”换算。真实模型曾把烽火净利润 `38,392,482.56` 元写成 `0.038` 亿元，最终守卫将其确定性纠正为 `0.38` 亿元，且不会改写紫光、锐捷的经营现金流。
- Chrome 在历史会话 `7eb2eb9a-e4cc-430b-a872-e269791b721c` 通过 Enter 发送最终问题；Run `feda0331-b3fe-474f-ab2a-7126622f41b3` 为 `stock_research / completed`，用时约 10 秒，输出守卫通过。最终页面同时显示营收、净利润、毛利率、经营现金流、主营构成、可比与不可比边界，来源仅包含三份中兴相关通用资料。
- `/system/data-health` 最终为 `54/54 healthy`；`peer-operating:000063.SZ` 检查为 healthy，锚定报告期 `2026-03-31`，同报告期同行 `3/3`，主营构成同行 `3/3`。
- 对话质量复盘当前分数为 `56`，主要被本轮为同一问题连续重复回归形成的高相似历史和 11 次守卫修复拉低，不代表最终单次回答失败；仍真实暴露的下一项产品问题是流式生成/分阶段延迟，以及避免把开发验收对话混入日常用户质量评分。

## 2026-07-22 P-003 分段耗时与市场参与面守卫

- 真实 Hermes Run 现在分别保存 `routing_and_evidence_seconds`、`agent_setup_seconds`、`model_seconds`、`guard_seconds`、`agent_total_seconds` 和 `request_total_seconds`；对话质量 API 按有样本的 Run 计算分段 P50，前端“分析复盘”摘要直接展示。
- Chrome 实测“分析复盘”显示：`质量分 56 · 6 个对话 · 44 条消息 · 22 个 Run · 4 类待改善问题 · 4 项开放数据需求 · 分段P50：证据 1.52s / 模型 9.21s / 守卫 0.003s / 总计 10.74s`。
- 新建历史对话后，在 Chrome 输入框直接按 Enter 发送“美股盘中为什么上涨”；页面立即显示 Hermes 检索提示，发送按钮在等待期间禁用，完成后保留为可切换的历史对话。
- 最终 Run `4062d103-522c-4f20-b663-86f008228cd7` 为 `market_brief / completed`，模型为 `deepseek-chat`，`api_calls=1`；证据准备 `1.807s`、Agent 准备 `0.005s`、模型 `9.682s`、守卫 `0.004s`、总计 `11.499s`。原始回答中的指数口径问题被行级修复，最终输出守卫通过。
- 针对上一轮真实回答曾出现的“成交量未放大，说明上涨参与面较窄”，Market Brief Skill 新增明确边界：成交量、成交额、量能和量比不能证明上涨参与面或市场覆盖范围；全市场广度必须来自涨跌家数，板块内部参与面必须来自成分覆盖与内部涨跌家数。
- 新增输出守卫和回归测试，覆盖“成交量未放大，说明上涨参与面较窄”“上涨覆盖面有限，主要是由于量比偏低”、明确否定边界以及守卫删行后的可读性。真实 Chrome 最终回答不再出现该错误推断，并明确“不能确认上涨的持续基础”。
- 全量自动化回归 `237 tests collected` 并全部通过；`ruff check app tests scripts` 通过；重启后的 `/health` 为 `ok`，Hermes 启用，后台运行中，数据健康 `54/54 healthy`。
- 当前边界仍如实保留：Hermes CLI/供应商调用尚未提供可观测的首字时间和 token 流，因此 P-003 只完成了分段测量与等待状态，不把一次性返回包装成已实现流式输出。

## 2026-07-22 P-003 真实阶段事件与守卫误杀复盘

- 对话请求新增随机 `request_id`，现有 `/events` SSE 只发布请求 ID、阶段、耗时和用户可见文案，不发布问题正文、答案或个人资料。真实后端依次产生 `routing_started`、`evidence_ready`、`model_started`、`guard_started`、`completed`；异常时可出现 `fallback_started`。
- Chrome 使用 Enter 发送“美股这次上涨有哪些反方证据”，已看到“正在识别问题，并检索实时证据与资料库…”并最终原位替换为带风险证据、资讯线索和证据边界的回答；历史对话和自动 K 线保持正常。
- Run `0063d7fc-39b1-4def-be85-71c03a7c4f99` 为 `market_brief / guarded`，证据 `1.071s`、模型 `9.872s`、守卫 `0.005s`、总计 `10.951s`。复盘发现回答中的“达到 1.56%”只是已有 ATR 指标事实，“三大指数由涨转跌”是资讯标题，但旧守卫把两者误判为自造阈值和四指数混用。
- 守卫现只在“达到某值才/方/后/时/则/视为/确认/有效”等条件语境识别自造阈值；“三大指数”与第四指数必须在同一分句混写才判冲突。新增真实语境回归，保留对真正阈值发明和四指数错称的拦截。
- 当时确认 Hermes `-z` 单次模式源码设置 `stream_delta_callback=None`，所以该阶段实现只是准确的状态反馈，不是伪 token 流；后续已通过独立 `AIAgent` 流式桥接完成真实正文增量，并继续保留最终完整数字和证据守卫，见下方“P-003 私有守卫流式完成”。
- 修复后再次在 Chrome 新建对话并用 Enter 发送“美股本次反弹最值得警惕的证据是什么？”；页面可见阶段约为检索 `0.08s`、Hermes 生成 `1.67s`、保存 `11.81s`，最终回答、历史对话和自动 K 线均正常。守卫阶段实际仅数毫秒，用户未必能肉眼看到，但事件顺序由 API 自动化完整验证。
- 第二次 Run `7f31aff0-a334-4192-a43e-435f5bbd74d9` 为 `market_brief / completed`，证据 `1.591s`、模型 `10.185s`、守卫 `0.008s`、总计 `11.789s`；行级修复成功，未整篇退回确定性模板。
- 同次回答仍暴露三项新冲突：把纳指正的 60 日收益写成“20日、60日均为负”，在已有市场资讯时声称“未提供消息面驱动资讯”，以及由回撤推出“反弹空间有限”。现已新增三类确定性守卫与正反回归，并保留“标题不能证明唯一因果”“回撤不能证明空间有限”等安全边界表达。
- Chrome 在同一历史对话继续追问纠错。Run `bfaa2f9f-1484-4982-8a9a-b09912d9ead6` 能承认上一轮错误并引用资讯，但错误声称 60 日收益字段缺失且暴露 `return_60d_pct`；现已让显式询问“60日收益”的问题保留该指标，清理原始字段名，并禁止把已有指标说成缺失。
- Run `8ae8b4dd-e18a-4d29-88d1-a19363d01848` 正确回答纳指 20 日 `-1.23%`、60 日 `+5.76%`，但把实时证据中的趋势状态错写；新增“指数名称—趋势状态”一致性守卫。对该真实回答重放后，错误趋势行可删除，价格事实、资讯线索和数据边界完整保留。
- Run `ebcbfc8d-0576-437e-951c-640d95f5ac9e` 验证趋势守卫已拦截“中期偏弱→确认下行”的越权表达；同时发现确定性降级摘要未回显用户点名的 60 日收益。现已让 `trend_reversal` 摘要在问题明确提到 60 日时同时展示 20 日、60 日收益和原始趋势状态。
- 最终全量自动化回归 `246 tests collected` 全部通过，`ruff check app tests scripts` 通过；服务重启后 `/health` 为 `ok`、Hermes 启用、后台运行中、数据健康 `54/54 healthy`。

## 2026-07-22 P-003 私有守卫流式完成

- 新增 `scripts/hermes_stream_bridge.py`：独立子进程直接调用 Hermes `AIAgent`，以 JSONL 输出 `delta / boundary / final / error`；真实桥接测试取得 `551` 条 delta、`1` 条 final、`0` 条 error，不再把阶段提示包装成正文流。
- 新增 `app/services/agent_stream.py` 与私有端点 `/me/chat/stream/{request_id}`：流状态按用户和随机请求 ID 隔离，跨用户访问被拒绝；服务保存累计安全草稿，断线重连会重放，`agent_stream_complete / agent_stream_error` 后自动结束。
- Agent 对原始 delta 按句/行执行流式预守卫，证据外数字、目标价、内部信息和其他策略禁令不会进入草稿；最终答案仍经过完整 `deterministic_numeric_and_policy_guard_v2`。桥接失败自动回退原 Hermes CLI，并记录首模型片段、首安全可见片段、原始 delta、可见更新和拦截段落。
- Chrome 第一轮真实问题“中兴通讯最近下跌最需要警惕的证据是什么？”对应 Run `5351c1a1-c4df-484f-a584-4f9cd9b83a0f`，为 `stock_research / completed`；原始 delta `835`、安全可见更新 `23`、拦截段落 `1`，首模型片段 `4.477s`、首安全可见 `5.276s`、总耗时 `12.806s`。最终页面只有用户问题和一条最终回答，自动联动 `000063.SZ` 日 K 线。
- Chrome 第二轮全过程采样问题“美股这次回落有哪些反方证据和失效条件？”对应 Run `4a080748-43ae-482e-bd5f-efbc65733ae3`，为 `market_brief / completed`；原始 delta `666`、可见更新 `1`、拦截段落 `23`，首模型片段 `2.629s`、首安全可见 `3.508s`、总耗时 `10.936s`。浏览器采样在 `36ms` 已看到发送按钮禁用，约 `5.062s` 出现 `streaming-draft`，`11.103s` 完成最终替换，消息没有重复。
- 第二轮只出现一次可见更新的根因不是模型没有流式返回，而是预守卫把“最终回答必须包含失效条件”错误应用到每个尚未完成的单句；因此除包含“失效条件”的句子外，其余安全事实都被暂缓。`guarded_sentence_stream_v2` 只把这类全文完整性检查延后到最终守卫，证据外数字、策略禁令、内部信息和不受支持推断仍逐句拦截；用第二轮最终回答离线重放，安全可见段由 `1` 个提升为 `20` 个，实际不安全段为 `0`。
- Chrome 第三轮使用标准 `deepseek / deepseek-chat` 发送“美股反弹现在最不成立的证据是什么？失效条件怎么判断？”。Run `33a97956-3466-40c9-bdcf-4e040c9ae707` 为 `market_brief / completed`，原始 delta `591`、安全可见更新 `22`、误暂缓段落 `0`，首模型片段 `2.087s`、首安全可见 `2.925s`、总耗时 `9.620s`。浏览器在约 `3.547s` 显示第一段，随后草稿长度连续增长并原位替换为最终答案；完整输出守卫 `passed=true`。
- 同轮浏览器验证历史对话栏共 `11` 个会话，可在美股和中兴通讯对话间切换并恢复；自动 K 线分别恢复为“美国股市 · 标普500”实时 1 分钟 K 和“中兴通讯 · 000063.SZ”日 K，页面无横向溢出。
- 自动化新增跨用户隔离、断线重放、终态结束、不安全流式段落不展示、全文完整性检查延后、最终完整守卫、桥接失败回退、首片段指标落库和前端发送顺序回归。全量 `254` 项测试通过，`ruff check app tests scripts` 通过；重启服务 `/health` 为 `ok`、Hermes 和后台任务运行中，数据健康 `54/54 healthy`。Chrome 产品页面来源没有 warning/error；仅发现两条与产品无关的 Zotero 扩展连接错误。

## 2026-07-22 P-011 验收对话隔离与 P-012 成交额时间锚点

- `conversations` 新增 `quality_scope=user/evaluation`；`/demo?qa=1` 页面明确显示“验收隔离”，创建与更新对话 API 均支持该字段。普通用户质量复盘排除验收对话、消息和 Run，但历史研究仍完整保存；复盘方法升级为 `deterministic_conversation_quality_review_v2`。
- 已将既有明确开发回归对话隔离，并继续由 QA 页面自动隔离新样本。2026-07-22 Chrome 复盘页面显示质量分 `96`，普通样本为 1 个对话、6 条消息、3 个 Run、1 类待改善问题和 2 项开放数据需求；同时显示已隔离 15 个开发验收对话和 32 个 Run。
- A股全市场快照不再把开盘前抓取自然日当作成交额市场日期：使用 XSHG 交易日历、抓取时间和最新 tick 推断交易日，周末、休市日和开盘前回退到上一交易日；历史量能比较只接受严格早于当前市场日期的同口径快照。
- 最新 `/markets/breadth` 实测 `market_date=2026-07-21`、`latest_tick_time=15:36:00`、沪深京成交额 `29,734.49亿元`、覆盖 `5,528/5,528`；历史比较为同口径样本积累中，不判断放量或缩量。
- Chrome 在 QA 隔离页新建对话并按 Enter 发送“今天A股全市场成交额是多少？它能说明资金净流入吗？请给出证据时间和不能确认的部分。”。Run `4202a175-644c-49bd-8b75-2d2936a992d4` 为 `market_brief / economy / completed`，Hermes 回答正确给出 `2026-07-21`、`15:36` 和 `29,734.49亿元`，明确成交额不等于净流入，并说明北向/主力/大单方向和放量缩量当前不能确认；页面未暴露 `building_history` 或内部状态值。
- 该 Run 使用 `deepseek-chat`，首模型片段 `1.512s`、首安全可见 `1.643s`、总耗时 `6.721s`；最终输出守卫 `passed=true`，以行级修复移除一条与现有成交额证据冲突的缺失声明，没有回退为整篇确定性模板。
- 最终全量测试 `261 passed in 7.71s`，`ruff check app tests scripts` 通过；服务 `/health` 为 `ok`，Hermes 与后台任务运行中，数据健康 `54/54 healthy`。

## 2026-07-22 P-013 个股当前报价、历史日线与同日归因

- Chrome 首次询问“中兴通讯今天为什么跌”时暴露严重时间错位：Yahoo 最近完整日线是 `2026-07-20` 收盘 `33.73元 / -6.31%`，但腾讯最新报价快照是 `2026-07-21 16:14` 的 `34.88元 / +3.41%`；旧回答错误把上一根日线称为“7月21日今日下跌”。
- 个股证据新增 `current_quote`，当其时间晚于历史日线时，“今天/当前/最新”问题必须优先引用报价快照；模型必须把 `metrics/provenance` 明确标为最近完整历史日线。输出守卫新增方向、价格和必答完整性检查，不能在删行修复后丢失当前报价。
- 个股涨跌归因新增 `stock_market_context`：根据用户问题方向选择当前报价日或上一根完整日线作为目标交易日；代表性指数保存最近5根日线并按目标日期对齐；全市场广度和热门板块只有日期一致时才能用于支持或排除系统性/行业拖累。
- 行业证据只接受公司行业名称与板块名称的同日精确匹配；当前中兴通讯行业为“通信设备”，热门半导体、电子等宽泛类别不能替代。D-002 的完整行业指数、成分和板块内涨跌家数仍保留为外部数据缺口。
- Agent 页面同时显示“报价快照至 07/21 16:14 · 日 K 至 07/20 09:30”，价格卡为 `34.88 / +3.41%`，技术指标仍基于最后完整日K，不再混为同一时间口径。
- 最终 Chrome Run `97765c86-d40a-494f-980c-9567a49bc82e` 为 `stock_research / economy / completed`：先明确7月21日上涨3.41%，再把用户所指下跌定位到7月20日；引用7月20日上证 `+0.85%`、深证 `-0.71%`，明确没有精确通信设备同日板块数据；摩根大通减持媒体报道发表于7月21日晚，不能解释7月20日跌幅。
- 为防守卫降级再次变成长报告，“为什么涨/跌”新增聚焦的确定性摘要，只保留双时间价格、同日指数/广度、精确行业缺口、公告新闻、情绪和不能确认的因果。Markdown 表格会自动转换为网页可读项目符号，原始布尔字段和 `low_to_medium` 等内部状态不再展示。
- 最终全量回归 `269 passed in 7.83s`，`ruff check app tests scripts` 通过。

## 2026-07-22 P-014 个股复杂回答的流式守卫分层

- Run `97765c86-d40a-494f-980c-9567a49bc82e` 的最终答案正确，但流式指标只有 `1` 次可见更新、`73` 个可见字符和 `34` 个暂缓段落。离线逐段重放确认，主因是“用户询问今日时必须给出更新报价并区分历史日线”属于全文完整性要求，却被重复应用到每个单句；只有恰好包含 `34.88元 / +3.41%` 的时间锚点句能够展示。
- 流式模式升级为 `guarded_cumulative_stream_v3`：每个新段先独立检查目标价、交易指令、无证据数字、内部字段、错误涨跌方向和跨日期因果，再与既有安全段组成累计草稿检查跨句上下文；失效条件、证据边界、全市场广度等最终完整性要求继续延后到最终守卫。
- 当前报价是“今天/当前”问题的首次展示必备锚点。锚点出现前的旧日线开场只在后台暂缓；锚点出现后从包含最新报价的最短安全后缀开始展示，避免用户先看到上一交易日跌幅、数秒后才看到“今天其实上涨”的纠错。
- 对同一份 `answer.rejected.md` 离线重放：完整分段 `76` 个、非空段 `35` 个；由旧版 `1` 次、`73` 字可见提升为 `31` 次、`1,286` 字可见，首个草稿是“最近完整日线 2026-07-20；当前最新报价 2026-07-21 16:14，34.88元，+3.41%”。错误“今天应指7月20日下跌”、跨日期系统性拖累推断和不受支持的主营原因段仍被拦截。
- 新增回归验证：当前报价出现前不展示旧日线开场；报价出现后草稿连续增长；错误“今天下跌6.31%”在任何草稿中均为零；既有目标价/无证据数字拦截、全文完整性延后、最终答案替换和 CLI 回退测试保持通过。
- Chrome QA 隔离页使用回车真实发送同一中兴通讯问题。页面约 `4.553s` 首次渲染安全正文，第一段即说明7月20日完整日线下跌、7月21日最新报价 `34.88元 / +3.41%`；随后记录到 `22` 次草稿增长，页面始终为欢迎消息加一条助手消息，没有重复气泡，约 `13.488s` 原位替换最终答案。
- 对应 Run `e6acde0c-e216-40f7-b2d6-4edfb6257f8a` 为 `stock_research / economy / completed`，模型 `deepseek-chat`；后端首模型片段 `2.746s`、首安全可见 `3.409s`、原始 delta `777`、可见更新 `22`、可见字符 `894`、拦截段落 `3`、总耗时 `13.243s`。最终输出守卫 `passed=true`，行级修复删除了当前价格误用、跨日期归因和不受支持主营推断，没有回退确定性模板。
- 全量回归 `270 tests collected` 且全部通过，`ruff check .` 通过；服务重启后 `/health=ok`、Hermes 已启用，数据健康 `54/54 healthy`。

## 2026-07-22 P-015 中兴通讯同日精确行业归因

- 现有个股归因只在热门板块前十中寻找公司行业，导致“通信设备”不在榜单时只能声明缺失；宽泛“电子器件/半导体”又不能替代精确行业。审计公开源后确认中证指数官网提供可审计闭环：`931160` 为“中证全指通信设备指数”，官网说明其从中证全指中选取业务涉及通信设备领域的上市公司证券，调整频率为每半年。
- 新增 `CSIIndustryIndexProvider`：通过精确行业名发现指数代码，读取中证官网基础信息、`index-details-data` 返回的官方样本与权重文件，并调用中证官方 `perf/index-perf` 历史行情；`.xls` 使用锁定依赖 `xlrd 2.x` 解析。结果保存到 SQLite `market_cache`，指数日线另写入 `market_bars`，缓存过期且刷新失败时可返回带过期标记的历史证据。
- 通信设备真实数据：样本名单日期 `2026-07-21`、权重日期 `2026-06-30`、样本 `50` 只；中兴通讯位于官方样本中，权重 `3.741%`。`2026-07-20` 行业指数收盘 `19546.69`、涨跌 `-1.42%`；中兴通讯同期 `-6.31%`，确定性计算相对行业 `-4.89` 个百分点。该结果支持“行业当日也承压，但个股跌幅明显更深”，不等于证明具体下跌原因。
- `stock_market_context` 新增同日 `exact_industry_index`，同时保存指数定义、目标日涨跌、公司相对行业、官方样本数量、样本/权重日期、公司是否为成分及权重。Prompt 与确定性摘要都要求：指数只能描述样本整体表现；目标日成分涨跌家数缺失时，不得声称行业普涨、普跌、多数成分同向或参与面广窄。
- 第一轮 Chrome Run `55a34e55-a05d-434e-a40a-46d999afd3b2` 已引用行业指数与 `-4.89` 个百分点，但真实验收又发现“没有全市场系统性拖累”仅凭两只指数下结论，以及把一季报误写成“6月30日公告”。两类错误分别转成跨日期/缺广度系统性结论守卫和财报公告日期近邻检查；另新增缺行业成分广度时的普涨普跌/参与面守卫。
- 修复后 Chrome Run `e3cc8590-249f-478d-b9ae-4bc206339d91` 为 `stock_research / economy / completed`，模型 `deepseek-chat`；页面始终只有一条助手消息，记录 `15` 次安全草稿更新，后端首安全可见 `4.787s`、总耗时 `11.925s`。最终回答明确给出通信设备 `-1.42%`、中兴 `-6.31%`、相对行业 `-4.89` 个百分点，并写明50只官方样本的目标日涨跌家数尚未取得；不再断言全市场系统性拖累已排除，也不再保留错误财报公告日期。
- 行级修复从原始模型答案中删除财报日期冲突、当前价格误用和主营推断；最终完整守卫 `passed=true`。用户可见标题“能确认的非系统性拖累”进一步被确定性清理为中性的“市场与行业对照”，避免标题先行暗示结论。
- 全量回归当前为 `274 tests collected` 且全部通过，`ruff check .` 通过。仍未完成的是其他A股行业/概念的完整映射、目标日全体成分涨跌家数和成分贡献；D-002 因此从“待授权”更新为“部分补齐”，没有被误标为完整完成。

## 2026-07-22 P-016 行业成分广度与静态贡献闭环

- 行业指数提供器已从“只有指数涨跌”扩展到成分级证据：按目标交易日并发抓取官方样本的前复权日线，保存覆盖率、上涨/下跌/平盘家数、涨跌幅中位数和固定广度分类；再用官方权重快照乘以成分涨跌幅，计算静态估算贡献、估算合计、官方指数收益和对账差。
- 通信设备 `931160` 在 `2026-07-20` 实测覆盖 `50/50`：上涨 `16` 只、下跌 `34` 只、平盘 `0` 只，固定分类为“普跌”；中兴通讯权重 `3.741%`，静态估算贡献 `-0.2359` 个百分点。成分估算合计 `-1.8077`，官方指数收益 `-1.42%`，对账差 `0.3877` 个百分点，因此用户可见回答始终明确这不是中证官方逐日归因。
- 已建立电池→`931719`、白酒Ⅱ→`399997`、银行Ⅱ→`399986` 的显式映射。覆盖不完整时不会冒充完整行业结论：电池目标日为 `49/50` 可比，只输出部分覆盖描述，并保留停牌、上市日期和历史成分状态的后续数据需求。
- 模型输出守卫新增行业家数和标的贡献必答项；若模型漏项，系统保留其余正文并定向追加证据。组合修复器可先删除错数字、内部字段、跨日全市场数据和越权因果，再补家数/贡献并重新执行完整守卫。`68%成分下跌`、`下跌方向占有效样本68%` 等自然表达不会再被数字守卫误杀。
- 真实 Chrome 最终 Run `5e35a9ee-971f-4f48-a4db-f5e8e8e38138` 为 `stock_research / economy / completed`，模型 `deepseek-chat`。页面用 Enter 发送问题后保留针对性模型正文，浏览器实测草稿连续增长；后端记录首模型片段 `2.958s`、首安全可见 `4.409s`、原始 delta `988`、可见更新 `27`、可见字符 `1,295`、总耗时 `14.554s`。
- 最终回答完整给出 `16/34/0`、行业 `-1.42%`、中兴 `-6.31%`、约 `-0.236` 个百分点静态贡献和 `0.39` 个百分点对账差。最终守卫以行级修复删除了“行业广度直接证明个股原因”的句子；没有使用 7月21日全市场家数或成交额解释 7月20日，也没有保留“个股自身压力”“提前定价”“技术指标导致”或仅凭代表性指数排除系统性拖累的说法。
- 全量回归现为 `284 tests collected` 且全部通过，`ruff check .` 通过；服务重启后 `/health=ok`、Hermes 启用、后台任务运行中、数据健康 `54/54 healthy`。

## 2026-07-22 股票研究空间渐进披露与首屏重排

- 重新核对同事同步的《清数智算_超级完整产品与技术PRD_V2.1_审校修订版》《清数智算_技术落地设计与实施规范_V1.0_正式版》及“个股评分库”视觉稿。正式口径继续使用“个股研究 / 股票研究空间”，不恢复综合评分、目标价或强烈看多。
- 旧版“当前研究判断”跨两行占位并被右侧变化/任务高度拉伸，完整页面形成大块空白。现改为全宽判断卡：用户研究主线与系统确定性观察并排；最新重要变化和今日待处理在下一行并排，页面高度和阅读路径明显收敛。
- 当前研究摘要的价格、盈利、现金流、证据四个维度改为可点击行，并增加明确箭头与键盘焦点。点击后复用研究证据详情阅读器，展示真实数据时间、报表机械影响、支持/反方证据、未确认原因、复核事项和研究边界。
- 浏览器实测“现金流：利润与经营现金流双重承压”详情可展开：包含2026一季报与2025一季报对比、毛利桥、费用机械影响、营运资金线索、未确认原因和复核事项；关闭后返回原股票空间，页面控制台无 error。
- 视觉验收确认：首屏行情、K线、研究摘要仍保持同事视觉稿的“主内容 + 右侧研究摘要”结构；下半区不再出现被强行拉高的空白卡片，变化与任务均保持可点击详情。
- 全量 `pytest`、`ruff check .` 与内联 JavaScript `node --check` 均通过。
- 仍需继续：数据与证据标签的信息密度需要进一步渐进披露；用户观察任务已在下一工作包补齐，但操作计划、个人操作与交易复盘仍缺少真实流水模型。

## 2026-07-23 用户观察任务与唯一研究对话

- 新增独立 `observation_tasks` 与 `observation_task_history`，和后台 EvidenceTask、系统 ResearchAction 分开。任务支持待处理、处理中、等待数据、完成、忽略、取消与重开；编辑和状态变化生成新版本，完成必须填写结果或引用证据。
- 正式 API 覆盖按用户/股票列表、创建、详情、编辑和状态流转；所有查询与写入均按 `user_id` 过滤，`base_version` 冲突返回 409，同一系统建议重复保存不会生成第二条任务。
- 股票研究空间“任务与操作”页分为“我的观察任务”和“系统建议的下一步”。系统建议只有用户点击确认后才进入个人任务；保存后原建议从建议区隐藏，避免重复操作。
- 真实网页使用“大族激光”完成：保存“经营改善候选线索”→开始处理→编辑标题和说明→填写核验结果→完成→查看版本 1—4 历史。页面显示任务结果和每次状态迁移，控制台无 error/warn。
- 通用选股候选的“让 Agent 继续研究”和李总策略的“让 Agent 核验”已改为创建或复用该股票的 DeepStock 会话。真实网页验证候选进入既有“个股研究｜大族激光”对话并预填针对性问题，没有另建普通对话。
- 全量 `410 tests collected` 全部通过，`ruff check .` 与内联 JavaScript 语法检查通过。操作计划、持仓流水与真实交易复盘仍未完成，不能把观察任务描述成交易功能。

## 仍未完成全部实测

- `economy/deep/vision` 环境路由已实现；三条路由都已走通当前 Hermes 默认模型，但尚未配置并对比三个实际不同的 provider/model。
- `Dockerfile` 与 `docker-compose.yml` 已提供；当前机器没有 Docker 命令，因此未完成容器构建验证。

## 原始 MVP 目标完成审计

| 原始要求 | 当前实现 | 最终证据 |
|---|---|---|
| 中日韩美与伦敦金实时首页 | 五市场交易日历、开收盘状态、延迟和当日 K 线，30 秒刷新 | `/markets/live` 实测 `5/5`；浏览器 5 张行情卡、5 个 K 线 Canvas，当前开盘市场优先 |
| 实时金融数据库和数据工具 | SQLite 保存指数/个股复权日线与分钟线、全市场广度、板块、估值、财务、主营构成、分析师一致预期与研报元数据、详细三表、财报全文、财报原因摘录、财报质量、利润驱动、事件、情绪、同行、校准、报告、研究结果、用户和 Run | `/system/data-health` 实测 `54/54 healthy`；沪深京广度覆盖 `5,528/5,528`，五只 A 股九份最近财报全文、主营构成、分析师预期快照、A 股固定同行经营快照、四只研究标的一年日线、详细三表和财务分析快照均已落库 |
| A 股新闻、公告和情绪 | 公告 > 媒体 > 股吧社区分层，股吧按股票代码过滤，确定性情绪评分入库 | 后台 A 股信息刷新 `5/5 completed`；默认 A 股及用户新增 A 股均进入刷新集合 |
| 无人值守后台 | 行情、文章、A 股信息、A 股财报全文、主营构成、股东结构、分析师预期、A/美股财务、财报质量、利润现金流驱动、同行、校准、研究报告、结果回填、市场资讯、证据任务和质量审计共十八类任务 | 清理旧进程后重启实测：`running=true`，十八类任务均已真实运行 |
| Hermes Agent 与个性化 | 24 个 Skills、`economy/deep/vision` 路由、HttpOnly 会话、用户工作区、自选股理由和确认后记忆 | 真实市场、分析师预期、主营结构、事件脉络、财报质量、财报原文原因、利润驱动、研究行动、结果复盘和图像 Run 均 `completed`；自动化测试验证跨用户隔离与 confirmed-only Prompt |
| 个股、板块、大盘分析与不瞎预测 | 确定性指标 + 牛熊反证 + 风险委员会 + 三情景条件展望 + 五年无前视样本外走查 | `probability=null`；守卫拒绝目标价、未来概率、确定收益和 BUY/HOLD/SELL；失败方向会降低置信度 |
| 默认自选股与即点即读报告 | 中兴通讯、中际旭创、英伟达服务器预生成研究快照 | 后台报告 `3/3`；浏览器实测英伟达弹窗包含 SEC、固定同行和预测边界 |
| 面向用户的前端 | 实时市场、倒序滚动洞察流、热门板块、自选报告、Agent 对话、记忆确认和图片上传 | 浏览器实测洞察 4 条、滚动容器 `260px/387px`、无横向溢出；禁止文案匹配数为 0 |
| 多模态研究 | 图片清洗与隔离、`image_id`、Vision Skill、最终答案提取和输出守卫 | Run `9503bb41-5570-4d9a-b222-9928042406cf` 为 `visual_research / vision / completed`，附件发送后自动清空 |

以上证据证明技术原型已可持续运行，并已具备可内部验证的最小 MVP 主链；但股票研究空间、AI 候选写回、个人操作记录与交易复盘仍在收敛，不应夸大为成熟商业产品。

## 生产化仍缺失

- 当前是单设备匿名安全会话；生产版仍需账户登录/身份绑定、多设备恢复、OIDC/JWT、行级安全、配额、审计、加密和 CSRF 强化；
- PostgreSQL/TimescaleDB、Redis、独立任务队列和多实例协调；
- 有 SLA 的授权实时行情，以及交易所日历数据的年度更新监控；
- 完整行业指数、估值历史分位、分析师一致预期、完整行业供需、机构持仓和 Level-2 数据；
- 更多美股公司 CIK、官方财务和同行样本覆盖；
- 价格规则的跨市场/跨制度稳健性、事件与基本面联合校准、交易成本模拟和持续漂移监控；
- 自动交易和任何收益承诺。

## 2026-07-23 李总策略全市场稳定批处理与后台隔离

- 全市场数据层新增 `a_share_universe` 稳定快照：批量读取交易日历、全部上市A股和最近完整交易日 `daily_basic`，统一保存沪市 `.SS`、深市 `.SZ`、北交所 `.BJ`、市值亿元口径、数据版本和覆盖率。真实发布快照为 `2026-07-22`，覆盖 `5530` 只，其中 `5526` 只有总市值、`4` 只缺失，市值覆盖率超过95%发布门槛。
- 修复盘前日期错位：交易日历已经包含当日开市、但当日 `daily_basic` 尚未发布时，系统会向前查找最近真正有市值数据的完整交易日；全市场和逐股快照使用同一回退规则，日线、复权和涨跌停窗口不会包含回退日期之后的数据。
- 不稳定全市场批次不再生成5530条伪“已评估”记录，也不会触发逐股多年数据请求。历史上由全市值缺失产生的异常预筛 Run 已标记为 `failed / unstable_universe_market_cap_snapshot`，候选、覆盖率和详情查询均排除失败 Run，但原始记录保留用于审计。
- 李总策略采用两层批处理：市值不超过150亿元的股票直接保存 `not_qualified`，缺市值保存 `data_incomplete`；只有严格大于150亿元的股票才补齐多年ROE、股东、约400个交易日行情、复权因子和真实涨跌停价。相同交易日重跑幂等，逐股同步失败继续保留上一稳定快照。
- 策略从通用后台调度线程拆为独立 Worker，默认每批 `10` 只、批后等待 `30` 秒继续；真实运行期间 `/markets/live` 返回 `200`，实测约 `0.02—0.03s`，没有被策略长任务阻塞。页面覆盖率从 `78.2%` 持续增长到 `78.4%`，后台随后继续推进到 `78.55%`，未宣称全市场已经100%完成。
- 进程启动会修复历史遗留的 `running` 后台任务、Tushare同步 Run 和策略 Run，将其标记为 `failed / process_restarted_before_completion`；真实数据库中的多条僵尸任务已完成修正，当前只保留本进程真实运行中的任务。
- 权限边界真实验证：普通个人会话读取 `/v1/stock-strategies/li-zong/runs/latest` 返回 `200`；调用逐股重算和全市场重算接口均返回 `403`。未配置 `QINGSHU_ADMIN_API_TOKEN` 时所有手工重算关闭，网页没有管理员重算按钮。
- 浏览器真实验收：选股页显示策略版本、数据交易日、`已评估/全市场`、覆盖率、市值预筛通过数、剩余待处理数、候选、今日触发和数据不完整数；页面控制台无 error/warn，统计状态合计不再大于已评估数量。
- Tushare 只读连通性脚本通过：返回5行日线、完整OHLC/涨跌/成交字段和最新交易日，未输出Token。最终全量 `418` 项 Python 测试全部通过，`ruff check .` 和内联 JavaScript `node --check` 均通过。

仍未完成：全市场高市值股票仍在后台增量补齐，当前候选/触发为真实空结果而非演示数据；用户策略订阅、通知中心、可调参数确认界面、今日观察完整触发流和全市场历史回放仍需后续工作包完成。

## 2026-07-23 Hermes 选股主链与网页流式回答修复

- 复现原始失败 Run `22121d3e-399f-43bf-8e7d-4433dd63b1b1`：证据构建正常，但 `HERMES_BIN=hermes` 被当成相对文件路径检查，约 `0.003s` 即以 `FileNotFoundError` 降级为确定性摘要。根因不是证据不足或输出守卫，而是命令解析错误。
- 新增可移植 Hermes 运行时解析：命令名按 `PATH` 查找；流式桥接从控制台脚本或 shell 包装器识别 Hermes Python；保留虚拟环境 `python` 符号链接入口，避免解析到基础解释器后丢失 `hermes_cli` 模块。可用 `HERMES_PYTHON_BIN` 覆盖非标准安装，但不写死开发者路径。
- 修复后一次性真实 Run `ed2700f1-af03-404e-9a56-3a65ac0db098` 为 `stock_screen / completed`，模型调用 `1` 次，输出守卫通过；不再返回 `degraded` 或固定 preview。
- 修复流式桥接后，真实单股 Run `40829a67-ea38-4cc1-a701-47e56bee0b44` 为 `completed`，记录 `15` 次安全累计草稿更新；最终守卫通过。后端在完成守卫后额外发布一条与数据库答案完全一致的 verified final delta，网页不会在 HTTP 完成时突然换成另一段回答。
- 李总策略 Agent 现在严格区分全市场候选池和单股规则核验：候选池只含 `qualified/triggered` 的真实结果；未通过、数据不完整、已失效和未处理股票不会称为候选。全市场未完成时必须同时给出交易日、已评估/总数、覆盖率和剩余数量，不得宣称全市场无候选。
- 移除 Agent 证据中容易混淆的全市场市值预筛总数，待处理数量只使用 `remaining_symbols`。真实复验不再把市值达标总数误写成“仍在评估队列”。普通用户回答使用中文状态，不展示内部状态码、profile key 或策略版本标识。
- 浏览器验收主动发现模型自行增加 `T+3` 复核周期，并把“候选为 0”推断成特定规则“尤其严格/同时满足极少”。现已新增李总策略专属 Prompt 和确定性守卫：缺少研究计划时拒绝自造 `T+N` 周期；缺少逐规则汇总统计时拒绝瓶颈、稀缺度和主要约束推断，并支持删除越界行后重新完整验守。
- 最终真实网页 Run `e49c6b12-1bd8-48d5-9945-a6c8fee3694f` 为 `stock_screen / completed`，`streaming.enabled=true`、安全可见更新 `18` 次、最终守卫 `passed=true`。页面回答使用 `2026-07-22` 数据，显示 `4484/5530` 已评估、覆盖约 `81.1%`、剩余 `1046` 只，未出现 `T+3`、规则瓶颈臆测或内部英文状态。
- 真实网页验证 Enter 直接发送成功；完成后只有一条用户问题、一条欢迎语和一条最终回答，没有 pending 消息、残留 streaming draft 或重复回答，浏览器控制台无 error/warn。另修复“新建研究对话仍残留上一只股票预填问题”，现在标题恢复“新的研究对话”、消息区仅保留欢迎语且输入框为空。
- 全量 `425 tests collected` 并全部通过；`ruff check app tests scripts` 通过；内联 JavaScript 语法检查通过。运行中 `/health=ok`、Hermes 启用、后台和李总策略 Worker 正常、数据健康 `54/54 healthy`。

仍未完成：李总策略全市场覆盖仍由后台增量推进；用户订阅通知、今日观察触发流、参数确认界面和历史回放仍属于后续工作，不因 Agent 主链恢复而提前标记完成。

## 2026-07-23 P0-07 结构化回答、逐 Claim 引用与判断确认写回

- 新增 `ai_citations`、`ai_writeback_candidates` 和 `StructuredAIService`。股票研究 Run 直接消费 `research_claims`，保存事实、推断、反证、待验证假设、信息缺口、失效条件、下一步核验、结论边界与逐 Claim 引用；没有从 Markdown 正文反推正式写回内容。
- 普通问题和未完成 Run 不产生判断候选；只有 `completed` Run 且用户明确要求形成或更新判断草稿时生成 `pending_confirmation`。候选公开字段不包含用户、工作区、会话和内部来源键。
- 新增 `/v1/ai-writebacks` 列表、详情、确认和拒绝 API。确认前 active thesis 不变；确认后生成新版本并 supersede 旧版本；拒绝不改变 active thesis；base version 冲突返回 409 并把候选标记为 stale；跨用户访问统一返回 404。
- SSE 新增并可断线重放 `agent_citation`、`agent_writeback_candidate`、`agent_structured_partial`、`agent_structured_failed`；历史消息 metadata 保存 `structured_answer`，preview 深化成功后会替换为完成 Run 的结构化回答。
- 前端已展示结构化事实、证据推断、反证、假设、缺口、失效条件和下一步核验。证据点击后展示来源、时间、摘录与口径限制；判断草稿支持确认、拒绝和刷新后恢复状态。
- 真实 Hermes Run `c95e887a-1891-4ae8-90e7-55b9e3053a79` 为 `stock_research / completed`、`error=null`。页面用 Enter 发送“分析中兴通讯当前支持证据、反方证据和失效条件，并形成判断草稿，供我确认，不要直接修改正式判断”，最终只保留一条助手回答，显示 8 条事实、8 条反证、7 项待核验和 12 项引用。
- 确认前候选为 `pending_confirmation`、base version 1、active thesis version 1。点击确认后候选为 `confirmed` 并生成版本化判断。真实验收主动发现 `seed_demo_watchlist` 在每次鉴权时重复写默认 thesis，导致新版本被立即覆盖；已改为只在用户创建/旧空间迁移时补缺失默认股票，普通请求不再重置或复活用户已删除的自选股。
- 修复后将测试产生的演示用户判断恢复为 active version 4；页面刷新、`/session` 和关注列表请求后仍保持该版本，控制台无 error/warn。新增回归测试覆盖默认初始化不得覆盖已确认判断或恢复已删除股票。
- SQLite 现有数据库成功创建两张新表，`PRAGMA foreign_key_check` 无结果；Tushare 只读验证返回 5 行和最新交易日 `20260109`，未输出 Token；`/health=ok`、Hermes 启用、后台与李总策略 Worker 正常、数据健康 `54/54`。
- 最终全量 `432 tests collected` 全部通过；`ruff check app tests scripts` 和内联 JavaScript 语法检查通过。

当前边界：AI writeback 当前只支持版本化正式判断 `thesis`。用户任务、操作计划和正式复盘必须等对应领域对象完成后再按同一确认协议接入；确认流程由 StockDomain 的两个事务组成，MVP 已验证一致性，但生产版仍需收敛为单事务或补偿事务。

## 2026-07-23 P0-08 七阶段完成门禁与历史重审

- 七阶段推进现在要求 Run 为 `completed`、最终输出守卫明确通过、核心证据包可用；每个阶段还必须独立达到对应六维覆盖门槛，并至少保存一条可追溯来源或明确用户输入。
- 宽泛“完整分析”不再按整包证据一次性完成多个阶段。公司经营、财务现金流、估值同行、事件情绪、反方证据、失效条件分别判断；证据薄、反证不具体、失效闭环不完整或来源为空时标记 `needs_review`。
- 阶段保存 `coverage_gate`、`source_refs`、`review_reasons`、`last_evaluated_run_id` 和 `stage_completion_gate_v2`。薄证据或守卫缺失不会冒充完成，后续取得充分来源后可以恢复为完成状态。
- 历史完成阶段在读取时自动重审。能从原 Run 证明完成、守卫、覆盖和来源的阶段继续保留；缺少 Run 或不能满足新版门禁的阶段降为“待复核”。第二次读取保持阶段、进度和未决项不变，证明重审幂等。
- 真实中兴通讯空间保留公司、财务、估值、事件和反证等六个有依据的完成阶段，将旧版“失效条件与下一证据”降为待复核；网页明确显示 `研究流程 6/7` 和具体复核原因。
- 真实页面验收主动发现 `6/7` 时仍显示“七个阶段已完成”。根因是公开会话只把 `in_progress` 识别为当前阶段，没有包含 `needs_review`。后端和前端均已修复并补充回归测试，现显示“待复核：失效条件与下一证据 · 绑定对话已有 28 条消息”。
- 最终定向测试 `tests/test_deep_stock.py` 7 项通过；全量 `435 tests collected` 全部通过；`ruff check app tests scripts`、内联 JavaScript 解析和真实浏览器控制台检查均通过，控制台无 error/warn。

当前边界：阶段完成表示研究证据链达到当前流程门槛，不表示公司质量高、预测正确或应当买卖。历史旧阶段如果原 Run 已删除，只能降为待复核，不能凭旧状态反推证据。

## 2026-07-23 P0-09 研究概览证据地图

- 股票研究概览新增五栏“研究证据地图”：支持证据、反方证据、风险与失效条件、信息缺口、下一证据。内容直接来自 `StockWorkspaceService` 的 Claim Ledger、`counterevidence`、`invalidation_conditions` 和 `next_evidence`，没有引入演示数据或第二套分析结果。
- 概览每栏显示真实总数和两条摘要；长证据限制四行预览，“查看全部”保留完整主张、依据、来源、数据时间和口径边界。完整证据仍在原“数据与证据”页按六维覆盖展开。
- “查看完整证据”真实切换到数据与证据标签；“让 Agent 核验下一证据”复用当前股票唯一研究会话，只预填问题而不自动发送。英伟达实测输入框带入“行业供需与一致预期尚未接入”。
- 浏览器视觉验收主动发现 CSS 网格最初把证据地图放到变化/待办之后，且五栏原文过密；已调整为“当前判断 → 证据地图 → 变化与待办”，并增加桌面五栏、中屏两栏、窄屏单栏响应式布局。
- 英伟达真实概览显示支持 9 项、反证 2 项、风险与失效 5 项、信息缺口 1 项、下一证据 10 项；中兴通讯同样展示可追溯来源和风险边界。页面控制台无 error/warn。

当前边界：证据地图用于组织研究事实和未决问题，不把证据数量解释成公司评分、置信概率或买卖信号；“下一证据”只表示最值得继续核验的资料，不是操作建议。

## 2026-07-23 P0-09 六维四层证据与历史 Agent 解释恢复

- `StockWorkspaceService` 新增 `stock_workspace_evidence_layers_v1`，将公司经营、财务质量、行业相对、估值、技术状态和风险事件六个维度统一拆分为原始数据、系统计算、Agent 解释和缺失项。原始值、来源和数时间不会被系统推断或 AI 文字覆盖。
- Agent 解释优先使用当前股票绑定会话中最新可恢复的 `structured_answer`。绑定会话没有结构化结果时，仅在同一 `user_id` 且证券代码完全匹配时回收该股票最近历史回答，并返回 `ai_source=same_stock_history`；跨用户和其他股票均不会混入。
- 修复长对话恢复时取到“最早 200 条”而非“最新 200 条”的问题；新增回归覆盖 205 条历史后仍能读取最新结构化回答。
- 前端六维卡片升级为桌面两列、窄屏单列，每卡可打开“四层详情”阅读器。阅读器标注证据覆盖、来源、时间与 Agent 解释所属会话范围，未生成时明确显示空状态。
- 真实网页验收先复现了切换股票后标签 `active`、`aria-selected` 和面板 `hidden` 状态可能不一致，修复后切换到中兴通讯会唯一选中“研究概览”，再打开“数据与证据”时六张卡片与面板状态一致。
- 真实中兴通讯空间已从同用户、同股票的最近结构化研究中恢复公司经营、财务质量、技术状态和风险事件解释；详情页明示“同一用户的该股票最近研究对话”。英伟达没有结构化历史时仍诚实显示“尚未生成可恢复的 Agent 解释”。
- 自动化验证：全量 `437 tests collected` 并通过，`ruff check app tests scripts` 通过，内联 JavaScript 语法解析通过。最终重启后 `/health=ok`、Hermes 启用、数据健康 `54/54`；桌面两列与 720px 窄屏单列均已真实验收，页面无水平溢出，控制台无 error/warn。

当前边界：AI 解释只从已保存的结构化回答恢复，不从旧 Markdown 正文推测结论，也不会把未绑定证券代码的普通对话强行拼入股票空间。

## 2026-07-23 PRD 六页导航与个人中心基础版

- 一级导航真实收敛为今日观察、我的关注、个股研究、AI研究、复盘中心、个人中心六页；“透明选股”移动到 AI研究的二级模式，进入透明选股后左侧 AI研究继续保持当前入口语义，资料库保留为资源入口。
- 新增诚实版个人中心，读取当前用户真实关注股票、股票研究空间、历史对话、资料库文档和待处理研究项；同时显示用户标识、创建时间、完整年份的会话有效期、Hermes 状态、数据健康和后台刷新状态。
- 页面明确说明当前仍是单设备匿名个人空间，不展示虚构会员、青豆、支付档位或账户安全能力；正式账号、偏好、额度、安全审计、导出、账户删除、多设备和 BYOK 仍属于后续工作。
- 真实网页验证“管理资料库”和“进入 AI 研究”两个入口可用；AI研究的“对话研究 / 透明选股”可往返切换，透明选股继续读取真实李总策略覆盖率和确定性筛选结果。
- 桌面与 `390×844` 窄屏均已检查；个人中心指标自动从五列降为两列，卡片改为单列，六个一级入口仍保留。可交互按钮具备可见焦点样式，产品页面控制台无 error/warn；验收后已恢复浏览器正常视口。
- 定向回归 `uv run pytest -q tests/test_api.py` 为 `57 passed`；合并策略与 Agent 修复后，全量 `442` 项测试通过，`ruff check app tests scripts` 与内联 JavaScript 解析通过。

当前边界：个人中心是内部 MVP 的真实状态页，不是完整账户系统。COL-IA-01 已完成，但 COL-IA-08 和 P1-07 仍有偏好、额度、安全、审计、导出和账户生命周期等未完成项。

## 2026-07-23 李总策略深度口径、Agent 回答与自动刷新修复

- 全市场名单同步支持同一请求日期复用稳定快照，后台每个小批次不再重复消耗 Tushare 全市场请求；仍保留 `force=True` 的受控强制刷新能力。
- 市值达标股票只按上市日期预判“上市后量价窗口是否客观不足”。主动复核发现不能用“上市未满五年”推断五年 ROE 不可得，因为公开财务数据可能包含上市前年度；该情形现标记为 `history_unknown` 并进入真实深度查询，只有量价窗口明确不足才直接保存为数据不完整。中国电信 `601728.SS` 回归证明不足五年但量价历史充足的股票会进入完整规则执行，同日旧 `history_precheck` 状态也会重新排入深度队列。
- 名单状态覆盖和深度规则执行改为两个独立口径。最终浏览器样本显示全市场名单 `5530`、可深度核验 `1170`、深度处理 `753/1170`、量价历史不足 `46`、财务历史待实查 `154`；页面不再把约九成的名单状态覆盖率写成“全市场规则已完成”。其中“财务历史待实查”表示不能仅凭上市日期确认五年 ROE 是否可得，相关股票仍进入真实深度查询，不等同于财务历史已经完整。
- 真实 Agent 首轮测试主动发现仍只回答“已评估 5090/5530”，遗漏用户明确询问的深度口径。现已补充专属 Prompt、确定性摘要规范化和流式覆盖率混淆守卫；最终真实回答列出全市场名单 `5530`、名单状态 `5170/5530`、可深度核验 `1170`、深度处理 `733/1170`、上市后量价历史不足 `46`、财务历史待实查 `154`、候选或触发 `0`，并明确名单状态比例不是深度规则完成率，零候选只代表当前已深度处理范围且不构成买卖建议。该回答是生成时证据快照；同一页面随后自动更新到 `753/1170（64.4%）`，证明后台进度与历史回答快照能够正确区分。
- 前端新增 `stock_strategy_updated` 事件处理。透明选股页面保持打开时，真实进度从 `616/1016` 自动更新到 `626/1016`，无需刷新或重新进入页面。
- 主动复盘还发现长期停牌股票在“最近 400 个市场交易日”窗口内只能取得 `356` 根实际日线。现将市场日历缓冲扩大为 `520` 个交易日，并把日历回看期扩大到 `1100` 天；测试快照确认日线、复权因子和涨跌停价使用同一扩大窗口。
- 深度执行后仍缺数据的股票不会被猜补。主动核验发现 Tushare 会对个别年度缺少直接 ROE，而当前项目已有的东方财富 F10 结构化 Provider 能补齐大部分样本。现在仅对 Tushare 缺失的具体年度读取东方财富已报告加权 ROE，不覆盖已有年度；逐年证据保留公告日、来源、URL 和补充原因。两个来源仍都无值时继续标记数据不完整。
- 对当前 `0` 候选进行逐规则只读审计：在 `725` 只完整可判定股票中没有任何一条规则异常地拦截全部样本；最接近的江海股份、广合科技均通过 `8/9` 条，只因最近十日存在多次约 `-10%` 阴线而未通过。因此当前零候选是规则结果，不是明显的计算故障；若后续展示“差一条规则”的股票，只能定义为待观察样本，不能混入正式候选。
- 最终全量 `442 tests collected` 全部通过；Ruff 与内联 JavaScript 解析通过。重启后 `/health=ok`、Hermes 已启用、李总策略 Worker 正常，数据健康 `54/54`。

当前边界：全市场深度处理仍由后台持续推进，当前候选/触发为空不能外推为最终全市场结论；Tushare 与东方财富都缺少年度 ROE 时继续保持数据不完整，不能用模型推测补齐。

## 2026-07-23 今日观察聚合、首屏工作流与 ROE 第二来源

- 新增 `TodayOverviewService` 和 `GET /v1/today/overview`，统一聚合盘前/盘中/盘后/非交易日状态、四大指数、全市场广度、成交与涨跌结构、行业表现、用户观察任务、确定性研究行动、待确认判断草稿和连续研究变化。
- 聚合接口按组件独立降级：指数、广度或行业任一失败时，个人任务和变化仍能返回；公开响应只给出用户可理解的缺失说明，不暴露供应商异常。单元测试注入指数失败后，接口保持 `partial`，个人事项仍完整返回。
- 今日观察首屏新增“今日需要处理”和“与我相关的变化”两栏。待办固定按高风险、到期任务、判断草稿确认、证据缺口排序，最多 5 项，并显示排序原因；变化显示证券、事实时间、规则版本和结论边界。未完成事件白名单验收的公告、解禁、增减持等类别不会作为自动提醒展示。
- 真实网页验收主动发现同一股票、同一日期的变化重复展示，现改为同日同股合并，并优先保留包含实质变化的记录。随后又发现点击中际旭创事项时被旧的英伟达页面状态覆盖，现以 `pendingDeepStockSymbol` 固定跳转目标；真实复测正确选中 `300308.SZ` 并打开中际旭创研究空间。
- 桌面 `1280px` 实测首屏无横向溢出；今日事项和相关变化可直接进入股票研究空间，离开今日观察后首屏组件不再参与其他页面布局。当前盘前页面显示 5 项优先事项和 4 项同日同股去重后的相关变化。
- 李总策略新增 `eastmoney_reported_roe_v1` 第二来源：Tushare 直接年度 ROE 优先；只有必需年度缺失时调用现有东方财富 F10，且只接受公告日在策略数据日之前、带加权 ROE 的正式报告期。补充值不覆盖 Tushare，逐年保存公告日、来源、URL 和 `fallback_reason`，数据版本包含补充结果。
- 真实后台补齐后，页面显示深度处理 `808/1170（69.1%）`、数据待补从 `145` 降至 `135`；证明第二来源已进入真实 Worker，而不是只停留在单元测试。
- 最终全量 `446 tests collected` 全部通过；Ruff 与内联 JavaScript 解析通过；`/health=ok`，Hermes、后台任务和独立李总策略 Worker 正常。

当前边界：今日观察尚未完成正式事件白名单、风格指数、风险日程和科创50前端卡片替换；四时段已能正确识别并标注，但盘前日程、盘中延迟细则和非交易日周度摘要仍需后续完善。东方财富 ROE 只补其正式披露可验证的缺失年度，仍缺数据的股票继续保持 `data_incomplete`。

## 2026-07-23 操作计划、冻结上下文与即时交易复盘

- 新增操作计划领域服务和 `/v1` API。真实页面创建“减少持仓 100 股”计划，依次完成草稿、检查、保存，并在记录实际操作后推进为已执行；计划方向和关联计划会自动带入操作表单。
- 真实录入期初持仓 1000 股、成本 36、已知费用 5；关联计划记录 2026-07-16 减仓 100 股、成交价 40、费用 5 后，持仓确定性派生为 900 股，移动平均成本 36.005，已实现净结果 394.5。
- 操作时生成不可变上下文快照和交易复盘。系统读取 2026-07-17、07-20、07-21 三个后续交易日，复盘自动进入“可生成”；价格结果由数据库计算为第三个后续交易日复权收盘 34.880001、相对操作价变化 -12.80%。
- 第一次真实 Hermes Run 暴露两个问题：独立验收服务遗漏 `HERMES_ENABLED=true` 时只返回 preview；启用后模型已实际完成，但结构化 JSON 中两个证据外派生数字导致整行被守卫丢弃。前者通过正确启动配置解决，后者改为只删除越界子句并重新完整验守。
- 修复后的真实 Run `9a13efe8-9011-4f47-9083-549ea6b5768c` 为 `trade_review / completed`，模型 `deepseek-v4-pro`、Provider `deepseek`、守卫通过，修复方法为 `drop_unsupported_trade_review_clauses_v1`。最终逻辑结果来自本次即时生成，不是预存报告或固定模板。
- 页面先保存 AI 版本 1，再由用户改写逻辑结果形成用户版本 2，随后确认形成版本 3 并归档；刷新页面后仍恢复归档状态、用户版本和原确定性价格结果。AI 版本保存 `source_run_id`，用户版本不覆盖历史 AI 文本。
- 主动验收发现计划检查结果把 `passed/missing_items/checks` 原始结构直接显示给用户，现收敛为“检查通过；已记录目标数量、金额或仓位”。
- 桌面 `1280×720` 实际截图验收通过，页面无横向溢出。全量 `494 tests collected` 全部通过；`uv run ruff check .`、内联 JavaScript `node --check`、`git diff --check` 均通过。

当前边界：复盘中心尚未提供跨股票交易复盘列表和筛选；真实操作仍由用户手工记录，不连接券商、不自动交易。生产环境仍需 MySQL/Alembic、任务队列、Outbox/审计和通知。

## 2026-07-23 跨股票交易复盘中心与今日提醒

- 新增当前用户级 `GET /v1/trade-reviews` 聚合接口，跨股票返回真实交易复盘，支持 `status`、`symbol`、`q` 和 `limit`；非法状态返回 `422`，其他用户无法看到本用户复盘。
- “复盘中心”新增默认“交易复盘”标签，左侧支持关键词和状态筛选，右侧分开显示确定性价格结果、操作时记录、原计划、AI/用户逻辑版本、偏差标签和下一次改进；可继续生成 AI 草稿、编辑确认、归档或进入股票研究空间。
- 后续交易日已经齐备的 `ready` 复盘和等待用户确认的 `draft` 复盘会进入“今日观察”优先事项；顶部新增今日提醒入口，点击普通研究事项进入对应股票空间，点击交易复盘事项直接定位到跨股票复盘中心。
- 真实浏览器在 `8773` 验收：顶部提醒显示 5 条当前用户研究事项，点击中际旭创提醒后进入 `300308.SZ` 股票研究空间且提醒面板自动关闭；复盘中心默认选中“交易复盘”，当前无交易数据时展示真实空状态而非演示数据。
- 视觉验收：桌面 `1280px` 无横向溢出、控制台无错误；移动端 `450×910` 首次发现提醒面板左侧越界，修复为固定左右 `10px` 后面板边界为 `10px/440px`，不再裁切。
- 主服务重启时主动发现直接执行 `uvicorn` 未继承 Hermes 开关；现按项目一键启动脚本的正式环境口径恢复 `HERMES_ENABLED=true`，健康检查返回 `hermes_enabled=true`，文字路由固定为 `deepseek / deepseek-v4-pro`，未读取或写入密钥。
- 在 `/demo?qa=1` 隔离验收中按 Enter 发送“美股为什么收盘跌了？请基于当前可用行情和新闻，区分事实、可能原因、反方证据和下一步需要核验什么。”：页面识别为“美国股市 · 标普500”，自动加载最近收盘分钟 K 线，并即时生成价格事实、市场解释、反方证据和待核验项。Run `9ba5ae84-0083-4661-a57a-5853db80a1d6` 为 `market_brief / economy / completed`，Provider `deepseek`、Model `deepseek-v4-pro`、守卫通过、无错误；总耗时 `34.196s`，首个安全可见内容 `27.783s`。
- 全量 `495 tests collected` 全部通过；`uv run ruff check .`、内联 JavaScript 解析和 `git diff --check` 均通过。

当前边界：顶部提醒是由今日事项实时聚合的 MVP 入口，尚未实现持久化已读状态、邮件/短信/微信投递、用户订阅规则和通知审计；真实操作仍由用户手工记录，不连接券商、不自动交易。

## 2026-07-23 大盘 Agent 运行上下文与真实响应延迟优化

- 保留 `app/skills/market-brief/SKILL.md` 作为完整开发与审计规则，新增 `PROMPT.md` 作为运行时精简规则；`AgentService._load_skill` 对存在运行时版本的 Skill 优先加载 `PROMPT.md`，其他 Skill 保持原兼容路径。
- 市场资料库运行时上下文从最多 3 项收敛为 2 项，单项摘录从 800 字收敛为 500 字；在相同数量下优先保留用户资料和“市场涨跌原因 / 市场趋势与风险”等针对性规则，连续追问仍继承当前市场焦点。市场资讯不再传入 `engagement`，标题和摘要分别限制为 220/320 字；完整证据仍保留在 Run 证据文件和数据库中。
- 使用与上一轮完全相同的问题，在 `/demo?qa=1` 新建隔离对话并按 Enter 发送。新 Run `820f92ef-4a8a-4d57-abac-72ed4a26f9e7` 为 `market_brief / economy / completed`，Provider `deepseek`、Model `deepseek-v4-pro`，输出守卫通过、无错误。
- 前后对照：Prompt 从 `26,162` 降至 `17,853` 字节；输入从 `7,256` 降至 `5,308` tokens；首 Token 从 `26.015s` 降至 `14.245s`；首个安全可见内容从 `27.783s` 降至 `14.843s`；请求总耗时从 `34.196s` 降至 `23.684s`。原始 `evidence.json` 均为 `32,496` 字节，证明没有通过删除底层证据换取速度。
- 最终页面正确识别“美国股市 · 标普500”，加载最近收盘分钟 K 线；即时回答包含四个指数价格事实、三组市场资讯线索、成交/趋势反方证据和下一步财报、油价、市场广度核验。页面控制台无 warning/error，没有出现固定摘要替代模型回答或前后两段跳变。
- 全量 `498 tests collected` 并全部通过；`uv run ruff check .`、内联 JavaScript `node --check`、`git diff --check` 和 `uv lock --check` 均通过。运行中 `/health=ok`、`hermes_enabled=true`、数据健康 `54/54`。

当前边界：本次同题实测明显改善，但 Provider 侧推理时间会随问题和服务负载波动，不能把单次 `14.843s` 当成固定 SLA；下一阶段应继续优先优化真实用户路径和金融证据质量，而不是增加固定回复或更多低收益守卫。

## 2026-07-23 重要变化事件白名单与用户反馈闭环

- 新增公共 `change_events` 和用户 `user_change_links` 两层数据模型。公共事件保存证券、事件类型、事实时间、发现时间、来源、规则版本、去重哈希和数据状态，服务层再按冻结规则派生提醒严重度与结论边界；用户层独立保存已读、待判断相关性、与我有关和与我无关，任何用户反馈都不会修改公共事实。
- 当前自动白名单只启用两类：官方财务披露，以及最近一根完整日线绝对涨跌达到 `5%` 的价格异常。价格事件明确排除当天盘中报价，低于阈值不生成；停复牌、限售解禁和股东增减持因尚未完成逐类来源与规则验收，继续保持关闭。
- 新增 `GET /v1/changes`、`GET /v1/changes/{link_id}`、`POST /v1/user-changes/{link_id}/read` 和 `POST /v1/user-changes/{link_id}/relevance`。接口按当前用户隔离，重复刷新使用同一去重事件，其他用户访问详情返回 `404`。
- 今日观察、顶部提醒和股票研究空间统一读取同一用户事件对象。标记“与我无关”后事件不再进入待处理提醒，但历史事实仍保留；股票空间的“最新重要变化”与今日观察显示相同事实时间、规则状态和结论边界。
- 真实数据库生成并展示中兴通讯 `2026-07-22 +7.51%`、中际旭创 `-6.66%` 的完整日线事件，以及英伟达 SEC `10-Q`、`10-K` 官方财务披露。中兴事件详情正确显示 Yahoo Finance 来源、完整日线已确认、盘中报价排除说明和相关性反馈按钮。
- 真实浏览器在 `http://127.0.0.1:8773/demo` 验收：今日观察能打开统一变化详情抽屉；个股研究空间复用同一条中兴通讯事件；桌面页面无横向溢出。页面重载期间未捕获 `console` 或 `pageerror`，Codex 浏览器自身的 Statsig 网络超时不属于产品页面错误。
- 价格异常去重键冻结为“股票 + 完整交易日 + 规则版本”，不包含可能被校正的涨跌幅数值；同一完整交易日的数据校正会更新原事件，不会重复提醒。
- 自动化验证：全量 `503 tests collected` 并全部通过；`uv run ruff check .`、内联 JavaScript `node --check`、`git diff --check` 和 `uv lock --check` 均通过。运行服务 `/health=ok`、`hermes_enabled=true`、数据健康 `54/54`，后台 `change_event_refresh` 已启用。

当前边界：事件体系仍是严格白名单 MVP，不把媒体报道、盘中异动或尚未验收的公司行动自动升级为用户提醒；“与我有关/无关”只控制个人提醒与后续排序，尚未实现邮件、短信、微信投递、订阅规则和由变化直接创建用户观察任务。

## 2026-07-23 真实 Agent 观察任务写回与 Hermes 流式桥接修复

- 实际复现问题“分析中兴通讯当前最需要核验的证据，并把下一步保存为核验任务，供我确认”。新建对话并未与旧问题串线；旧 Run `d5dc4995-0e8a-458d-8ef4-0e26298630f3` 的真正故障是流式桥接导入项目 `app/utils.py`，覆盖 Hermes 自身顶层 `utils`，触发 `ImportError` 后回退到 oneshot。
- oneshot 保持了 Hermes 默认工具能力，模型因而在尚未经用户确认时就在个人工作区写入任务文件，随后因缺少当前报价时间锚点被输出守卫拒绝，页面只剩冗长的确定性摘要。该未确认文件已清理。
- 流式桥接现在于导入 Hermes 前移除脚本目录的模块影子。直接真实运行 `deepseek-v4-pro` 已返回 delta 和 final，不再出现 `atomic_replace` 导入错误。同时 oneshot 回退显式限定为空工具集，Prompt 明确要求模型只返回研究文本，不得写文件、数据库、记忆、任务或用户状态。
- 修复后真实网页 Run `47f5f698-8a32-40b4-87fd-f38fe5b474f2` 为 `stock_research / completed`，`streaming.enabled=true`，流式模式为 `guarded_cumulative_stream_v3`，错误为空。页面针对本轮问题生成了毛利率、财务费用、经营现金流和订单证据的可读回答，并明确“确认后才会生效”。
- 页面同时展示“AI 整理的观察任务·需要你确认”卡片。确认前正式任务不存在；点击确认后，candidate `7f1b8be9-4d6b-499c-8417-2aa8bc06159b` 变为 `confirmed`，并创建唯一任务 `8f7e3e04-6325-45c4-8ab9-2ba07d0e47b4`。中兴通讯股票空间的“今日待处理”已展示该任务，页面刷新后仍保留；同名待处理任务数量为 `1`。
- 浏览器实测 Enter 发送成功，新对话 URL 与问题、回答、K 线标的均一致；个股空间刷新后任务恢复，控制台无 error/warn。
- 最终自动化验收：`528 tests collected` 全部通过；Ruff、内联 JavaScript 解析、`git diff --check` 和 `uv lock --check` 通过。运行中 `/health=ok`、`hermes_enabled=true`、数据健康 `54/54`。

当前边界：观察任务内容目前由结构化 Claim 的下一证据项聚合，仍可以继续做问题聚焦与优先级压缩；生产版需将候选确认、正式任务创建和通知 Outbox 收敛为单事务或可补偿流程。

## 2026-07-23 问题驱动 ResearchPlan、股东守卫与首页阅读顺序

- 新增问题驱动的 `ResearchPlanService`。个股问题先识别行情原因、财务与现金流、股东、主营、分析师预期、事件、估值同行或综合研究，再只加载相关证据模块与 Skills；价格、当前资讯和最新估值每轮刷新，报告期型稳定证据按模块时效复用。单模块失败返回 `partial`，不会丢弃其他已取得证据。
- 行情原因问题固定加载行情、财务、公司资讯、事件与历史条件走查，不再额外加载分析师预期；“现在值得继续研究什么”不再被“现在”误判为行情问题，会进入完整综合研究。A 股行业标签优先读取已加载分析师预期或 Tushare 策略快照，并为默认研究标的保留目录级行业兜底。
- 旧股东 Run `bfd1fb04-788e-4aa8-a35e-e36176e31ff3` 把“不能等同北向资金”“不能当作实时持仓”反向误判为违规。现按分句识别明确否定边界，同时防止“不是实时持仓，而是当日资金流”这类反转表述绕过。真实网页 Run `0eaba169-9bd5-4408-9dea-b7e9382cd7d9` 为 `shareholder_structure / completed`，Provider `deepseek`、Model `deepseek-v4-pro`，输出守卫通过，页面保留股东户数变化、报告期边界、北向资金边界和下一次核验项。
- 行情真实 Run `7a6fa40a-e21d-4ac4-8bfa-0a381225ac6c` 为 `stock_research / completed`，Provider `deepseek`、Model `deepseek-v4-pro`，输出守卫通过；当前报价 `35.92` 已正确写成低于 MA20 `37.28` 与 MA60 `37.30`，并保留当天完整日线尚未入库的时间边界。输入 `16,660` tokens，首个安全可见内容 `22.156s`，总耗时 `29.133s`。
- 浏览器复验继续发现“公告引发调整”“个股自身因素”“获利回吐与基本面隐忧共振”“独立表现”“普跌解释解释”等没有直接证据或影响阅读的措辞。运行时清理与最终守卫现统一降级为“相对表现事实、候选线索、具体驱动未确认”，上述真实句式均已加入回归测试。
- 首页真实顺序调整为“全球行情 → A 股全景 → 研究与市场洞察 → 继续问清楚 → 我的研究待办与重要变化”。个人待办仍可用，但不再挡在用户浏览行情、洞察和发起研究之前。
- 最终自动化验收：`549 passed`；`uv run ruff check .`、`uv run python -m compileall -q app tests`、`uv lock --check` 和 `git diff --check` 全部通过。打包测试会忽略编译检查生成的 `__pycache__/*.pyc`，不再把本地编译产物误认为产品静态资产。最终服务 `/health=ok`、Hermes 已启用、数据健康 `54/54`。

当前边界：大模型首个可见回答仍受 Provider 推理时间影响；“为什么涨跌”的具体因果只有在同日官方归因或可验证事件研究存在时才能升级为确认结论，否则必须保持候选线索与未确认边界。

## 2026-07-23 统一操作计划与交易复盘候选写回

- `StructuredAIService` 的股票研究范围从单一 `stock_research` 扩展到财报质量、利润现金流、主营、股东、分析师预期和事件等专项 Run。专项模块现从既有确定性结论生成同一份 Claim Ledger，并保留报告期、来源、限制和下一步核验，不再因为路由到 `earnings_quality` 等意图而跳过结构化卡片。
- `candidate_type` 现统一支持 `thesis`、`observation_task`、`action_plan` 和 `review_draft`。操作计划与交易复盘都先创建 `pending_confirmation` 候选；确认前不修改正式领域对象，确认后复用原领域服务，重复确认幂等，版本冲突会把候选标记为 `stale`，跨用户读取和确认返回 `404`。
- 操作计划候选只使用用户本轮明确给出的核验条件。模型运行提示要求触发条件逐字保留用户原句，不得用“即、例如、或、且”等扩写比率、比较基准或技术价位；确定性候选同时把目标数量、金额和仓位固定留空。
- 真实浏览器在 `http://127.0.0.1:8773` 用 Enter 发送“分析中兴通讯并生成操作计划，核验条件：如果下一期经营现金流继续恶化，由我重新评估是否减仓；不要填写目标价、数量或仓位。”。Run `004182f2-12d0-4cdd-a5db-1f80caa93039` 为 `earnings_quality / completed`，页面即时生成针对性财务回答和 5 项可展开引用，并显示待确认操作计划卡。
- 确认前当前用户的 `action_plans` 为 0；点击确认后 candidate `eeb6f8e0-d30d-4875-96fb-88b1dd330a4d` 变为 `confirmed`，创建计划 `24c04e15-0448-4411-a026-d457aa5ffde1`。计划只包含用户原条件，`target_quantity`、`target_amount`、`target_position_percent` 均为空；刷新原对话仍显示已确认，并可在中兴通讯“任务与操作”页恢复为“减少持仓 · 草稿”。
- 复盘 API 回归覆盖：`generate-draft` 只返回 `review_draft` 候选，确认前 `current_version` 为空；确认候选后才保存 AI 版本并关联原 `trade_review / completed` Run，之后仍可由用户编辑、确认和归档。
- 全量 `553 tests collected` 全部通过；Ruff、`compileall`、内联 JavaScript、`uv lock --check`、`git diff --check` 和敏感文件检查全部通过。运行中 `/health=ok`、Hermes 已启用、数据健康 `54/54`。

当前边界：候选确认与正式领域写入在 MVP 中已经可恢复和幂等，但生产环境仍需把跨表确认收敛为单事务或补偿事务，并补齐 Outbox、正式通知和失败模块独立重试。

## 2026-07-24 诊断一级入口、唯一股票会话与 Hermes 失败恢复

- AI 研究新会话新增 `agentEntryHub`，提供“诊大盘、诊个股、智能选股”三个高频入口。用户发出第一条问题、打开历史对话或进入股票内嵌 Agent 后，入口自动隐藏。
- 大盘入口调用 `startNewConversation()` 后才运行 `sendChat(marketDiagnosisPrompt)`，避免把大盘问题写入此前股票绑定会话。真实浏览器创建会话 `dd38f4ad-5c83-4226-ba17-eca3da2e2891`，约 17 秒完成；页面识别“中国A股 · 上证综指”，展示上证综指 K 线、全市场上涨/下跌家数、行业涨幅、数据时间、反方边界和 9 条引用。刷新后同一会话、诊断侧栏和引用恢复。
- 个股入口调用 `/v1/search` 精确解析。输入“中”返回中兴通讯 `000063.SZ`、中际旭创 `300308.SZ`、中关村 `000931.SZ` 并要求补全，没有静默选择。输入“贵州茅台”解析为 `600519.SS`，复用 `/me/deep-stock` 唯一会话，进入 `/stocks/600519.SS?tab=ai` 后自动发起 Hermes。
- 贵州茅台真实回答约 13 秒完成，包含 2026-07-24 09:20:30+08:00 最新报价、2026-07-23 完整日线、2026 一季报、估值口径、公告、分析师预期、反方证据、失效条件和 11 条引用；不输出目标价或买卖建议。等待股票空间异步恢复完成后刷新，URL、唯一会话、回答、引用与 K 线标的全部恢复。
- “智能选股”入口已验证进入 `/research?mode=screening`，继续使用透明确定性规则，不把 Agent 当作黑盒股票排名器。
- `DeepStockResearchService.observe_chat()` 对 `market_brief` 增加早返回。回归测试确认股票阶段、覆盖快照、覆盖历史和 `latest_run_id` 均不被市场证据修改。
- 前端在 Hermes 未连接时不调用 `/me/chat` 生成基础预览，不保存确定性摘要冒充 AI 回答；失败节点明确说明没有形成 AI 结论，并提供“重新用 Hermes 研究”。非 `completed` 回包显示降级边界并保留整轮重试。
- 使用端口 `8774` 启动独立 `HERMES_ENABLED=false BACKGROUND_JOBS_ENABLED=false TUSHARE_ENABLED=false` 服务进行真实浏览器故障注入。失败卡和重试按钮可见；重试重新读取健康状态、复用原问题，DOM 始终只有 1 条用户问题和 1 条当前失败回答。临时服务已正常关闭，主服务未受影响。
- 全量 569 项测试全部通过；Ruff、`compileall`、内联 JavaScript 解析、`uv lock --check`、`tests/test_portability.py` 和 `git diff --check` 均通过。

当前边界：整轮模型失败和守卫降级已有清楚的用户状态与重试；失败证据模块仍没有独立重试 API。全局搜索尚未实现拼音简称；市场复盘仍不是带版本、状态和后续验证链的正式结构化对象。

## 2026-07-24 PostgreSQL 生产主库与持久化任务队列

- 业务仓储 `Database` 新增 PostgreSQL 兼容连接池和方言适配，保留 SQLite 本地模式；用户、会话、自选股、对话消息、Run 和后台运行记录已在本机 PostgreSQL 17 真实读写通过。
- 业务库和运维库分别记录 `domain_schema_migrations` /
  `qingshu_schema_migrations` 版本；`/health.storage` 返回两个后端和版本。
- `OperationalDatabase` 已实现 SQLite/PostgreSQL 双后端的幂等入队、持久化周期计划、优先级、原子租约、`FOR UPDATE SKIP LOCKED`、心跳、租约过期恢复、指数退避、最大次数失败归档、取消、人工重试与队列健康统计。
- 跨进程事件写入 `persistent_events`，Web 端用单一轮询线程向所有 SSE 订阅者广播；独立进程探针已验证直接写库后 `/events` 收到 `cross_process_probe`，不会把内部序号暴露给前端。
- 18 类原进程内 `time.monotonic()` 后台刷新和李总策略已迁入持久化队列；本地可使用 embedded Worker，生产使用 `python -m app.worker` 独立进程并支持多 Worker。
- 管理 API 仅在有效用户会话和 `X-Qingshu-Admin-Token` 同时存在时开放，支持查看、手工幂等入队、取消和重试，不在用户网页展示。
- Docker Compose 更新为 PostgreSQL + Web + Worker 三服务，Web 不执行后台任务，Worker 与 Web 共用 PostgreSQL 和工作区卷。
- 新增只读源库迁移工具 `scripts/migrate_sqlite_to_postgres.py`。真实将仓库 52MB SQLite 样本的 67 张表迁移到 PostgreSQL：45 张非空表、18,107 行，逐表行数一致；迁移后应用 `/health=ok`、数据健康 `healthy`，可继续创建用户。
- 迁移工具新增 `--source-workspace-root`。提供后会把用户、上传和 Run 的绝对
  `workspace_path` 按相对目录重写到目标 `--workspace-root`；路径越界立即失败。
  源库存在路径时必须显式选择重写或 `--preserve-workspace-paths`，不再静默保留。
  文件复制仍保持显式步骤，避免只迁数据库后留下旧机器绝对路径。
- SQLite 队列/后台专项 12 项通过；真实 PostgreSQL 专项 5 项通过，覆盖多 Worker 单次抢占、重启后租约恢复、周期计划、失败重试、跨进程事件、业务核心读写和 SQLite→PostgreSQL 迁移。
- 常规全量回归为 `601 passed, 5 skipped`；5 项跳过均为需要
  `QINGSHU_TEST_POSTGRES_URL` 的 PostgreSQL 集成测试，并已在本机真实
  PostgreSQL 17 上单独全部通过。Ruff 当前通过。
- 新代码重启后的 8773 服务显示业务库 schema v1、运维库 schema v2、embedded Worker 正常、异常退出遗留本机租约自动回收、队列无积压、数据健康 54/54；浏览器 Runner 再次 `12 passed / 0 failed`。

当前边界：结构化业务数据和任务状态已支持 PostgreSQL，但用户工作区文件仍在共享卷；生产多机还需对象存储/共享文件系统、数据库备份恢复演练、指标告警、正式认证、staging 与回滚演练。任务语义为 at-least-once，处理函数需保持幂等。

## 2026-07-24 自选股每日研究摘要、报告审计阅读与即时 Hermes

- `StockAssetListService` 现在为每个当前用户股票空间返回报告元数据与 freshness、报价/完整日线/财务报告期/报告时间、最多三条反方证据、失效条件、下一证据，以及最新用户变化的 `link_id/read_at/handled_at/relevance_status`。公共报告明确标为“服务器公共证据快照”，用户判断和反馈状态继续隔离。
- 回归创建 1 只当前用户自选和 25 只非自选报告；`GET /v1/stock-workspaces` 仍只返回该用户的 1 只股票及其正确报告。测试同时把 `ResearchReportService.generate()` 替换为失败函数，证明列表 GET 不触发同步报告生成。
- “我的关注”不再请求全局 `/research-reports?limit=20/100`。首屏“今日研究更新”直接显示每只资产的 `completed/partial/failed/missing` 状态、报告生成时间、完整日线日期、公共证据边界与最新变化；桌面真实页面显示 4 只当前关注股票，数据来自同一用户空间。
- “阅读报告”改用 `/v1/stocks/{symbol}/workspace`。真实点击中兴通讯后，阅读器显示服务器报告正文，并追加“证据覆盖与来源、反方证据、失效条件、下一步证据、公共快照边界”；元数据明确为“生成于 07/24 09:44 · 完整日线截至 2026-07-23”，不会把两个时间混成一个“数据时间”。
- 卡片可直接标记变化已读、与我有关或与我无关。服务/API 回归验证保存后刷新仍恢复 `read_at/handled_at/relevance_status`；真实浏览器未替当前用户执行不可逆反馈操作。
- 首轮真实“让 Agent 即时汇总”被旧关键词误路由到 `research_actions`，虽即时生成却只写单一“数据截止时间”。新增专属日摘要路由、用户范围 `research_assets` 证据与 Agent 时间合同后重新运行。
- 最终会话 `0b9cb63d-d037-447f-a42b-c4d008a8daca` 为新的 Hermes 生成结果：覆盖杰瑞股份、中兴通讯、英伟达、中际旭创；每只均单列最新报价及市场时区、最近完整日线、最新财务报告期，并给出研究顺序、反方证据或压力、失效条件与下一任务。页面显示 5 项证据引用，不含目标价或买卖指令。
- 刷新 `/research/0b9cb63d-d037-447f-a42b-c4d008a8daca` 后，问题、完整回答、三个时间锚和引用数量均从历史会话恢复；页面同时提示历史回答保留生成时证据，需用新问题获取最新行情。
- 最终完整回归：`572 tests collected`，全部通过；`ruff check .`、`compileall`、内联 JavaScript 解析、`uv lock --check`、`tests/test_portability.py` 与 `git diff --check` 通过。
- 最终服务进程 PID `35960`；`/health=ok`、`hermes_enabled=true`、数据健康 `54/54`，通用后台与李总策略 Worker 均运行。

当前边界：现有后台报告多数处于 `degraded/partial`，页面按实际状态展示，不能宣传为完整 Agent 报告；服务器报告是公共证据快照，用户私有判断和任务不会写入公共报告。当前下一工作包是阶段 0 权威口径统一，并审计/封口包含真实操作与复盘的完整 R1 E2E。结构化市场复盘后置到 R2；单证据模块独立重试进入后续“选股与个股分析可信度”阶段。

## 2026-07-24 PostgreSQL 备份恢复与队列生产可观测性

- 运维库升级到 Schema v3，新增持久化 Worker 注册表和运维计数器。每个 Worker
  线程保存主机、进程、队列、启动时间、最近心跳、当前任务与累计领取/成功/失败数；
  正常退出标记 `stopped`，心跳超时标记 `offline`，历史离线记录保留 7 天。
- 运维库随后升级到 Schema v4，周期计划新增配置启用、人工暂停和暂停时间。管理员
  可列出、暂停和恢复计划；Worker 重新注册计划时不会覆盖人工暂停，恢复时仍服从
  当前配置是否允许执行。
- 队列健康新增 ready/delayed/retrying、最老 ready 任务延迟、过期运行租约、
  最近 24 小时成功/失败数和失败率，以及租约恢复、死亡本机 Worker 恢复累计数。
  ready 任务没有活跃 Worker、存在过期租约或排队延迟超阈值时不再误报 `ok`。
- 数据健康任务同步执行终态队列保留：成功/取消默认保留 7 天，失败归档保留 30 天；
  清理只匹配带完成时间的终态任务，不删除 queued/running，防止高频计划长期运行
  后队列表无限增长。业务库后台成功审计和数据健康快照保留 30 天，失败审计保留
  90 天，running 审计不清理。
- 新增管理员专用 `GET /admin/operations/health`，聚合业务库/运维库 Schema、完整
  队列指标、Worker 列表和备份新鲜度；继续要求有效用户会话与管理员 Token，不在
  用户网页展示。
- 新增 `scripts/check_operations.py`，统一检查 PostgreSQL 要求、两个 Schema、
  活跃 Worker 数、队列延迟、过期租约、24 小时失败率和备份新鲜度；失败返回非零
  退出码。Worker 容器健康检查改用该真实运行条件，不再只验证数据库能连接。
- 新增 `/ready` 就绪探针：需要后台任务时若没有活跃 Worker，或 Schema、队列超过
  生产阈值，返回 HTTP 503；`/health` 继续只承担 Web liveness。Compose Web 健康
  检查已切到 `/ready`。
- 新增 `scripts/postgres_backup.py`：使用 `pg_dump` custom format 原子写入，
  密码只经子进程环境传递，不进入命令行；生成 SHA-256 manifest、`latest.json`，
  支持保留天数、最少份数、周期运行与备份过期健康检查。归档发布前必须通过
  `pg_restore --list` 解析，损坏文件会删除且不会覆盖最近成功备份。
- 新增显式确认恢复 `scripts/postgres_restore.py` 和非破坏性日常演练
  `scripts/postgres_restore_drill.py`。演练在同一服务器创建随机临时库，校验
  checksum，执行 `pg_restore`，核对核心业务/队列表、两个 Schema 版本和关键
  行数，最后强制清理临时库。
- Docker 镜像加入 PostgreSQL client；Compose 新增 `qingshu-backup` 服务和独立
  `qingshu-backups` volume。备份服务启动即备份，默认每 24 小时运行、保留 14 天
  且至少 7 份；Web 只读挂载备份卷用于管理员健康检查。
- 业务库与运维库初始化分别增加 PostgreSQL 事务级 advisory lock。Web 和多个
  Worker 同时启动时，Schema DDL 串行执行；专项测试用三个独立连接池并发初始化，
  验证锁覆盖整个迁移事务。
- Compose Web 端口改为 `QINGSHU_HTTP_PORT` 可配置，并新增
  `staging.env.example`。Staging 使用独立 Compose project、18000 端口和独立
  PostgreSQL/工作区/备份卷，避免验收数据污染生产。
- PostgreSQL 连接池从每进程硬编码业务 20 + 运维 10，调整为可配置的保守默认值
  业务 8 + 运维 4；运维报告返回连接池统计。按 Web + 两个 Worker 估算，默认连接
  上限由 90 降至 36，给 PostgreSQL 管理连接和备份恢复留出余量。
- 使用本机 PostgreSQL 17 完成最新真实演练：备份 202,254 bytes，归档解析与
  manifest checksum 一致；恢复到随机临时库后识别 74 张表，业务 Schema v1、
  运维 Schema v4，演练状态 `passed`，临时库已删除。
- SQLite 持久化队列与备份工具专项通过；真实 PostgreSQL 业务库/Worker/队列专项
  7 项通过。全量收集 626 项，常规环境 `619 passed, 7 skipped`；Ruff、compileall、
  diff 检查和 Compose YAML 解析通过。7 项跳过均需独立 PostgreSQL，已在本机
  PostgreSQL 17 上单独真实执行。

当前边界：数据库结构和任务状态已有自动备份与真实恢复演练；用户工作区文件仍需
对象存储或共享卷快照。当前没有接入外部告警平台，管理员健康接口已提供告警所需
数据，下一步部署时可由 Prometheus/云监控采集并设置延迟、失败率、Worker 离线和
备份过期阈值。

## 2026-07-26 李总策略首次加载即时反馈

- 根因是无历史缓存时，前端在候选与历史接口完成之前隐藏整个李总策略面板；后台刷新约需数秒时，用户会看到空白，旧浏览器门禁也会在等待面板可见时超时。
- `liZongPanel` 现在使用 `idle/loading/ready/error` 状态。切换到李总策略后立即显示面板和“正在读取最新策略结果”，请求失败时保留页面与重试入口；已有结果刷新失败时继续展示最近结果。
- 浏览器 Runner 先验证面板即时可见，再等待真实结果进入 `ready`，并记录两段耗时。实测桌面为 `0.091s / 2.899s`，390px 为 `0.079s / 3.389s`。
- 1280×720 与 390×844 的今日观察、透明选股、个股概览、个股历史、复盘中心和 AI 研究共 `12 passed / 0 failed`；控制台错误、页面异常、非预期失败响应和页面级横向溢出均为 0。报告：`tmp/browser-smoke-20260726-li-zong-first-load/result.json`。
- 完整回归收集并通过 `686` 项；Ruff、Python `compileall`、内联 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。
- 当前 `/health=ok`、Hermes启用，领域库 PostgreSQL Schema v2，运维库与持久化队列 Schema v4，独立 Worker 活跃，队列无积压，数据健康 `53/54 healthy`、`1 attention`、`0 critical`。
- `scripts/check_operations.py` 当前唯一降级项为本机 `~/.qingshu/backups` 缺少最新备份；这不影响本次页面修复，但仍是生产就绪门槛，不能把 liveness 通过表述为完整生产就绪。

## 2026-07-28 个股研究信息架构、复盘与个人中心渐进披露

- 真实390×844页面复验：个股概览四类核心卡片宽344px，无横向溢出；证据正文12px；七阶段流程不在概览显示。
- 真实390×844“AI研究”复验：绑定对话节点位于七阶段流程之前，页面无横向溢出。
- 复盘中心顶部英文 `REVIEW` 已改为“研究复盘”；桌面与390px浏览器门禁均通过。
- 个人中心内层标题改为“我的研究空间”。账户标识、服务状态、后台刷新、隐私与版本边界收进默认关闭的“服务、隐私与版本说明”；展开后字段12px、说明11px，桌面与390px均无横向溢出。
- `scripts/browser_smoke.py` 新增个人中心长期检查；最终 `artifacts/browser-smoke-w6-information-architecture/result.json` 为18通过、0失败。
- `uv run --extra dev pytest -q`：`792 tests collected`，全量通过；唯一提示为Starlette TestClient上游弃用警告。
- `uv run ruff check .`、`uv run python -m compileall -q app tests scripts`、全部 `app/static/*.js` 的 `node --check`、`uv lock --check` 与 `git diff --check` 全部通过。
- 运行态：`/health=ok`、Hermes启用、领域库Schema v5、运维库Schema v4、外部Worker 2个；队列ready/delayed/retrying均为0，数据健康53/54、0 critical。

## 2026-07-28 W22 条件选股渐进披露与真实首次路径验收

- 真实进入 `/research?mode=screening` 并执行默认模板，生产数据库返回12只候选。旧页面每张卡同时铺开6项指标、3条重复入选理由和2个动作；锐捷网络真实结果为近20日 `+43.44%`、行业超额 `+65.87%`、PE TTM `204.33`，用户需要自己从密集数字中识别风险。
- 四个模板的前端名称改为“近期强于行业（波动可能较大）”“经营指标开始改善”“估值处于约束范围”“回撤后等待确认”，选择区同步说明各自适合什么时候使用；后端确定性规则、排序和候选合同没有改变。
- 候选卡首层按模板只显示3项核心指标：趋势使用20日收益、行业超额与PE；经营改善使用营收同比、净利润同比与ROE；估值模板使用PE、PB与总市值；回撤模板使用20日收益、5日收益与量比。
- 每张卡新增基于已取得事实的“先核验”说明，不生成推荐、评级或综合分。完整行情、估值、已取得财务指标、逐条入选理由和字段级缺口进入默认关闭的“查看完整数据与入选依据”；左侧实际规则也默认关闭并显示规则条数。
- 浏览器Runner新增确定性候选夹具，不写数据库、不调用选股POST接口，但在桌面和390px都实际渲染已填充候选卡，并验证每卡3项核心指标、详情和规则默认折叠、完整数据与3条入选理由仍存在、模板用途与核验提示可读、390px候选卡宽度不低于300px。
- 人工桌面真实数据截图和自动化桌面/390px已填充截图均完成视觉检查；390px候选卡三项指标保持单行，核验提示和两个下一步正常换行，无页面级横向溢出。
- `artifacts/browser-smoke-w22-screening-disclosure-final/result.json` 为桌面与390px十个核心页面 `20 passed / 0 failed`；控制台错误、页面异常和非预期本地失败响应均为0。
- 完整回归收集并通过 `822` 项；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check` 和 `git diff --check` 全部通过。运行态 `/ready=ok`，PostgreSQL领域库Schema v5、队列Schema v4，2个Worker active，ready/delayed/retrying均为0。

当前边界：本轮只改善候选解释和阅读顺序，没有放宽规则、增加黑盒评分或把历史强势包装成推荐。选股结果仍只是下一步研究对象；用户进入个股空间后仍需核验财务、公告、行业变化和反方证据。
