# 清数智算后端运行流程与回答上下文

## 一张图看懂系统

\`\`\`mermaid
flowchart LR
    subgraph Sources[外部与用户数据]
        S1[全球指数与个股行情]
        S2[A股公告 财报 新闻 社区]
        S3[股东 研报统计 SEC披露]
        S4[用户自选 关注理由 上传资料]
    end

    subgraph Ingestion[确定性采集层]
        I1[证券代码与市场标准化]
        I2[时间 交易日 复权 口径校验]
        I3[来源优先级 去重 缓存 降级]
    end

    subgraph Storage[持久化层]
        D1[(SQLite金融数据库)]
        D2[(通用资料库)]
        D3[(用户专属工作区)]
        D4[(历史对话 Agent Run 研究报告)]
    end

    subgraph Analytics[算法与金融 Skills]
        A1[行情指标<br/>收益 均线 RSI MACD ATR 波动 回撤]
        A2[财务分析<br/>财报质量 利润桥 现金流 主营结构]
        A3[公司信息<br/>股东变化 券商预期 事件 情绪]
        A4[市场分析<br/>指数 板块 广度 行业成分与贡献]
    end

    Q[用户问题] --> R[意图与标的识别]
    Sources --> Ingestion --> Storage --> Analytics
    R --> T[选择工具与 Skills]
    Analytics --> T
    Storage --> C[上下文组装]
    T --> C
    Q --> C
    C --> H[Hermes Agent]
    H --> G[数字 方向 时间 策略边界守卫]
    G --> O[网页回答与图表]
    G --> D4

    B[后台调度器] -.定时刷新.-> Ingestion
    B -.预生成.-> Analytics
    B -.发布洞察与变化提醒.-> O
\`\`\`

## 后台长期运转

后台调度器负责持续刷新，而不是等用户提问后才临时找全部资料：

1. 高频刷新正在交易市场的分钟行情并写入数据库。
2. 定期刷新默认股票和全部用户自选股的公告、新闻、社区情绪、财务、股东和研报统计。
3. 使用已经落库的数据运行财报质量、利润现金流、事件分类、研究变化和市场文章算法。
4. 为中兴通讯、中际旭创、英伟达及用户自选股预生成研究报告，供网页直接打开。
5. 将新证据、研究报告和历史变化写入资料库，使后续 Agent 对话能够再次检索，而不是一次性使用后丢失。

## 一次用户回答包含哪些上下文

系统不会把所有数据无差别塞进模型。每次回答按问题动态选择以下内容：

| 上下文 | 具体内容 | 作用 |
|---|---|---|
| 当前问题 | 用户原话、识别到的市场或证券、意图、研究深度 | 决定这次回答到底要解决什么 |
| 对话上下文 | 当前历史对话最近的有效消息、已绑定深度诊股阶段 | 支持“那它呢”“继续分析风险”等追问 |
| 实时证据 | 当前行情、历史 K 线、指标、财务、股东、公告、新闻、研报统计、事件 | 给回答提供可核验事实和时间锚点 |
| 用户上下文 | 自选股、关注理由、用户确认的长期记忆、个人资料库 | 形成个性化研究，而不是通用模板 |
| 通用资料 | 金融方法、公司长期资料、预生成报告、过去证据变化 | 复用已经完成的研究成果 |
| Skills | 市场、个股、财报质量、利润现金流、股东、事件等专门分析规则 | 规定该类问题应读取什么、怎样分析和怎样表达 |
| 回答约束 | 数字校验、方向校验、时间校验、来源边界和禁止项 | 降低编造、过度推断和不当建议 |

## 数据、算法、大模型与 Agent 的分工

- 数据工具负责“取得事实”：价格、成交、财务字段、公告、新闻和股东记录。
- 确定性算法负责“计算与归类”：收益率、技术指标、财报质量、利润桥、情绪分数和事件标签。
- 数据库负责“长期保存”：行情、结构化证据、用户资料、历史对话、研究报告与 Agent Run。
- Hermes 大模型负责“语言综合”：针对用户问题解释事实、连接证据、提出反方证据和下一步核验点。
- Agent 负责“组织工作”：识别问题、选择工具和 Skill、检索资料、组装上下文、调用模型、保存结果。
- 守卫负责“最后校验”：不允许模型编造证据外数字、反转涨跌方向、混淆时间、输出目标价、收益承诺或交易指令。

## 回答质量原则

当前阶段采用克制原则：

1. 能确认的事实直接说，并附数据时间。
2. 只有线索、没有完成交叉验证时，不把线索升级为确定原因。
3. 证据不足时缩短回答，给出已经确认的部分和最关键的待核验点。
4. 不用“大概率”“必然”“主力正在”等缺乏可核验证据的说法制造确定感。
5. 页面不向普通用户显示供应商报错、降级链路和内部调试状态。

## 当前边界

- 免费公开数据没有生产级 SLA，专业实时行情仍需授权数据源。
- 行业供需、客户订单、机构持仓和长历史一致预期仍需更多可靠或授权资料。
- Agent 能改善解释和检索，但不能把缺失的数据变成事实。
- 产品只提供研究辅助，不生成目标价、收益承诺、确定性涨跌概率、买卖或仓位指令。
