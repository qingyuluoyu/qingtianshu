# 同事 3.3 代码库吸收指南

更新时间：2026-07-26
用途：交给主 Agent，指导对同事最新 `3.3 realyy.zip` 的分析、筛选和安全吸收。

## 1. 当前权威边界

本项目的权威代码库是：

`/Users/chr/Documents/qingtianshu`

当前工作分支：

`codex/structured-ai-writeback`

当前基线提交：

`97d3464 feat: add durable schedule controls and postgres CI`

主 Agent 必须以本目录为唯一实现基线。不要把旧目录 `/Users/chr/Documents/青树金融交易/qingshu-agent-demo` 或 GitHub `ruizhi` 分支当作当前权威实现。

同事资料：

`/Users/chr/Documents/qingshu_ruizhi_ziliao/20260725/3.3 realyy.zip`

同事压缩包包含完整 Git 历史、`.env`、`.venv`、`data`、`.runtime`、日志和缓存。它是运行快照，不是干净源码包。禁止直接解压覆盖权威库，禁止读取、复制或提交 `.env` 与运行数据。

## 2. 已确认的同事实际成果

同事分支内部当前为 `feat/frontend-polish`，基于 `zhuowoshuang/qingtianshu` 的 `v3.4` 线。相对于其 `origin/v3.4`，实际代码变更主要有以下几类：

### 2.1 图表和前端交互

实际代码已包含：

- `app/static/qs-charts.js` 图表引擎抽离；
- K 线拖拽平移历史窗口；
- K 线十字光标、OHLC 悬浮详情；
- 价格图与成交量图联动参考线；
- MA 均线按全量 K 线计算后再切片，修复缩放左边缘错误；
- 雷达图标签按角度对齐、画布边界钳制和数值标注；
- 涨跌颜色集中为图表常量；
- `high-fidelity-demo.html/js/css` 的高保真页面和视觉布局。

这是同事版本最值得吸收的部分，但应作为前端能力移植，不能覆盖当前交易复盘页面。

### 2.2 研究任务队列基础

实际代码已包含：

- `app/migrations/0005_research_task_reliability.sql`；
- `app/services/research_queue.py`；
- `app/services/research_worker.py`；
- `app/research_worker_main.py`；
- Redis Stream 发布基础；
- 数据库 outbox、任务租约、重试基础；
- 研究任务迁移和队列测试。

这部分有架构参考价值，但实现基于同事的 SQLite 体系，不能直接替换当前 PostgreSQL durable worker。

### 2.3 既有业务能力的展示型整合

同事快照还带有或整合了以下模块：

- AI Research；
- 今日看板；
- 个股评分库；
- 个股研究工作台；
- 我的关注；
- 股票复盘；
- 市场复盘；
- Tavily 搜索；
- 五维分析 Skill；
- 李总策略；
- 研究报告、同行比较、投资组合和交易复盘基础服务。

这些能力不能仅凭文件存在就判定为生产完成。必须区分“文件存在”“接口可用”“测试通过”“真实数据跑通”“端到端完成”。

### 2.4 设计文档多于实现代码

同事最新提交中有大量设计和计划文档，主要包括：

- 前端第三轮专业化打磨；
- 个股研究工作台；
- 今日看板数据对齐；
- 个股评分性能和布局；
- 自选股—交易复盘生命周期；
- 独立 AI Skill 工作区；
- 生产级研究执行；
- 后端生产可靠性。

这些文档应作为需求参考，不得直接标记为“已经实现”。

## 3. 我们当前已经领先的能力

权威库已经具备或正在具备：

- PostgreSQL 持久化和兼容层；
- durable schedules、Worker、任务租约、重试、心跳；
- 备份、恢复、就绪检查、运维观测和 PostgreSQL CI；
- 持仓台账与操作流水；
- 交易上下文快照；
- 交易复盘草稿、用户编辑、确认、归档；
- 结构化 AI 写回及 `pending_confirmation` 用户确认边界；
- 李总策略参数化运行；
- 李总策略无前视历史回放；
- 个股页面、证券主数据、价格变动分析；
- 全局搜索；
- 今日总览和研究计划；
- 观察任务、研究行动和结果复盘；
- R1 交易复盘 E2E 测试。

因此，吸收目标是“增强展示和补齐可复用基础设施”，不是重建后端业务主链。

## 4. 建议吸收矩阵

| 同事能力 | 建议 | 吸收方式 | 禁止做法 |
|---|---|---|---|
| `qs-charts.js` 图表引擎思想 | P0 | 抽取为当前前端的独立图表模块，保留当前 API 和数据口径 | 直接覆盖 `demo.html` 或替换现有业务状态 |
| K 线拖拽、十字光标、OHLC | P0 | 接入现有个股页面，使用当前后端历史 K 线和时间元数据 | 用前端静态数据模拟历史行情 |
| 雷达图标签防溢出 | P1 | 复用布局算法和边界钳制逻辑 | 把研究完成度渲染成投资评分或买卖结论 |
| 高保真页面布局 | P0 | 逐区块迁移视觉结构，保留真实 API 和当前业务边界 | 整页替换当前产品，丢失交易复盘和确认写回 |
| Redis Stream 研究队列 | P1 | 将思想映射到现有 PostgreSQL outbox、durable worker、租约和重试 | 直接复制 SQLite 数据库、线程模型或状态表 |
| 研究任务取消/重试/并发限制设计 | P1 | 结合当前 job 状态模型增加 admission、cancel、retry 端点 | 只做按钮，不做持久化状态和幂等性 |
| 独立 AI Skill 工作区 | P1 | 接入当前 Skill 加载器、证据快照和用户隔离 | 让五维分析读取另一会话的历史消息 |
| 今日看板设计 | P1 | 复用 `today_overview`，先补数据日期、覆盖率和缺失状态 | 使用静态卡片数字冒充实时数据 |
| 个股评分库 | 暂缓 | 只吸收信息组织，不恢复平台综合评分 | 不引入目标价、胜率、强烈看多或综合投资分 |
| 自选股—交易复盘生命周期 | P1 | 映射到当前 relation、position ledger、trade workflow、review | 建第二套持仓和交易流水模型 |
| Tavily 搜索 | P2 | 仅作为补充检索 provider，保留来源、时间和失败边界 | 将搜索摘要当成金融事实或核心数据源 |
| 同事旧版李总策略 | 不直接吸收 | 只对照规则和接口，继续使用权威库现行版本 | 用旧版覆盖当前无前视历史回放和数据覆盖守卫 |
| 同事设计文档 | 参考 | 转成当前项目的 issue/plan，并标注实现状态 | 把 plan/spec 直接写进完成清单 |

## 5. 具体实施顺序

### 阶段 A：建立隔离分析副本

1. 从 `3.3 realyy.zip` 只提取源码、测试、前端资源和文档。
2. 排除 `.env`、`.venv`、`data`、`.runtime`、日志、缓存、用户工作区和 `.git/objects`。
3. 在临时目录建立 Git 分析副本，不修改权威库。
4. 记录同事版本的文件清单、分支、提交和测试基线。
5. 用 `git diff --stat`、模块清单和测试清单形成差异表。

### 阶段 B：先吸收图表交互

1. 识别当前 `app/static/demo.html` 中个股 K 线的入口和绘图函数。
2. 将同事图表算法拆成独立、可测试的纯前端模块。
3. 保留当前后端 K 线数据、复权口径、数据日期和缺失状态。
4. 增加以下前端验收：
   - 拖拽可查看历史窗口；
   - 当前窗口左边缘 MA 数值正确；
   - 十字光标对应正确交易日；
   - OHLC 与成交量同日联动；
   - 雷达标签不溢出；
   - 390px 窄屏无横向溢出；
   - 刷新后图表状态可恢复。

### 阶段 C：把研究队列思想接入现有生产架构

1. 先阅读权威库的 durable worker、PostgreSQL outbox、schedule、lease 和 retry 实现。
2. 只提取同事版本的状态机和队列边界，不复制 SQLite 表结构。
3. 统一任务状态、幂等键、租约、重试次数、错误分类和取消语义。
4. 研究任务必须保存：用户、任务类型、请求指纹、证据快照版本、Skill 版本、开始/结束时间和最终状态。
5. 多 Worker 场景必须验证：
   - 同一请求不会重复执行；
   - 租约过期可回收；
   - 重试有上限和退避；
   - 用户之间完全隔离；
   - 页面刷新可以恢复任务状态；
   - Worker 崩溃后不会留下永久 pending。

### 阶段 D：吸收页面结构和产品语义

1. 先把同事页面中的静态字段映射为当前真实接口。
2. 对每个卡片增加 `data_time`、`source`、`coverage`、`missing_reason` 和 `status`。
3. 将“评分库”改造为证据维度展示，不恢复综合投资评分。
4. 将“我的关注”拆成关系状态、研究状态和持仓状态，分别从当前服务读取。
5. 将“股票复盘”接到现有 `trade_workflow` 和 `position_ledger`，不新建平行数据模型。
6. AI 生成内容仍然只能作为候选；正式判断、计划、任务和复盘必须由用户确认。

## 6. 主 Agent 的执行任务清单

### P0

- [ ] 阅读本 handover、当前权威库 handover/README/VERIFICATION 和同事差异材料。
- [ ] 建立“同事文件 → 权威模块”的映射表。
- [ ] 不修改当前后端主链，先完成图表组件抽取设计。
- [ ] 为 K 线拖拽、十字光标、OHLC 联动建立前端测试和窄屏验收。
- [ ] 明确哪些高保真页面字段来自真实接口，哪些需要删除或改为空状态。

### P1

- [ ] 设计研究任务状态统一协议。
- [ ] 将同事队列能力映射到 PostgreSQL durable worker。
- [ ] 增加研究任务幂等、取消、重试、租约恢复和跨用户隔离测试。
- [ ] 将今日看板、个股研究空间和关注页逐步接入真实数据。
- [ ] 将独立 Skill 工作区接入当前证据快照和用户确认边界。

### P2

- [ ] 评估 Tavily 是否有实际数据覆盖价值。
- [ ] 优化图表视觉细节和雷达标签布局。
- [ ] 评估同事设计文档中尚未实现的产品功能。

## 7. 验收规则

每一项吸收都必须同时记录以下状态：

1. 设计存在；
2. 代码已实现；
3. 单元测试通过；
4. API 测试通过；
5. 真实数据验证通过；
6. 浏览器端到端通过；
7. 生产部署条件满足。

没有证据的项目只能标记为“设计参考”或“待实现”，不能写成“已完成”。

最低验证命令：

```bash
cd /Users/chr/Documents/qingtianshu
python -m compileall -q app scripts tests
node --check app/static/demo.js
git diff --check
pytest -q
```

高风险改动还必须补充：

- PostgreSQL 测试；
- durable worker 测试；
- 浏览器刷新恢复测试；
- 390px 窄屏测试；
- 用户隔离测试；
- 敏感文件扫描；
- 真实 Tushare 数据时间和覆盖率检查。

## 8. Git 交付规则

- 权威实现只能在 `/Users/chr/Documents/qingtianshu` 完成。
- 当前工作树有用户未提交修改，禁止 `git reset --hard`、`git clean -fd`、覆盖式复制或 `git add -A`。
- 每项功能使用独立 `codex/` 分支或用户明确指定的分支。
- 只暂存明确的源码、测试和文档路径。
- 提交前运行 `git diff --cached --check` 和敏感文件扫描。
- 不把 `.env`、`.venv`、`data`、`.runtime`、日志、缓存、数据库文件和用户工作区提交到 GitHub。
- 需要更新 `ruizhi` 时，目标是 `qingyuluoyu/qingtianshu:ruizhi`，它只是同事交付/交换分支，不是权威实现分支。
- 推送后必须用 `git ls-remote --heads origin <branch>` 核对远端 SHA。

## 9. 最终决策

优先吸收同事的“图表交互和视觉组织”，谨慎吸收“研究队列状态机”，暂不直接吸收其 SQLite、旧版李总策略和未经实现验证的产品计划。

最终目标不是让权威库变成同事代码库，而是：

> 用同事的高保真展示和交互增强当前产品，同时保留我们已经完成的 PostgreSQL、证据时间边界、用户确认写回、持仓台账和交易复盘闭环。
