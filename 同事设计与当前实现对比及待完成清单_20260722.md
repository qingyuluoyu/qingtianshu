# 清数智算：同事设计、当前实现与待完成工作总清单

更新日期：2026-07-23
目标项目：/Users/chr/Documents/青树金融交易/qingshu-agent-demo
交付对象：主 Agent、产品、前端、后端、数据/算法、测试与运维

## 0. 结论先行

当前项目已经不是一个空壳 Demo。它已经具备较强的金融研究能力：市场与个股行情、确定性技术指标、A 股财务与现金流分析、主营结构、股东、券商预期、同行比较、事件时间线、七阶段个股研究、证据任务、研究行动、T+3/T+5/T+10 结果跟踪、Agent 对话、资料库、用户隔离和较完整的自动化测试。

但当前强项主要是“研究分析能力”，同事设计的核心则是“用户股票事实与复盘闭环”。真正缺失的主链路是：

> 唯一股票研究空间 → 关系与判断版本 → 期初持仓 → 操作/调整流水 → 派生持仓快照 → 来源化 AI 研究 → 用户确认写回 → 复盘草稿 → 用户确认复盘

因此，下一阶段不能继续以“再加一个分析指标、再加一个 Agent 角色”为主。最高优先级应当是补齐领域对象、事务、版本、写回确认和前端闭环；随后再完成 Tushare 标准化数据管道、事件白名单、生产架构和商业后台。

当前总体判断：

| 领域 | 当前成熟度 | 结论 |
| --- | --- | --- |
| 个股确定性研究算法 | 较强 | 已形成多模块证据，但需要统一 Claim、覆盖状态和时间口径 |
| 李总策略 | MVP 已打通 | 已有12条确定性规则、全市场稳定市值预筛、独立增量 Worker、版本化快照、逐规则证据、触发去重、API、Agent/个股空间接入和前端卡片；用户订阅仍待完成 |
| Agent 对话与研究编排 | 中高 | 会话、流式输出、守卫和七阶段已存在；正式写回协议不完整 |
| 股票研究空间 | 高 | 聚合对象、关系历史、判断版本、策略证据、期初持仓、操作计划、操作/调整、冻结上下文、修订、派生快照和真实交易复盘已接入 |
| 今日观察与我的关注 | 中 | 市场、自选股和单股真实持仓摘要可用；批量操作、注意事项/行业组合筛选仍不完整 |
| 个人交易复盘 | 中高 | 已有等待数据、确定性价格结果、即时 AI 草稿、用户编辑/确认/归档和跨股票聚合；正式通知持久化和生产队列仍待完成 |
| Tushare | 中 | 15类逐股数据集、全市场名单/市值快照、同步运行、稳定版本保留和真实权限 PoC 已完成；规范化事实表、生产任务队列和商业许可仍待完成 |
| 前端工程 | 中低 | 单页功能丰富，但不是 Next.js 组件化应用，正式产品页面仍不齐 |
| 后端工程 | 中 | FastAPI 和测试较扎实；仍是 SQLite、巨型 main.py 和单机线程 |
| 安全、运维与发布 | 低到中 | 有会话隔离、健康检查和 Docker；缺审计、限流、CI、监控、备份恢复 |
| 会员、额度、青豆、BYOK、管理后台 | 很低 | 基本未实现，按 R2/R3 分阶段建设 |

## 1. 比较口径与状态定义

### 1.1 权威文件

本清单按以下优先级判断：

1. 产品与业务规则：清数智算_超级完整产品与技术PRD_V2.1_审校修订版.docx
2. 目标工程实现：清数智算_技术落地设计与实施规范_V1.0_正式版.docx
3. 视觉参考：清数智算_七页高保真交互演示.html
4. 当前事实：qingshu-agent-demo 的代码、数据库结构、测试和实际验证结果

视觉稿、旧接口、旧字段或旧说明与 PRD V2.1 冲突时，以 PRD V2.1 为准。HTML 中的评分、旧状态和旧页面表达不能作为正式需求。

### 1.2 状态定义

| 状态 | 含义 |
| --- | --- |
| 已完成并验证 | 有真实代码，并有自动化测试、实连或页面验收证据 |
| 部分完成 | 已有可复用实现，但缺正式领域对象、异常、事务、版本或端到端闭环 |
| 仅骨架/设计 | 有接口、页面占位或文档，没有完整可用链路 |
| 未完成 | 当前代码中未找到实质实现 |
| 与 V2.1 冲突 | 当前实现或旧说明会把产品带回被 V2.1 否定的方向 |

### 1.3 本次验证快照

- 自动化测试：2026-07-23 最新为 515 tests collected，全部通过。
- Tushare：配置可用；日线连通性检查返回 5 行，字段包括 ts_code、trade_date、open、high、low、close、pre_close、change、pct_chg、vol、amount。
- 本次 Tushare 检查使用固定日期范围，返回的最新日期为 2026-01-09；它证明“连接和 daily 接口可用”，不证明当前全量数据、全部接口权限或夜间同步已经完成。
- Ruff：2026-07-23 `ruff check .` 全项目通过。
- 便携安装：已在全新临时目录执行 `uv sync --frozen --extra dev`，使用全新数据目录启动服务后 `/health` 与 `/demo` 均返回 200，SQLite 与用户工作区可正常创建；本机没有 Docker，容器构建仍待具备 Docker 的环境复核。
- 当前开发目录不是独立 Git 工作树；正式 GitHub 镜像仓库位于 `/Users/chr/Documents/qingtianshu`。同步时必须排除 `.env`、数据目录、数据库、缓存、虚拟环境和密钥，不得用 reset、checkout 或批量清理覆盖用户文件。

## 2. 同事设计事项清单

下面是把 PRD V2.1 与技术规范 V1.0 合并后的目标清单。这里列的是“应当实现什么”，不代表当前已经完成。

### 2.1 产品边界与版本路线

- [ ] COL-PROD-01：产品定位为 A 股盘后/日频的个人股票研究与复盘工作台，不是高频行情终端。
- [ ] COL-PROD-02：核心对象是“一名用户对一只股票唯一的长期股票研究空间”。
- [ ] COL-PROD-03：正式产品不展示综合评分、平台目标价、强烈看多、BUY/HOLD/SELL、仓位建议或确定性涨跌概率。
- [ ] COL-PROD-04：不自动同步券商持仓，不自动执行交易，不把截图识别作为 R1 依赖。
- [ ] COL-PROD-05：公告、财报和研报只保存链接、元数据、合规摘要和用户备注，不保存未经授权全文。
- [ ] COL-PROD-06：R1 完成研究、持仓、操作和复盘核心闭环；R2 再做事件白名单、通知和商业试运行；R3 再做 BYOK、精细青豆账本和高级能力。
- [ ] COL-PROD-07：付费意愿、持续录入意愿和提醒价值必须由灰度数据验证，不能用“功能做完了”代替商业验证。

### 2.2 信息架构与页面

- [x] COL-IA-01：六个正式一级页面为今日观察、我的关注、个股研究、AI研究、复盘中心、个人中心。
- [x] COL-IA-02：今日观察首屏同时呈现用户待处理事项和精简市场概览，后续再展示与我相关变化和市场结构。
- [ ] COL-IA-03：我的关注把用户股票组织为研究资产，而不是普通自选股列表。
- [ ] COL-IA-04：个股研究是股票研究空间，第一屏展示关系、当前判断、1—3 条重要变化和最多 3 项待处理。
- [ ] COL-IA-05：股票研究空间固定为研究概览、AI研究、数据与证据、任务与操作、历史与复盘五个标签。
- [ ] COL-IA-06：AI研究支持单股、2—5 股比较、行业、公告/财报解读、组合和开放专题。
- [ ] COL-IA-07：复盘中心区分我的交易复盘和市场复盘，价格结果与逻辑结果分开。
- [ ] COL-IA-08：个人中心承载偏好、会员/额度、用量、隐私、安全、导出、账户删除和后续 BYOK。
- [ ] COL-IA-09：提供全局搜索、通知中心和提醒规则。
- [ ] COL-IA-10：提供十二类标准小窗口：全局搜索、通知、股票预览、加入股票、变化详情、AI详情、任务详情、记录操作、时间线详情、单笔复盘、复盘编辑器、会员额度。

2026-07-23 回填：一级导航已严格收敛为 PRD 六页；透明选股改为 AI研究二级视图，资料库保留资源入口。个人中心已完成真实研究资产、匿名会话和服务状态基础展示，但正式账户、偏好、额度、设备、导出、删除、管理员审计和 BYOK 仍未完成。

2026-07-23 回填：今日观察已把“我的研究待办”和“与我相关的重要变化”前置到完整全球市场与 A 股结构之前；顶部“今日关键事实”继续提供精简市场概览。AI研究页隐藏模型供应商开关、来源数量和自动展开的内部过程，默认使用当前可用 AI 对每个问题即时生成回答。

2026-07-23 回填：复盘中心新增“交易复盘”一级标签，按当前用户跨股票聚合真实操作复盘并支持状态、关键词筛选；研究结果、回答记录、证据链和质量复盘继续独立保留。交易复盘到期或草稿待确认时同步进入今日观察和顶部提醒。正式市场复盘产品页仍未完成，因此 COL-IA-07 继续保持部分完成。

### 2.3 核心领域对象与状态机

- [ ] COL-DOM-01：用户与股票的关系只分持仓、关注、已结束；关注优先级高/普通/低单独保存，不能混入关系枚举。
- [ ] COL-DOM-02：跟踪状态为跟踪中/暂停跟踪；工作流状态为空闲/研究中/等待数据；注意事项允许多标签。
- [ ] COL-DOM-03：股票关系转换保留历史；结束或全部卖出不删除原股票空间。
- [ ] COL-DOM-04：用户判断包含为什么关注/持有、观察什么、何时重判，并按草稿、待确认、正式、被替代、失效形成版本链。
- [ ] COL-DOM-05：AI 只能生成判断草稿，不能自动覆盖正式判断。
- [x] COL-DOM-06：期初持仓只创建一次，记录基准日期、数量、成本、费用口径和备注。
- [x] COL-DOM-07：买卖和持仓调整只追加流水；修正使用 revision 或调整记录，禁止直接覆盖历史事实。
- [x] COL-DOM-08：持仓快照由期初持仓、交易流水和调整流水派生，不允许用户直接编辑结果。
- [x] COL-DOM-09：操作计划有草稿、检查、保存、部分执行/执行、取消和过期状态；实际操作可关联已保存计划，系统只检查用户条件，不生成交易建议。
- [ ] COL-DOM-10：观察任务关联股票空间，并可关联判断或变化；完成结果必须有证据或用户输入。
- [ ] COL-DOM-11：重要变化包含事件类型、事实时间、发现时间、来源、规则版本、去重键和用户相关性。
- [x] COL-DOM-12：复盘按等待数据、可生成、AI草稿、用户确认、归档形成状态链。
- [x] COL-DOM-13：判断、任务、计划、操作修正和复盘均已版本化或具有不可变修订链。

### 2.4 金融数据、算法与规则

- [ ] COL-DATA-01：Tushare 是 V1 结构化金融数据主源；AI 问答优先读取本地标准化库，不在每次问答实时调用供应商。
- [ ] COL-DATA-02：接入并逐项验证股票基础、交易日历、日线、daily_basic、三张财务报表、财务指标、业绩预告、快报、披露日程、指数和可用行业数据。
- [ ] COL-DATA-03：每个数据集保存交易日/报告期、抓取时间、供应商更新时间、同步批次、来源版本和数据状态。
- [ ] COL-DATA-04：夜间任务按依赖顺序完成基础维表、行情、daily_basic、财务、质量门禁、事件、用户相关变化和缓存预热。
- [ ] COL-DATA-05：数据质量门禁覆盖完整性、唯一性、时间一致性、合理区间、财务版本、来源可追溯和稳定发布。
- [ ] COL-DATA-06：不完整批次不能覆盖上一稳定版本；失败时展示上一版本并明确延迟。
- [ ] COL-DATA-07：技术指标只描述已发生状态，公式、窗口、复权、样本和数据日期可查看，不生成买卖信号。
- [ ] COL-DATA-08：相对行业表现、成交变化、估值分位、上涨成分占比和财务同比均展示原始值、窗口和有效样本。
- [ ] COL-DATA-09：事件自动检测只覆盖冻结的 R1 白名单，并具备来源权限、规则版本、去重、严重度和失败降级。
- [ ] COL-DATA-10：用户确认变化为有关/无关，反馈只调整提醒权重，不修改公共事实。
- [x] COL-DATA-11：持仓成本默认移动加权平均；费用缺失、部分卖出、基准和数据完整性已冻结口径，分红/复权/停牌通过非交易调整与边界状态处理。
- [x] COL-DATA-12：复盘价格结果由确定性行情计算，逻辑结果由 Agent 草稿和用户确认独立保存；费用或数据不完整时不展示伪精确净收益、胜率或仓位偏差。

### 2.5 AI、Agent、证据与记忆

- [ ] COL-AI-01：AI 会话可创建、命名、继续、归档和软删除；被正式对象引用后不能破坏历史。
- [x] COL-AI-02：系统按问题自动选择必要数据和分析维度，普通用户不需要手工编排多个 Agent。
- [ ] COL-AI-03：输出固定区分已确认事实、证据推断、待验证假设、反证与风险、信息缺口、数据时间、来源和结论边界。
- [ ] COL-AI-04：每条关键事实和推断关联可点击 citation，不能只给一段无法定位来源的 Markdown。
- [ ] COL-AI-05：AI 运行支持 accepted、gathering、analyzing、delta、citation、writeback_candidate、completed、partial、failed 等 SSE 事件。
- [ ] COL-AI-06：AI 失败时保留已经取得的基础数据和部分结果，失败维度可独立重试。
- [x] COL-AI-07：结构化输出包含 candidate_writebacks；写回判断、任务、计划或复盘前必须逐项由用户确认。
- [ ] COL-AI-08：记忆分为账户偏好、股票正式判断、研究历史、行为与复盘、候选记忆；正式判断和长期偏好必须由用户确认。
- [ ] COL-AI-09：联网搜索只补充最新公开信息，来源冲突并列展示，来源不足时明确无法确认。
- [ ] COL-AI-10：建立固定 AI 评测集，回归事实引用、输出 Schema、风险缺口、写回边界、失败和提示词注入。

2026-07-23 回填：`COL-AI-02` 已由问题驱动的 `ResearchPlanService` 完成。行情原因、财务现金流、股东、主营、分析师预期、事件和估值同行问题会选择不同的证据模块与 Skills，短追问继承研究焦点；价格与当前资讯强制刷新，报告期型证据按时效复用。单模块失败会保存其他证据并把本轮标为 `partial`，但尚无完整统一 SSE 事件合同和失败模块独立重试接口，因此 `COL-AI-05`、`COL-AI-06` 仍为部分完成。

### 2.6 后端、API、数据库与任务系统

- [ ] COL-BE-01：采用 FastAPI 模块化单体，按 accounts、market、workspace、research、plans_trades、reviews、billing、notifications、admin_audit 划分领域。
- [ ] COL-BE-02：MySQL 是唯一业务事实库；金额、价格、比例使用 DECIMAL 或字符串，不使用二进制浮点作为事实值。
- [ ] COL-BE-03：Redis 只用于缓存、限流、锁、短期会话和 Celery，不保存唯一业务事实。
- [ ] COL-BE-04：Celery Worker 承担数据同步、事件检测、AI 长任务、导出和通知，并具备重试、幂等和死信处理。
- [ ] COL-BE-05：OpenAPI 是接口事实源，自动生成 TypeScript 类型；禁止前后端手写两套重复 DTO。
- [ ] COL-BE-06：正式接口使用 /v1，统一响应包络、request_id、data_time、data_status 和错误码。
- [ ] COL-BE-07：高价值写操作支持 Idempotency-Key、乐观锁和事务；重复请求不能生成重复流水、快照或扣费。
- [ ] COL-BE-08：领域写入提交后生成审计事件或 Outbox 事件，支持异步补偿。
- [ ] COL-BE-09：股票外部标识统一使用 Tushare ts_code，例如 300750.SZ 和 600519.SH；兼容旧 .SS 仅放在适配层。
- [ ] COL-BE-10：数据库迁移采用 Alembic 和 expand/contract 流程，应用与旧新结构在过渡期兼容。
- [ ] COL-BE-11：配置、平台密钥和用户 BYOK 分层管理；密钥不进入仓库、不返回前端、不写日志。

### 2.7 前端工程与交互

- [ ] COL-FE-01：正式 Web 使用 Next.js App Router、React 和 TypeScript，按路由拆包。
- [ ] COL-FE-02：服务端数据使用查询缓存；抽屉、筛选和未提交表单使用局部状态或轻量 Zustand。
- [ ] COL-FE-03：建立 DataTimestamp、Money、Percent、Quantity、EvidenceBadge、AsyncState、ConfirmWriteback、StatusTag、DrawerShell、ModalShell、SourceLink 等统一组件。
- [ ] COL-FE-04：所有页面支持加载、空、部分成功、失败、权限不足、数据过期和来源冲突状态。
- [ ] COL-FE-05：判断、操作和复盘未保存离开时二次确认；保存成功明确说明对象和状态变化。
- [ ] COL-FE-06：抽屉和弹窗支持焦点管理、Tab 顺序、Esc 关闭、恢复焦点和未保存确认。
- [ ] COL-FE-07：桌面优先并具备响应式；复杂图表和复盘编辑在移动端可提示使用桌面端。
- [ ] COL-FE-08：普通页面 3 秒内可操作、LCP P75 目标不高于 2.5 秒、CLS 不高于 0.1、搜索首批结果 500ms 内。
- [ ] COL-FE-09：AI 3 秒内显示真实阶段进度，首 Token 目标不高于 8 秒，不能用伪造百分比。
- [ ] COL-FE-10：外链显示来源域名和日期，使用新窗口及 noopener/noreferrer。

### 2.8 账户、安全、隐私与审计

- [ ] COL-SEC-01：正式账户支持密码强哈希、登录限流、会话撤销、设备管理和异常登录提醒。
- [ ] COL-SEC-02：Cookie 使用 HttpOnly、Secure、SameSite；Cookie 写操作具有 CSRF 防护。
- [ ] COL-SEC-03：所有私人资源在服务端按 user_id 和资源所有权过滤，越权不泄露对象存在性。
- [ ] COL-SEC-04：高风险动作重新验证；管理员使用独立入口、更短会话和 MFA。
- [ ] COL-SEC-05：管理员默认不能查看用户正文；应急访问必须填写目的、最小授权、二次认证并形成用户可见审计。
- [ ] COL-SEC-06：BYOK 密钥加密存储、掩码展示、域名白名单、连接测试、轮换和删除销毁。
- [ ] COL-SEC-07：联网抓取具备 SSRF、DNS 重绑定、私网地址、响应大小和内容类型防护。
- [ ] COL-SEC-08：日志不记录密码、密钥、完整敏感对话或不必要持仓明细。
- [ ] COL-SEC-09：数据导出使用异步任务和短时签名链接；账户删除有二次验证、等待期和可取消流程。
- [ ] COL-SEC-10：所有管理员访问、关键修正、密钥操作、导出和计费都写不可由业务管理员删除的审计日志。

### 2.9 部署、可观测、测试和发布

- [ ] COL-OPS-01：Caddy 是唯一公网入口，负责 HTTPS、HSTS、安全头、压缩、反向代理和限流。
- [ ] COL-OPS-02：Next.js、FastAPI、MySQL、Redis、Worker 只暴露在 Docker 私网或主机回环。
- [ ] COL-OPS-03：区分 local、dev、staging、production，staging 使用脱敏数据并执行迁移、回归和恢复演练。
- [ ] COL-OPS-04：日志统一包含 timestamp、service、environment、request_id、trace_id、job_id/run_id、duration_ms、error_code、data_version。
- [ ] COL-OPS-05：监控 HTTP 可用性和延迟、AI 队列与失败、数据任务覆盖、数据库、账本和通知。
- [ ] COL-OPS-06：MySQL 每日全量备份并支持 PITR；目标 RPO 不高于 24 小时、RTO 不高于 4 小时。
- [ ] COL-OPS-07：具备 Tushare 故障、AI 供应商故障、数据库高负载、账本不平和错误数据发布 Runbook。
- [ ] COL-TEST-01：测试分为单元、MySQL/Redis/Celery 集成、OpenAPI 契约、E2E、数据质量、AI 评测、安全和恢复测试。
- [ ] COL-TEST-02：CI 门禁包含 lint、format、typecheck、测试、OpenAPI diff、前端构建、容器构建、漏洞扫描和 E2E smoke。
- [ ] COL-TEST-03：生产镜像使用 commit SHA，不在生产服务器现场构建；应用和 Prompt/公式均可版本化回滚。

### 2.10 会员、额度、青豆、BYOK 与管理后台

- [ ] COL-BIZ-01：R1 灰度只使用简单 AI 额度，不接真实支付和青豆交易。
- [ ] COL-BIZ-02：R2 使用固定会员套餐、简单额度、真实支付、退款、对账和基础客户支持。
- [ ] COL-BIZ-03：R3 才启用精细青豆账本、白名单 BYOK、多模型路由和按功能成本治理。
- [ ] COL-BIZ-04：AI 请求经过预计消耗、预扣、执行、结算和退款；失败不收费，全部动作幂等可审计。
- [ ] COL-BIZ-05：管理后台覆盖用户、会员/额度、市场内容、数据任务、AI 配置、审计和运营配置。
- [ ] COL-BIZ-06：管理员查看用户正文的审计记录在用户个人中心可见。
- [ ] COL-BIZ-07：商业化前书面确认 Tushare 许可、第三方数据展示、缓存、衍生和收费边界。

### 2.11 用户新增强制需求：李总策略

本节不是同事原 PRD 的既有内容，而是 2026-07-22 用户新增并确认的必须实现项。它覆盖 PRD 中“自动选股暂不规划”的旧结论，但仍遵守“不自动交易、不生成平台买卖指令”的产品边界。

策略正式名称：李总策略
策略标识：li_zong
首个版本：li_zong_v1
适用范围：A 股、完整交易日日频数据
产品定位：确定性研究候选筛选 + 进入关注介入条件监控

李总策略分为两层：

1. 候选池筛选：股票必须同时满足基本面、股性和量价三组规则。
2. 进入关注介入触发：已进入候选池的股票，只要满足三类触发条件之一，就进入“李总策略触发池”，生成通知和人工复核任务。

“介入”在系统中的含义是进入重点关注和人工复核，不代表自动买入、自动下单或平台投资建议。

#### 2.11.1 基本面规则

- [ ] LZ-F-01：最近完整交易日总市值严格大于 150 亿元。
- [ ] LZ-F-02：最近连续 5 个已披露完整年度的 ROE 均大于等于 10%。
- [ ] LZ-F-03：ROE 默认阈值为 10%，允许用户调整；调整后生成新的参数版本，历史筛选结果不回写。
- [ ] LZ-F-04：最新已披露股东结构中，非自然人股东去重数量大于等于 5。

非自然人股东的初版统一定义：

- 使用最新已披露的十大股东和十大流通股东；
- 按规范化股东名称合并去重；
- 公司、基金、资管计划、保险、社保、信托、证券、银行、国资机构和其他明确法人/机构计为非自然人；
- 明确个人姓名计为自然人；
- 无法可靠分类的股东标记 unknown，不计入非自然人数，也不擅自按名称猜测；
- 结果必须展示披露报告期、原始股东名称、分类理由和未知数量。

#### 2.11.2 股性规则

- [ ] LZ-C-01：最近约 1 年完整交易日内，收盘涨停次数严格大于 5，即至少 6 次。
- [ ] LZ-C-02：最近约 1 年内至少出现过一次连板；连板定义为连续至少 2 个交易日收盘涨停。
- [ ] LZ-C-03：最近 10 个完整交易日内至少出现过一次收盘涨停。
- [ ] LZ-C-04：最近 10 个完整交易日内没有出现“跌幅达到或超过 5% 的阴线”。

“跌幅达到或超过 5% 的阴线”统一定义为：

- 当日 close < open；
- 且当日 pct_chg <= -5.0%；
- 两项同时满足才判定该规则失败。

涨跌停判定必须使用股票当日真实涨跌停价，不能全市场统一按 10% 粗略计算。需要处理主板、ST、创业板、科创板、北交所、上市初期无涨跌幅限制、停牌和制度变化；优先使用 Tushare stk_limit 或等价的正式涨跌停价数据。

#### 2.11.3 量价规则

- [ ] LZ-VP-01：最近 20 个完整交易日中，至少有一个交易日创最近 1 年半价格新高。
- [ ] LZ-VP-02：出现连续 3 个交易日放量，且这 3 日每天成交量均达到放量前基准成交量的 2 倍或以上。

初版冻结口径：

- “1 年半”按最近约 360 个交易日计算，并展示实际起止交易日；
- 新高使用同一复权口径的 adjusted high，避免分红送转产生伪新高；
- 判断某日是否创新高时，只使用该日及其之前已经存在的数据，禁止未来数据倒灌；
- “放量前基准成交量”使用三日放量序列开始前 20 个完整交易日的日均成交量；
- 三个连续交易日的 volume 均需 >= 2.0 × baseline_volume；
- 停牌、成交量缺失或基准窗口不足时标记 data_incomplete，不自动判定通过；
- 放量倍数和基准窗口作为策略参数保存，但 li_zong_v1 默认固定为 2.0 倍和 20 日。

#### 2.11.4 进入关注介入条件

通过全部候选池规则后，在每个完整交易日盘后检查以下条件。三项满足任意一项即触发：

- [ ] LZ-T-01：当日收盘涨停。
- [ ] LZ-T-02：当日相对前收盘高开 3.5% 或以上，且收阳线，即 close > open。
- [ ] LZ-T-03：当日振幅严格大于 9.8%，且收阳线，即 close > open。

统一公式：

- gap_open_pct = (open / pre_close - 1) × 100
- amplitude_pct = (high - low) / pre_close × 100
- 涨停以当日 up_limit 为准
- 收阳线为 close > open，不使用涨跌幅正负替代 K 线阴阳

触发后的系统动作：

1. 写入 strategy_trigger_event，不覆盖历史触发。
2. 自动进入“李总策略触发池”。
3. 向订阅该策略的用户生成通知和人工复核任务。
4. 展示触发规则、当日 OHLC、前收盘、涨停价、振幅和数据时间。
5. 用户确认后可加入个人关注，关系仍为 watching，优先级可设为 high。
6. 不自动写入持仓、不自动创建交易、不自动下单。

#### 2.11.5 策略状态

- not_qualified：至少一条完整规则明确不通过。
- data_incomplete：关键数据缺失，不能可靠判断。
- qualified：全部候选池规则通过，但介入条件尚未触发。
- triggered：候选池通过，且当日满足至少一项进入关注介入条件。
- invalidated：此前通过，但最新重算已不满足候选池规则。

每只股票必须返回逐规则结果，不只返回一个布尔值：

- rule_id
- passed / failed / data_incomplete
- actual_value
- threshold
- evidence_date 或 report_period
- source
- formula_version
- limitations

## 3. 我们已经完成并验证的事项清单

本节只列有代码和验证证据的能力。README 或旧报告中的单独声明不作为完成依据。

### 3.1 后端与用户隔离

- [x] CUR-BE-01：FastAPI 应用可启动，提供健康检查、静态 Demo、OpenAPI 和业务 API。
- [x] CUR-BE-02：用户和会话已落 SQLite；会话 Token 使用 SHA-256 后的哈希保存。
- [x] CUR-BE-03：会话 Cookie 已验证包含 HttpOnly 和 SameSite=Strict。
- [x] CUR-BE-04：对话、流式请求、知识资料、记忆、关注列表和 Run 具有用户归属检查。
- [x] CUR-BE-05：自动化测试覆盖跨用户流式访问拒绝、会话注销和资源隔离。
- [x] CUR-BE-06：数据库已包含用户、会话、关注、记忆、Run、对话、资料库、行情、财务、公告、主营、同行、股东、券商预期、事件、报告、研究阶段、任务、结果和校准等 MVP 表。

主要证据：

- app/main.py
- app/db.py
- tests/test_api.py
- tests/test_agent_stream.py
- tests/test_agent_guard.py

### 3.2 Agent、对话、资料与记忆

- [x] CUR-AI-01：支持创建、列表、继续、重命名、归档对话，并保存消息与 Run。
- [x] CUR-AI-02：支持 SSE 流式输出，并限制其他会话读取私人流。
- [x] CUR-AI-03：Agent 能读取结构化证据、相关资料库片段和用户确认记忆。
- [x] CUR-AI-04：候选记忆具有确认/拒绝流程，未确认内容不会直接作为正式长期记忆。
- [x] CUR-AI-05：存在大量金融输出守卫，禁止编造目标价、交易评级、仓位、无来源因果和时间口径错误。
- [x] CUR-AI-06：回答前端能够展示结构化证据来源、资料来源和时间。
- [x] CUR-AI-07：支持用户 TXT、Markdown、CSV、JSON 资料入库、检索、阅读和删除。
- [x] CUR-AI-08：支持图片上传并在 Agent 请求中引用，含大小和像素限制。
- [x] CUR-AI-09：研究对话网页与 API 默认即时执行 AI，预生成报告和历史回答只能补充证据；可显式传 `execute_agent=false` 进入确定性兼容模式。

主要证据：

- app/services/agent.py
- app/services/agent_stream.py
- app/services/knowledge.py
- app/main.py
- app/static/demo.html
- tests/test_agent_guard.py
- tests/test_conversation_quality.py

### 3.3 市场、行情与技术分析

- [x] CUR-MKT-01：提供指数、全球市场、A 股市场宽度、涨跌分布、成交和热门行业接口与页面。
- [x] CUR-MKT-02：提供个股分时、日线、周线和历史行情。
- [x] CUR-ALG-01：已实现 MA20、MA60、5/20 日收益、20 日年化波动率和 60 日最大回撤。
- [x] CUR-ALG-02：已实现 RSI14、MACD(12,26,9)、20 日布林带、ATR14 占收盘比例和 5/20 日量比。
- [x] CUR-ALG-03：当前报价时间和最近完整日线时间在页面中分开表达，避免把上一日收盘当盘中涨跌。
- [x] CUR-ALG-04：市场宽度和分布使用确定性计算，不依赖 LLM 生成数值。

主要证据：

- app/services/analysis.py
- app/services/live_market.py
- app/providers/market.py
- tests/test_analysis.py
- tests/test_live_market.py
- tests/test_api.py

### 3.4 Tushare 与选股研究

- [x] CUR-TS-01：Tushare SDK 适配器已存在，Token 从环境变量加载，错误信息不回显 Token。
- [x] CUR-TS-02：已封装 query、stock_basic、trade_cal、daily、daily_basic 和 fina_indicator。
- [x] CUR-TS-03：Tushare 实连 daily 成功，返回行数、字段和交易日，未输出凭证。
- [x] CUR-SCR-01：已实现四种可解释选股模板：经营改善、相对行业增强、估值约束、回撤后待复核。
- [x] CUR-SCR-02：选股使用股票基础、交易日历、日线、daily_basic 和财务指标，支持市场范围、估值、市值、流动性、收益、行业超额和财务过滤。
- [x] CUR-SCR-03：选股不计算综合分，返回规则、数据日期、缺失字段、样本范围和“不构成推荐”边界。
- [x] CUR-SCR-04：普通筛选和李总策略候选均可“保存线索并研究”，保存来源、日期、候选状态、命中理由和待补证据后进入同一只股票的长期研究空间。

主要证据：

- app/providers/tushare.py
- app/services/stock_screener.py
- scripts/verify_tushare.py
- tests/test_stock_screener.py
- tests/test_deep_stock.py
- tests/test_api.py

### 3.5 个股基本面、事件与证据算法

- [x] CUR-STK-01：A 股基础信息、公告、新闻和社区情绪数据链已存在。
- [x] CUR-STK-02：已实现结构化财务期、财务指标和三表明细保存。
- [x] CUR-STK-03：已实现财报质量分析，覆盖增长、利润率、经营现金流、杠杆、矛盾和复核点。
- [x] CUR-STK-04：已实现利润和现金流驱动拆解，区分确定性机械影响、公司原文解释、候选线索和未确认因果。
- [x] CUR-STK-05：已实现主营产品/地区/行业结构、集中度、可比期变化和覆盖边界。
- [x] CUR-STK-06：已实现固定同行估值和同报告期经营比较，保留样本和不可比边界。
- [x] CUR-STK-07：已实现股东结构、股东户数和十大股东快照。
- [x] CUR-STK-08：已实现券商评级分布、A/E EPS 和历史快照中的上修/下修，不输出目标价。
- [x] CUR-STK-09：已实现事件分类、官方披露/监管文件/媒体分层、去重和风险事件列表。
- [x] CUR-STK-10：已实现多方证据板，把支持证据、反方证据和风险委员会分开。

主要证据：

- app/services/fundamentals.py
- app/services/earnings_quality.py
- app/services/financial_drivers.py
- app/services/business_structure.py
- app/services/peer_comparison.py
- app/services/shareholders.py
- app/services/analyst_expectations.py
- app/services/event_timeline.py
- app/services/analysis.py
- 对应 tests/test_*.py

### 3.6 七阶段股票研究空间与研究闭环

- [x] CUR-WS-01：DeepStockResearchService 已按用户和股票保存长期研究会话，并绑定现有 conversation_id。
- [x] CUR-WS-02：已有七阶段：原始研究逻辑、公司与行业、财务与现金流、估值与同行、事件与情绪、反方证据、失效条件与下一证据。
- [x] CUR-WS-03：阶段能记录问题、Run、证据模块、覆盖状态、未决问题和下一问。
- [x] CUR-WS-04：已有研究报告生成、保存、检索和通用资料索引。
- [x] CUR-WS-05：已有研究变化记录，用于比较连续报告中的新证据。
- [x] CUR-WS-06：已有研究优先级和研究行动，能生成“下一步核验什么”的确定性待办。
- [x] CUR-WS-07：已有证据补齐任务，可自动处理市场资讯、行情、A 股信息、公告和研究报告缺口。
- [x] CUR-WS-08：已有 T+3/T+5/T+10 研究结果跟踪和固定规则 walk-forward/prequential 校准。
- [x] CUR-WS-09：已有数据健康和对话质量审计。
- [x] CUR-WS-10：筛选候选入口保存为用户独立 `screening_entry`，不会自动完成研究阶段或覆盖正式判断；研究空间展示入口来源、命中理由、待补证据并复用唯一研究对话。
- [x] CUR-WS-11：已新增用户 ObservationTask，与系统 EvidenceTask/ResearchAction 分离；任务状态、结果、证据、编辑版本和状态历史均落库并按用户隔离。
- [x] CUR-WS-12：操作计划支持创建、编辑、完整性检查、保存、取消、过期和实际操作推进；使用乐观锁、幂等键、用户/股票空间隔离和版本历史。
- [x] CUR-WS-13：每笔减少持仓或卖出会冻结操作时判断、计划、持仓、已完成日线、指数背景、估值/报告等可用上下文，并创建独立交易复盘对象。
- [x] CUR-WS-14：交易复盘在三个后续交易日数据齐备后开放；价格结果由确定性工具计算，Hermes/DeepSeek 当次即时生成逻辑草稿，用户编辑生成新版本，之后可确认和归档。

主要证据：

- app/services/deep_stock.py
- app/services/research_reports.py
- app/services/research_tracking.py
- app/services/research_priority.py
- app/services/research_actions.py
- app/services/evidence_tasks.py
- app/services/research_outcomes.py
- app/services/calibration.py
- app/services/data_health.py
- app/services/conversation_quality.py
- tests/test_deep_stock.py
- tests/test_research_tracking.py
- tests/test_research_actions.py
- tests/test_evidence_tasks.py
- tests/test_research_outcomes.py
- tests/test_calibration.py
- tests/test_stock_workspace.py
- tests/test_observation_tasks.py
- tests/test_trade_workflow.py
- tests/test_trade_workflow_api.py

### 3.7 当前前端

- [x] CUR-FE-01：当前单页应用已按 PRD 收敛为今日观察、我的关注、个股研究、AI研究、复盘中心、个人中心六个一级页面；透明选股位于 AI研究二级模式，资料库保留为独立资源入口。
- [x] CUR-FE-02：股票研究空间已经有研究概览、AI研究、数据与证据、任务与操作、历史与复盘五个标签。
- [x] CUR-FE-03：个股首屏已能展示当前判断、重要变化、待处理、行情、估值、K 线和财务摘要。
- [x] CUR-FE-04：支持在股票空间内编辑当前判断，并同步关注理由和 Agent 上下文。
- [x] CUR-FE-05：支持关注股票增删、行情详情、分时/日线/周线切换。
- [x] CUR-FE-06：支持会话历史、资料库、来源阅读器、Markdown 渲染和流式草稿。
- [x] CUR-FE-07：已有基础响应式样式、部分键盘 Enter/Space 操作、一个统一阅读抽屉和 Esc 关闭。
- [x] CUR-FE-08：页面明确说明证据覆盖不是投资评分，选股不是推荐。
- [x] CUR-FE-09：候选卡片可直接保存线索并进入个股研究；股票名称、会话标题、下拉选项和研究概览保持一致，并明确候选理由仍需核验。
- [x] CUR-FE-10：任务与操作页区分“我的观察任务”和“系统建议”；系统建议只有用户确认后才持久化，任务支持开始、等待数据、完成、忽略、取消、重开、编辑和查看版本历史。
- [x] CUR-FE-11：个人中心基础页只展示当前用户真实研究资产、匿名会话和服务状态，并明确标注正式账户、会员计费、设备管理、导出、删除和 BYOK 尚未完成。
- [x] CUR-FE-12：“我的关注”已改为长期股票研究资产视图，支持持仓/关注/已结束、高/普通/低、暂停和等待数据筛选；单项关系修改使用 base_version，结束与恢复保留原空间、判断和历史。
- [x] CUR-FE-13：“任务与操作”已接入真实操作计划和持仓流水；“历史与复盘”已接入等待数据、即时 AI 草稿、用户编辑确认和归档，并能在刷新后恢复。

主要证据：

- app/static/demo.html
- tests/test_api.py
- VERIFICATION.md 中已有页面验收记录

### 3.8 工程与验证

- [x] CUR-QA-01：当前自动化测试共 495 项并全部通过。
- [x] CUR-QA-02：测试覆盖 API、用户隔离、Agent 守卫、市场、财务、主营、股东、券商预期、同行、事件、研究阶段、任务、结果和校准。
- [x] CUR-QA-03：真实网页已验证“大族激光”候选进入研究空间的完整链路，顶部名称、标准会话标题、证券代码、筛选线索和候选理由一致，浏览器控制台无警告或错误。
- [x] CUR-QA-04：真实网页已验证观察任务“待处理→处理中→编辑→填写结果→完成”的完整状态链，版本 1—4 均可查看；保存后的系统建议不重复展示，控制台无错误。
- [x] CUR-OPS-01：已有单应用 Dockerfile、docker-compose、健康检查和持久卷。
- [x] CUR-OPS-02：已有进程内后台调度器、任务运行记录、失败状态和数据健康快照。
- [x] CUR-OPS-03：README、`.env.example`、启动脚本和默认配置已移除开发者绝对路径，提供 uv、标准 venv、Windows PowerShell、macOS 和 Docker 安装说明；`tests/test_portability.py` 阻止重新引入本机路径。

注意：Ruff 已于 2026-07-22 恢复通过；类型、契约、前端构建和完整 R1 E2E 等发布门禁仍未建立。

## 4. 三方对比矩阵

| 领域 | 同事目标 | 当前实现 | 状态 | 主要差距 |
| --- | --- | --- | --- | --- |
| 产品核心 | 股票空间内持续保存判断、变化、操作、快照和复盘 | 研究阶段、证据、报告、对话、持仓、操作计划、操作、冻结快照、交易复盘和跨股票聚合已接入 | 大部分完成 | 正式通知持久化和生产工程仍缺失 |
| 一级导航 | 六页：今日、关注、个股、AI、复盘、个人 | 已按 PRD 完成六页收敛；透明选股在 AI研究二级模式，资料库为资源入口 | 已完成 | 后续迁移到正式前端路由时保持同一信息架构 |
| 用户关系 | 持仓/关注/已结束，优先级独立 | 已新增唯一 stock_workspaces、relation_history、优先级、跟踪/流程、注意标签和真实单股持仓摘要；旧 watchlist 继续兼容 | 大部分完成 | 批量操作和组合筛选尚未接入 |
| 判断版本 | 草稿、确认、正式、替代、失效 | 已新增 thesis_versions、草稿/待确认/正式/替代/拒绝、base_version 冲突检查，并将判断、任务、计划和复盘草稿统一接入 candidate_writeback 确认闭环 | 大部分完成 | 尚缺生产级单事务/补偿事务和 Outbox |
| 期初持仓 | 一次性基准事实 | 已有唯一期初持仓、基准日期、数量、成本、费用口径和备注 | 已完成 | 后续仅需与复盘上下文衔接 |
| 操作/调整 | 不可变流水、修正链、幂等事务 | 已有计划、买入/增加/减少/卖出、非交易调整、revision、幂等键和事务回滚 | 大部分完成 | 尚缺 outbox/正式审计事件 |
| 持仓快照 | 从流水派生 | 已有移动加权成本、费用完整性、已实现结果、可重放快照和操作时冻结上下文 | 大部分完成 | 生产迁移和公司行动自动化仍未完成 |
| 复盘 | 真实操作后生成草稿并由用户确认 | 已有等待数据、确定性价格结果、即时 AI 逻辑草稿、用户版本、确认、归档、跨股票列表和筛选 | 大部分完成 | 缺正式通知持久化和生产队列 |
| 今日观察 | 待处理 + 市场概览 + 与我相关变化 | 已有统一聚合、5 项待办、相关变化、四时段识别和上证/深证/创业板/科创50完整日线卡片 | 部分完成 | 缺正式事件白名单、四时段差异化内容、风格和风险日程 |
| 个人中心 | 偏好、额度、用量、隐私、安全、导出、删除、BYOK | 已展示真实研究资产、匿名会话、AI 实时研究状态、数据健康和后台状态 | 部分完成 | 正式账户、偏好、额度、设备、导出、删除、管理员审计和 BYOK 未完成 |
| 我的关注 | 多维关系/优先级/流程/注意事项 | 已有正式研究资产列表、关系/优先级/暂停/等待数据筛选、单项状态修改、判断/变化/下一事项和真实持仓摘要 | 大部分完成 | 缺注意事项/行业/待处理组合筛选、批量操作和列设置 |
| 个股空间 | 判断、变化、六维证据、AI、任务、操作、快照、历史 | 已有统一聚合对象、五标签、七阶段、判断版本、证据、策略触发、报告、期初持仓、操作计划、操作/调整、冻结上下文、修订、快照和真实复盘 | 大部分完成 | 缺正式通知持久化和正式前端工程化 |
| AI会话 | 结构化输出、引用、候选写回 | 已有多历史对话、默认即时 AI、统一股票研究 Schema、逐 Claim citation、SSE 事件，并支持判断、观察任务、操作计划和复盘草稿四类确认写回 | 大部分完成 | 仍缺失败模块独立重试、完整统一 SSE 终态和生产事务编排 |
| 任务 | 用户观察任务状态机 | 已有独立用户任务、截止时间、完成结果、证据、版本和状态历史 | 大部分完成 | 缺通知、批量处理和跨页面任务中心 |
| 事件 | R1 白名单、去重、严重度、用户相关化 | 通用事件时间线和报告变化 | 部分完成 | 未冻结白名单、规则版本、用户有关/无关闭环 |
| Tushare | 本地标准化库、夜间同步、质量门禁 | Provider、15类逐股快照、全市场名单/市值快照、sync run、data_version、稳定版本保留和权限 PoC | 部分完成 | 缺规范化事实表、生产任务队列和商业权限确认 |
| 李总策略 | 独立候选池、逐规则证据、盘后介入触发 | 规则引擎、七表、全市场稳定预筛、上市历史预判、深度处理口径、独立增量 Worker、ROE 第二来源、自动页面刷新、Agent 与股票空间已接通 | 大部分完成 | 缺用户订阅通知、今日观察完整融合、参数设置界面和全市场历史回放 |
| 技术指标 | 透明状态，不生成信号 | 指标丰富，另有条件展望和方向校准 | 部分完成/需收敛 | 隔离 evidence_score 和方向一致率，不做 R1 信号产品 |
| 前端工程 | Next.js + React + TypeScript + 共享组件 | 约 6000 行单 HTML/CSS/JS | 仅 MVP 完成 | 组件化、路由、类型、状态、性能和可测试性不足 |
| 后端架构 | 模块化 FastAPI、MySQL、Redis、Celery | FastAPI + SQLite + 单机线程 | 仅 MVP 完成 | 生产事实库、队列、迁移、缓存和模块边界未落地 |
| API契约 | /v1、包络、错误码、OpenAPI 生成类型 | 多个旧路径，Pydantic 但无生成合同 | 部分完成 | 兼容迁移和契约测试缺失 |
| 安全 | 正式认证、CSRF、限流、MFA、审计 | 匿名/轻账户、哈希会话、用户隔离 | 部分完成 | 正式账户、管理员、重验、审计和安全门禁缺失 |
| 可观测/容灾 | SLO、指标、告警、备份、恢复、Runbook | 健康检查、后台任务和数据健康 | 部分完成 | 无监控系统、PITR、恢复演练和 Runbook |
| 测试发布 | 多层测试和 CI/CD 门禁 | 当前 Python 全量测试与 Ruff、内联 JavaScript 语法检查通过 | 部分完成 | 无正式前端 typecheck/build、契约、MySQL/Redis/Celery、安全和恢复测试 |
| 商业与后台 | R1额度、R2支付、R3青豆/BYOK、后台 | 基本无 | 未完成 | 必须按版本分阶段，不能一次性堆营销占位 |

## 5. 与 V2.1 冲突、必须先收敛的事项

### 5.1 关系枚举不能继续沿用旧说明

旧的个股增强说明示例使用 holding、priority_watch、candidate、normal_watch、ended。V2.1 已明确纠正“关系和优先级混合”的问题。

主 Agent 必须采用：

- relation_type：holding、watching、ended
- priority：high、normal、low，仅对 watching 生效
- tracking_status：active、paused
- workflow_status：idle、researching、waiting_data
- attention_tags：可多选

不得继续把“重点关注”“候选观察”做成 relation_type。

### 5.2 一级导航已按正式产品收敛

已完成本轮收敛：“透明选股”保留为 AI研究中的二级模式，“资料库”保留为资源入口；它们不再挤占 V2.1 的六个正式一级页面。当前主链路为今日观察、我的关注、个股研究、AI研究、复盘中心和个人中心。

个人中心当前为诚实基础版，只展示真实研究资产、会话、Hermes、数据健康和后台刷新状态。正式账号、偏好、简单额度、安全审计、导出、账户删除、多设备和 BYOK 仍按 P1/P2 继续建设，不因页面入口存在而提前标记完成。

### 5.3 资料上传不属于 R1 正式承诺

当前资料库和图片上传已经可用，但 V2.1 明确 R1 不承诺任意文件上传或本地材料解析。处理方式不是删除代码，而是：

- R1 正式构建使用功能开关隐藏或限定白名单格式；
- 完成版权、恶意文件、内容隔离、删除、容量和隐私评审后再开放；
- 不把“已能上传文本”写成“R1 已完成材料研究”。

### 5.4 条件展望、方向分数和结果校准需要隔离

当前 build_conditional_outlook、evidence_score、price_signal_label、direction_consistency 等机制可以用于内部研究质量实验，但容易被理解为价格预测信号。

R1 处理要求：

- 不在普通用户页面或 Agent 答案中显示数值方向分、胜率或未来概率；
- 把结果回填解释为“研究条件是否兑现、证据是否遗漏”，不是荐股表现；
- 若保留方向实验，必须进入独立 evaluation scope，不参与正式判断、持仓或操作。

### 5.5 研究优先级分数只能做内部排序

research_priority 的 priority_score 可用于确定性排序，但前端应优先显示触发规则、证据和待办，不把 0—100 分作为股票质量或交易紧迫度的伪精确结论。

### 5.6 生产数据源需重新定权

当前使用多个公开行情、东方财富、Yahoo、新闻等 Provider。它们可继续作为开发或降级源，但正式 V1 的结构化金融事实应按 PRD 统一到 Tushare 本地标准化库，并明确第三方来源许可。不能把免费源实时抓取等同于生产数据底座。

## 6. 我们需要完成的事项清单

以下是给主 Agent 的直接实施 Backlog。每项都包含结果和最低验收标准。

## 6.1 P0：产品事实与 R1 核心闭环

### P0-01 冻结领域模型和接口合同

- [ ] 建立 workspace、relation_history、thesis_versions、observation_tasks、action_plans、initial_holdings、trade_records、trade_revisions、holding_adjustments、holding_snapshots、trade_snapshots、reviews、review_versions、ai_writeback_candidates。
- [ ] 明确每个对象的 user_id、space_id、version、status、created_at、updated_at 和审计字段。
- [ ] 生成 /v1 OpenAPI 草案和状态枚举，旧 /me/* 接口只做兼容适配。
- [ ] 金额、价格、数量和比例使用 Decimal 字符串，不继续把 REAL 浮点作为正式事实。

验收：

- 同用户同股票只能有一个股票空间。
- 关系、优先级、跟踪、流程和注意事项互不混用。
- OpenAPI 能生成前端类型，CI 能检测破坏性变更。

### P0-02 实现统一 StockWorkspaceService

- [x] 新增 app/services/stock_workspace.py，先聚合当前已有的关系、判断、报价时间、重要变化、待处理、证据覆盖、对话、报告和研究历史。
- [ ] 在操作、持仓和复盘领域对象落地后，将其真实对象接入 StockWorkspaceService；当前只允许返回诚实空状态。
- [x] 提供 GET /v1/stocks/{ts_code}/workspace。
- [x] 再提供 evidence、timeline、actions 等延迟加载接口。
- [ ] 我的关注、今日观察、股票研究空间和 Agent 使用同一份领域对象，不各自拼字段。

2026-07-22 主 Agent 回填：

- 已实现 `StockWorkspaceService` 和 `/v1/stocks/{symbol}/workspace`，同时保留 `/api/v1/...` 兼容别名。
- 已提供 `/v1/stocks/{symbol}/workspace/evidence`、`timeline`、`actions` 延迟加载接口，分别返回结构化证据链、研究时间线和待处理动作。
- 聚合结果已覆盖候选预览/正式关系、版本化 active 判断、当前报价与最近完整日线双时间锚点、最多三条去重变化、待处理、七阶段进度、六维覆盖、反证、失效条件、下一证据、报告、对话和历史摘要。
- 尚无正式持仓事实链时只返回 `position_snapshot.status=not_configured`，不冒充已支持持仓与对账。
- 已验证无股票空间空状态、跨用户私人判断/对话不泄露、公共报告可共享、接口字段稳定。
- 个股研究首屏已优先读取聚合接口；财务、股东和事件详细卡片暂继续使用原模块接口。今日观察、我的关注和 Agent 尚未全部切换，因此最后一项保持未完成。
- 自动化证据：`tests/test_stock_workspace.py`、`tests/test_deep_stock.py` 和 Demo 页面相关测试通过；真实桌面与 390px 窄屏验收无页面横向溢出，控制台无错误。

验收：

- 打开任一股票首屏可在一次聚合请求中得到关系、正式判断、最多三条变化和三项待处理。
- 缺数据、过期、冲突和部分失败均独立标记。
- 旧页面和新页面读取结果一致。

### P0-03 完成用户关系和判断版本

- [x] 支持持仓、关注、已结束关系及关系历史。
- [x] 支持高/普通/低优先级、跟踪中/暂停、流程状态和多注意标签。
- [x] 把当前 watchlist.thesis 迁移为判断初始版本或关注理由，不直接覆盖。
- [x] 判断编辑使用 base_version；并发冲突返回 409 和差异。
- [x] AI 只能创建 pending confirmation 草稿，用户确认后才 active。

2026-07-22 主 Agent 回填（后端事实链已落地，前端与 Agent 接入仍未完成）：

- 已新增 `stock_workspaces`、`stock_relation_history` 和 `thesis_versions`，同用户同股票使用唯一空间。
- 支持 `watching/holding/ended`、`high/normal/low`、`active/paused`、`idle/researching/waiting_data` 和多注意标签；结束后保留空间和关系历史，重新加入时恢复原空间。
- 旧 `watchlist.thesis` 会迁移为正式判断初始版本；旧页面再次确认新判断时生成新 active 版本并把旧版本标为 superseded，不再静默覆盖历史。
- 已提供 `/v1/stocks/{symbol}/relation`、`/v1/stocks/{symbol}/theses`、确认和拒绝接口；后端候选支持 draft、pending_confirmation、active、superseded 和 rejected。
- 正式接口执行 base_version 检查并对冲突返回 409。前端“编辑当前判断”现通过 `/v1/stocks/{symbol}/theses` 创建用户草稿并确认；若其他页面已更新，会显示最新版本和当前正式判断，不静默覆盖。
- AI 来源候选必须关联当前用户的 completed Run，且确认前不会改变 active 判断。Agent 在用户明确要求形成或更新判断草稿时生成 `candidate_writeback`，页面支持确认、拒绝和刷新后恢复状态。
- 自动化证据：`tests/test_stock_domain.py`、`tests/test_structured_ai.py` 覆盖唯一空间、版本迁移、用户/AI 草稿、确认/拒绝、409 冲突、结束/恢复和跨用户隔离；真实网页验证版本号、编辑器和不覆盖提示。

验收：

- 旧判断不可丢失，历史版本可查看。
- AI Run 失败、守卫回退或用户未确认时不能修改正式判断。
- 结束并恢复股票后继续复用原空间和历史。

### P0-04 完成期初持仓、操作、调整与派生快照

- [x] 建立一次性期初持仓，记录基准日期、数量、成本、费用口径和完整性。
- [x] 实现买入、增加、减少、卖出和非交易数量/成本调整；分红、送转、拆并以明确调整类型记录，不伪装成交易。
- [x] 同一事务写 operation、position snapshot 和 revision；任一校验失败整笔回滚。
- [x] 重复 Idempotency-Key 只生成一次结果。
- [x] 使用移动加权平均计算成本；部分卖出计算已实现结果，剩余持仓保持成本口径。
- [x] 修正通过不可变 revision，不允许直接 UPDATE 历史流水。

2026-07-23 主 Agent 回填：前端“任务与操作”已提供期初持仓、操作、调整和修订入口；“历史与复盘”展示操作与快照数量。费用为空不会被当作 0，超量卖出事务回滚，全部卖出后保留股票空间。Outbox/正式审计事件、操作计划和操作时研究上下文仍属于后续工作包。

验收：

- 买入、加仓、部分卖出、全部卖出、费用缺失、超量卖出、重复提交和修正样例通过。
- 任一子写入失败时全部回滚。
- 持仓快照能从全部事实流水重放得到相同结果。

### P0-05 完成用户观察任务和操作计划

- [x] 区分现有 EvidenceTask 与用户 ObservationTask。
- [x] 用户任务支持待处理、处理中、等待数据、完成、忽略、取消、重开。
- [x] 任务支持关联股票、判断版本、变化和证据。
- [x] 操作计划保存用户自定义触发条件、数量/金额和检查结果，但不输出买卖建议。
- [x] 任务完成必须填写结果或选择证据。

2026-07-23 主 Agent 回填（观察任务与操作计划均已完成）：

- 已新增 `observation_tasks` 和 `observation_task_history`；编辑和状态变化追加新版本，不直接覆盖历史快照。
- 已提供任务列表、股票任务列表、创建、详情、编辑和状态流转接口；使用 `base_version` 防止并发静默覆盖，冲突返回 409。
- 系统研究建议只有用户点击“保存为我的任务”后才创建；同一来源建议使用去重键，不重复生成任务。
- 完成任务必须填写结果或提供证据引用；已完成、忽略和取消任务需要先重开才能继续编辑。
- 股票研究空间聚合返回用户任务，并优先展示进行中的任务；系统 EvidenceTask 继续只负责后台补证，前端不再把两者混为一谈。
- 真实网页已验证创建、处理中、编辑、完成、历史查看和重复建议隐藏；全量 410 项测试、Ruff 和内联 JavaScript 语法检查通过。
- `action_plans` 已支持草稿、检查、保存、取消、过期、部分执行/执行、版本历史和幂等写入；Agent 只把用户本轮明确条件整理为候选，确认后才进入“任务与操作”。

验收：

- 任务详情可编辑、完成、忽略、取消并保留历史。
- AI 只能提出任务/计划草稿，用户确认后才创建。
- EvidenceTask 不再被前端误当成用户投资任务。

### P0-06 完成真实个人交易复盘

- [x] 交易和持仓对账后创建 waiting_data 复盘记录。
- [x] 按交易日窗口、复权、现金分红、费用、部分卖出、停牌和基准生成价格结果。
- [x] 同时冻结操作时有效判断、计划、市场、行业、股票数据和已知变化。
- [x] AI 只生成复盘草稿；用户编辑、确认后形成正式版本。
- [x] 价格结果和逻辑结果分栏；盈利不自动等于逻辑正确，亏损不自动等于逻辑错误。
- [ ] 用户确认的改进只能生成新任务或判断草稿。

验收：

- “期初持仓→加仓→部分卖出→全部卖出→等待数据→草稿→确认→归档”E2E 通过。
- 数据不完整时阻止精确净收益和正式复盘确认。
- 当前 research_outcomes 页面明确改名为研究结果复核，不能冒充交易复盘。

### P0-07 统一 AI 结构化输出、引用和写回

- [x] 定义 answer_summary、confirmed_facts、evidence_based_inferences、hypotheses_to_verify、counter_evidence_and_risks、information_gaps、conclusion_boundary、citations、candidate_writebacks。
- [x] 每条事实携带 citation_ids 和 data_time；每条推断携带 supporting_citation_ids。
- [x] 建立 ai_citations 和 ai_writeback_candidates 持久对象。
- [x] SSE 增加 citation、writeback_candidate、partial 和 failed 事件。
- [x] 提供当前判断候选的确认、拒绝和查看差异接口。

2026-07-22 主 Agent 回填（Claim 证据链与连续对话已增强，正式写回协议仍未完成）：

- 已新增 `app/services/research_claims.py`，把个股研究证据整理为稳定 Claim，区分 `supports`、`weakens` 和 `unresolved`，并携带来源、数据时间、报告期、覆盖状态、限制和下一步核验。
- 股票研究空间、研究报告和 Agent 证据包已复用同一份 Claim Ledger；用户可在前端分别查看支持证据、反方压力、待补证据和判断失效条件。
- 已修复连续股票对话中“重新回答上一题/上一问”丢失证券对象的问题；即使 Hermes 输出未通过守卫，也会回退到当前股票的确定性证据摘要，不再退回无关通用资料。
- 个股研究最终回答中的百分比、比率、均线和金额统一最多保留两位小数；历史完整日线与更新报价保持独立时间锚点。
- 当前仍未建立独立 `ai_citations`、`ai_writeback_candidates` 持久对象，也未完成 SSE citation/writeback 事件与正式确认差异协议，因此本工作包保持未完成。
- 自动化证据：全量 403 项测试与 Ruff 通过；真实网页验证 Enter 发送、连续对话继承、Hermes 最终回答、两位小数和报价时间边界均通过。

2026-07-23 主 Agent 回填（结构化回答与当前判断确认写回已完成，任务/计划/复盘写回仍待领域对象完成）：

- 新增 `app/services/structured_ai.py`，直接消费结构化 Claim Ledger，不从 Markdown 正文正则猜测事实或正式写回内容。股票研究回答保存事实、推断、反证、待验证假设、信息缺口、失效条件、下一步核验、结论边界和逐 Claim 引用。
- 新增 `ai_citations` 与 `ai_writeback_candidates`；公开 API 和 SSE 只返回用户可读字段，不暴露 `user_id`、`workspace_id`、`conversation_id` 和内部 `source_key`。
- 普通个股问题、preview、guarded、degraded 或 failed Run 不会生成正式判断候选。只有 `completed` Run 且用户明确要求形成或更新判断草稿时，才生成 `pending_confirmation` 候选。
- 新增 `/v1/ai-writebacks` 列表、详情、确认和拒绝接口。确认前正式判断不变；确认后复用 `thesis_versions` 生成新 active 版本并替代旧版本；base version 冲突返回 409 并将候选标记为 `stale`。
- SSE 已发布 `agent_citation`、`agent_writeback_candidate`、`agent_structured_partial` 和 `agent_structured_failed`，并支持断线重放；深化研究会替换消息中的旧结构化回答。
- 前端可展开事实、推断、反证、假设、缺口、失效条件和下一步核验，点击证据可查看来源、时间和口径限制；判断草稿可确认或拒绝，刷新后恢复真实状态。
- 真实 Hermes Run `c95e887a-1891-4ae8-90e7-55b9e3053a79` 为 `stock_research / completed`，回答针对中兴通讯列出支持、反证和失效条件；页面只有一条最终回答，结构化证据 8 条事实、8 条反证、7 项待核验和 12 项引用。
- 真实网页确认时主动发现演示自选初始化会把新判断覆盖回默认判断；已移除每次鉴权请求的重复初始化并增加回归测试。修复后候选保持 `confirmed`，新判断保持 active，页面刷新和后续会话不再覆盖。
- 当前全量 `432 tests collected` 全部通过，Ruff、内联 JavaScript、Tushare 实连、数据库外键检查和真实网页控制台均通过。
- 本工作包仍不视为所有正式对象的通用写回完成：`candidate_type` 当前只开放 `thesis`；ObservationTask、ActionPlan 和 Review 必须在各自 P0 领域对象完成后按同一确认协议接入。

2026-07-23 后续回填（四类候选写回已统一）：

- `candidate_type` 已开放 `thesis`、`observation_task`、`action_plan` 和 `review_draft`。普通个股研究以及财报质量、利润现金流、主营、股东、分析师预期和事件等专项 Run 复用同一 Claim Ledger、citation 与确认协议。
- 操作计划候选只保存用户本轮明确给出的核验条件；不把行情价、均线、估值、比率或系统建议追加为计划门槛，目标价、数量、金额和仓位保持为空。确认前正式计划数量不变，确认后才进入股票空间“任务与操作”。
- 交易复盘生成接口先返回 `review_draft` 候选；确认前复盘 `current_version` 为空，确认后才保存带原 Run 引用的 AI 版本。重复确认幂等，版本冲突标记 `stale`，其他用户无法查看或确认。
- 真实网页 Run `004182f2-12d0-4cdd-a5db-1f80caa93039` 为 `earnings_quality / completed`。DeepSeek 正文只复述“如果下一期经营现金流继续恶化”这一用户条件；候选 `eeb6f8e0-d30d-4875-96fb-88b1dd330a4d` 确认后创建计划 `24c04e15-0448-4411-a026-d457aa5ffde1`，目标价、数量、金额和仓位均为空，刷新对话与“任务与操作”页均可恢复。
- 自动化验收为 `553 tests collected` 全部通过；Ruff、compileall、lockfile 与 diff 检查在本工作包收尾执行。

验收：

- 不能通过 Markdown 正则猜测正式写回内容。
- 任何正式判断、任务、计划和复盘写入均能回溯到用户确认和原 Run。
- 模型失败、取消或守卫不通过时不创建正式对象。

### P0-08 收紧七阶段完成规则和 Claim Ledger

- [x] 阶段完成要求 Run completed、守卫通过、证据非 missing/failed、核心覆盖达标、至少一条来源或用户输入。
- [x] 部分覆盖使用 partial/needs_review，不能简单标记 completed。
- [x] 为 Claim 建立 supports、weakens、unresolved、source、data_time、limitations。
- [x] 最强反证、失效条件和下一证据必须关联具体 Claim 和来源。
- [x] 宽泛“完整分析”不得一次完成全部阶段，除非每阶段独立通过门禁。

2026-07-23 主 Agent 回填（完成）：

- 已拆分“证据可沉淀”和“阶段可推进”状态：`preview` 只保存六维覆盖、补证任务与覆盖历史，不再完成研究阶段；只有 `completed` Run 才进入阶段完成判断。
- `guarded`、`failed` 等状态继续保持不推进且不降低历史充分证据。
- 阶段推进新增统一门禁：Run 必须为 `completed`，`usage.output_guard.passed` 必须为 `true`，核心证据包不能是 `missing/failed/unavailable`。
- “完整分析”按公司经营、财务质量、估值同行、事件情绪、反方证据、失效条件分别执行独立覆盖门槛；单个阶段没有足够覆盖或来源时显示 `needs_review`，不会跟随其他阶段一起误完成。
- 每个阶段保存 `coverage_gate`、`source_refs`、`review_reasons` 和门禁版本；来源可以是明确用户输入、确定性数据源、公告/财报原文或 Claim Ledger 引用。
- 历史已完成阶段会自动重审：能从原 Run 证明守卫、覆盖和来源的继续保留；无法回溯或不满足新版规则的降为“待复核”，重审幂等且不会重复追加未决项。
- 已实现 Claim Ledger 和逐 Claim 来源关系；失效条件可追溯到具体 Claim，反方证据与下一步核验均保留来源、数据时间和限制。
- 前端新增“待复核”状态、具体原因和正确的当前阶段文案。真实网页主动发现 `6/7` 被误写为“七个阶段已完成”，已修复后端 `current_stage` 识别和前端防御性文案。
- 真实中兴通讯研究空间显示 `研究流程 6/7`、`待复核：失效条件与下一证据`，其余有完整 Run 和来源的阶段保持完成；浏览器控制台无 error/warn。
- 自动化回归现为 `435 tests collected` 且全部通过；Ruff 和内联 JavaScript 语法检查通过。

验收：

- 模型超时、守卫回退、只有确定性摘要、来源为空和核心模块缺失均有测试。
- 前端展示“流程/证据覆盖”，不展示投资质量完成度。

### P0-09 收敛股票研究空间前端

- [ ] 第一屏固定展示股票基础、关系/优先级/跟踪、派生持仓完整性、正式判断、三条变化、三项待办。
- [x] 研究概览展示支持、反证、风险、缺口和下一证据。
- [x] AI研究复用绑定 conversation_id，不新建第二套聊天系统。
- [x] 数据与证据按公司经营、财务质量、行业相对、估值、技术、风险六维展示原始数据、系统计算、AI解释和缺失。
- [ ] 任务与操作展示用户任务、计划、期初持仓、流水、调整和快照。（除操作计划外均已接入）
- [ ] 历史与复盘展示判断版本、变化、会话、Run、操作、快照和正式复盘时间线。（除正式复盘外均已接入）

验收：

- 五标签均读取真实后端对象，不使用演示假数据。
- 页面没有综合评分、目标价、强烈看多、仓位或内部供应商错误。
- 判断、操作和复盘未保存离开有二次确认。

2026-07-23 主 Agent 回填：

- 股票空间内“与 Agent 研究”和选股候选卡片的“让 Agent 继续研究/核验”均先创建或复用 DeepStock 会话，再打开同一 `conversation_id`。
- 真实网页验证“大族激光”候选进入已有“个股研究｜大族激光”历史对话，当前阶段问题和候选来源被带入输入框，没有另建普通对话。
- 研究概览新增“研究证据地图”，直接消费 StockWorkspace 的 Claim Ledger、反方证据、失效条件、信息缺口和下一证据，分为支持证据、反方证据、风险与失效条件、信息缺口、下一证据五栏；每栏显示数量和两条摘要，完整来源与边界通过“查看全部”展开。
- “查看完整证据”切换到现有六维数据与证据页；“让 Agent 核验下一证据”复用该股票唯一 `conversation_id` 并预填最优先证据问题，不自动发送或生成新会话。
- 真实网页验收主动发现证据地图被 CSS 网格排到变化/待办之后、五栏原文过密；已调整为“当前判断 → 证据地图 → 变化与待办”，并限制两条预览和四行截断，完整证据仍可展开。
- 英伟达真实页面显示支持 9 项、反证 2 项、风险与失效 5 项、缺口 1 项、下一证据 10 项；联动到 Agent 后输入框为“行业供需与一致预期尚未接入”，控制台无 error/warn。
- “数据与证据”六维卡片已拆分为原始数据、系统计算、Agent 解释和缺失项四层，并保留来源、数据时间、覆盖状态和完整阅读器；窄屏自动收敛为单列。
- Agent 解释优先读取该股票绑定研究会话的最新结构化回答；绑定会话没有时，只回收同一用户、同一证券代码的最近历史回答并标注来源范围，不跨用户或跨股票拼接。
- 真实网页验收中修复切换股票后标签 `active/aria-selected/hidden` 不一致；中兴通讯现可恢复同用户、同股票的最新结构化 Agent 解释，英伟达无历史结构化结果时仍诚实显示“尚未生成”。

### P0-10 修复工程门禁并建立 R1 E2E

- [x] 删除 scripts/build_incremental_report_20260722.py 中两个未使用导入，恢复 Ruff 通过。
- [ ] 增加 relation/thesis/trade/holding/review 单元测试。（relation/thesis/trade/holding 已覆盖，review 待完成）
- [x] 增加事务、幂等、修正链、跨用户、数据不完整和版本冲突测试。
- [ ] 建立一条可重复 R1 E2E。

R1 E2E 最低链路：

> 新用户 → 搜索股票 → 加入关注 → 确认判断 → 录入期初持仓 → 股票内 AI → 确认任务 → 记录买入/卖出 → 生成持仓快照 → 达到复盘窗口 → 编辑并确认复盘

验收：

- Pytest、Ruff、类型检查和 E2E 全部通过。
- 用户 A 无法读取或修改用户 B 的任何私人对象。

### P0-11 实现独立的“李总策略”

该策略是必须实现项，不并入现有 quality、trend、value、pullback 四个模板，也不能只靠前端拼过滤条件。

#### A. 策略引擎与代码边界

- [x] 定义 StockSelectionStrategy 接口和 StrategyRegistry。
- [x] 新增 app/services/strategies/li_zong.py，策略方法固定为 deterministic_li_zong_v1。
- [x] 当前 StockScreenerService 继续负责轻量截面筛选；李总策略单独负责长周期、跨数据集和盘后触发计算。
- [x] 策略参数、公式和规则均版本化，历史结果绑定 strategy_version 和 parameter_version。
- [x] 全市场筛选使用后台批任务，页面读取已发布快照，不在用户请求中逐股临时拉取五年数据。

#### B. 数据依赖

- [x] stock_basic：证券、市场、上市状态和板块识别。
- [x] trade_cal：完整交易日窗口。
- [x] daily：OHLC、pre_close、pct_chg 和 volume。
- [x] daily_basic：最近交易日总市值。
- [x] adj_factor：统一复权的新高计算。
- [x] fina_indicator：最近五个已披露年度 ROE。
- [x] top10_holders 和 top10_floatholders：非自然人股东分类与去重。
- [x] stk_limit：每只股票、每个交易日的真实 up_limit/down_limit。
- [x] 所有数据均保存 sync_run_id、data_version、source_updated_at、trade_date/report_period 和 data_status。

Tushare 接口权限不足时允许使用经过授权且口径明确的替代源，但必须在结果中展示来源，不能静默换源或用新闻标题推算涨停。

#### C. 数据库对象

- [x] strategy_definitions：策略名称、标识、状态和所有者。
- [x] strategy_versions：规则、公式、发布时间和变更说明。
- [x] strategy_parameter_versions：ROE 阈值等参数版本。
- [x] strategy_screen_runs：as_of_date、数据版本、覆盖率、状态和警告。
- [x] strategy_candidate_snapshots：每次筛选的候选状态。
- [x] strategy_rule_results：每只股票每条规则的实际值、阈值、来源和通过状态。
- [x] strategy_trigger_events：三类进入关注介入触发及 OHLC 证据。
- [ ] user_strategy_subscriptions：用户是否订阅通知、默认优先级和提醒设置。

#### D. API

- [x] GET /v1/stock-strategies：列出可用策略及最新数据时间。
- [x] GET /v1/stock-strategies/li-zong：返回规则、公式、参数和边界。
- [x] POST /v1/stock-strategies/li-zong/runs：管理员或任务系统发起重算，普通页面不直接触发全市场同步。
- [x] GET /v1/stock-strategies/li-zong/candidates：候选、触发、数据不完整和失效列表。
- [x] GET /v1/stock-strategies/li-zong/candidates/{ts_code}：逐规则证据详情。
- [x] GET /v1/stock-strategies/li-zong/triggers：最新介入触发事件。
- [ ] PATCH /v1/me/stock-strategies/li-zong：调整允许开放的参数和通知设置。
- [ ] POST /v1/me/watchlist/from-strategy-trigger：用户确认后加入关注，并保留 trigger_event_id。

#### E. 前端

- [x] 在选股研究二级页面增加独立“李总策略”卡片，不与其他模板混排为一个下拉值。
- [x] 首屏展示策略版本、数据交易日、全市场覆盖率、候选数、今日触发数和数据不完整数。
- [x] 支持候选、今日触发、数据不完整、已失效四个标签。
- [x] 候选表展示市值、五年 ROE 最低值、非自然人股东数、近一年涨停数、是否连板、近十日涨停、近十日大阴线、新高日期、三日放量区间和当前触发。
- [x] 详情抽屉逐条展示规则实际值、阈值、日期、公式和来源。
- [ ] ROE 阈值调整前展示影响和新参数版本；重算不覆盖旧结果。
- [x] 触发后提供“加入重点关注”和“进入个股研究”操作，不提供自动买入按钮。

#### F. 调度与系统融合

- [x] 最近完整交易日名单和市值达到稳定门槛后，由独立后台线程持续运行李总策略候选池；不稳定批次不发布。
- [x] 每个交易日完整日线发布后运行三类介入条件检查。
- [ ] 触发事件进入今日观察、通知中心和股票研究空间的重要变化。
- [x] 用户加入关注后，策略证据进入 StockWorkspaceService 的来源列表。
- [ ] ResearchActionService 可生成“核验李总策略触发”的人工任务，但不得生成仓位或交易动作。
- [ ] ResearchOutcomeService 可跟踪规则是否持续满足、数据是否失效；不得把后续涨跌包装成策略胜率。

2026-07-22 主 Agent 回填：

- 已实现 `StockSelectionStrategy`、`StrategyRegistry`、`deterministic_li_zong_v1` 和 12 条逐规则证据；缺失关键数据统一为 `data_incomplete`。
- 已新增策略定义、版本、参数、运行、候选、规则结果和触发事件七张表；同一策略/参数/数据版本/股票结果幂等，参数版本不可覆盖。
- 已提供策略列表、定义、运行、候选、详情和触发 API，以及逐股 Tushare 快照查询/刷新 API；普通选股页面只读取已发布结果。
- 前端已增加独立李总策略区域、五类结果标签、摘要指标、逐规则证据抽屉、加入关注、进入个股研究和 Agent 核验入口。
- Agent 可直接回答李总策略问题；普通 A 股个股研究会自动携带该股票最新策略快照。触发结果已进入 `StockWorkspaceService` 的重要变化和待核验事项。
- 真实 Tushare 烟测：`000063.SZ`、`300308.SZ`、`600519.SH` 在 2026-07-22 均取得 9/9 必需数据集稳定快照；三股本次均为 `not_qualified`，没有用演示候选替代真实结果。预告/快报空值按可选数据缺口保留。
- Tushare 组合快照的 `data_version` 已改为仅由实际数据内容生成；相同内容重复同步保持同一版本，不再受抓取时间影响。
- 策略结果已保存 `stock_basic` 名称、行业和市场；历史候选缺少该字段时，会从已发布稳定快照补齐，因此未预置股票不再只显示证券代码。
- 本轮新增并通过同内容版本稳定、未预置股票名称和历史候选兼容回归；全量 398 项测试及 Ruff 静态检查通过。真实桌面页面已验证平安银行、宁德时代名称、用户空状态文案和 12 条逐规则证据抽屉，浏览器控制台无错误。
- 2026-07-23 已改为全市场增量 Worker：一次批量发布5530只A股名单和最近完整交易日市值，市值覆盖率低于95%时不生成预筛结果；只有市值严格大于150亿元的股票才分批补齐多年ROE、股东、复权日线和真实涨跌停价。
- 李总策略从通用后台调度线程拆出独立线程，默认每批10只、批后等待30秒，真实验证策略运行期间 `/markets/live` 仍约0.02秒返回；进程重启遗留的 running 任务会自动标记为中断失败，不再永久显示“运行中”。
- 真实页面验收时覆盖率从78.2%继续增长到78.4%，页面显示数据交易日2026-07-22、5530只全市场、1216只通过市值预筛和剩余待处理数；没有管理员重算按钮，普通会话调用两个重算接口均返回403。
- 通知订阅、今日观察触发流、参数设置界面和全市场历史回放仍未完成，因此对应条目保持未勾选。
- 2026-07-23 最终口径复核：全市场名单 `5530`、可深度核验 `1170`、页面深度处理已自动推进到 `753/1170（64.4%）`、量价历史不足 `46`、财务历史待实查 `154`、候选/今日触发均为 `0`。真实 Agent 历史回答保留生成时的 `733/1170` 快照，并完整说明 `154` 只财务历史待实查股票仍会进入真实查询；页面实时进度与历史证据快照没有混用。
- 对零候选进行逐规则审计：当前完整可判定样本中各条规则均有通过股票，没有单一规则异常拦截全部股票；最接近的两只股票均因可追溯的近十日大阴线证据未通过。当前零候选保持为真实结果，不使用演示候选填充。
- 年度 ROE 已增加版本化第二来源 `eastmoney_reported_roe_v1`：只补 Tushare 缺失的必需年度，不覆盖已有年度，并逐年保存公告日、来源、URL 与补充原因。真实 Worker 运行后深度数据缺口已由 `20` 降至 `8`；两个来源都无值、旧量价短窗或成交量缺失的股票继续保持数据不完整。

#### G. 必测边界

- [x] 市值 150 亿元整不通过，严格大于 150 亿元才通过。
- [x] 五年 ROE 每一年均需 >= 阈值；四年满足、一年缺失或不足均不通过或 data_incomplete。
- [x] ROE 只使用 as_of_date 当时已经公告的数据，禁止未来财报倒灌。
- [x] 非自然人股东合并去重、自然人、机构和 unknown 分类测试。
- [x] 最近一年 5 次涨停不通过，6 次通过。
- [x] 连续两个交易日涨停通过；被停牌日隔开的两个涨停不算连板。
- [x] 主板、ST、创业板、科创板、北交所和无涨跌幅限制日分别测试。
- [x] 最近十日有涨停、无涨停，以及 -5% 阴线边界测试。
- [x] pct_chg <= -5% 但收阳、或 close < open 但跌幅小于 5%，均不错误判定为“5%阴线”。
- [x] 最近二十日内创新高、刚好在二十日前创新高、复权前后差异和历史不足测试。
- [x] 三日每天均达到 2 倍才通过；只有两日、某日 1.99 倍或基准不足不通过。
- [x] 高开恰好 3.5% 且收阳通过；振幅恰好 9.8% 不通过，必须严格大于 9.8%。
- [x] 触发重复运行幂等，不重复通知。
- [x] 任一关键数据缺失时不能把股票误放入 qualified 或 triggered。
- [ ] 全量历史回放使用 as-of 快照和当时可得财报，完成无前视检查。

验收：

- 使用至少 3 只命中、3 只不命中和 3 只数据不完整股票完成固定样本验收。
- 每只候选能解释全部规则为何通过，不能只显示“命中李总策略”。
- 全市场任务可重跑且同一 data_version 结果一致。
- 触发事件可进入今日观察和个股空间，并可由用户确认加入重点关注。
- 不输出自动买入、仓位、目标价或收益承诺。

## 6.2 P1：数据、事件、今日观察和完整产品页面

### P1-01 建设 Tushare 标准化数据管道

- [x] 为每个实际可用接口建立权限 PoC 清单，记录成功、字段、配额、空值和限制。
- [x] 扩展 Tushare Provider：income、balancesheet、cashflow、forecast、express、disclosure_date、index_daily 和按权限可用行业接口。
- [ ] 建立 stocks、industries、stock_industry_map、trade_calendar、daily_quotes、index_quotes、daily_basic、financial_periods、financial_metrics 等标准化表。
- [ ] 建立 data_sync_runs、data_quality_issues、data_version 和 stable release。
- [ ] 夜间任务使用 Worker 运行，不由用户请求临时拉全市场。
- [x] 数据不完整时保留上一稳定版本。

验收：

- 每条进入 AI 的 Tushare 事实都可追溯 dataset、sync_run_id、source_updated_at、trade_date/report_period。
- 行情和 daily_basic 日期不一致时禁止比较。
- Tushare 失败不导致基础页面整体不可用。

2026-07-22 权限 PoC 回填：

- 已实连成功：trade_cal、stock_basic、daily、daily_basic、fina_indicator、adj_factor、stk_limit、top10_holders、top10_floatholders、income、balancesheet、cashflow、forecast、express、disclosure_date、index_daily。
- `forecast`、`express` 对部分股票可返回 0 行；当前按“接口可用但该股票/期间无记录”处理，不解释为权限失败，也不阻断九类策略必需数据集发布。
- 快照保存 dataset、sync_run_id、data_version、source_updated_at、trade_date/report_period 和 data_status；部分或失败同步不会覆盖上一稳定版本。
- 仍未完成：全市场标准化事实表、行业映射、Worker 夜间流水线、跨表日期门禁和商业许可书面确认。

### P1-02 冻结并实现 R1 事件白名单

- [ ] 优先实现财报/预告/快报、停复牌、限售解禁、股东增减持、价格/成交异常、任务到期/待复盘。
- [ ] 每类冻结来源许可、频率、dedupe_key、severity、rule_version 和失败降级。
- [ ] 区分公共 change_event 和用户 user_change_link。
- [ ] 用户可标记有关、无关、待确认并创建任务。
- [ ] 与我相关变化进入今日观察和股票空间。

验收：

- 同一事件跨批次不重复。
- 判断为空时不声称“影响你的原逻辑”。
- 用户标记无关只调整提醒权重，不删除公共事件。

### P1-03 完成今日观察

- [x] 建立 GET /v1/today/overview 聚合接口。
- [x] 首屏按风险、到期、财报、复盘和草稿确认排序 3—5 项，并可查看排序依据。
- [x] 同屏显示四大指数、沪深成交和数据时间。
- [ ] 第二屏显示用户相关变化、证据缺口和股票表现。
- [ ] 第三屏显示涨跌结构、行业强弱、近 5 日增强/转弱、风格和风险日程。
- [ ] 支持盘前、盘中、盘后、非交易日四种状态。

2026-07-23 进度：后端聚合已返回四大指数、广度、成交、行业、个人任务、判断草稿和研究变化；前端首屏已接入 5 项排序待办和同日同股去重后的相关变化，并修复事项跳转被旧股票状态覆盖的问题。市场区现固定展示上证综指、深证成指、创业板指和科创50；科创50已进入统一指数目录和腾讯完整日线 Provider。索引选择会在同一市场日期优先采用完整历史，避免单点行情覆盖 100 日历史。四时段差异化内容、风格和风险日程仍待完成。

验收：

- AI 摘要失败时基础数据仍可用。
- 持仓不完整时不计算组合收益。
- 无事件白名单时不伪造“与我相关变化”。

### P1-04 完成我的关注

- [x] 支持全部、持仓、关注、已结束和高/普通/低筛选。
- [ ] 支持流程、注意事项、行业、待处理和暂停状态组合筛选。
- [ ] 支持关系修改、暂停、结束、恢复和批量操作。
- [x] 列表展示当前判断、最新变化、下一事项和条件持仓字段。
- [ ] 提供列设置并允许隐藏成本、盈亏等敏感字段。

2026-07-23 进度：新增 `GET /v1/stock-workspaces`，按用户返回全部 `watching/holding/ended` 股票空间，并聚合正式判断版本、最新变化、下一事项、观察任务、行情和诚实的持仓空状态。前端已改为研究资产视图，支持关系、优先级、暂停、结束和恢复的单项操作；已结束空间继续保留原 `workspace_id`、判断和历史。流程/注意事项/行业/待处理组合筛选、批量操作和列设置尚未完成，因此第二、第三和第五项保持未完成。

验收：

- 返回页面后保留筛选、排序和滚动。
- 点击任一股票进入同一股票研究空间。
- 删除只允许无任何历史的误添加；其余使用结束/归档。

### P1-05 完成 AI 研究类型

当前已完成“对话研究 / 透明选股”二级切换、股票研究空间联动及 2—5 股统一口径比较；行业、组合和完整专题研究仍需继续扩展。

- [x] 支持 2—5 股统一口径比较。
- [ ] 支持行业研究，并在覆盖不足时明确不生成股票推荐。
- [ ] 支持用户选择的持仓/关注组合研究；金额不完整时只分析数量、行业覆盖和共性风险。
- [ ] 支持指定维度重算、预计消耗、版本保留和部分失败重试。
- [ ] 支持历史搜索、收藏、重命名和按对象/时间/类型筛选。

2026-07-23 进度：已完成具名 A 股行业研究的首个可用闭环。类似“半导体行业怎么样”的问题会
进入市场/行业研究，而不是普通资料库检索；后端读取官方行业指数历史、成分与权重、行业
成分涨跌家数、全市场广度和市场资讯，再由 Hermes DeepSeek V4 Pro 针对本轮问题即时生成。
半导体实测使用中证全指半导体产品与设备指数（H30184），覆盖 245 个交易日和 87 只成分股；
真实 Run `c59d477f-2bc2-4ca5-8029-c35151b8ee87` 状态为 `completed`，回答包含行业 1/5/20/60 日
表现、29 涨/58 跌/0 平、与全市场对照及风险边界，不再退回英伟达单家公司摘要。尚需继续
扩展无法精确映射到官方指数的行业、行业专属新闻/产业链证据和行业研究页面，因此本项保持
部分完成。

验收：

- 不同公司和报告期不可比时明确标记。
- 重算不覆盖旧版本。
- 任何输出继续遵守无交易建议边界。

### P1-06 完成全局搜索、通知和十二类小窗口

- [ ] 后端搜索股票、行业、AI 会话、用户股票和复盘。
- [ ] 建立 notification 和 reminder_rule 对象、已读/已处理分离和去重。
- [ ] 统一 DrawerShell、ModalShell、焦点、Esc、未保存和恢复焦点行为。
- [ ] 按 PRD 完成十二类窗口，复用同一对象和接口，不复制页面逻辑。

验收：

- 搜索只返回当前用户有权访问的私人对象。
- 通知同类合并但不丢失原事件。
- 键盘完整可操作，焦点不丢失。

### P1-07 完成个人中心基础版

当前进度：已完成真实研究资产数量、匿名个人空间与会话信息、Hermes/数据健康/后台刷新状态，以及资料库和 AI研究入口；未虚构会员、青豆、支付或账户安全能力。以下正式账号与高风险操作仍未完成。

- [x] 展示真实研究资产数量、匿名会话和实际服务状态。
- [ ] 支持偏好、R1 简单额度、真实用量、安全和管理员访问记录。
- [ ] 支持数据导出、账户删除、设备列表和退出其他设备。
- [x] R1 不展示虚构青豆余额、支付套餐或任意 BYOK Base URL。

验收：

- 所有数字来自真实后端。
- 高风险动作重新验证。
- 管理员具有正文访问能力时，用户端必须同步展示记录。

## 6.3 P2：生产架构、安全、运维和受控内测

### P2-01 前端迁移到 Next.js

- [ ] 建立 apps/web、packages/contracts、packages/ui。
- [ ] 使用 OpenAPI 生成 TypeScript 类型。
- [ ] 按六个正式页面和十二窗口拆路由、组件和查询。
- [ ] 迁移过程中保留当前 demo.html 作为回归参考，不在两个前端同时长期新增业务。

验收：

- 前端 build、lint、typecheck 和基础 E2E 进入 CI。
- LCP、CLS、首屏 JS 和搜索响应达到目标。

### P2-02 后端模块化与 MySQL 迁移

- [ ] 把 app/main.py 中业务逻辑迁入领域模块和服务。
- [ ] 使用 SQLAlchemy 和 Alembic。
- [ ] MySQL 成为业务事实库；SQLite 仅保留本地单测/轻量开发适配。
- [ ] 建立 repository 接口，避免领域规则绑定数据库实现。

验收：

- staging 完成真实迁移、回滚点和数据一致性检查。
- 金额和持仓算法不再依赖浮点事实值。

### P2-03 Redis、Celery 和持久任务

- [ ] 将数据同步、事件、AI 长任务、导出和通知迁入 Celery。
- [ ] Redis 只做 broker、缓存、锁和协调。
- [ ] MySQL 保存 job 事实、重试、结果和补偿状态。
- [ ] 建立队列隔离、并发限制、幂等、DLQ 和任务重放。

验收：

- 进程重启后任务不静默丢失。
- Redis 丢失不导致业务事实丢失。
- 长 AI 任务离开页面后继续，用户可查看状态。

### P2-04 正式认证和安全

- [ ] 建立注册、登录、密码重置、会话撤销、设备管理和登录限流。
- [ ] 生产 Cookie 强制 Secure，写操作增加 CSRF 防护。
- [ ] 增加 API/用户/IP 限流和恶意大请求保护。
- [ ] 外部 URL 加 SSRF、防 DNS 重绑定和内容限制。
- [ ] 增加 XSS、CSRF、SSRF、越权、密钥和日志脱敏测试。

验收：

- 高危安全测试为发布阻断。
- 所有私人资源猜测 ID 时返回 404/403，不泄露存在性。

### P2-05 管理后台与审计底座

- [ ] 建立独立 /admin 入口、角色、MFA 和短会话。
- [ ] 建立 audit_logs、admin_access_requests、数据任务看板和 AI 配置。
- [ ] 管理员访问正文必须有原因、范围、二次认证、request_id 和用户可见记录。
- [ ] 管理员永远不能读取密码和 API Key 明文。

验收：

- 审计日志不可由普通业务管理员删除。
- 管理员正文访问 E2E 通过。

### P2-06 可观测、备份、恢复和发布

- [ ] Caddy 作为唯一公网入口。
- [ ] 建立结构化日志、指标、告警和 trace/request/job/run 关联。
- [ ] 建立 MySQL 备份、PITR、恢复演练和 RPO/RTO 记录。
- [ ] 编写 Tushare、AI、数据库、账本和错误数据 Runbook。
- [ ] 建立不可变镜像、staging smoke、人工生产批准和回滚流程。

验收：

- staging 可从空服务器恢复。
- 定期恢复测试有时间和结果记录。
- CI/CD 完成全部发布门禁。

## 6.4 P3：R2/R3 商业、额度、青豆和 BYOK

### P3-01 R1 简单额度

- [ ] 先实现按功能/模型的简单额度和用量明细。
- [ ] AI 运行记录预计与实际使用，失败不消耗或自动恢复额度。
- [ ] 不接真实支付，不展示青豆营销余额。

### P3-02 R2 固定会员和支付

- [ ] 固定套餐、权益、订单、支付、退款、发票和对账。
- [ ] 后台配置价格和额度，前端不硬编码。
- [ ] 建立客服和人工补偿流程。

### P3-03 R3 青豆账本

- [ ] 使用只追加 ledger，支持 reserve、settle、refund、adjustment。
- [ ] reference_id 和幂等键唯一，余额不允许人工直接修改。
- [ ] 建立对账任务和负余额/未结算告警。

### P3-04 R3 白名单 BYOK

- [ ] 只支持审核通过的供应商和固定协议，不允许任意 Base URL。
- [ ] 密钥使用数据库密文和 KMS/主密钥封装，只显示后四位。
- [ ] 支持连接测试、默认模型、失败降级、发送数据范围和服务费说明。

### P3-05 商业合规

- [ ] 书面确认 Tushare 商业许可和展示/缓存/衍生边界。
- [ ] 书面确认公告、财报、研报和网页内容许可。
- [ ] 明确数据保留、账户注销、订单/账本和审计保留期限。

## 7. 建议主 Agent 的实施顺序

为避免“先在旧架构堆一遍、迁移时再重写一遍”，建议采用合同优先、双轨推进：

### 轨道 A：产品闭环

1. 冻结状态枚举和 OpenAPI。
2. StockWorkspaceService。
3. 关系和判断版本。
4. 期初持仓、操作、调整和快照。
5. 用户任务和操作计划。
6. 结构化 AI、citation 和确认写回。
7. 交易复盘。
8. 股票空间、今日观察、我的关注前端。
9. 李总策略候选池、介入触发、通知和个股空间联动。

### 轨道 B：生产底座

1. 后端模块边界和 repository。
2. MySQL + Alembic。
3. Redis + Celery。
4. Next.js + 生成合同。
5. 正式认证、审计和安全。
6. 监控、备份、恢复和 CI/CD。

轨道 A 的领域规则必须通过 repository 和 OpenAPI 实现，不能继续直接堆进 main.py 或只塞进 SQLite JSON；这样轨道 B 迁移时才能复用。

## 8. 主 Agent 每个工作包的交付格式

每完成一个工作包，必须更新本文件对应复选框，并提交以下证据：

1. 改了哪些领域对象、接口、页面和迁移。
2. 哪些旧接口保持兼容，哪些进入废弃期。
3. 自动化测试命令、测试数量和结果。
4. 页面实际打开后的视觉与交互验收。
5. Tushare 或其他外部依赖的实连结果，只报告接口、行数、字段、日期和错误类型，不输出凭证。
6. 尚未完成的异常、权限、许可和数据边界。
7. 是否存在与 V2.1 冲突的旧代码，如何隔离或删除。

禁止只以“接口已写”“页面已画”“README 已更新”作为完成证据。

## 9. 明确不做

- 不自动交易，不接券商自动下单。
- 李总策略中的“介入”只触发策略池、通知和人工复核，不自动写入持仓或交易。
- 不输出 BUY/HOLD/SELL、目标价、仓位、止损价和收益承诺。
- 不把证据覆盖率、流程完成度、研究优先级或模型 confidence 当成股票评分。
- 不保存未经授权的研报、公告或财报全文。
- 不把媒体、社区情绪、资金字段或单一技术指标写成确定性因果。
- 不因 Tushare 已配置就声称全部接口、全市场数据、商业许可和夜间同步已经完成。
- 不修改 /Users/chr/Documents/tradingagents；该目录继续只读。
- 不覆盖或清理当前未跟踪的用户文件。

## 10. R1 最终验收门槛

只有同时满足以下条件，才能把 R1 标记为“真实完成”：

- [x] 同用户同股票空间唯一。
- [ ] 关系、优先级、跟踪、流程和注意事项状态正确。
- [x] 判断、操作、调整和复盘版本不可静默覆盖。
- [x] 期初持仓、交易/调整流水和持仓快照能够完整对账和重放。
- [ ] AI 输出事实、推断、假设、反证、缺口和来源分离。
- [ ] AI 只生成候选写回，用户确认后才修改正式对象。
- [x] 个人交易复盘价格结果与逻辑结果分离，数据不完整时不伪精确。
- [ ] Tushare 数据时间、报告期、来源版本和数据状态可追溯。
- [ ] 李总策略全部基本面、股性、量价和三类介入规则按版本运行，逐规则证据可追溯。
- [ ] 李总策略历史回放无前视，缺失数据不误判，盘后触发幂等且能进入今日观察和个股空间。
- [ ] 跨用户、CSRF、XSS、SSRF、限流和敏感日志测试通过。
- [ ] 今日观察、我的关注、股票空间、AI、复盘和个人中心主链路可用。
- [ ] Pytest、Ruff、类型、契约、前端构建和 R1 E2E 全部通过。
- [ ] 页面无综合评分、目标价、确定性建议和内部敏感信息。
- [ ] 备份恢复和应用回滚在 staging 演练通过。

---

最终实施原则：

> 先把用户自己的判断、持仓、操作和复盘做成可确认、可对账、可追溯的事实，再让现有强研究算法为这些事实服务。不要让分析模块的丰富度掩盖业务闭环尚未完成。

## 11. 2026-07-23 主 Agent 续跑：真实 Hermes 选股回答与主动问题修复

- [x] 修复 `HERMES_BIN=hermes` 被错误当成当前目录文件的问题；支持按 `PATH` 解析 Hermes 命令，不写死本机路径。
- [x] 修复流式桥接丢失 Hermes 虚拟环境的问题；从控制台脚本或包装器识别 Python，并保留虚拟环境符号链接入口。
- [x] 李总策略宽泛问题只返回真正进入候选池或触发池的股票，不再把未通过、数据不完整、失效或未处理股票称为候选。
- [x] 李总策略单股问题明确回答已触发、已进入候选、未通过、数据不完整或状态已失效，并列出实际规则证据。
- [x] 部分覆盖时同时说明数据交易日、已评估/全市场、覆盖率和剩余待处理数；不能推断未处理股票，也不能宣称全市场没有候选。
- [x] 修复市值预筛总数与待处理队列混用；Agent 的待处理口径只读取 `remaining_symbols`。
- [x] 流式回答结束前发布经过完整守卫的 verified final delta，最终网页正文与数据库保存答案一致，不再先显示长草稿后突然换成另一段。
- [x] 新增李总策略专属越界守卫：拒绝证据外 `T+3/T+5` 复核周期；没有逐规则汇总时不得猜测主要瓶颈、最严格规则或满足规则的股票稀缺度。
- [x] 修复新建研究对话残留上一只股票预填问题；新对话输入框清空，Enter 仍直接发送，Shift+Enter 保留换行。
- [x] 真实网页验收：Run `e49c6b12-1bd8-48d5-9945-a6c8fee3694f` 为 `completed`，安全流式更新 `18` 次，守卫通过；页面无重复消息、无残留草稿、控制台无 error/warn。
- [x] 自动化与运行验收：全量 `425` 项测试、Ruff、内联 JavaScript 均通过；`/health=ok`、Hermes 启用、数据健康 `54/54`、后台与策略 Worker 正常。
- [x] 修复李总策略分类页只取混合状态前 200 条再前端筛选的问题；候选、触发、数据不完整、已失效和未满足现按状态分别请求，避免真实候选因分页位置被错误隐藏。
- [x] 修复稳定快照零候选仍显示“首批快照准备中”的错误空状态；真实网页明确显示当前候选为空，并验证数据不完整 138 条、未满足 200 条独立结果。
- [x] 分类快速切换增加请求令牌，旧响应不会覆盖新标签；页面控制台无 error/warn。
- [x] 全市场深度同步使用最多 2 个受控并发，复用全市场 stock_basic/daily_basic 和交易日历，跳过 6 个非策略必需接口；临时失败同日最多重试 3 次，次日重置，稳定成功后清零。
- [x] 新增分类查询、稳定零候选、并发、请求复用和重试状态机回归；全量测试更新为 469 项。

本工作包没有把 R1 标记为全部完成。期初持仓、操作/调整、修订、派生快照、操作计划、操作时研究上下文、AI 交易复盘草稿、用户确认复盘和跨股票复盘聚合已经完成；仍需继续：完整 R1 E2E、全市场增量覆盖、正式通知持久化、用户订阅和正式个人中心。

## 12. 2026-07-23 操作计划、冻结上下文与即时 AI 交易复盘

- [x] 新增 `TradeWorkflowService`，完成操作计划创建、编辑、检查、保存、取消、过期、部分执行/执行、乐观锁、幂等键、用户隔离和版本历史。
- [x] 实际操作只能关联方向一致且已保存的计划；计划卡进入“记录实际操作”会自动带入方向和计划。真实浏览器已验证减少持仓计划从草稿、检查、保存推进到执行。
- [x] 减少持仓或卖出时冻结操作、计划、正式判断、持仓快照、最近已完成日线、市场指数、可用估值/报告/变化和来源；缺失项保留在快照中，不用当前数据静默回写过去。
- [x] 新增交易复盘等待数据、可生成、草稿、确认、归档状态链；后台定期刷新待复盘对象，三个后续交易日齐备后计算确定性价格路径。
- [x] `trade-review` Skill 要求 Hermes 只返回结构化逻辑复盘，价格结果由后端计算；AI 草稿只接受本用户本次完成 Run，并保存 `source_run_id`。
- [x] 修复结构化 JSON 因单个越界数字导致整段被守卫丢弃的问题：只删除无法直接核验的子句并重新完整验守，其余本次即时 DeepSeek 回答继续保留。
- [x] 用户编辑不会覆盖 AI 版本；真实页面已验证 AI 版本 1、用户版本 2、确认版本 3、归档和刷新恢复。
- [x] 真实 DeepSeek v4-pro Run 完成并保存草稿；操作价、第三个后续交易日价格和价格变化由确定性数据库生成，逻辑结果来自当次模型回答而非预存文案。
- [x] 页面把内部 `passed/missing_items/checks` 结构收敛为“检查通过”等用户语言；桌面 `1280×720` 无横向溢出。
- [x] 自动化验证：495 项测试全部通过，Ruff、内联 JavaScript 解析和 `git diff --check` 通过。

当前边界：复盘中心已增加跨股票列表、状态/关键词筛选和当日提醒入口；提醒目前是实时聚合事项，不是具备已读、订阅渠道和投递审计的正式通知系统。生产版仍需要 MySQL/Alembic、任务队列、Outbox/审计和通知。真实操作仍由用户手工记录，不连接券商、不自动下单。

2026-07-23 最终运行回填：`8773` 主服务已按正式启动环境恢复 Hermes，健康检查为启用状态；隔离验收问题“美股为什么收盘跌了”由 `deepseek-v4-pro` 即时生成针对性回答，Run `9ba5ae84-0083-4661-a57a-5853db80a1d6` 完成且守卫通过。当前仍需重点改善的是首个安全可见内容约 `27.8s` 的等待体验，而不是继续增加回答模板。

## 13. 2026-07-23 对话稳定呈现与守卫误判修复

- [x] 未校验的流式草稿不再进入主回答正文，只显示“检索证据、生成回答、核对行情和数字”等真实进度；收到 verified final 后才首次呈现正文。
- [x] verified final 移到个股数值规范化之后发布，保证流式最终文本与数据库保存答案一致；HTTP 回包只补充结构化证据、引用和操作按钮，不再整段替换相同正文。
- [x] 修复全局进度事件覆盖已完成回答的竞争条件；最终节点一旦可见，后续“正在保存”等进度不能再改写正文。
- [x] 修复守卫把“下跌 4.24%”误判为缺少 `-4.24%`、把正文内联“下行失效条件”误识别成新章节并扫描后文的问题；同一次被拒的精炼模型答案重新校验后完整通过。
- [x] 真实浏览器使用 `deepseek-v4-pro` 连续追问中兴通讯，Run `508ff133-14cf-4c57-ba71-a86d6fdb3e6f` 为 `completed`，最终守卫通过；回答严格按“关系 / 反方证据 / 下一步核验”三个小节生成，使用 2026-07-23 14:57 盘中报价 36.03 元、下跌 3.92%。
- [x] 最终正文在结构化证据和引用追加后保持 771 字完全一致，连续观察未再发生跳变；生成期间未校验长草稿没有外显。
- [x] 全量 `515` 项测试、Ruff、内联 JavaScript、`git diff --check` 通过；`/health=ok`、Hermes 启用、数据健康 `54/54`。

当前边界：本轮解决了“答案跳变”和“好答案被误判后退化成证据堆砌”两类核心可用性问题。DeepSeek 服务侧首 token 仍有波动，本次复杂个股追问首 token 约 `39.39s`；下一阶段应优先做问题驱动的 ResearchPlan 和稳定证据复用，降低不必要的全模块扫描与超长提示词。

## 14. 2026-07-23 首页优先级、全局搜索与可刷新研究路径

- [x] 按真实使用优先级重排今日观察：首页首屏先展示全球市场实时行情和 A 股核心指数，其后才是“我的研究待办/重要变化”和研究洞察；不再把待办作为产品首页的主角。
- [x] 新增统一搜索服务和 `GET /v1/search`，可同时搜索全 A 股股票、行业、当前用户股票研究空间、历史对话和交易复盘；研究、对话与复盘严格按当前用户隔离。
- [x] 搜索支持输入联想、完整分组结果页、上下方向键和 Escape 收起；普通 Enter 进入 `/search?q=...`，只有显式选中联想项后才直接打开对象。股票结果进入长期股票空间，行业结果进入透明选股并自动填写行业，不向用户展示后台数据源失败或任务状态。
- [x] 新增 `/today`、`/watchlist`、`/stocks/{symbol}`、`/research/{conversationId}`、`/reviews`、`/account` 和 `/knowledge` 人类页面路由；页面刷新、前进和后退可恢复一级页面、股票、股票空间标签、研究对话和复盘记录。
- [x] 股票 AI 页面刷新首帧即显示“正在恢复绑定对话”，数据返回后原位恢复唯一历史会话；全过程保持 `/stocks/000063.SZ?tab=ai`，不闪现新建研究 CTA。
- [x] 后台研究报告目标从默认标的和自选股扩展到 DeepStock/选股线索建立的研究会话股票，保证由筛选进入长期研究的股票也会进入后续报告更新队列。
- [x] 恢复既有安全 Tushare 运行配置后，后台真实发布 2026-07-23 全 A 股稳定快照：5530 只股票，市值字段覆盖率约 99.93%；网页搜索“通信设备”返回 137 只股票，搜索“中兴通讯”可直接进入 `000063.SZ` 股票空间。
- [x] 真实浏览器验证：首页行情优先顺序、行业搜索、股票搜索、键盘打开、股票标签 URL 同步及浏览器前进/后退均通过。
- [x] 真实 DeepSeek 冒烟验证：问题“通信设备板块今天有什么变化”创建历史对话并由 Hermes 即时生成针对性回答；Run `177d39f2-b6e5-4929-8f99-457c7240bb5a` 为 `completed`，回答包含行情事实、可能驱动、反方证据、数据边界和下一步核验。
- [x] 自动化验证：全量 521 项测试全部通过；Ruff、内联 JavaScript `node --check`、`git diff --check` 和 `uv lock --check` 通过。

当前边界：全市场搜索依赖已发布的稳定 A 股快照；未配置 Tushare 时只保留少量默认研究标的兜底，不能宣称全市场可搜。生产部署仍需把凭证注入、后台任务状态、快照新鲜度告警和失败恢复纳入正式 Runbook；凭证不写入 Git。

## 15. 2026-07-23 观察任务 AI 写回与真实流式主链修复

- [x] 用户明确要求“保存为核验任务”时，个股结构化回答会生成 `observation_task` 候选，不再只停留在自然语言建议。
- [x] 候选卡在确认前不创建正式任务；确认后复用 `ObservationTaskService`，并支持用户隔离、刷新恢复和重复确认幂等。
- [x] 修复 Hermes 流式桥接中项目 `app/utils.py` 对 Hermes 顶层 `utils` 的导入遮蔽；真实 `deepseek-v4-pro` 已直接返回 delta/final。
- [x] 回退 oneshot 显式关闭全部工具；Agent Prompt 禁止模型自行写文件、数据库、记忆、任务或用户状态，用户确认写回只能由宿主系统执行。
- [x] 真实网页 Run `47f5f698-8a32-40b4-87fd-f38fe5b474f2` 为 `completed`，最终守卫通过，页面呈现针对本轮中兴通讯问题的即时回答、结构化证据与待确认观察任务卡。
- [x] 真实页面点击确认后，任务进入中兴通讯唯一研究空间；刷新后仍存在，同名任务只有 1 条，控制台无 error/warn。
- [x] 全量 `528` 项测试、Ruff、内联 JavaScript 解析、lockfile 一致性和 diff 空白检查全部通过。

下一步高价值收敛：

1. 将股票空间的 AI 标签直接嵌入该股票的唯一绑定对话，减少“开始 AI 深度研究”等重复入口。
2. 将全局搜索与“向 AI 提问”分离，避免用户对搜索资产和开始新研究的预期混淆。
3. 普通复盘中心只保留“我的交易复盘、市场复盘”，证据链和质量缺口迁到开发白盒页。

2026-07-23 后续回填：以上三项已在工作包 16 完成，不再作为未实施事项。

## 16. 2026-07-23 股票内嵌 Agent、搜索分流与普通复盘收敛

- [x] 股票空间的“AI研究”标签直接承载该股票唯一绑定的真实 Agent DOM，继续使用原 conversation API、历史消息、K 线、证据侧栏和 Enter 发送；URL 保持 `/stocks/{symbol}?tab=ai`，刷新后恢复同一会话。
- [x] 删除股票页重复的顶部“开始/继续 AI 深度研究”入口；同一用户的一个会话不能绑定两只股票，另一会话也不能静默替换现有股票主会话，冲突统一返回 `409`。
- [x] 新增 `/search?q=...` 完整搜索页；普通 Enter 展示股票、行业和私人研究资产分组结果，方向键显式选中后才直接打开对象。“向 AI 提问”只预填问题，不自动创建对话或 Run。
- [x] 行业结果进入透明选股并自动填写行业条件，不再被改写为 AI 问题；异步联想结果在进入搜索页后不会竞态重现。
- [x] 普通复盘中心只展示“我的交易复盘、市场复盘”，且普通模式不加载开发白盒 API；Run、研究结果、证据链和质量缺口仅在 `?qa=1` 开发评估模式显示。
- [x] 首页顺序调整为全球行情 → A 股全景 → 市场洞察 → 继续提问 → 我的研究待办/重要变化，个人待办保持靠后。
- [x] 股票会话首次恢复期间显示“正在恢复绑定对话”，不再短暂出现新建研究 CTA；启动阶段的会话列表请求使用 Promise 去重，创建或更新会话后才强制刷新。
- [x] 最终验收：全量 `530` 项测试、Ruff、内联 JavaScript、`uv lock --check` 和 `git diff --check` 全部通过；真实浏览器验证刷新首帧、历史会话恢复、搜索回车、行业进入透明选股、AI 仅预填、普通复盘双标签和股票内嵌 Agent，控制台无 error/warn。

当前边界：市场复盘目前以已保存的市场短文形成时间档案，尚未完成带市场状态、观点版本、后续验证和正式确认链的结构化市场复盘；开发白盒仍保留给 `qa=1` 内部评估使用。

## 17. 2026-07-23 问题驱动 ResearchPlan、证据复用与股票空间上下文

- [x] 股票问题先分类为行情原因、财务现金流、股东、主营、分析师预期、事件、估值同行或综合研究；短追问从最近用户问题继承研究焦点。
- [x] 每类问题只加载必要证据模块与 Skills。行情问题不再无差别执行全部财务、股东和主营重型分析，财务、股东和估值问题分别使用不同取证路径。
- [x] 行情、当前资讯与最新估值每轮刷新；财报质量、利润现金流驱动、主营、股东、预期、事件、同行和历史走查按模块时效复用最近报告证据。
- [x] 预生成报告只提供可复用的模块证据，本轮自然语言回答继续由 Hermes/DeepSeek 即时生成，不能直接返回历史文案。
- [x] 单个模块失败时保留其他已取得证据，必需模块失败把证据状态记为 `partial`；用户回答只说明可理解的证据缺口，不暴露供应商异常和内部堆栈。
- [x] 所有主要个股 Agent 意图统一读取当前用户的精简股票空间上下文，包括关系、正式判断版本、持仓、操作计划、观察任务、实际操作、复盘和重要变化；跨用户判断隔离已有回归测试。
- [x] Agent Run 保存本轮 ResearchPlan、逐模块状态、选中 Skills 和 Prompt 字符数；SSE 展示与实际模块一致的核验阶段。
- [x] 新增问题分类、连续追问、无关重型模块不调用、稳定证据复用、单模块部分成功、专项 Agent 上下文和跨用户隔离测试。

当前边界：`COL-AI-02` 已完成。现有 SSE 已能显示真实规划和模块阶段，但还不是覆盖 citation、候选写回和所有终态的统一事件合同；失败维度也尚无独立重试 API，因此 `COL-AI-05`、`COL-AI-06` 保持部分完成。下一步应先做真实浏览器的多问题路径验收，再评估通知中心或确认写回的下一个工作包。
