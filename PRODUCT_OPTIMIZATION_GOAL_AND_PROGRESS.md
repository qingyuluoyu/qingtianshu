# 清数智算持续产品目标与进度

更新日期：2026-08-01

权威仓库：`/Users/chr/Documents/qingtianshu`

当前分支：`codex/structured-ai-writeback`

## 持续目标

把清数智算建设成真正可日常使用的个人金融研究工作台：用户能清楚找到功能，通过真实行情、资料库、分析方法和 Hermes/DeepSeek 即时回答完成“发现问题—获取证据—形成判断—持续观察—记录操作—复盘”的完整闭环。确定性数据、AI解释和用户正式确认必须分开；不以预存文案冒充即时回答，不以界面完整或测试通过冒充生产就绪。

## 十项产品要求进度

| # | 目标 | 当前进展 | 下一步与验收口径 |
|---|---|---|---|
| 1 | 功能清晰直白，合并重复入口 | 一级导航现为市场、透明选股、关注、个股、金融顾问、复盘、个人；选股不再藏在对话入口下 | 持续用桌面与390px真实首访检查“用户是否知道下一步”，只保留有明确任务差异的入口 |
| 2 | 学习并吸收同事方案 | 已吸收K线放大、拖拽、滚轮、十字光标、OHLC/成交量和渐进披露；同事代码只作参考 | 每项继续按设计、代码、测试、API、真实数据、浏览器、生产七层记录，拒绝虚构行情和内部数据外泄 |
| 3 | K线详情与多周期 | 已有分时、日K、周K、月K、范围切换和放大详情；本轮新增真实年度OHLCV聚合与年K | 用真实5年日线核验年K开高低收、成交量、缩放和移动端布局 |
| 4 | Agent流式与长回答阅读 | 已实现受金融校验保护的流式输出、用户上滚保持、最终答案替换和刷新恢复 | 持续抽样真实DeepSeek长回答，记录首个安全正文、总耗时和滚动行为 |
| 5 | 字号、信息密度与标题 | 已完成市场、条件选股、个股概览、复盘详情，并提高策略、证据和历史元数据等复杂区域字号 | 每轮浏览器验收必须人工看截图，不能只看元素可见或无溢出 |
| 6 | 金融顾问与教育者 | 已有金融知识库、基金/ETF事实库、七项风险问卷和confirmed-only个性化 | 扩展基金公告、费率、跟踪误差和更多品类；保持适合性边界，不指定唯一产品或承诺收益 |
| 7 | 后端预分析、在线取证与检索 | 自选股走预分析证据路径，非自选股强制在线刷新；选股候选的研究重点、真实指标风险线索现已进入个股空间和Agent上下文 | 提高正式披露和媒体来源覆盖，继续抽样Agent是否基于新证据核验入口线索，而不是机械复述筛选结果 |
| 8 | 自主体验并迭代 | 已建立双视口20项浏览器门禁；W25完成真实四模板数据审计、候选到个股空间链路和桌面/390px截图复核 | 每个工作包继续覆盖首次路径、失败恢复、刷新恢复、控制台和真实模型抽样 |
| 9 | 回答真正解决问题 | 已取消无内容时机械栏目，修复无证据因果、重复结构、内部术语和模板回退 | 重点提高直接证据与分析方法，不再主要依靠增加Guard或文字清理提升观感 |
| 10 | 李总策略空仓期计算沪深300 | 本轮把组合版本升级为`li_zong_2w_no_cost_full_benchmark_v5`，沪深300按完整区间连续计算 | 三档已建立审计基线；新交易日继续按区间独立重算，逐档核验空仓日指数净值仍变化 |

## 本轮 W23 工作包

### 已实现

1. 李总策略继续保持零交易成本、最短10个交易日换仓和冷却期仅执行最新候选集合。
2. 沪深300不再跟随策略进出场；每日基准净值统一为“当日收盘价 ÷ 区间首日收盘价”，空仓期仍连续变化。
3. 组合缓存版本升级到v5，旧v4同暴露结果不能被当前接口误读。
4. 页面图例和说明统一为“沪深300连续基准”“策略空仓期指数仍继续变化”。
5. K线组件新增真正的年K：由5年真实日线按自然年聚合开盘、最高、最低、收盘和成交量，并与周期、范围、放大、缩放和拖拽共用同一组件。

### 本轮必须完成的证据

- 李总策略专项测试与K线纯函数/页面合同通过；
- 3个月、1年、3年生产结果使用v5重新发布；
- 至少找到一段策略空仓但相邻沪深300净值变化的生产曲线；
- 条件选股候选进入个股空间时，`entry_context`进入研究资料，并在针对性Agent Prompt/回答中可追溯；
- 全量测试、Ruff、Python编译、全部静态JavaScript、锁文件和差异检查通过；
- 桌面与390px真实页面无横向溢出、控制台错误或用户不可理解的内部状态；
- 只显式暂存本轮文件，排除`artifacts/`、环境变量、数据库、日志、缓存和用户工作区，随后提交并推送GitHub。

## 当前生产边界

- 产品仍是内部MVP，不是自动交易系统，也不提供目标价、仓位、确定性预测或收益承诺。
- PostgreSQL领域库和持久化队列已运行，但正式生产仍缺少可核验备份、商业数据授权/SLA和长期故障恢复演练。
- 用户正式判断、任务、计划、风险画像和复盘必须由用户明确确认；AI只能生成候选。
- `artifacts/`保存本机验收材料，不进入Git提交。

## 每轮更新规则

每完成一个工作包，在本文件补充：真实问题、实现变化、自动化证据、浏览器证据、真实模型或真实数据证据、未完成边界、Git提交和远端SHA。历史失败保留用于复盘，不以新描述覆盖旧证据。

## W23 验收结果

- 首次三档同日v5审计快照均发布至2026-07-27：近3个月、近1年、近3年的连续沪深300收益分别为`-1.4036%`、`+17.0437%`、`+21.6699%`；策略净值与v4逐点保持一致。2026-07-28 20:15交付核对时，近3个月已严格重算为策略`-5.1586%`、沪深300`-4.2219%`、超额`-0.9367%`，近1年为`+44.9446% / +13.6962% / +31.2484%`；近3年仍保留2026-07-27稳定结果。
- 三档基准按历史指数收盘独立复算，最大误差小于`5e-9`；三档都找到连续空仓但指数净值变化的日期段。
- 中兴通讯年K和近5年范围在桌面与390px真实页面完成切换；移动端可见图表宽342px，无工具栏或页面横向溢出。
- 条件选股候选的入选理由、数据日和缺失项已进入Agent证据与Prompt；单股候选逻辑追问保持个股研究路由。
- 当前自动化基线为`824 passed`；双视口仓库门禁为`20 passed / 0 failed`，报告位于`artifacts/browser-smoke-w23-benchmark-yeark-agent-context/result.json`。
- 当前仍未完成：2026-07-28的近3年全市场状态由持久化Worker继续追补；商业数据SLA、可核验PostgreSQL备份和长期故障恢复仍是正式生产门槛。

## W24 信息架构与可读性优化

### 真实问题

- “透明选股”此前只存在于金融对话和个股研究的二级入口，选股页还会高亮对话导航，首次用户无法直接理解两者关系。
- “AI投研对话”偏系统术语，不如“金融顾问”直接说明用户能获得什么服务。
- 移动端导航仍按6项旧结构书写；李总策略、证据状态、研究阶段和历史会话元数据存在较多8–10px文字。

### 已实现

1. 一级导航改为“市场总览—透明选股—我的关注—个股研究—金融顾问—复盘中心—个人中心”，透明选股拥有独立入口和独立高亮状态。
2. 原“AI投研对话”统一改为“金融顾问”，工作区标题和个股绑定会话说明同步使用用户任务语言；Agent即时生成、历史会话和证据链逻辑不变。
3. 390px下七个入口使用三列网格完整展示，不依赖横向滚动发现隐藏功能。
4. 会话筛选、研究阶段、证据地图、李总策略统计/回测/历史、回答来源和行情元数据的用户可见字号提高到至少11–12px。
5. 浏览器门禁修复隐藏分组误计问题：只统计实际可见的快捷入口标签，不把隐藏的个股专属分组误报为普通顾问入口。

### 验收证据

- 当前自动化基线为`830 tests collected`且全量通过；Ruff、Python `compileall`、全部静态JavaScript和`git diff --check`通过。
- `artifacts/browser-smoke-w24-navigation-readability-final/result.json`覆盖桌面与390px十个核心页面，`20 passed / 0 failed`；控制台错误、页面错误、失败响应和页面级横向溢出均为0。
- 人工检查桌面透明选股、390px透明选股和390px金融顾问截图：七项导航全部可见；选股页只高亮透明选股；顾问页只高亮金融顾问；候选卡、核验提示和操作按钮未被裁切。
- 本轮不改变筛选算法、李总策略、Agent模型、金融取证、数据库或用户确认边界；生产门槛仍包括商业数据SLA、可核验PostgreSQL备份与恢复演练。

## W25 选股候选研究价值与Agent上下文

### 真实问题

- 四个模板能返回真实数字，但“回撤后等待确认”此前按近5日收益从高到低排列，会优先展示已经大幅反弹的股票，与“接近企稳”的产品语义冲突。
- 候选进入个股研究空间时只保存命中理由和缺失项，单日急跌、高估值、利润下降、低ROE和高负债等真实风险数字没有成为后续研究重点。
- 首轮浏览器验收发现“先核验：先核验……”和连续标点，说明链路已通但用户文案仍需收口。

### 已实现

1. 回撤模板继续保持近5日收益不低于0的原规则，不新增未经金融专家确认的硬阈值；排序改为近5日收益从低到高，优先展示刚转正或更接近企稳的样本。
2. 每只候选新增确定性的`research_focus`和最多4条`attention_flags`。线索只由已展示真实数字生成，可覆盖单日跌幅不低于5%、PE/PB偏高、净利润同比下降、ROE低于3%、资产负债率不低于75%和回撤模板近5日已大幅反弹等情况。
3. 候选卡首层直接展示研究重点与风险线索；字段经`entry_context`持久化到个股研究空间，并进入股票工作区、Agent压缩证据和Prompt。旧客户端不传新字段时仍保持兼容。
4. 个股研究概览把入口研究重点、风险线索、命中理由和缺失项分开呈现；正式关注判断、任务和复盘仍不会由筛选结果自动写入。
5. 浏览器夹具补充高估值和单日急跌样本，长期检查桌面与390px风险线索的显示、卡片宽度和渐进披露。

### 验收证据

- 真实生产数据的回撤候选按近5日`0.00%、0.00%、0.00%、0.00%、+0.03%……`排序，不再把近5日大幅反弹样本放在最前。
- 真实候选北方国际显示净利润同比`-37.54%`、ROE`0.99%`两条优先风险；点击进入研究空间后，同一研究重点与风险线索成功保存并可供Agent读取。
- 当前自动化基线为`832 tests collected`且全量通过；Ruff、Python `compileall`、全部静态JavaScript、`uv lock --check`和`git diff --check`通过。
- `artifacts/browser-smoke-w25-screening-research-context-final-v2/result.json`覆盖桌面与390px十个核心页面，`20 passed / 0 failed`；桌面和移动选股截图已人工检查，风险线索无裁切、重复标题或标点叠加。

### 下一断点

- 继续真实抽样经营改善、趋势和估值候选进入Agent后的即时回答，重点核验公告、现金流、行业与反方证据是否足以支持或推翻入口线索。
- 当前风险阈值用于研究优先级提示，不是荐股打分或交易规则；任何新增硬筛选条件仍需金融专家确认。

## W26 个股 Agent 真实证据使用与长会话效率

### 当前完成

1. 北方国际单股问题已经稳定进入 `stock_research`，财务与企稳追问不再错误加载全市场候选和全部研究模块。
2. 财务证据新增报告期、营收同比、净利润同比、经营现金流同比变化，以及5/20/60日收益窗口起止日期。
3. 真实 DeepSeek Pass 5—9 已保存到 `artifacts/w26-north-international-retest-20260729/`。五次回答都使用了真实财务、现金流、行情和公告信息，但只有 Pass 8 接近通过，其余仍出现现金流概念混淆、财务原因过度解释、股价因果外推或公告窗口错误。
4. 同一长会话追问的 Prompt 从92,100字符降至33,827字符，输入Token从35,575降至14,738，首个安全正文从19.178秒降至13.741秒，总耗时从27.534秒降至20.605秒；两次均有9次安全流式正文更新。

### 当前没有完成

- 经营改善、相对行业增强、估值约束三个模板尚未分别完成真实候选、真实 DeepSeek、Prompt、截图和人工评分的闭环验收。
- 模型仍偶发把经营现金流净额称为现金流入，把静态毛利桥写成经营原因，把财务压力说成股价回撤原因，并混淆公告是否处于20日窗口。
- 首个安全正文仍高于10秒目标；真实页面的Enter发送、用户上滚保持、最终原位替换、刷新恢复和390px长回答阅读尚未在最新代码上完成浏览器验收。
- 基金正式文件与完整费率、债券与利率数据、直接事件原文证据和成熟的长会话分层记忆仍需扩充。

### 下一断点与完成标准

- 优先改善证据字段、Prompt压缩和模型生成合同，不以固定回复、预存报告正文或继续堆大量Guard替代。
- 三个选股模板每个至少一个真实样本达到人工评分14/16，且当前标的绑定与数字可靠性必须满分；任何现金流概念错误、公告窗口错误或股价因果外推均直接判为不通过。
- 最新代码必须完成桌面与390px真实浏览器长会话验收，并记录首正文、滚动行为、最终替换、刷新恢复、控制台和截图。

## 2026-07-29 W26 连续主营结构追问与真实证据边界

### 当前完成

1. 修复“当前不能确认”被误判为股价异动问题，连续主营追问不再丢失真实分部、地区和毛利率数据。
2. 主营专项 Prompt 去除旧筛选入口、季度净利润等无关候选线索，新增 `coverage_limits`，让 DeepSeek 能准确区分已披露事实和当前不能确认的内容。
3. 主营回答必须使用当前公司的真实分部名称；多项错误数字触发一次基于当前证据包的聚焦重试，重试前清除错误流式草稿。
4. 修复百分点、百分比区间和“同比下降近4成”等正常金融数字表述被错误拦截的问题。
5. 真实 Run `9f07844b-9410-48c6-98a6-f4c9c7ae2995` 为 `business_structure / completed / deepseek-v4-pro`，首 Token `2.744s`、首可见正文 `3.194s`、总耗时 `9.438s`；回答使用真实分部、地区、毛利率、反方证据和披露边界，864px页面完成后定位在回答顶部。
6. 当前全量收集 `876` 项并执行至100%通过；Ruff、Python编译、全部静态JavaScript、依赖锁和差异检查均通过。

### 仍未完成

- 主营结构专项通过不能替代经营改善、相对行业增强和估值约束三个模板的真实 DeepSeek 闭环验收。
- 每个模板仍须保存真实候选、真实 Run、Prompt、最终回答、桌面与390px截图和人工评分，并逐句确认公告、财务、经营现金流、行业和反方证据的主体与时间归属。
- 基金正式文件与完整费率、债券与利率数据、十轮长会话效率及直接事件原文证据仍需扩充。
- 后续真实模型抽样改用隔离 evaluation 用户，避免重复测试问题污染普通用户的历史对话。

## 2026-07-29 W26 经营改善真实证据与单模型回答收口

### 当前完成

1. 修复“市场需求”误判为大盘、“行业边界”误判为切换行业，以及“毛利率”误触发债券知识库；只有用户明确要求大盘、行业或重新选股时才离开当前个股。
2. 经营改善、相对行业和估值约束问题现在进入各自的定向研究计划；经营改善证据压缩优先保留投资者关系活动记录表、同报告期财务、经营现金流、主营分部、行业边界和反方事实，并移除重复模块。
3. 经营改善 Prompt 从基线约 39,560 字符继续压缩到约 29,918 字符。真实 Run `afac8ea8-ab8b-47f2-a77b-4134d0157474` 使用 `deepseek-v4-pro`，输入 12,668 tokens、输出 563 tokens、API 调用 1 次；首个安全正文 12.245 秒，总请求耗时 32.716 秒，较 48.802 秒基线缩短约 33%。
4. 该真实回答实际引用宁德时代投资者关系活动记录表原文“库存增加主要是为下半年市场需求而提前备货”，并同时使用营收、利润、经营现金流、两期覆盖比率、销售收现率、动力电池和储能分部毛利率、同行数据边界和反方证据，不再机械复述候选入口。
5. 质量专项初稿若只有局部越界，现在先做确定性句级修复，保留模型其余真实分析；只有回答过短或无法安全修复时才调用第二次质量编辑模型。最新真实 Run 因此从两次 DeepSeek 调用降为一次。
6. 新增“利润增长尚未得到经营现金流和回款节奏同等确认”的跨口径识别与局部改写。用上述真实 Run 的初稿和证据包离线复验后，公告原文、财务数字、现金流两期比率、主营分部、行业边界均保留，越界句被替换为“这些差异是需要继续核验的反方事实，但不能合并推断回款节奏”。
7. 最终生成合同要求第一句以当前公司中文名称开头，减少流式初稿结束后仅因补标的名称造成的整段 DOM 替换。

### 当前验证与边界

- `tests/test_agent_evidence_compaction.py`、`tests/test_agent_prompt_contracts.py`、`tests/test_agent_guard.py` 已完整通过；相关 Ruff 检查通过。真实样本离线复验无剩余专项相关性问题。
- 本次没有用预存报告冒充即时回答；真实 Run 是一次新的 DeepSeek 生成，离线修复只用于验证最新代码，不作为新的模型验收 Run。
- 经营改善已经接近可用，但仍需在最新服务上补一次真实 DeepSeek 和桌面/390px流式页面验收，确认第一句标的名、用户上滚保持、最终原位完成和刷新恢复。
- 相对行业增强和估值约束仍需分别完成真实候选、真实 Run、Prompt、最终回答、页面截图和人工评分；基金正式文件、债券与利率、十轮长会话及更多直接事件正文仍是后续重点。

## 2026-07-29 W26 经营改善最终真实页面验收

### 已完成

1. 服务重启后，在已有 60 条消息的宁德时代长期研究会话中再次发起同一经营改善问题，没有新建隔离的短会话，也没有用历史报告正文替代即时生成。
2. 最新真实 Run `b1686023-3727-431a-8cce-d5568f4f35d0` 为 `stock_research / completed / deepseek-v4-pro`，`api_calls=1`；Prompt 29,659 字符、输入 11,366 tokens、输出 448 tokens。
3. 回答第一句以“宁德时代”开头，实际使用公司原文“库存增加主要是为下半年市场需求而提前备货”，并保留经营现金流 602.2 亿元及同比 2.6%、两期现金流/利润比率 1.93→1.39、两期销售收现率 124.6%→94.8%、动力电池与储能毛利率、行业比较边界和“最重要的反方事实是”。
4. 模型首 Token 9.156 秒、首个安全正文 10.682 秒、请求总耗时 25.748 秒；从用户按 Enter 到页面首段约 18.067 秒、最终完成约 26.584 秒。路由与证据装配占 6.707 秒，长会话首屏等待仍有继续优化空间。
5. 页面采样显示安全正文从 86 字连续追加到 754 字，11 次可见正文更新均保持前缀增长；最终阶段只移除生成状态并附加元数据，正文长度与内容不变，没有整篇跳换、数字精度跳变或残缺 Markdown。
6. 本轮没有出现“现金流整体充裕”等过度结论；一次跨口径表述由本地句级修复处理，`guard_seconds=0.011`，没有触发第二次 DeepSeek。

### 验证与下一步

- `tests/test_agent_guard.py`、`tests/test_agent_prompt_contracts.py`、`tests/test_chat_streaming.py`、`tests/test_api.py` 完整执行至 100% 并返回退出码 0；相关 Ruff、两个流式前端 JavaScript 语法和 `git diff --check` 通过。
- 经营改善模板已完成“真实长期会话—真实 DeepSeek—真实公告/财务/现金流/主营/反方证据—连续流式—最终原位完成”闭环。
- 下一优先级转为相对行业增强与估值约束的同标准真实验收；同时继续压缩长会话的路由证据时间与首个安全正文等待。基金正式文件、债券与利率数据及更多直接事件原文证据仍未完成。

## 2026-07-29 W26 相对行业真实证据与单模型回答收口

### 已完成

1. “宁德时代今天相对CS电池指数……”这类自然表达现在会同时触发相对行业专项研究计划和行业市场取证，不再只有计划命中、实际证据包却为空。
2. 官方指数目标日收益缺失时，回答必须直接说明无法形成正式指数相对判断；50只成分的涨跌分布和中位数只能作为旁证，不能冒充“跑赢/跑输指数”。
3. 修复正常金融边界表达被误拒绝的问题，包括“无法直接判断/计算”“不能据此断定”“不能直接等同为”“无法升级为正式判断”，以及官方数据补齐后的条件式改判说明。
4. 成分覆盖允许零数量类别省略：当50只成分中41只上涨、9只下跌且二者已经覆盖全部样本时，不再强迫模型机械补写“0只平盘”。
5. 未复权行情口径补充不再只列两只名称却写“其余1只”，现在明确显示“共49只成分使用未复权日线，另有1只使用前复权日线”，避免用户误解总样本数。
6. 最新真实 Run `bc7fcb24-58d6-47c7-b50e-4c35b9f81dd2` 为 `stock_research / completed / deepseek-v4-pro`，`api_calls=1`；Prompt 12,245 字符，输入 4,489 tokens、输出 1,204 tokens，请求总耗时 29.235 秒。
7. 最终回答保留宁德时代同日上涨1.53%、CS电池50/50成分覆盖、41涨9跌、中位数2.0021%、近60日-8.98%、最大回撤24.18%、分类体系差异和49只未复权口径；没有虚构指数差值，也没有触发第二次DeepSeek。

### 验证与下一步

- `tests/test_agent_guard.py`、`tests/test_agent_prompt_contracts.py`、`tests/test_research_plan.py`、`tests/test_chat_routing_modules.py`、`tests/test_agent_evidence_compaction.py` 完整执行至100%并返回退出码0；相关Ruff与 `git diff --check` 通过。
- 本轮完成真实API与Run工件验收，尚未补做最新代码下的桌面/390px页面流式、上滚保持、最终原位完成、刷新恢复和截图验收。
- 下一优先级是估值约束模板的同标准真实DeepSeek验收；基金正式文件、债券与利率数据、直接事件原文证据及长会话首段等待仍需继续改进。

## 2026-07-30 W26 估值对话阶段交付

### 本阶段结果

1. 估值约束回答改为优先保留信息丰富的 DeepSeek 首稿，只对确定的金融口径错误做局部修复；不限制固定字数、段数、标题或栏目。
2. 本地恢复链路在模型重试失败时会继续校验并恢复最佳已生成稿，不再直接退回机械 preview；近似 PB/PE 表述可归一到同日真实值，已有同行段只补日期，不重复追加整份快照。
3. 真实 evaluation Run `a4f79acd-a7ca-4dae-9332-8f764986c8be` 为 `completed / deepseek-v4-pro / api_calls=1`，最终回答 1123 字；首 Token 27.119 秒、首可见正文 27.664 秒、请求总耗时 43.180 秒。
4. 根据用户当前优先级，本次不再继续扩充 Guard 或等待新的真实模型、双视口浏览器抽样；以“对话质量总体可用、工程门禁通过、尽快进行阶段 GitHub 交付”为收口标准。

### 上传前验证与边界

- `977 tests collected`，全量执行至 100% 通过；唯一提示为 Starlette TestClient/httpx 上游弃用警告。
- `uv run ruff check .`、Python `compileall`、全部 `app/static/*.js` 的 `node --check`、`uv lock --check` 和 `git diff --check` 全部通过。
- 本次是 W26 阶段交付，不代表长期目标完成。估值回答首段等待仍明显偏长；基金正式事实、债券与利率数据、长会话治理和生产运维门槛仍需后续推进。

### 2026-07-30 用户语言收口

- 用户侧不再展示难懂的“失效条件”，统一改为“什么时候需要重新判断”或“什么情况会推翻当前判断”。
- 底层 `invalidation_conditions` 数据字段保持兼容；旧模型输出会在展示前转成通俗表达，新问法也能正确触发路由、证据准备和完整性校验。
- 相关 Guard、Prompt、路由、预览、页面与流式回归通过；全量静态门禁再次通过。本阶段随 Hermes 调用链、测试和示例配置一起交付 GitHub。

## 2026-07-31 个股连续追问质量进展

### 已完成

1. 修复个股涨跌原因首问后的长追问回退到综合诊股；当前会继承上一轮目标交易日、`price_cause` 和相关证据范围。
2. 区分“现在的报价”和“你现在最有把握能确认什么”，后者不再触发无关的最新报价前缀。
3. 连续追问改用两项确认事实与一个关键未知的小证据框架，移除邻近日解禁、完整财务、技术指标、估值、股东和同行等诱导模型跑题的材料。
4. 最新真实首问与追问 Run 分别为 `b456863e-d6b5-47b1-87e5-2cc45798e9a6`、`091f050d-c0b7-4514-b6c7-7991e321e878`，均 `completed / deepseek-v4-pro / api_calls=1`，Guard 未介入。
5. 追问 Prompt 从失败样本约 94k 字符降至 14,947 字符；最终回答为三个自然段，无标题、无最新报价、无技术跑题和解禁猜测。
6. `1035 tests collected` 全量通过，Ruff、编译、全部 JavaScript、依赖锁和差异检查通过。

### 继续目标

- 继续用不同股票、不同日期和更长上下文抽样，确认上下文继承不会误抓旧主题。
- 优先优化路由、证据选择和 Prompt 冲突；只有确定性金融错误且无法靠上游修复时才保留局部 Guard。
- 当前 Worker 离线且 `/ready=503`，对话主服务可用但完整后台运行态需要单独恢复，不能表述为生产就绪。

## 2026-08-01 W27 主营换题与自然边界回答

### 已完成

1. 主营问题中的否定表达不再触发价格主题；“先不谈涨跌”“不要给股价和技术指标”稳定进入 `business_structure`。
2. 主营路径只加载分部结构模块与对应 Skill，减少行情、财务全景和风险画像对普通业务问题的干扰。
3. 证据压缩删除三项以内无意义的“前三项合计100%”，并把分部标签、具体 SKU/品牌和毛利率解释边界直接交给模型。
4. 用户追问“哪些结论不能直接推出”时，最后一段必须自然回答至少两项边界；没有行业分部基准时不得评价行业高低。
5. 隔离真实 Run `d11b4557-a159-43af-8264-dcde1558b89f` 使用 `deepseek-v4-pro / api_calls=1`，总请求 26.355 秒，输出守卫直接通过。回答没有股价、技术指标、具体 SKU、系列酒品牌、无意义前三项100%或无证据行业比较。
6. 全量 `1042` 项测试通过；Ruff、Python 编译、全部 JavaScript、依赖锁和差异检查通过。

### 继续目标

- 保持“自然对话与证据质量优先”，后续先抽样不同公司和更长上下文，不把局部措辞问题重新扩大成大范围 Guard 建设。
- 继续降低真实模型首段等待；当前主营追问总请求仍为 26 秒级，主要耗时在模型调用。
- Worker、备份恢复、商业数据 SLA、基金/债券正式事实与长期生产门禁仍未完成；本次只做阶段 GitHub 交付，长期目标保持进行中。

## 2026-08-01 W27 财报质量自然对话阶段交付

### 已完成

1. “财报到底好不好、财报质量、经营质量”等自然表达进入 `quality_review`；分析师式聊天不再触发分析师预期，否定行情要求会移除市场模块。
2. 质量专项从原始财报证据重新保留经营现金流公司解释，避免前置压缩只留下财务费用说明；主营默认只保留产品维度，地区维度按用户问题加入。
3. 数字覆盖接受两期经营现金流金额替代机械重复同比，接受“净流出19.79亿元”“负的1.51倍”等自然负数表达；销售收现率跨越100%的直接比较不再误删整句。
4. 两次隔离真实 DeepSeek 复验均正确回答收入、利润、毛利率、经营现金流、主营结构与反方事实，无股价、技术指标和报告目录，也不再声称公司没有解释经营现金流。
5. 全量 `1051` 项测试通过；Ruff、Python 编译、全部 JavaScript、依赖锁和差异检查通过。

### 继续目标

- 两次真实复验仍分别发生 2 次和 3 次 API 调用，单调用稳定性尚未完成；后续优先改善首稿措辞和本地句级修复，不扩大 Guard。
- 当前按用户“对话质量可用即可、尽快上传”的优先级完成阶段 GitHub 交付。Worker 为 0、`/ready=503`，长期产品和生产目标继续进行。
